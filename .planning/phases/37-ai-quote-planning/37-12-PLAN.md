---
phase: 37-ai-quote-planning
plan: 12
type: execute
wave: 7
depends_on: ["37-11", "37-10", "37-05", "37-06"]
files_modified:
  - web/tests/phase-37-quote-ai.spec.ts
  - .planning/phases/37-ai-quote-planning/37-VALIDATION.md
autonomous: true
requirements: [FINAI-03, FINAI-04, FINAI-05]

must_haves:
  truths:
    - "A user can suggest line items, review them one at a time, and only then send the quote — end to end in a browser"
    - "The Send Quote button is disabled and explained while any suggested line is unreviewed"
    - "A quotes.view-only user sees no variance card on a quote page and issues zero requests to the variance path"
    - "The drill-down's Quoted vs Actual by Trade card renders and exports"
    - "The whole web suite is green at --workers=2 --retries=1"
  artifacts:
    - path: "web/tests/phase-37-quote-ai.spec.ts"
      provides: "the phase's browser-level proof of SC1, SC2, SC3 and SC4"
      contains: "suggest-line-items-trigger"
  key_links:
    - from: "the spec"
      to: "the login page"
      via: "logging in through the UI so Redux auth and permissions populate before SPA navigation"
      pattern: "page.getByLabel|page.fill"
---

<objective>
Prove the phase in a browser: suggest, review, blocked send, review-all, send
succeeds — plus the Trap 8 zero-request pair and the drill-down card.

Purpose: every other test in this phase mocks a layer. This one drives the real
components through the real router with the API mocked at `/api/proxy`, which is
the only place the review gate, the disabled button and its explanation are
proven to work together.

Output: `web/tests/phase-37-quote-ai.spec.ts`, a green web suite at the sanctioned
worker count, and a completed validation map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-ai-quote-planning/37-UI-SPEC.md
@.planning/phases/37-ai-quote-planning/37-VALIDATION.md
@CLAUDE.md

<interfaces>
The shipped recipe to copy is `web/tests/phase-36-ai-findings.spec.ts`:
mock `/api/proxy`, log in through the UI so Redux auth and permissions populate,
then SPA-navigate. Its fixture constants are declared once at the top and every
assertion reads text back off the fixture, so a fixture edit can never leave a
stale sentence asserted somewhere else in the file.

```ts
const FINANCE_PERMISSIONS = ["projects.view", "finance.view"];
const NON_FINANCE_PERMISSIONS = ["projects.view", "jobs.view"];
```
Phase 37 needs a third set: quote management WITHOUT finance
(`["quotes.view", "quotes.edit"]`) for the Trap 8 half, and
`["quotes.view", "quotes.edit", "finance.view", "projects.view"]` for the
permitted half.

Test hooks (37-UI-SPEC "Test Hooks"): `suggest-line-items-trigger`,
`suggest-trigger-reason`, `suggestion-notice`, `unreviewed-banner`,
`ai-line-sub-row-{i}`, `confidence-chip-{i}`, `line-basis-{i}`,
`review-marker-{i}`, `accept-line-{i}`, `quote-pricing-basis-note`,
`quote-labor-note`, `quote-ai-disclosure`, `send-blocked-alert`,
`quote-variance`, `quote-variance-figure`, `quote-variance-interpretation`,
`project-quote-variance`, `project-quote-variance-total`.

Hard rules for this file:
- **Never `page.goto` a quote or drill-down.** A hard navigation resets Redux
  `isAuthenticated`, disables `usePermissions`, and every finance surface is
  correctly denied — a test that would pass with the gate deleted (32-04 / 35-11
  / 36-06 lesson). Log in through the UI, then navigate via the sidebar and the
  in-page links.
- Run at `--workers=2 --retries=1`. At default workers, four full runs returned
  16/4/7/24 failures with a shifting set; at two workers with one retry the suite
  was 173 passed, 2 failed, 0 flaky, and those two are the documented
  pre-existing Phase 21 URL drift (`ai-intake`, `ai-interview`).
- A mock must answer EVERY route a new query introduces with a real body. A
  shell-chatter fallback returning `[]` for a shape that is not a list made a
  Phase 36 query error and retry mid-test, breaking an unrelated
  exactly-one-refetch assertion (reproduced 3/3 with `--repeat-each=3`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The suggest, review and send flow</name>
  <files>web/tests/phase-37-quote-ai.spec.ts</files>
  <read_first>
    - web/tests/phase-36-ai-findings.spec.ts (whole file — the login, mock and SPA-navigation recipe)
    - web/tests/phase-35-financials.spec.ts (the request-counter recipe)
    - .planning/phases/37-ai-quote-planning/37-UI-SPEC.md states 1-36
  </read_first>
  <action>
Create `web/tests/phase-37-quote-ai.spec.ts` with the fixture block first: one
draft quote with two hand-built lines, and the suggestion response the
`suggest-line-items` route returns (three AI lines across the three bands, each
with a basis sentence). Every assertion reads its expected text off the fixture —
never a retyped sentence — except the frame strings the UI-SPEC byte-locks, which
are declared once as spec constants.

Test 1 — the full flow, in one browser session:
1. Log in through the UI as a user holding `quotes.view`, `quotes.edit`,
   `finance.view` and `projects.view`.
2. SPA-navigate to the quotes list, open the draft quote, click Edit.
3. Assert the trigger reads `Suggest line items` and is enabled.
4. Click it; assert the pending label `Analyzing history...` appears and the
   trigger is disabled while the mutation is in flight.
5. After the response: assert three `ai-line-sub-row-*` rows exist, that the three
   chips carry `Strong history`, `Limited history` and `Thin history`, that each
   basis renders its fixture sentence in full, that `unreviewed-banner` shows the
   plural heading for three, and that the three card captions
   (`quote-pricing-basis-note`, `quote-labor-note`, `quote-ai-disclosure`) are
   present with the disclosure last.
6. Assert exactly three `accept-line-*` buttons exist and that no control's
   accessible name matches the bulk-approve pattern.
7. Navigate to the quote detail; assert `send-blocked-alert` is visible and the
   `Send Quote` button is disabled with `aria-describedby` pointing at the alert's
   id.
8. Return to the editor, click each `accept-line-*` in turn, save the draft.
9. Back on the detail page: assert `send-blocked-alert` is gone and `Send Quote`
   is enabled; click through the send dialog and assert the success path.

Test 2 — the cold-start refusal: with the suggest route answering
`refusal_reason: "insufficient_history"`, assert `suggestion-notice` renders the
heading naming the trade and a body containing the response's `required_count`
and `comparable_count`, and that no line item was added to the table.

Test 3 — the regenerate confirmation: with one unreviewed line present, click
`Suggest again`, assert the dialog title `Replace unreviewed suggestions?` and
the count body, cancel it, and assert no request reached the suggest route.
  </action>
  <acceptance_criteria>
    - `grep -c "page.goto" web/tests/phase-37-quote-ai.spec.ts` returns 1 or fewer, and any occurrence targets the login page only
    - `grep -c "suggest-line-items-trigger" web/tests/phase-37-quote-ai.spec.ts` returns 1 or more
    - `grep -c "Analyzing history\.\.\." web/tests/phase-37-quote-ai.spec.ts` returns 1
    - `cd web && npx playwright test tests/phase-37-quote-ai.spec.ts --workers=2 --retries=1` passes
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx playwright test tests/phase-37-quote-ai.spec.ts --workers=2 --retries=1</automated>
  </verify>
  <done>The suggest → review → blocked send → review-all → send flow passes in a browser, along with the cold-start and regenerate branches.</done>
</task>

<task type="auto">
  <name>Task 2: The Trap 8 zero-request pair and the drill-down card</name>
  <files>web/tests/phase-37-quote-ai.spec.ts</files>
  <read_first>
    - web/tests/phase-36-ai-findings.spec.ts (the SC2 deny + zero-request-counter pair)
    - .planning/phases/37-ai-quote-planning/37-UI-SPEC.md states 37-54 and the double-lock block
  </read_first>
  <action>
Add to the same spec file.

Test 4 — the finance boundary on the quote page. Log in through the UI as a user
holding `quotes.view` and `quotes.edit` and NOT `finance.view`. Install a request
counter on `**/api/v1/quotes/*/variance` before navigating. SPA-navigate to an
approved quote's detail page and assert:
- no element with testid `quote-variance` exists,
- no element with testid `financials-deny-panel` exists — this page must not
  announce the card's absence with a yellow panel,
- the counter is zero,
- every other sidebar card still renders (the actions card and the financial
  summary), so the assertion cannot pass because the page failed to load.
Add an in-file comment recording the layering: `FinanceGate` short-circuits
before `QuoteVarianceSection` mounts, so the request half is only observable once
the render half is already broken — which is what makes the hook's `enabled` a
second independent lock rather than a redundant one. The proof that `enabled`
holds on its own lives in the hook's jest test.

Test 5 — the permitted quote-variance card: as a finance holder on the same
approved quote, assert `quote-variance` renders, `quote-variance-figure` carries
the signed figure and `quote-variance-interpretation` the matching sentence.

Test 6 — the drill-down card: as a finance holder, SPA-navigate through the
sidebar to `/financials`, open a project, and assert `project-quote-variance`
renders below the margin trend with a `project-quote-variance-total` row, and
that the card's title reads `Quoted vs Actual by Trade`.

Every route the new queries touch must be mocked with a real body shape — a
fallback returning `[]` for an object shape makes the query error and retry
mid-test.
  </action>
  <acceptance_criteria>
    - `grep -c "quotes/\*/variance\|quote-variance" web/tests/phase-37-quote-ai.spec.ts` returns 2 or more
    - `grep -c "financials-deny-panel" web/tests/phase-37-quote-ai.spec.ts` returns 1 or more (asserted ABSENT)
    - `grep -c "project-quote-variance-total" web/tests/phase-37-quote-ai.spec.ts` returns 1
    - `cd web && npx playwright test tests/phase-37-quote-ai.spec.ts --workers=2 --retries=1` passes with 6 or more tests
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx playwright test tests/phase-37-quote-ai.spec.ts --workers=2 --retries=1</automated>
  </verify>
  <done>The quote page's double lock is proven in a browser and both variance surfaces render for a permitted user.</done>
</task>

<task type="auto">
  <name>Task 3: The web phase gate and the validation map</name>
  <files>.planning/phases/37-ai-quote-planning/37-VALIDATION.md</files>
  <read_first>
    - .planning/phases/37-ai-quote-planning/37-VALIDATION.md (the per-requirement table to complete)
    - .planning/STATE.md Phase 36 entry on web suite flake and the sanctioned worker count
  </read_first>
  <action>
Take the web half of the phase gate:
1. `cd web && npm test` — the whole jest suite green.
2. `cd web && npx tsc --noEmit` and `cd web && npm run lint` — both clean; lint
   runs at `--max-warnings 0`, so an orphaned constant left behind by any of this
   phase's extractions fails the gate.
3. `cd web && npx playwright test --workers=2 --retries=1` — the whole Playwright
   suite. The two pre-existing Phase 21 URL-drift failures (`ai-intake`,
   `ai-interview`) are the documented allowance; anything else is fallout to fix
   in this task, not to defer.

Then complete `.planning/phases/37-ai-quote-planning/37-VALIDATION.md`:
- fill the assigned task ID for every remaining row (web rows land on 37-03,
  37-05, 37-06, 37-08, 37-10 and this plan),
- set each row's status,
- tick the four Validation Sign-Off boxes that are now true and set
  `wave_0_complete: true` in the frontmatter,
- leave the two manual-only rows as they are: the chip's visual fidelity against
  the 35/36 palette, and one live Claude call against a real trade history (every
  automated test patches the client).
  </action>
  <acceptance_criteria>
    - `cd web && npm test` reports zero failures
    - `cd web && npx tsc --noEmit` exits 0 and `npm run lint` exits 0
    - `cd web && npx playwright test --workers=2 --retries=1` reports at most the two documented Phase 21 failures and zero others
    - `grep -c "❌ Wave 0" .planning/phases/37-ai-quote-planning/37-VALIDATION.md` returns 0
    - `grep -c "wave_0_complete: true" .planning/phases/37-ai-quote-planning/37-VALIDATION.md` returns 1
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm test && npx tsc --noEmit && npm run lint && npx playwright test --workers=2 --retries=1</automated>
  </verify>
  <done>The web suite, types and lint are green at the sanctioned worker count, and the validation map records where every behavior landed.</done>
</task>

</tasks>

<verification>
- `cd web && npx playwright test tests/phase-37-quote-ai.spec.ts --workers=2 --retries=1` green
- `cd web && npm test && npx tsc --noEmit && npm run lint` green
- `cd backend && .venv/bin/pytest -q` still green in one process
- 37-VALIDATION.md has no remaining Wave 0 markers
</verification>

<success_criteria>
- The suggest → review → send flow is proven end to end in a browser
- A `quotes.view`-only user gets no variance card, no deny panel and zero variance requests
- Both variance surfaces render for a permitted user
- The full web suite is green at `--workers=2 --retries=1`, with only the two documented Phase 21 failures
</success_criteria>

<output>
After completion, create `.planning/phases/37-ai-quote-planning/37-12-SUMMARY.md`
</output>
