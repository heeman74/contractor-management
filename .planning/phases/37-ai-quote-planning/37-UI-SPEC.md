---
phase: 37
slug: ai-quote-planning
status: approved
reviewed_at: 2026-07-30
shadcn_initialized: true
preset: base-nova (detected via existing web/components.json — not re-run this phase)
created: 2026-07-30
---

# Phase 37 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope note:** Phase 37 is **web-only** (mobile quote AI is deferred — 37-CONTEXT `<deferred>`). It ships **three surfaces**:

1. **The draft quote editor** (`/quotes/[id]/edit`) — the `Suggest line items` trigger, the AI line-item anatomy (confidence chip + cited basis + per-line review affordance), the cold-start refusal, the regenerate confirmation, and the unreviewed-lines banner.
2. **The quote detail page** (`/quotes/[id]`) — the send-blocked explanation, and a **`Quoted vs Actual` card behind an explicit double lock** (37-RESEARCH Trap 8: this page has **no** `FinanceGate` today).
3. **The project financials drill-down** (`/financials/[projectId]`) — a `Quoted vs Actual by Trade` card beside margin and budget.

Everything inherits the **"Job Ticket"** design system unchanged from the Phase 30/32/33/34/35/36 contracts. **Zero new hex values, zero new type sizes, zero new weights, zero new spacing values, zero new npm or shadcn dependencies.** Two new *class strings* are authored, both composed entirely from shipped tokens — see [The third confidence band](#the-third-confidence-band-the-trap-7-resolution).

**Pre-populated from:** 37-CONTEXT.md (D-01..D-14), 37-RESEARCH.md (Traps 1–8, Patterns 1–5, Pitfalls 1–10, the Variance Definition section, Open Questions 1–4), 36-UI-SPEC.md (palette, red rule, chip recipes, caption voice, AI-disclosure convention), 35-UI-SPEC.md via shipped constants, ROADMAP Phase 37 SC1–SC4, `~/.agents/skills/clean-code`, and direct reads of `components.json`, `globals.css`, `FinanceFlagChip.tsx`, `FinanceGate.tsx`, `hooks.ts`, `types.ts`, `financials-format.ts`, `MarginSummarySection.tsx`, `chart-card.tsx`, `chart-empty-state.tsx`, `scope-budget-bars.tsx`, `project-financials-dashboard.tsx`, `lib/format.ts`, `quotes/[id]/page.tsx`, `quote-actions-card.tsx`, `quote-status-alerts.tsx`, `quote-line-items-card.tsx`, `edit/page.tsx`, `line-items-table.tsx`, `sortable-line-item-row.tsx`, `_lib/quote-form.ts`, `_hooks/use-quote-editor.ts`, `_hooks/use-quote-detail.ts`, `types/api.ts` (2026-07-30). **User input required: none.**

**Checker corrections applied 2026-07-30** (0 blockers + 5 recommendations, all adopted):

1. **The Trap 8 double lock was self-contradictory** — the lock-1 snippet implied the card fetched, the implementation-shape section said the *page* owned the hook, and a page-owned hook is a **sibling** of the gate, which React cannot call conditionally. The layering claim ("the gate short-circuits the mount") was therefore false, and the stated mutation test was wrong: weakening `enabled` with the gate intact **would** have fired a request. Resolved by option (a) — a thin **`QuoteVarianceSection`** container owns the hook and is rendered **inside** `<FinanceGate>`, matching the shipped `/financials` precedent where `project-financials-dashboard.tsx` owns every hook as a child of the layout-mounted gate. Snippet, files table, implementation shape and the mutation-verification description all updated to match.
2. **`Limited history` was invisible on an unreviewed row** — a `bg-secondary` chip on a `bg-secondary` tint is 1.00:1 fill contrast with ink matching the adjacent marker, and a fresh medium-band suggestion is unreviewed *by definition*, making it the most common state. Both offered fixes adopted: the note recipe gains `border border-gray-300`, **and** the tint is now scoped to the input row only, leaving the sub-row on the white card surface.
3. **`fallback={null}` needed an explicit `undefined` check** — `fallback ?? <pulse/>` collapses omitted and `null` (`null ?? x === x`) and would have rendered the 256px pulse and the yellow deny panel on the quote page, breaking States 37/38. The guard form is now stated literally.
4. **The regenerate confirm button variant is now byte-locked** to `variant="default"`, so an executor cannot reach for `variant="destructive"` on a surface where `--destructive` is declared unused.
5. Cosmetic: `separator.tsx` added to the primitives row; the `space-y-4` parenthetical moved from the lg row to the md row; `text-gray-700`-on-white corrected 10.30 → **10.31**.

**The one thing this contract cannot lock:** the **qualitative half of each basis string** is AI-written. Per 37-RESEARCH Pattern 4 the **sample-count clause is composed server-side in code** and the AI writes only the interpretive half, so the basis is *partly* byte-assertable. This document locks every frame string byte-for-byte and bounds the AI half by **length and grounding only**. No plan task may assert byte-identity on the AI-authored half. Every other string here is a test constant.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `web/components.json` present; **no gate action, no new blocks installed**) |
| Preset | `style: base-nova`, `baseColor: neutral`, `iconLibrary: lucide`, `cssVariables: true`, `registries: {}` — read verbatim from `web/components.json` 2026-07-30 |
| Component library | base-ui (`@base-ui/react`) via existing wrappers — this phase uses only `card.tsx`, `button.tsx`, `input.tsx`, `select.tsx`, `alert.tsx`, `dialog.tsx`, `separator.tsx`, `skeleton.tsx`. **No new primitive.** The editor's shipped `@dnd-kit` sortable table and `react-hook-form` + `zod` form state are extended, never replaced. |
| Chart library | **none this phase** — variance is a signed comparison, which is a table, not a plot. `ChartCard` is reused as *furniture* (see the card contract). |
| Icon library | lucide-react — **`Bot`** (the codebase's established AI-provenance glyph: `ChatBubble.tsx`, `TypingIndicator.tsx`, the AI intake affordances on `projects/page.tsx` and `quotes/page.tsx`, and the Phase 36 finding card) and **`Scale`** for the quoted-vs-actual cards. `Sparkles`, `Wand`, `BrainCircuit` are deliberately **not** introduced — one meaning, one glyph. |
| Font | Geist Sans (body) per `globals.css`. No display face, no mono except the **shipped** `font-mono` line total in `sortable-line-item-row.tsx`, which is untouched. |
| Platform | **Web only.** No Flutter widget, no mobile string, no FCM template. Suggestion and variance are desk work. |

---

## Spacing Scale

Declared values (all multiples of 4) — unchanged from the Phase 32/33/34/35/36 contracts. This phase authors **no new value** and uses six of them:

| Token | Value | Usage this phase |
|-------|-------|------------------|
| xs | 4px | Chip↔basis gap (`gap-1`), caption stack rhythm (`space-y-1`), marker→basis offset (`mt-1`), shipped table cell inline padding (`px-1`) |
| sm | 8px | AI sub-row content gap (`gap-2`), shipped table cell vertical padding (`py-2`), banner→table offset (`mt-2`) |
| md | 16px | Notice/refusal block padding (`p-4`), banner block padding (`p-4`), card interior top padding (`pt-4`), variance table top offset (`mt-4`), card-level caption stack offset (`mt-4`), shipped sidebar card rhythm (`space-y-4`) |
| lg | 24px | Page section rhythm (`space-y-6`) — inherited |
| xl | 32px | In-card error block vertical padding (`py-8`) |
| 3xl | 64px | `ChartEmptyState` vertical padding (`py-16`) — shipped, reused unchanged |

**Exceptions (three, all inherited, none authored here):**

- `py-0.5` — **2px** vertical padding inside every chip recipe. The two new confidence recipes reuse it so all four finance chips share one geometry.
- `mt-0.5` — **2px** offset on the shipped inline field-error text in `sortable-line-item-row.tsx`. Untouched.
- `mt-3` — **12px** on the shipped `Add Row` button in `line-items-table.tsx`. Untouched; no new 12px value is authored.

---

## Typography

**No new size and no new weight.** The project-wide declared set stays **12 / 14 / 20 / 30** at authored weights **400 and 600** (500 and 700 are inherited). Phase 37 uses **four** sizes, and 30 only inside the shipped `ChartCard` KPI slot.

| Role | Size | Weight | Line Height | Phase-37 usage |
|------|------|--------|-------------|----------------|
| Caption / meta | 12px (`text-xs`) | 400 | 1.5 | Basis string, review-state markers (`Needs review` / `Accepted` / `AI-originated, user-edited`), the pricing-basis caption, the unburdened-labor caption, the scope-labor caption, the trigger's disabled explanation, the AI disclosure line, all three confidence chip labels, the variance interpretation line — all `text-gray-500` on the white card surface except where noted below |
| Caption / eyebrow | 12px (`text-xs`) | **600** | 1.5 | The shipped line-items table column headers (`font-semibold uppercase tracking-wide text-gray-500`) — the new **`Trade`** header joins them unchanged; and the variance table's own column headers, identical recipe |
| Body | 14px (`text-sm`) | 400 | 1.5 | Every table cell value, the notice/refusal body, the banner body, the dialog body, variance row labels and figures, `ChartEmptyState` body |
| Body / emphasis | 14px (`text-sm`) | **500 (`font-medium`)** | 1.5 | Notice and banner headings, `ChartEmptyState` heading (`text-muted-foreground` — the shipped recipe, reused byte-for-byte), the in-card error line (`text-red-800` — the shipped inline-finance-error weight) |
| Page heading | 20px (`text-xl`) | 600 | 1.2 | **Inherited, not authored here** — the editor and detail `<h1>` already exist |
| KPI | 30px (`text-3xl`) | **700** | 1.2 | **Inherited from `ChartCard` only** — the `Quoted vs Actual by Trade` card's `kpiValue` slot renders `text-3xl font-bold text-gray-900` because that is `ChartCard`'s own markup. This phase authors no 30px numeral of its own and **never colors that slot** (see the red rule). |

**Weight declaration — read this before writing a jest weight assertion.** The **authored** set is still **400 and 600**: 600 appears on table column headers only. **500 (`font-medium`) is an inherited weight** arriving with `ChartEmptyState` and the inline-error recipe. **700 is inherited** and appears only inside `ChartCard`'s KPI markup, which this phase does not modify.

**Money and percent inside AI text (hard rule, carried forward from 36).** The basis string arrives as an **already-formatted string from the backend**, grounded against the payload by the typed validator (37-RESEARCH Traps 4 & 5). The client **never** parses, re-formats, re-rounds or re-derives a figure out of it, and **never** runs `formatCurrency` over basis prose. `parseFloat` over a basis string appears nowhere in this phase. A client-side reformat would break the grounding audit trail — the stored payload JSONB proves what the *validated* text said.

**Variance figures are formatted, not computed.** `quoted`, `actual`, `variance` and `variancePercent` all arrive as backend Decimal-as-strings. The client applies `formatSignedCurrency` (sign leads the symbol) and `formatMarginPercent` (drops a trailing `.0`) and nothing else. **No client-side subtraction, no client-side percentage.** A second definition of variance is exactly the drift 34-02/35-05/35-06 recorded rules against.

**No numeric confidence score, anywhere (D-06).** The DOM must never contain a confidence percentage, a 0–10 score, a star rating, a progress bar, a `spreadRatio`, or a bare `comparableCount` field. The **only** number describing the evidence is the sample count **inside the server-composed basis prose** ("median of 7 comparable plumbing scopes") — which is evidence, not a self-graded score. A jest assertion pins the absence of any element carrying a confidence numeral.

---

## Color

**Zero new hex values.** Every value below is a Job Ticket CSS token or a Tailwind step already used by this codebase.

| Role | Value | Usage this phase |
|------|-------|------------------|
| Dominant (60%) | `--background` `#f7f6f2`, `--foreground` `#0e1726` | Page background, body ink (inherited) |
| Secondary (30%) | `--card` `#ffffff`, `--secondary` `#ece9e1`, `--border` `#e4e1d8` | Card surfaces, **the unreviewed input-row tint (`bg-secondary`)**, the refusal/notice block (`bg-secondary`), **the `Limited history` chip fill (`bg-secondary`)**, the `ChartCard` icon box, card hairlines |
| Accent (10%) | `--brand` `#f5a623` | **One new usage: the `Thin history` confidence chip**, via the shipped `FINANCE_FLAG_CHIP_CLASS` (`bg-brand/15 text-amber-900`). Nothing else on any Phase 37 surface is `--brand`. |
| Destructive | `--destructive` `#d64545` | **Not used this phase.** See the red rule below. |

**Accent reserved for:** checked permission toggles, active nav item + its 4px rail, focus rings, links, primary CTAs, the unrated-hours chip (32), the incomplete-data chip (33), the `Nearing budget` chip (34), the warning-band bullet-bar fill and the active trend-window button (35), the `Margin warning` severity chip (36), and — new this phase — the **`Thin history` confidence chip**. Nothing else. There are **no** amber backgrounds, borders or headings on the quote editor, and the unreviewed-row treatment is deliberately **not** amber (see below).

**The send-blocked alert's amber is a different scale and is not the accent.** `bg-amber-50 / border-l-4 border-amber-400 / text-amber-800` is the **shipped `QuoteStatusAlerts` expired-quote recipe** (`quote-status-alerts.tsx`), in place since Phase 25. Phase 37 reuses that class set byte-for-byte for the blocked-send banner rather than authoring a new alert skin. Reusing a shipped alert recipe is not authoring an accent usage; `--brand` is untouched by it.

### The red rule (inherited, size-based)

`#d64545` (`--destructive`) measures **4.38:1 on white** and therefore clears AA only at ≥24px text or as a non-text mark. **Every red glyph this phase authors is 12–14px**, so every one of them uses the `red-800` family:

| Red | Value | Applies to | Contrast |
|-----|-------|------------|----------|
| `text-red-800` on white | `#991b1b` | The over-quoted variance figure (14px), the in-card variance error line (14px, 500) | **8.31:1** ✓ |
| `text-red-800` on `bg-red-100` | `#991b1b` / `#fee2e2` | **Nothing this phase** — no alert-tier chip is authored on a quote surface | n/a |
| `text-destructive` | `#d64545` | **Nothing this phase** | n/a |

**Do not copy the Phase 33 `NEGATIVE_FIGURE_CLASS` recipe.** `MarginSummarySection.tsx:20` ships `text-sm font-semibold text-destructive` — a 14px destructive figure that predates the size-based red rule. It is shipped and stays untouched, but it is **not** the recipe for the variance figure. The new variance figure uses `text-red-800`. An executor grepping for "how does this codebase color a negative money figure" will find the Phase 33 line first; this paragraph exists so they do not propagate it.

**Green is not in this design system and is not introduced here.** A variance where actual cost came in **under** the quoted price is good news, and it renders `text-gray-900` — the same ink as any other figure. There is no green, no check glyph and no "on target" chip. Reserving loudness for problems is what makes the amber chip mean something.

### The third confidence band (the Trap 7 resolution)

37-RESEARCH Trap 7 is correct: `FinanceFlagChip.tsx` ships exactly two recipes and **both mean "bad"** — amber = "there is a data gap here", red = "an alert tier". D-06 needs **three** bands, and the shipped vocabulary has no "good".

**The resolution is to invert what the chip grades.** The chip does **not** grade the number's correctness — it names **how much history stands behind it**. Loudness therefore rises as evidence thins, which is exactly the shipped amber semantic, and no band ever reads as an endorsement. This is also why none of the three labels contains the word "confidence": a chip reading "High confidence" on an unreviewed AI price is precisely the false comfort SC2 exists to prevent.

| Band (code-computed, D-05) | Chip label | Chip class | Composition | Why |
|---|---|---|---|---|
| `high` | **`Strong history`** | `QUOTE_CONFIDENCE_CHIP.high` → `FINANCE_OUTLINE_CHIP_CLASS` = `rounded-full border border-gray-300 px-2 py-0.5 text-xs text-gray-600` | **NEW class string, zero new hexes.** Chip geometry byte-identical to the two shipped finance chips; border + ink lifted from the **shipped revision badge** on `quotes/[id]/page.tsx:83` (`border border-gray-300 … text-xs text-gray-600`) | The quietest possible mark. It states the evidence is thick and then gets out of the way. Never green, never filled — nothing in this design system says "good", and a filled all-clear badge on an unreviewed AI number is the exact defect SC2 targets. |
| `medium` | **`Limited history`** | `QUOTE_CONFIDENCE_CHIP.medium` → `FINANCE_NOTE_CHIP_CLASS` = `rounded-full border border-gray-300 bg-secondary px-2 py-0.5 text-xs text-gray-700` | **NEW class string, zero new hexes.** `bg-secondary` is the Job Ticket 30% token; `border-gray-300` is the same edge the outline recipe uses; `text-gray-700` is the Phase 36 eyebrow ink | Not a warning, not an all-clear. It carries visible weight against the white card without borrowing a signal hue. **The border is load-bearing:** the `bg-secondary` fill measures only **1.21:1** against the white card and **1.00:1** against another `bg-secondary` surface, so without an edge the chip would dissolve into whatever sits behind it. The border keeps it a shape everywhere it can appear. |
| `low` | **`Thin history`** | `FINANCE_FLAG_CHIP_CLASS` — `rounded-full bg-brand/15 px-2 py-0.5 text-xs text-amber-900` | **Shipped, imported byte-for-byte** | Literally a data-quality flag, which is the recipe's stated purpose. Thin or wildly-spread comparables *are* a data gap. Same hue, same meaning, same class string as the unrated-hours and incomplete-data chips. |

**Red is not available on this surface.** A suggestion is not an alert. `FINANCE_ALERT_CHIP_CLASS` appears nowhere in Phase 37.

**Where the recipes live (the DRY split, matching the 36-06 precedent).** The two new class constants are exported from **`features/finance/components/FinanceFlagChip.tsx`**, beside `FINANCE_FLAG_CHIP_CLASS` and `FINANCE_ALERT_CHIP_CLASS` — that module is the codebase's one chip-recipe home, and a four-step weight ladder (`OUTLINE` → `NOTE` → `FLAG` → `ALERT`) in one file is what stops a fifth recipe being invented. The **band → `{label, className}` map** lives with the feature that owns the bands (`quotes/[id]/edit/_lib/confidence-band.ts`), exactly as the Phase 36 `SEVERITY_CHIP` map lives in the finding card while its red recipe lives in `FinanceFlagChip`. Recipe is shared; meaning is local.

**Band name = chip label = accessible name, one name per band** (the Phase 35 rule: the same condition must never have two names). The chip renders a plain `<span>` with the mapped class — it does **not** use the `FinanceFlagChip` component, which hardcodes the amber recipe. This matches how the Phase 36 finding card renders its severity chip.

### The unreviewed-line treatment (deliberately not accent)

An unreviewed AI line is marked by **three redundant, non-hue signals**:

1. **Input-row tint** — the line's **input row** carries `bg-secondary` (#ece9e1). Reviewed rows return to the white card surface. A block of pending work is visible at a glance and visibly shrinks as it is cleared.
2. **A text marker** — `Needs review`, always rendered on the AI sub-row, `text-xs text-gray-700` (10.31:1 on the white sub-row surface). It is a step heavier than the `text-gray-500` basis beneath it, so the pending state outranks the evidence supporting it.
3. **The `Accept` button's presence** — it exists only while the line is unreviewed.

**The tint stops at the input row; the AI sub-row stays on the white card surface.** This is deliberate and load-bearing. A `bg-secondary` chip on a `bg-secondary` surface is **1.00:1** fill contrast, and its `text-gray-700` ink matches the adjacent `Needs review` marker — so a tinted sub-row would have rendered the `Limited history` band as two undifferentiated gray strings. A medium-band suggestion is unreviewed **by definition** on arrival, making that the single most common state on the surface. The block still reads as one unit because the tinted input row sits directly above its own sub-row and the marker names the state in words. Belt and braces: the note recipe carries `border border-gray-300` regardless, so neither neutral chip is ever edgeless on any surface it may later appear on.

**No amber rail, no red border, no colored row.** Amber stays chip-only so `Thin history` keeps one meaning; and a row that is *both* unreviewed *and* thin-history would otherwise carry the same hue twice for two different reasons. Meaning is never carried by color alone here (WCAG 1.4.1) — the marker text alone is sufficient.

**Grays:** `text-gray-900` for figures and table values, `text-gray-700` for the `Needs review` marker and the `Limited history` chip, `text-gray-600` for the `Strong history` chip, `text-gray-500` for every caption and the reviewed-state markers, `text-muted-foreground` for empty states — matching shipped conventions exactly.

---

## Copywriting Contract

### Standard elements

| Element | Copy |
|---------|------|
| Primary CTA | **`Suggest line items`** (first run) / **`Suggest again`** (once the quote holds at least one AI line). Pending label: **`Analyzing history...`** — the shipped `Saving...` / `Downloading PDF...` ASCII-ellipsis voice. |
| Empty state heading | `Not enough {tradeName} history yet` (cold start) · `No suggestions this time` (grounding drop) · `Add a trade to a line first` (unresolvable trade) · `No completed work to compare` (variance, drill-down) · `Not comparable yet` (variance, quote detail) |
| Empty state body | See the fixed-string tables below — every one names **why** it is empty and **what would change it**, never an all-clear. |
| Error state | `Couldn't generate suggestions. Try again.` (suggest/regenerate mutation) · `Couldn't load quoted vs actual. Refresh to try again.` (variance query) |
| Destructive confirmation | **`Replace unreviewed suggestions?`** — the regenerate dialog. It is the phase's only destructive action; see the table below. |

### Fixed strings — the trigger and its refusals (verbatim; jest and Playwright assert these)

| String | Where |
|--------|-------|
| `Suggest line items` | Trigger button label, `variant="outline" size="sm"`, `Bot` icon, in the `Line Items` `CardHeader` right slot |
| `Suggest again` | Same button once `aiLineCount > 0` |
| `Analyzing history...` | Same button while the mutation is pending; button `disabled` |
| `Save this draft first — suggestions read the quote's trade.` | 12px `text-gray-500` caption beneath a disabled trigger on `/quotes/new/edit` (no quote id exists yet) |
| `Save your changes first — suggesting rewrites the line items.` | 12px `text-gray-500` caption beneath a disabled trigger while `form.formState.isDirty` |
| `Not enough {tradeName} history yet` | Refusal notice heading (14px/500) when the backend returns `refusalReason: "insufficient_history"` |
| `AI suggestions need at least {requiredCount} completed {tradeName} jobs with recorded costs and an issued invoice. You have {comparableCount}. Record costs as work happens and invoice completed jobs — suggestions turn on by themselves.` | Refusal notice body (14px/400). **`requiredCount` and `comparableCount` are rendered from the response, never from a client constant** — the threshold is a backend constant (D-09, Claude's discretion) and must have exactly one home. |
| `Add a trade to a line first` | Refusal notice heading when the backend returns `refusalReason: "trade_unresolved"` (a project-level quote whose lines all carry an empty `field`) |
| `Suggestions match a line's trade against your completed work in that trade. Fill in the Trade column on at least one line, save the draft, then try again.` | Body for that variant |
| `No suggestions this time` | Refusal notice heading when the backend returns `refusalReason: "ungrounded"` — every drafted line cited a figure absent from the payload's allowed set and the whole set was dropped (fail closed) |
| `The AI's draft cited figures that aren't in your recorded history, so it was discarded rather than shown. Try again, or build the lines by hand.` | Body for that variant. **This is SC3 made visible**: the user learns the guard fired, not that "nothing happened". |
| `Couldn't generate suggestions. Try again.` | `toast.error(..., { duration: Infinity })` on a transport/5xx failure — the shipped editor-mutation error idiom (`use-quote-editor.ts`) |

### Fixed strings — the AI line-item anatomy

| String | Where |
|--------|-------|
| `Strong history` / `Limited history` / `Thin history` | The confidence chip label — one per band, per the map above |
| `Needs review` | Review marker on an unreviewed AI line (12px `text-gray-700`) |
| `Accepted` | Review marker once the line is accepted (12px `text-gray-500`) |
| `AI-originated, user-edited` | Review marker once any priced field of an AI line changes (12px `text-gray-500`). **D-08's exact words** — this marker is what lets the variance loop tell AI-seeded quotes from hand-built ones, so it is load-bearing and never abbreviated. |
| `Accept` | The per-line review button, `variant="outline" size="sm"`, `aria-label="Accept suggested line {n}"` where `{n}` is the 1-based row position |
| `Basis hidden — requires finance access.` | Replaces the basis line (and suppresses the chip entirely) for a `quotes.edit` holder **without** `finance.view`. See the permission bound below. |
| `Suggested prices come from what you charged for comparable work — not from your recorded cost. The cost comparison is in each line's basis.` | **D-13 stated once**, card-level caption (12px `text-gray-500`), rendered whenever the quote holds ≥1 AI line **and** the viewer holds `finance.view`. Once per card, never per row. |
| `Unburdened labor: Wage cost only — excludes payroll tax, insurance, overhead.` | Card-level caption when any AI line has `item_type === "labor"`. **Composed from the two shipped exported constants** — `` `${UNBURDENED_TITLE}: ${UNBURDENED_BODY}` `` from `CostBreakdownSummary.tsx` — so PITFALLS #2's caveat can never carry two texts. |
| `AI-suggested from your own completed work — every figure is from your recorded history, never an AI estimate.` | Card-level AI disclosure, **always last** in the caption stack whenever the quote holds ≥1 AI line. The Phase 36 disclosure sentence, re-voiced for suggestions. |

**The basis string itself is rendered verbatim** (12px `text-gray-500`), never truncated, never clamped, no "read more". Locked example shape (37-CONTEXT `<specifics>`): `median of 7 comparable plumbing scopes; past plumbing quotes ran 12% under actual`. The clause before the semicolon is **server-composed in code** from the comparable count; the clause after it is AI-written and grounding-validated. Bound: **≤ 200 characters**, enforced by the prompt contract **and** a DB `CHECK`, **rejected whole rather than shortened** (the Phase 36 rule — a shortened grounded sentence can lose the very figure that justifies it).

### Fixed strings — the unreviewed banner and the blocked send

| String | Where |
|--------|-------|
| `1 AI-suggested line still needs review` | Editor banner heading (14px/500) when `unreviewedCount === 1` |
| `{N} AI-suggested lines still need review` | Editor banner heading when `unreviewedCount > 1` |
| `Accept each line as-is or edit it, then save the draft. A quote can't be sent while any suggested line is unreviewed.` | Editor banner body (14px/400). It names the **save** step because review state travels with the draft — see the review-state contract. |
| `Review the AI-suggested line items before sending` | Quote-detail blocked alert heading (`<strong>`, the shipped `QuoteStatusAlerts` idiom) |
| `1 suggested line hasn't been accepted or edited yet. The check runs on the server, so a quote can't be sent from any screen until every suggestion is reviewed.` | Blocked alert body when `unreviewedCount === 1` |
| `{N} suggested lines haven't been accepted or edited yet. The check runs on the server, so a quote can't be sent from any screen until every suggestion is reviewed.` | Blocked alert body when `unreviewedCount > 1` |
| `Review Line Items` | Blocked alert's action button → `/quotes/{id}/edit` |
| `This quote has AI-suggested line items that have not been reviewed. Accept or edit every suggested line before sending.` | **The backend 409 `detail`** (`UNREVIEWED_AI_LINES_DETAIL`, 37-RESEARCH). User-visible, so it is locked here. The client surfaces it verbatim through the shipped `mutationErrorMessage` idiom — it must **not** be swallowed by the generic `Failed to send quote. Try again.` toast. |

**Why the button is disabled *and* explained.** D-07 is explicit that the UI must explain WHY, not just disable. The `Send Quote` button is `disabled` (so a click cannot burn a request into a guaranteed 409) **and** the always-visible banner above it names the count, the remedy and the fact that the rule is server-side. The button carries `aria-describedby` pointing at the banner, so the reason is announced with the control rather than sitting silently beside it. The 409 path stays live and honestly worded for a stale client, which is the case that proves the disable was never the guarantee.

### Fixed strings — the regenerate confirmation (the phase's only destructive action)

| Element | Copy |
|---------|------|
| Dialog title | `Replace unreviewed suggestions?` |
| Body (`unreviewedCount === 1`) | `1 unreviewed suggestion will be replaced with a fresh one. Lines you've accepted or edited stay exactly as they are.` |
| Body (`unreviewedCount > 1`) | `{N} unreviewed suggestions will be replaced with fresh ones. Lines you've accepted or edited stay exactly as they are.` |
| Confirm button | `Replace Suggestions` — **`<Button variant="default">`, byte-locked.** `variant="destructive"` is **forbidden**: this contract declares `--destructive` unused on every Phase 37 surface, and the shipped `Replace Line Items` template dialog — the exact composition being reused — uses the default variant for its confirm. The dialog's *words* carry the warning; the button does not need to shout it in a hue this phase does not own. |
| Cancel button | `Cancel` — `variant="outline"` |

The dialog reuses the shipped template-replace `Dialog` composition in `edit/page.tsx` verbatim (`DialogContent showCloseButton` → `DialogHeader`/`DialogTitle` → body `<p className="text-sm text-gray-600">` → `DialogFooter` with outline Cancel + default confirm). **It appears only when `unreviewedCount > 0`** — regenerating with nothing to destroy is not destructive and goes straight through.

### Fixed strings — `Quoted vs Actual` (quote detail, sidebar)

| String | Where |
|--------|-------|
| `Quoted vs Actual` | Card title; card `aria-label="Quoted vs Actual card"` |
| `Quoted (pre-tax)` | Row label. **`(pre-tax)` is load-bearing** — D-13/Phase 33 basis; a reader must not compare a tax-inclusive total against cost. |
| `Actual cost` | Row label |
| `Variance` | Row label |
| `Actual cost ran {amount} ({percent}%) above this quote's pre-tax price.` | Interpretation line when `variance > 0` (12px `text-gray-500`) |
| `Actual cost came in {amount} ({percent}%) below this quote's pre-tax price.` | Interpretation line when `variance < 0` |
| `Actual cost matched this quote's pre-tax price.` | Interpretation line when `variance === 0` |
| `Not comparable yet` | Empty-state heading (14px/500 `text-muted-foreground`) |
| `Quoted vs actual appears once the work from this quote has been invoiced.` | Empty-state body |
| `Not yet invoiced` | A per-trade row's value on a project-level quote whose matched job carries no invoice (12px `text-gray-500`) |
| `Scope spend excludes labor — labor is tracked at job level.` | Caption on a **trade-scope-anchored** quote. **Byte-identical to the shipped Phase 35 string**, imported from `scope-budget-bars.tsx` (`LABOR_NOTE` promoted to `export`, value unchanged) — one condition, one text. |
| `Unburdened labor: Wage cost only — excludes payroll tax, insurance, overhead.` | Caption whenever the actual-cost figure includes derived labor. Composed from the same two shipped constants. |
| `Couldn't load quoted vs actual. Refresh to try again.` | In-card error line (14px/500 `text-red-800`) |

### Fixed strings — `Quoted vs Actual by Trade` (`/financials/[projectId]`)

| String | Where |
|--------|-------|
| `Quoted vs Actual by Trade` | `ChartCard` title; `ariaLabel="Quoted vs Actual by Trade chart"` (the shipped `ChartCard` aria convention on that page) |
| `{percent}% over quoted` | `kpiValue` when the project's actual cost exceeds quoted revenue |
| `{percent}% under quoted` | `kpiValue` when it is below |
| `Matched quoted` | `kpiValue` at exactly zero variance |
| `No invoiced work yet` | `kpiValue` when no anchor in the project has an invoice |
| `Trade scope` / `Quoted` / `Actual` / `Variance` | Table column headers (12px/600 uppercase, the shipped line-items header recipe) |
| `Project total` | The final, hairline-separated summary row's label |
| `No completed work to compare` | `ChartEmptyState` heading |
| `Quoted vs actual appears once a trade's work has been invoiced.` | `ChartEmptyState` body |
| `quoted-vs-actual-by-trade.csv` | `csvFilename` |
| `Trade scope,Quoted (pre-tax),Actual cost,Variance,Variance percent` | CSV header row |
| `Scope spend excludes labor — labor is tracked at job level.` | Caption below the table, imported from the shipped constant |
| `Unburdened labor: Wage cost only — excludes payroll tax, insurance, overhead.` | Caption below the table when any row's actual includes derived labor |

**Copy rules honored:** no lorem ipsum; no "No data" / "Something went wrong"; every error line states the recovery step; every empty state names why it is empty and what would change it, never an all-clear; every honesty caption states the *consequence*, not just the fact. Sentence case with terminal periods on captions, bodies and notes; titles, chip labels, button labels and column headers carry no terminal period — the Phase 32/33/34/35/36 convention.

---

## Component & Screen Contract

### Surface 1 — the draft quote editor (`/quotes/[id]/edit`)

#### Line Items card anatomy

```
Card
  CardHeader                         flex items-center justify-between
    CardTitle  "Line Items"
    right:  [Button outline sm: (Bot) Suggest line items]     ◀── NEW, D-10 gated
            <p text-xs text-gray-500>{disabled reason}?       ◀── NEW, conditional

  CardContent
    [SuggestionNotice]?              ◀── NEW: refusal / grounding-drop, rounded-lg bg-secondary p-4
    [UnreviewedBanner]?              ◀── NEW: rounded-lg bg-secondary p-4, when unreviewedCount > 0

    <table min-w-[700px]>            (min-w-[840px] when the Trade column shows)
      thead: [ ] Type | Description | Qty | Unit | Unit Price | [Trade]? | Total | [ ]
      tbody: per line —
        ── input row ────────────────  bg-secondary when unreviewed, else default
           the SHIPPED SortableLineItemRow, unchanged except the new Trade cell

        ── AI sub-row ───────────────  AI lines only; <tr><td colSpan={all}>
           ALWAYS on the white card surface — never tinted (see the chip-edge note)
           line 1:  [Bot h-3 w-3 text-foreground/70] [confidence chip] [review marker]
                                                                   … [Accept button]?
           line 2:  <p text-xs text-gray-500>{basis}
    </table>

    [Add Row button]                 shipped, unchanged

    ── card caption stack ──────────  mt-4 space-y-1 text-xs text-gray-500
       [pricing-basis caption]       when aiLineCount > 0 && finance.view
       [unburdened-labor caption]?   when any AI line is item_type "labor"
       [AI disclosure line]          when aiLineCount > 0, always last
```

**Why a sub-row rather than more columns.** The basis string runs to ~200 characters. A seventh data column would either truncate it (forbidden — see the length contract) or collapse the shipped price inputs below usable width on a 700px-min table. A full-width sub-row keeps the shipped input row byte-identical, reads as a continuation of the line it annotates, and gives the chip, the marker and the `Accept` button one predictable home.

**Why the sub-row is never tinted.** See [the unreviewed-line treatment](#the-unreviewed-line-treatment-deliberately-not-accent): a `bg-secondary` chip on a `bg-secondary` surface is 1.00:1, and the medium band's default state is unreviewed. The tint stays on the input row, where there is no chip to swallow.

**The `Trade` column.** Shown **only for project-level quotes** (`job_id === null && trade_scope_id === null`), where `QuoteLineItem.field` is the trade key (37-RESEARCH Trap 3's ladder). A plain `Input`, `w-[140px]`, placeholder `e.g. Plumbing`. For job-anchored and scope-anchored quotes the trade comes from the anchor, so a per-line trade input would be a second source of truth — there, `field` round-trips as a **hidden form value** and no column renders. This is the Trap 2 fix: `field` must survive `mapQuoteToFormValues` → `buildQuotePayload` on **every** quote kind, and the editor must never again strip it.

#### The review-state contract (D-07, D-08)

```ts
type QuoteLineReviewState = "unreviewed" | "accepted" | "edited";

// Extends the shipped QuoteLineItem in types/api.ts
interface QuoteLineItem {
  // …shipped fields, plus id and field which already exist…
  ai_origin: boolean;
  review_state: QuoteLineReviewState;
  confidence_band: "high" | "medium" | "low" | null;  // null without finance.view
  basis: string | null;                                // null without finance.view
}
```

- **Review state is form state, persisted by `Save Draft`.** `Accept` is a **local form mutation, not a network call**. The editor's shipped reset effect is keyed on the `["quote", id]` query (`use-quote-editor.ts`), so *any* mid-edit cache write — an accept mutation's `invalidateQueries`, or a `setQueryData` — would fire `reset()` and silently discard every unsaved line edit. Local form state is the only design that does not race the shipped architecture. The banner body says "then save the draft" precisely because of this.
- **A changed AI line is marked `edited` server-side, not client-side.** On PATCH, the backend compares each `ai_origin` line's priced fields (`description`, `quantity`, `unit`, `unit_price`, `field`) against the stored suggestion and writes `review_state = "edited"` when any differ — regardless of what the client sent. An edit can never be laundered as an untouched acceptance.
- **No bulk-approve control exists in the DOM.** No "Accept all", no header checkbox, no select-all, no keyboard chord. A jest assertion pins the absence: with 12 unreviewed AI lines rendered, exactly 12 elements match the accept affordance and no element's accessible name matches `/accept all|approve all|select all/i`.
- **Stated bound (so a later phase does not mistake this for more than it is):** a scripted client could PATCH every line with an unchanged body and no field edits, leaving them accepted. SC2's guarantee is that **no affordance exists** to rubber-stamp, and that **the send gate is backend-enforced** — not that the API is unautomatable. The keystone is the 409 from `POST /quotes/{id}/send`, never the disabled button.

#### Trigger gating (D-10)

The trigger renders only when **all** hold: `quote.status === "draft"`, the viewer holds `quotes.edit` (already true — the editor route requires it), **and** the viewer holds `finance.view`. A viewer without `finance.view` simply does not see the button — a missing affordance, **not** a `FinanceGate` deny panel, because the quote editor is a legitimate non-finance surface and a yellow denial block would be the wrong story. There is no zero-request concern: the trigger is a mutation and fires only on click.

**The finance boundary on already-suggested lines.** `basis` cites money and percents derived from actual cost history, and `confidence_band` grades that history — both are `finance.view` data (Phase 30 D-06). A `quotes.edit` holder **without** `finance.view` opening an AI-seeded quote receives `confidence_band: null` and `basis: null` from the API (backend-scrubbed, not client-hidden), and the sub-row renders **no chip** and the locked caption `Basis hidden — requires finance access.` in place of the basis. They can still accept or edit lines — description, quantity and price are quote fields they already own, and blocking them would make an AI-seeded quote unsendable by anyone but a finance holder. This is a deliberate bound, stated so it is a decision rather than a leak.

### Surface 2 — the quote detail page (`/quotes/[id]`)

#### Placement

```
<div class="space-y-6">
  <h1>Quote #QT-XXXXXX — {job}</h1> <StatusBadge/>

  QuoteStatusAlerts                              ◀── NEW blocked-send branch
  <div class="grid lg:grid-cols-[1fr_360px] gap-8">
    main:     QuoteLineItemsCard                 ◀── chip + basis, read-only
              Admin Notes
              QuoteActivityCard
    sidebar:  QuoteActionsCard                   ◀── Send Quote disabled + aria-describedby
              [Project card]?
              [QuoteContractCard]?
              QuoteDetailsCard
              Financial Summary
              FinanceGate fallback={null}        ◀── NEW lock 1 — render
                └─ QuoteVarianceSection          ◀── NEW — owns the hook (lock 2 — fetch)
                     └─ QuoteVarianceCard        ◀── presentational, no hook
              [LinkedInvoiceCard]?
```

`QuoteLineItemsCard` renders AI lines read-only with the confidence chip and basis beneath each line (same sub-row shape, no `Accept` button, no row tint — review happens in the editor). The variance block sits **after** `Financial Summary` and **before** `LinkedInvoiceCard`: quoted totals first, then what the work actually cost, then the invoice that settled it.

#### The double lock (37-RESEARCH Trap 8 — the phase's finance-leak keystone)

`/quotes/[id]` has **no `FinanceGate`**: `FinanceGate` is mounted once, at `financials/layout.tsx`, and this page renders for any `quotes.view` holder. Putting actual-cost data here without an explicit gate is PITFALLS #4 word for word. Two locks, **both required** — and, critically, **layered rather than sibling**:

```tsx
// quotes/[id]/page.tsx — the page mounts the GATE ONLY. It owns no variance hook.
<FinanceGate fallback={null}>
  <QuoteVarianceSection quoteId={quote.id} isApproved={quote.status === "approved"} />
</FinanceGate>
```

```tsx
// quotes/[id]/_components/quote-variance-section.tsx — NEW. The thin container that
// owns the hook. It exists so the hook is a CHILD of the gate rather than a sibling of
// it: a hook called in page.tsx runs unconditionally on every render whatever
// FinanceGate decides, which would leave the gate worth nothing for the request half.
// This mirrors the shipped /financials layering, where project-financials-dashboard.tsx
// owns every hook as a child of the layout-mounted gate.
export function QuoteVarianceSection({ quoteId, isApproved }: Props) {
  const variance = useQuoteVariance(quoteId, isApproved);
  return <QuoteVarianceCard {...toCardProps(variance)} />;
}
```

```ts
// features/finance/hooks.ts, beside the four shipped financial hooks.
export function useQuoteVariance(quoteId: string, isApproved: boolean) {
  const { can } = usePermissions();
  return useQuery({
    queryKey: ["cost-entries", "financials", "quote-variance", quoteId],
    queryFn: () => fetchQuoteVariance(quoteId),
    // Not optional. FinanceGate stops the render; this stops the request. Without it an
    // unauthorized visit still issues the call and the SC2 zero-request assertion would
    // pass for the wrong reason.
    enabled: can(FINANCE_VIEW_PERMISSION) && !!quoteId && isApproved,
  });
}
```

- **`fallback` semantics (locked, with the guard form spelled out).** `FinanceGate` gains one optional prop: a node rendered **both** while permissions are loading **and** when the permission is absent. Omitting it must be byte-identical to the shipped behaviour — the 256px pulse, then the deny panel. Because `/quotes/[id]` passes `null`, the implementation **must** discriminate on `undefined`, never on falsiness:

  ```tsx
  // CORRECT — omitted and null are different answers.
  const denied  = fallback !== undefined ? fallback : <DenyPanel />;
  const loading = fallback !== undefined ? fallback : <div className="h-64 animate-pulse rounded-xl bg-muted" />;

  // WRONG — `null ?? x === x`, so the quote page would render the 256px pulse and the
  // yellow deny panel in its sidebar, breaking States 37 and 38.
  const denied = fallback ?? <DenyPanel />;
  ```

  `/quotes/[id]` passes `null` because a sidebar card the user may not be entitled to must never reserve layout, flash a skeleton, or announce its own absence with a yellow panel on a page they legitimately own. One gate component in the codebase, two call sites, one responsibility ("what renders when the gate is not open"). `financials/layout.tsx` is untouched.
- **The locks are layered, and the mutation verification reflects that.** `FinanceGate` short-circuits before `QuoteVarianceSection` mounts, so with the gate intact the hook never runs and no request is possible. Verify in this order, in the 36-02 / 35-11 style:
  1. **Delete `FinanceGate`** → the card renders for a `quotes.view`-only user → the *card-absent* assertion (State 38) fails. Restore.
  2. **With the gate deleted**, weaken `enabled` to `!!quoteId` → exactly one request to `/api/v1/quotes/*/variance` → the *zero-request counter* assertion fails. Restore both.

  The request half is only observable once the render half is already broken — **that is precisely what makes `enabled` a second independent lock rather than a redundant one**, and the spec file must carry that sentence in-file so a later reader does not "simplify" the hook's `enabled` away as dead code. The proof that `enabled` holds on its own lives in the hook's unit test, which drives it directly without the gate (the 36-02 precedent).
- **Backend half (restated because the UI test cannot see it):** the variance endpoint calls `require_permission("finance.view")`. A UI gate over an ungated endpoint is not a lock.

#### Card anatomy

```
Card  aria-label="Quoted vs Actual card"  data-testid="quote-variance"
  CardHeader > CardTitle  "Quoted vs Actual"   [Scale h-4 w-4 in rounded-md bg-secondary p-2]
  CardContent  space-y-2
    row  Quoted (pre-tax)   $12,400.00            text-sm, label gray-500 / value gray-900
    row  Actual cost        $13,640.00            text-sm
    <Separator/>
    row  Variance           +$1,240.00 · 10%      text-sm font-semibold,
                                                  text-red-800 when > 0, else text-gray-900
    <p mt-1 text-xs text-gray-500>  {interpretation line}
    ── caption stack ──────  mt-4 space-y-1 text-xs text-gray-500
       [scope-labor caption]?      trade-scope-anchored quotes only
       [unburdened-labor caption]? when the actual includes derived labor
```

**Project-level quotes (D-14).** The card becomes a small table: one row per `field` group matched to the per-field job that `_convert_project_quote` created at approval, plus a hairline-separated `Project total` row. A group whose matched job carries no invoice renders `Not yet invoiced` in its value column rather than a zero — a missing comparison is not a variance of nothing. **Phase 33's D-14 last-resort project-quote rule is deliberately not reused**: it governs revenue *resolution*, a different question, and a blended whole-project number would defeat the per-trade feedback loop this phase exists to close.

**No band chip on variance.** The sign, the ink and the interpretation sentence carry the whole signal. A fourth chip family on a surface that already carries three confidence bands would dilute all of them.

### Surface 3 — `/financials/[projectId]`

```
FinancialsLayout -> FinanceGate
  <div class="space-y-6">
    <Link>← Financials</Link> <h1>{project}</h1> <StatusBadge/>
    FinanceSummaryTiles                    [Revenue] [Cost] [Margin]
    ProfitabilityFindingCard               (Bot)      — Phase 36
    ChartCard "Margin Trend"               (TrendingUp)
    ChartCard "Quoted vs Actual by Trade"  (Scale)    ◀── NEW, full width
    <div class="grid md:grid-cols-2 gap-8">
      ChartCard "Budget vs Actual by Trade Scope"  (Gauge)
      ChartCard "Cost Category Mix"                (PieChart)
```

**Below the trend, above the two-up grid.** Reading order becomes **figures → interpretation → evidence over time → the quoting feedback loop → budget and cost detail**. Placing it inside the two-up grid would orphan a third card; placing it above the trend would put a backward-looking comparison ahead of the tiles' own evidence.

**Why `ChartCard` here but not in Phase 36.** The Phase 36 finding card refused `ChartCard` because a finding has no headline figure and nothing tabular to export. Variance has both: a genuine project-level KPI and a per-trade table that a contractor tuning their pricing will want in a spreadsheet. Reusing the shipped furniture buys the icon box, the title typography, the KPI slot and CSV export with no new component. Its `min-h-[300px]` child floor is inherited unchanged — the same floor the shipped bullet chart lives with.

**The KPI slot is never colored.** `ChartCard` renders `text-3xl font-bold text-gray-900` and this phase does not modify it. A 30px `text-destructive` numeral would technically clear the red rule, but the shipped card family has exactly one KPI ink and forking it for one card would make that card read as an alert panel.

**Hook ownership on this page is unchanged in shape.** `project-financials-dashboard.tsx` — already the only hook-owning component on the route, and already a child of the layout-mounted `FinanceGate` — takes the fourth hook. Its docstring rule holds: **four queries, four keys, four failure surfaces**, and the variance query stays **out** of the page loading gate. No new container is needed here; the layering this route needs already exists, which is exactly the precedent `QuoteVarianceSection` copies.

### Response contracts

```ts
// GET /api/v1/quotes/{quoteId}  — line items gain four fields (see above)

// POST /api/v1/quotes/{quoteId}/suggest-line-items
interface QuoteSuggestionResult {
  refusalReason: "insufficient_history" | "trade_unresolved" | "ungrounded" | null;
  tradeName: string | null;        // present for insufficient_history
  comparableCount: number | null;  // present for insufficient_history — RENDERED, never re-derived
  requiredCount: number | null;    // present for insufficient_history — the backend's one threshold
  suggestedLineCount: number;      // 0 on any refusal
}

// GET /api/v1/quotes/{quoteId}/variance
interface QuoteVariance {
  quoted: string | null;            // pre-tax, D-13 basis. null = nothing to compare
  actual: string | null;
  variance: string | null;          // actual - quoted; POSITIVE means under-quoted
  variancePercent: string | null;   // one decimal from the backend, never re-derived
  laborIncluded: boolean;
  scopeAnchored: boolean;           // drives the scope-labor caption
  trades: QuoteVarianceTrade[];     // [] for job/scope quotes; one per field group otherwise
}

// GET /api/v1/projects/{projectId}/financials/quote-variance
interface ProjectQuoteVariance {
  scopes: QuoteVarianceTrade[];
  total: QuoteVarianceTrade;
  laborIncluded: boolean;
  hasScopeAnchoredRows: boolean;
}

interface QuoteVarianceTrade {
  label: string;
  quoted: string | null;
  actual: string | null;
  variance: string | null;
  variancePercent: string | null;
}
```

- **Money and percent stay strings end-to-end.** Displayed verbatim through `formatSignedCurrency` / `formatMarginPercent`, never re-summed, never re-divided.
- **`null` means honest absence, never zero.** A trade with no invoiced work renders `Not yet invoiced`, not `$0.00`.
- **Sign convention (locked):** `variance = actual − quoted`, so **positive means the work cost more than it was quoted for** — i.e. under-quoted, i.e. the direction that hurts. This is the algebraic negative of margin percent when revenue resolves to `quoted`; it is derived from `margin_math`'s existing helpers rather than being a third definition (37-RESEARCH, Variance Definition).

### Implementation shape (clean-code / SRP)

- **Presentational components never fetch; the container that owns a hook renders inside its gate.** `QuoteVarianceCard` takes parsed props and owns nothing. `QuoteVarianceSection` is the thin container that owns `useQuoteVariance`, and it exists **only** so the hook is a child of `FinanceGate` rather than a sibling of it — a hook in `page.tsx` runs on every render whatever the gate decides. `SuggestionNotice`, `UnreviewedBanner`, `AiLineSubRow` and `ConfidenceChip` are pure and hold no state.
- **Intention-revealing names:** `ConfidenceChip`, `AiLineSubRow`, `SuggestionNotice`, `UnreviewedBanner`, `QuoteVarianceSection`, `QuoteVarianceCard`, `QuoteVarianceTable`, `useQuoteSuggestions`, `useQuoteVariance`, `unreviewedAiLineCount`, `varianceKpi`, `varianceInterpretation`, `suggestionRefusalCopy`, `triggerDisabledReason`. No `data`, `info`, `temp`, `card2`, `handleClick2`.
- **One map per concept.** `QUOTE_CONFIDENCE_CHIP: Record<ConfidenceBand, {label, className}>` and `REVIEW_MARKER: Record<QuoteLineReviewState, string>` — a band's copy and its color, and a state and its marker, can never drift apart. The backend holds the band map once for `confidence_band`.
- **Functions under ~20 lines, 0–2 args.** `unreviewedAiLineCount`, `varianceKpi`, `varianceInterpretation`, `suggestionRefusalCopy`, `triggerDisabledReason` and `unreviewedBannerCopy` are pure, exported and unit-tested without React.
- **Named constants, no magic values.** Every copy string, class string, length bound, query-key segment and testid is a named constant — the shipped `INCOMPLETE_CHIP_LABEL` / `FINANCE_FLAG_CHIP_CLASS` pattern.
- **No client-side money math.** `parseFloat` appears nowhere in this phase's new code except where the shipped `computeLineTotal` already does it for the editor's live row total, which is untouched.
- **Lint traps:** no reset-on-open `useEffect` (`react-hooks/set-state-in-effect`); no orphaned constant left behind by the `LABOR_NOTE` / `UNBURDENED_*` export promotions; both gates run at `--max-warnings 0`.
- **Jest path patterns escape route-group parens** — `npx jest "src/app/\(dashboard\)/quotes"`; the unescaped form matches zero files and exits 1 (Pitfall 9).
- **Extend the existing `jest.mock` module factory** in `financials/__tests__/project-financials.test.tsx` when the project variance hook lands — a factory replaces the whole module, so a newly-imported hook arrives `undefined` and every shipped test using that mock fails with "not a function" (Pitfall 10). Do **not** add a defensive import guard in production code.

### Files and extractions

| File | Change | Note |
|------|--------|------|
| `quotes/[id]/edit/_lib/quote-form.ts` | edit | **Wave 0.** Round-trip `id` and `field` through `lineItemSchema` / `mapQuoteToFormValues` / `buildQuotePayload`; add `ai_origin`, `review_state`, `confidence_band`, `basis`. Trap 1 + Trap 2 — nothing else in the phase is buildable first. |
| `quotes/[id]/edit/_lib/confidence-band.ts` | **new** | The band → `{label, className}` map and the review-marker map. Pure, React-free, unit-tested. |
| `quotes/[id]/edit/_components/confidence-chip.tsx` | **new** | A `<span>` with the mapped class — **not** the `FinanceFlagChip` component, which hardcodes the amber recipe (the Phase 36 severity-chip precedent) |
| `quotes/[id]/edit/_components/ai-line-sub-row.tsx` | **new** | Chip + marker + `Accept` + basis. Presentational, no hook, **never tinted**. |
| `quotes/[id]/edit/_components/suggestion-notice.tsx` | **new** | The three refusal variants, one component, copy from a map |
| `quotes/[id]/edit/_components/unreviewed-banner.tsx` | **new** | Heading + body from `unreviewedBannerCopy(count)` |
| `quotes/[id]/edit/_components/line-items-table.tsx` | edit | Trigger in the header, notice + banner above the table, the conditional `Trade` column, the AI sub-row, the card caption stack |
| `quotes/[id]/edit/_components/sortable-line-item-row.tsx` | edit | **Input-row** tint when unreviewed; the conditional `Trade` cell. Every other cell byte-unchanged. |
| `quotes/[id]/edit/_hooks/use-quote-suggestions.ts` | **new** | The suggest/regenerate mutations and the refusal state |
| `quotes/[id]/edit/page.tsx` | edit | The regenerate confirmation dialog, composed from the shipped template-replace dialog; confirm button `variant="default"` |
| `quotes/[id]/_components/quote-status-alerts.tsx` | edit | The draft + `unreviewedCount > 0` blocked branch, reusing the shipped expired-quote alert class set byte-for-byte |
| `quotes/[id]/_components/quote-actions-card.tsx` | edit | `Send Quote` disabled + `aria-describedby` |
| `quotes/[id]/_components/quote-line-items-card.tsx` | edit | Read-only chip + basis beneath AI lines |
| `quotes/[id]/_components/quote-variance-section.tsx` | **new** | **The hook-owning container.** Renders inside `FinanceGate` so the hook is a child of the gate, not a sibling. Carries the in-file comment explaining that the layering — not the `enabled` flag alone — is what makes the two locks independent. |
| `quotes/[id]/_components/quote-variance-card.tsx` | **new** | Presentational; takes parsed props, owns no hook |
| `quotes/[id]/page.tsx` | edit | Mounts `<FinanceGate fallback={null}><QuoteVarianceSection …/></FinanceGate>`. **Owns no variance hook** — that is the entire purpose of the section container. |
| `quotes/[id]/_hooks/use-quote-detail.ts` | edit | Surface the 409 `detail` verbatim instead of the generic send-failure toast |
| `features/finance/components/FinanceGate.tsx` | edit | Add the optional `fallback?: ReactNode` prop, discriminated on `!== undefined` (**never** `??`). Default behaviour byte-unchanged; `financials/layout.tsx` untouched. |
| `features/finance/components/FinanceFlagChip.tsx` | edit | Export `FINANCE_NOTE_CHIP_CLASS` and `FINANCE_OUTLINE_CHIP_CLASS` beside the two shipped recipes |
| `features/finance/components/CostBreakdownSummary.tsx` | **no change** | `UNBURDENED_TITLE` / `UNBURDENED_BODY` were already promoted to `export` in Phase 36 |
| `financials/[projectId]/_components/scope-budget-bars.tsx` | edit | Promote `LABOR_NOTE` to `export` — value unchanged. Two surfaces must not author the scope-labor caption twice. |
| `financials/[projectId]/_components/quote-variance-table.tsx` | **new** | The per-trade table + `varianceKpi` + `csvRows`, mirroring the `scope-budget-bars.tsx` module shape |
| `financials/[projectId]/_components/project-financials-dashboard.tsx` | edit | Owns the fourth hook (already a child of the layout-mounted gate), mounts the new `ChartCard`. **Four queries, four keys, four failure surfaces**; the variance query stays **out** of the page loading gate. |
| `features/finance/hooks.ts`, `api.ts`, `types.ts` | edit | `useQuoteVariance`, `useProjectQuoteVariance`, fetchers, `QuoteVariance` types |
| `features/finance/financials-format.ts` | **no change** | Nothing new to format — `formatSignedCurrency` and `formatMarginPercent` already exist |
| `financials/_components/financials-skeleton.tsx` | **no change** | The variance card mounts with its own loading state after the page renders |
| `types/api.ts` | edit | The four new `QuoteLineItem` fields |

---

## State Matrix

Test-asserted; the executor implements exactly these.

| # | State | Condition | Rendering |
|---|-------|-----------|-----------|
| **Editor — trigger** ||||
| 1 | Trigger hidden — non-draft | `quote.status !== "draft"` | No trigger, no notice, no banner. Existing AI lines still show chip + basis read-only (D-10) |
| 2 | Trigger hidden — no finance | viewer lacks `finance.view` | No trigger. **Not** a deny panel — the editor is a legitimate non-finance surface |
| 3 | Trigger disabled — unsaved new quote | `/quotes/new/edit` | Button disabled; caption `Save this draft first — suggestions read the quote's trade.` |
| 4 | Trigger disabled — dirty form | `form.formState.isDirty` | Button disabled; caption `Save your changes first — suggesting rewrites the line items.` |
| 5 | Trigger idle — first run | draft, clean, `aiLineCount === 0` | `Suggest line items` with the `Bot` icon |
| 6 | Trigger idle — regenerate | draft, clean, `aiLineCount > 0` | `Suggest again` |
| 7 | Trigger pending | mutation in flight | `Analyzing history...`, disabled. Table unchanged until the response lands — no optimistic rows |
| **Editor — refusals ★** ||||
| 8 | Cold start ★ | `refusalReason === "insufficient_history"` | `SuggestionNotice`: `Not enough {tradeName} history yet` + the threshold body rendering `requiredCount` and `comparableCount` **from the response**. No line item is added. **Claude is never called** — the backend keystone asserts `assert_not_awaited()` |
| 9 | Trade unresolved | `refusalReason === "trade_unresolved"` | `SuggestionNotice`: `Add a trade to a line first` + the Trade-column body |
| 10 | Grounding drop ★ | `refusalReason === "ungrounded"` | `SuggestionNotice`: `No suggestions this time` + the discarded-draft body. **The whole set is dropped, never partially shown** |
| 11 | Suggest transport failure | mutation `isError` | `toast.error("Couldn't generate suggestions. Try again.", { duration: Infinity })`. No notice block, no partial rows |
| **Editor — AI line anatomy** ||||
| 12 | AI line, high band | `ai_origin && confidence_band === "high"` | `Strong history` chip in `FINANCE_OUTLINE_CHIP_CLASS` |
| 13 | AI line, medium band | `confidence_band === "medium"` | `Limited history` chip in `FINANCE_NOTE_CHIP_CLASS` — **bordered**, on the untinted sub-row |
| 14 | AI line, low band | `confidence_band === "low"` | `Thin history` chip in `FINANCE_FLAG_CHIP_CLASS` (shipped amber) |
| 15 | Basis rendered | any AI line, viewer has `finance.view` | Basis on sub-row line 2, **verbatim, wraps, never truncated, no clamp, no "read more"** |
| 16 | Basis withheld | AI line, viewer lacks `finance.view` | **No chip** and the caption `Basis hidden — requires finance access.` The API sends `null` for both fields — the client does not hide data it received |
| 17 | Unreviewed line ★ | `ai_origin && review_state === "unreviewed"` | **Input row** `bg-secondary`; the AI sub-row stays on the white card surface so its chip keeps its edge and its ink; marker `Needs review` (`text-gray-700`); `Accept` button present |
| 18 | Accepted line | `review_state === "accepted"` | Default row surface; marker `Accepted`; **no `Accept` button** |
| 19 | Edited line | `review_state === "edited"` | Default row surface; marker `AI-originated, user-edited`; no `Accept` button. Chip and basis still render — the provenance survives the edit (D-08) |
| 20 | Hand-built line | `ai_origin === false` | **No sub-row at all.** No chip, no marker, no basis. Byte-identical to the shipped row |
| 21 | No bulk approve ★ | 12 unreviewed AI lines rendered | Exactly 12 accept affordances; **zero** elements whose accessible name matches `/accept all\|approve all\|select all/i`; no header checkbox |
| 22 | No numeric score ★ | any AI line | **Zero** elements carrying a confidence percentage, score, star rating or bare `comparableCount`. The only number about the evidence is inside the basis prose |
| **Editor — banner and captions** ||||
| 23 | Unreviewed banner, singular | `unreviewedCount === 1` | `1 AI-suggested line still needs review` + the save-the-draft body |
| 24 | Unreviewed banner, plural | `unreviewedCount > 1` | `{N} AI-suggested lines still need review` + the same body |
| 25 | All reviewed | `aiLineCount > 0 && unreviewedCount === 0` | **No banner.** Captions remain |
| 26 | Pricing-basis caption (D-13) | `aiLineCount > 0 && finance.view` | `Suggested prices come from what you charged for comparable work — not from your recorded cost. The cost comparison is in each line's basis.` — once, never per row |
| 27 | Unburdened-labor caption | any AI line `item_type === "labor"` | The composed shipped caption (PITFALLS #2) |
| 28 | AI disclosure | `aiLineCount > 0` | `AI-suggested from your own completed work — every figure is from your recorded history, never an AI estimate.` — **always last, never conditional** |
| **Editor — regenerate** ||||
| 29 | Regenerate with unreviewed ★ | `Suggest again` clicked, `unreviewedCount > 0` | Confirmation dialog: `Replace unreviewed suggestions?` + the count body + `Replace Suggestions` (`variant="default"`) / `Cancel` (`variant="outline"`) |
| 30 | Regenerate, nothing to destroy | `unreviewedCount === 0` | **No dialog** — the mutation runs immediately. Nothing destructive is happening |
| 31 | Regenerate result ★ | confirmed | Accepted and edited lines are **byte-identical, `id` included**; only unreviewed AI lines are replaced. Row order preserved for kept lines |
| 32 | Regenerate cancelled | `Cancel` or dismiss | No mutation, no state change |
| **Quote detail — send gate** ||||
| 33 | Send blocked ★ | draft + `unreviewedCount > 0` | Amber alert (shipped expired-quote class set): `Review the AI-suggested line items before sending` + the count body + `Review Line Items` button. `Send Quote` **disabled**, `aria-describedby` → the alert |
| 34 | Send clear | draft + `unreviewedCount === 0` | No alert; `Send Quote` enabled — shipped behaviour, byte-unchanged |
| 35 | Send 409 from a stale client ★ | `POST /send` → 409 | The backend `detail` surfaced verbatim: `This quote has AI-suggested line items that have not been reviewed. Accept or edit every suggested line before sending.` Quote stays `draft`. **Never** the generic `Failed to send quote. Try again.` |
| 36 | No AI lines at all | `aiLineCount === 0` | The entire Phase 37 editor and detail surface is invisible. A hand-built quote looks exactly as it does today |
| **Quote detail — variance (Trap 8) ★** ||||
| 37 | Permissions loading | `usePermissions().isLoading` | **Nothing renders** — `fallback={null}` covers the loading branch too, so no pulse and no reserved space. `QuoteVarianceSection` never mounts, so **zero** requests |
| 38 | Denied ★ | viewer lacks `finance.view` | **No card, no deny panel.** `FinanceGate` short-circuits before the section mounts, so the hook never runs: **zero** requests to `/api/v1/quotes/*/variance` |
| 39 | Not approved | `quote.status !== "approved"` | No card; `enabled` false, zero requests. Variance is meaningless before an anchor exists |
| 40 | Variance loading | query in flight | Card shell with two `Skeleton` bars (`h-4 w-full`, `mt-2 h-4 w-2/3`). The rest of the page renders normally |
| 41 | Not comparable yet | `quoted === null \|\| actual === null` | `Not comparable yet` / `Quoted vs actual appears once the work from this quote has been invoiced.` |
| 42 | Under-quoted | `variance > 0` | Variance figure `text-red-800`; `Actual cost ran {amount} ({percent}%) above this quote's pre-tax price.` |
| 43 | Over-quoted | `variance < 0` | Variance figure `text-gray-900` — **no green, no check glyph**; `Actual cost came in {amount} ({percent}%) below this quote's pre-tax price.` |
| 44 | Exactly even | `variance === 0` | `text-gray-900`; `Actual cost matched this quote's pre-tax price.` |
| 45 | Project-level quote (D-14) | `trades.length > 0` | One row per `field` group + a hairline-separated `Project total` row. Groups with no invoiced job render `Not yet invoiced` |
| 46 | Scope-anchored quote | `scopeAnchored === true` | The shipped `Scope spend excludes labor — labor is tracked at job level.` caption |
| 47 | Labor in the actual | `laborIncluded === true` | The composed unburdened caption |
| 48 | Variance query error | query `isError` | In-card line `Couldn't load quoted vs actual. Refresh to try again.` (`py-8 text-center text-sm font-medium text-red-800`). **Every other card on the quote page renders normally** |
| **Drill-down variance** ||||
| 49 | Card always mounted | any permitted visit | Full width, below the trend. Rendered for populated/loading/empty/error alike so the page never reflows |
| 50 | Loading | variance query in flight | Two `Skeleton` bars inside the `ChartCard` body. **The page does not wait for it** — the loading gate stays `financials.isLoading \|\| trend.isLoading` |
| 51 | Empty | no invoiced anchor in the project | `ChartEmptyState`: `No completed work to compare` / `Quoted vs actual appears once a trade's work has been invoiced.`; `kpiValue` = `No invoiced work yet` |
| 52 | Populated | ≥1 comparable trade | Table + `Project total` row + both honesty captions as applicable; `kpiValue` = `{percent}% over quoted` / `{percent}% under quoted` / `Matched quoted` |
| 53 | Export | `Export CSV` clicked | `quoted-vs-actual-by-trade.csv` with **true, unclamped, unrolled** figures — an export never inherits a display simplification (the 35-03 rule) |
| 54 | Variance query error | query `isError` | In-card error line; **tiles, finding, trend, scope bars and category mix all render normally** — four queries, four failure surfaces |

★ = the UI half of the keystone set.

---

## Test Hooks

| `data-testid` | Element |
|---------------|---------|
| `suggest-line-items-trigger` | The trigger button |
| `suggest-trigger-reason` | The disabled-trigger caption |
| `suggestion-notice` | The refusal / grounding-drop block (heading text distinguishes the three variants) |
| `unreviewed-banner` | The editor's unreviewed-lines banner |
| `ai-line-sub-row-{index}` | One AI line's sub-row |
| `confidence-chip-{index}` | The band chip (label text distinguishes the three bands) |
| `line-basis-{index}` | The basis string (or the withheld caption) |
| `review-marker-{index}` | `Needs review` / `Accepted` / `AI-originated, user-edited` |
| `accept-line-{index}` | The per-line `Accept` button (also `aria-label="Accept suggested line {n}"`) |
| `quote-pricing-basis-note` | The D-13 card caption |
| `quote-labor-note` | The unburdened-labor card caption |
| `quote-ai-disclosure` | The AI disclosure line |
| `send-blocked-alert` | The quote-detail blocked-send alert |
| `quote-variance` | The variance card wrapper (also `aria-label="Quoted vs Actual card"`) |
| `quote-variance-figure` | The variance row's signed figure |
| `quote-variance-interpretation` | The interpretation sentence |
| `quote-variance-empty` | `Not comparable yet` |
| `quote-variance-error` | The in-card error line |
| `quote-variance-skeleton` | The in-card loading block |
| `project-quote-variance` | The drill-down table |
| `project-quote-variance-total` | The `Project total` row |
| `project-quote-variance-labor-note` | The unburdened caption on the drill-down |

The shipped `scope-labor-note` testid is reused unchanged wherever the scope-labor caption renders — one condition, one testid, one text.

---

## Accessibility Notes

- **Meaning is never carried by color alone** (WCAG 1.4.1). The confidence band is a **text label** (`Strong history` / `Limited history` / `Thin history`), not a hue. The unreviewed state carries a **text marker** (`Needs review`) and an `Accept` button in addition to its tint. The variance sign is in the **formatted figure** (`+$1,240.00`) and in a full sentence, not only in the ink.
- **Contrast:** `text-gray-600` (#4b5563) on white **7.56:1** ✓; `text-gray-700` (#374151) on white **10.31:1** ✓ and on `bg-secondary` (#ece9e1) **8.50:1** ✓; `text-gray-500` (#6b7280) on white **4.83:1** ✓ — and **never used on `bg-secondary`**, where it measures only **3.98:1** (the Phase 36 eyebrow finding); `text-amber-900` on `bg-brand/15` ✓ (shipped, unchanged); `text-red-800` (#991b1b) on white **8.31:1** ✓; `text-amber-800` on `bg-amber-50` ✓ (shipped alert recipe, unchanged).
- **Chip edges are decorative, and the labels are the sole carriers of meaning.** `border-gray-300` (#d1d5db) measures **1.47:1** on white and **1.21:1** on `bg-secondary`; the `bg-secondary` fill measures **1.21:1** on white and **1.00:1** on itself. Neither chip is interactive and neither conveys information its label does not, so WCAG 1.4.11 (non-text contrast) does not bind them — the same reasoning that lets the shipped revision badge use `border-gray-300`. The test is that if every border and fill vanished, all three chips would still read correctly. **The 1.00:1 case is nevertheless designed out rather than merely excused**: the sub-row is never tinted and the note recipe always carries an edge, because a chip legible only to a screen reader is a chip that failed its job.
- **Reading order matches visual order** on every surface: line inputs → provenance → confidence → basis → review affordance; and quoted → actual → variance → interpretation → caveats.
- **The `Accept` button has a unique accessible name per row** (`Accept suggested line 3`), because a table of twelve buttons all named "Accept" is unnavigable by a screen-reader's control list.
- **`Send Quote` is `disabled` with `aria-describedby` pointing at the blocked alert**, so the reason is announced with the control. A disabled control with no programmatic reason is the failure mode D-07 explicitly rejects.
- **The refusal notice uses `role="status"`**, so a cold-start refusal announces itself after the trigger click instead of silently replacing nothing.
- **`Bot` is decorative** (`aria-hidden`); AI provenance is spoken through the always-rendered disclosure line and the `AI-originated, user-edited` marker, never through the glyph.
- **The regenerate dialog is the shipped `Dialog`** — focus trap, `Escape` to dismiss and focus restoration come with it. `Cancel` is the first focusable action; the confirm is never the default focus.
- **Financial text never renders outside a `finance.view`-gated mount, and never before permissions resolve.** On `/quotes/[id]` that is `FinanceGate fallback={null}` wrapping the hook-owning `QuoteVarianceSection`, so the gate's decision precedes the hook's existence; in the editor it is the `null` `basis` / `confidence_band` fields the API itself withholds.
- **Playwright must log in through the UI then SPA-navigate**, never `page.goto` a quote or drill-down: a hard navigation resets Redux `isAuthenticated`, disables `usePermissions`, and every finance surface is correctly denied — a test that would pass with the gate deleted (32-04 / 35-11 / 36-06 lesson). Run at `npx playwright test --workers=2 --retries=1` (Pitfall 8).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | **none** — no new component installed. Every surface composes the existing `card.tsx`, `button.tsx`, `input.tsx`, `select.tsx`, `alert.tsx`, `dialog.tsx`, `separator.tsx` and `skeleton.tsx` wrappers plus two lucide icons (`Bot`, already in use; `Scale`, from the already-installed icon set) | not required |
| Third-party | **none declared** — `components.json` `registries: {}`, verified empty 2026-07-30 | not applicable |

No `npx shadcn add`, no `--registry` flag, no new npm dependency, no new Python package.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-07-30
