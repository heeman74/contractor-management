---
phase: 37-ai-quote-planning
plan: 11
type: execute
wave: 6
depends_on: ["37-09"]
files_modified:
  - backend/tests/test_phase_37_e2e.py
  - backend/app/features/quotes/suggestion_service.py
autonomous: true
requirements: [FINAI-03, FINAI-04, FINAI-05]

must_haves:
  truths:
    - "A suggested unit_price or quantity absent from the payload's allowed set is blocked and no line is persisted"
    - "An ungrounded dollar or percent figure in the basis string is blocked on the same path"
    - "Regenerating leaves accepted and edited lines byte-identical, id included, and replaces only untouched AI lines"
    - "The confidence band written to a row is the code-computed one, even when the reply claims a different band"
    - "The whole backend suite is green in one process"
  artifacts:
    - path: "backend/tests/test_phase_37_e2e.py"
      provides: "keystones 2, 2b and 4 plus the pricing-basis and payload-variance proofs"
      contains: "test_regenerate_preserves_reviewed_lines"
  key_links:
    - from: "suggestion_service draft loop"
      to: "ungrounded_line_fields + validate_typed_grounding"
      via: "both validators gating one fail-closed drop"
      pattern: "ungrounded_line_fields"
---

<objective>
Harden the suggestion path with the remaining keystones and take the phase's
backend gate.

Purpose: keystone 2 must assert on a STRUCTURED field, because
`validate_grounding` returns ok for text with no figures — a basis-only test
passes vacuously (Pitfall 4). Keystone 4 must assert byte-identity including the
`id`, because that is the only thing distinguishing a preserved line from a
recreated look-alike (Pitfall 2).

Output: the four remaining keystone-grade tests, the D-13 and payload-variance
proofs, mutation verification of each, and a single-process full-suite run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-ai-quote-planning/37-VALIDATION.md
@.planning/phases/37-ai-quote-planning/37-RESEARCH.md
@CLAUDE.md

<interfaces>
On disk from earlier plans:
```python
# app/features/quotes/suggestion_service.py (plan 37-09)
class QuoteSuggestionService(TenantScopedService[Quote]):
    async def suggest(self, quote_id: uuid.UUID) -> SuggestionOutcome
# GROUNDING_RETRY_LIMIT = 1; the loop drops the WHOLE set on a second failure
# app/features/quotes/suggestion_payload.py (plan 37-07)
def ungrounded_line_fields(line, allowed: AllowedLineValues) -> tuple[str, ...]
# app/core/ai_grounding.py (plan 37-02)
def validate_typed_grounding(text, allowed: AllowedFigures) -> GroundingResult
# app/features/quotes/models.py (plan 37-01)
REVIEW_STATE_UNREVIEWED / REVIEW_STATE_ACCEPTED / REVIEW_STATE_EDITED
```

Test conventions already in `backend/tests/test_phase_37_e2e.py`:
`_make_mock_anthropic_response` copied from `tests/test_phase_26_e2e.py`, the
`_patched_claude` context manager patching
`app.core.ai_utils.get_anthropic_client`, `_token` / `_pm_headers` /
`_admin_headers` copied from `tests/test_phase_35_e2e.py`, and the
`async_session_factory()` + `SET LOCAL` f-string seeding helper.

Pitfall 6 (verified empirically in 36-07): this app binds structlog to the
stdlib bridge, so `%`-formatting is deferred to the handler. Log lines must be
rendered at the call site and asserted through `structlog.testing.capture_logs`;
the stdlib pytest logging fixture captures ZERO records from this configuration.
The acceptance grep below forbids that fixture's name anywhere in the test
module, comments included — describe the rule without naming it.

AI prose is never byte-asserted. Frame strings, counts, structure, bounds and
grounding verdicts are.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keystones 2 and 2b — ungrounded structured fields and ungrounded basis</name>
  <files>backend/tests/test_phase_37_e2e.py</files>
  <read_first>
    - backend/app/features/quotes/suggestion_service.py (the draft loop and its drop path)
    - backend/tests/test_phase_37_e2e.py (the mock-Claude helpers already in the file)
    - .planning/phases/37-ai-quote-planning/37-RESEARCH.md Pitfall 4
  </read_first>
  <behavior>
    - a reply whose line carries unit_price 999.99, absent from the allowed set, is retried once and then dropped: refusal_reason "ungrounded", suggested_line_count 0, zero AI rows in the database
    - a reply whose line carries a quantity absent from the allowed set takes the same path
    - a reply whose unit_price differs only in the cents place from an allowed price is still blocked — structured membership is exact
    - a reply whose basis cites a dollar figure absent from the allowed money set is blocked
    - a reply whose basis cites a percent absent from the allowed percent set is blocked, even when the same number is present as money
    - the retry is used exactly once: the mocked client is awaited twice for a persistently ungrounded reply
    - a drop is logged through a call-site-rendered template naming the offending literals
  </behavior>
  <action>
Add to `backend/tests/test_phase_37_e2e.py`:
`test_ungrounded_line_blocked` (KEYSTONE 2),
`test_ungrounded_quantity_blocked`,
`test_cent_level_price_drift_blocked`,
`test_ungrounded_basis_blocked` (KEYSTONE 2b),
`test_percent_cannot_borrow_a_money_value`,
`test_grounding_retry_used_exactly_once`,
`test_ungrounded_drop_is_logged`.

Keystone 2 asserts on a STRUCTURED field, not on prose. `validate_grounding`
returns ok for text with no figures at all, so a basis-only assertion can pass
because the model wrote nothing citable — which is the same class of false green
36-02 recorded ("a zero-request assertion that was never observed failing can
pass for the wrong reason").

`test_percent_cannot_borrow_a_money_value` is the typed-grounding proof at the
integration level: seed a payload whose money set contains 12 and whose percent
set does not, have the mock cite `"12%"` in the basis, and assert the drop. With
the shipped flat collector that citation would have passed.

Assert on all three of: the response's `refusal_reason`, the response's
`suggested_line_count`, and a direct database read showing zero `ai_origin` rows
on the quote. A response-only assertion would not catch a partial persist, and
fail-closed means the whole set is dropped, never part of it.

Mutation-verify each keystone and record the observed failure in the summary:
1. Make `ungrounded_line_fields` always return `()` → `test_ungrounded_line_blocked`
   and `test_ungrounded_quantity_blocked` fail. Restore.
2. Swap `validate_typed_grounding` for the untyped `validate_grounding` over the
   union of both sets → `test_percent_cannot_borrow_a_money_value` fails.
   Restore.
  </action>
  <acceptance_criteria>
    - The seven test function names exist in `backend/tests/test_phase_37_e2e.py`, none duplicating a name already in the module
    - `cd backend && .venv/bin/pytest tests/test_phase_37_e2e.py -k "ungrounded_line_blocked or ungrounded_basis_blocked" -q` selects 2 tests, both passing
    - `grep -c "caplog" backend/tests/test_phase_37_e2e.py` returns 0
    - `grep -c "capture_logs" backend/tests/test_phase_37_e2e.py` returns 1 or more
    - Each mutation listed in the action fails exactly the named tests and no others, then restores green
  </acceptance_criteria>
  <verify>
    <automated>cd backend && .venv/bin/pytest tests/test_phase_37_e2e.py -k "ungrounded or grounding_retry or percent_cannot_borrow" -x -q</automated>
  </verify>
  <done>Both grounding keystones assert the fail-closed drop on structured fields and on prose, and both are mutation-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keystone 4 — regeneration preserves reviewed work; the band is code-computed</name>
  <files>backend/tests/test_phase_37_e2e.py, backend/app/features/quotes/suggestion_service.py</files>
  <read_first>
    - backend/app/features/quotes/suggestion_service.py (the persist step's partition)
    - backend/app/features/quotes/service.py (`review_state_after`, the reconcile)
    - .planning/phases/37-ai-quote-planning/37-RESEARCH.md Pitfall 2
  </read_first>
  <behavior>
    - after a first suggestion run, accepting one line and editing another, a second run leaves both rows byte-identical on id, description, quantity, unit, unit_price and field
    - the second run leaves the accepted line's review_state "accepted" and the edited line's "edited"
    - the second run replaces every remaining unreviewed AI line
    - hand-built lines are untouched by both runs
    - the kept lines' relative order is preserved
    - a reply that names its own confidence band writes the code-computed band to the row instead
    - the quote's ai_suggestion_payload holds the payload the second run validated against
  </behavior>
  <action>
Add to `backend/tests/test_phase_37_e2e.py`:
`test_regenerate_preserves_reviewed_lines` (KEYSTONE 4),
`test_regenerate_replaces_untouched_ai_lines`,
`test_regenerate_leaves_hand_built_lines_alone`,
`test_regenerate_preserves_kept_line_order`,
`test_band_is_code_computed`,
`test_suggestion_payload_is_stored_for_audit`.

Keystone 4 must capture the full row tuple INCLUDING `id` before the second run
and compare tuples afterward. Without the id, a deleted-and-recreated look-alike
passes — which is exactly the failure Trap 1 caused and plan 37-01 fixed.

Reach the edited state through the real path: PATCH the quote with a changed
`unit_price` on that line so the server derives `edited` (plan 37-01's
`review_state_after`), rather than seeding the column directly. Reach the
accepted state by PATCHing with `review_state: "accepted"` and no field change.
The test then exercises the actual user route, not a fixture shortcut.

`test_band_is_code_computed` has the mock reply include a `confidence` or `band`
key claiming `"high"` on a comparable set the code bands as `"low"`, and asserts
the stored `confidence_band` is the computed one. Mutation-verify by making the
service read the reply's band — the test must fail — then restore.

If any of these reveals a gap in the persist partition, fix
`suggestion_service.py` in this task; that is why the file is in this plan's
`files_modified`.
  </action>
  <acceptance_criteria>
    - `cd backend && .venv/bin/pytest tests/test_phase_37_e2e.py -k regenerate_preserves -q` selects 1 or more tests, all passing
    - `grep -c "\"id\"" backend/tests/test_phase_37_e2e.py` returns 1 or more within the regenerate test (the id is part of the compared tuple)
    - `cd backend && .venv/bin/pytest tests/test_phase_37_e2e.py -k band_is_code_computed -q` selects 1 test, passing
    - The band mutation fails exactly `test_band_is_code_computed`, then restores green
  </acceptance_criteria>
  <verify>
    <automated>cd backend && .venv/bin/pytest tests/test_phase_37_e2e.py -k "regenerate or band_is_code_computed or payload_is_stored" -x -q</automated>
  </verify>
  <done>Regeneration is proven non-destructive at the row-identity level, and the band can only come from code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The D-13 pricing basis, the payload variance leg, and the backend phase gate</name>
  <files>backend/tests/test_phase_37_e2e.py, .planning/phases/37-ai-quote-planning/37-VALIDATION.md</files>
  <read_first>
    - .planning/phases/37-ai-quote-planning/37-CONTEXT.md D-12 and D-13
    - backend/app/features/quotes/suggestion_payload.py (the money and percent sets)
    - .planning/phases/37-ai-quote-planning/37-VALIDATION.md (the full per-requirement map this task completes)
  </read_first>
  <behavior>
    - the payload's rate rows carry a median quoted unit price drawn from comparable approved-quote line items, and a suggestion priced from it is above the comparable actual unit cost for a trade whose history is profitable
    - the payload carries the actual-cost figures under their own names, distinct from the quoted-price fields
    - the payload carries the trade's quoted-vs-actual variance percent as a named percent field
    - no field in the payload multiplies, adjusts or blends the quoted and actual legs
    - the full backend suite passes in one process
  </behavior>
  <action>
Add to `backend/tests/test_phase_37_e2e.py`:
`test_pricing_basis_comes_from_quoted_history` — seed a trade whose comparables
were quoted above their actual cost, run a suggestion whose mock reply copies the
payload's rate row, and assert the persisted `unit_price` equals the payload's
`median_quoted_unit_price` and is strictly greater than the payload's
`median_actual_unit_cost`-style figure. This is D-13 made a test: pricing from
the unburdened cost rate would make every suggestion at or below cost, which
PITFALLS #2 names as a defect verbatim.

`test_actual_cost_and_variance_are_separately_named_payload_fields` — assert the
payload dict contains the quoted-price field, the actual-cost field and the
variance-percent field under three distinct keys, and that no key's value is a
product of two others (assert the arithmetic explicitly on the seeded numbers).
D-12 forbids a multiplier; nothing may silently adjust a price.

`test_variance_in_payload` — the trade's quoted-vs-actual variance percent
appears as a named payload field and is inside the `AllowedFigures.percents` set,
so the model may state it in the basis.

Then take the backend phase gate:
1. `cd backend && .venv/bin/pytest` in ONE process — conftest TRUNCATEs every
   table per test, so two processes against `contractorhub_test` deadlock inside
   `seed_two_tenants` (STATE.md, Phase 35 blocker). Do not parallelise.
2. `cd backend && .venv/bin/ruff check . && .venv/bin/ruff format --check .`
3. Confirm `docker compose up migrate --build` still exits 0 at
   `0037_quote_line_review_state`.
Fix any fallout in this task rather than deferring it.

Finally, update `.planning/phases/37-ai-quote-planning/37-VALIDATION.md`'s
per-requirement table: set every backend row's status and fill its assigned task
ID, so the validation contract reflects what actually shipped.
  </action>
  <acceptance_criteria>
    - `cd backend && .venv/bin/pytest tests/test_phase_37_e2e.py -k "pricing_basis or variance_in_payload" -q` selects 2 or more tests, all passing
    - `cd backend && .venv/bin/pytest -q` reports zero failures in a single process
    - `cd backend && .venv/bin/ruff check . && .venv/bin/ruff format --check .` both exit 0
    - `grep -c "37-1[01]\|37-09\|37-07\|37-04\|37-02\|37-01" .planning/phases/37-ai-quote-planning/37-VALIDATION.md` returns 10 or more — every backend row carries its task ID
  </acceptance_criteria>
  <verify>
    <automated>cd backend && .venv/bin/pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <done>D-13 and D-12 are enforced by test, the full backend suite is green in one process, and the validation map records where each behavior landed.</done>
</task>

</tasks>

<verification>
- `cd backend && .venv/bin/pytest -q` green, single process
- Every mutation named in Tasks 1 and 2 fails exactly the tests it should, then restores green
- `.planning/phases/37-ai-quote-planning/37-VALIDATION.md` backend rows all carry a task ID
</verification>

<success_criteria>
- Keystones 2, 2b and 4 pass and are mutation-verified
- A structured-field citation outside the allowed set is blocked, exactly, at the cent
- Regeneration preserves reviewed rows including their ids
- The suggested price comes from quoted history, with the actual-cost and variance legs carried separately and never blended
</success_criteria>

<output>
After completion, create `.planning/phases/37-ai-quote-planning/37-11-SUMMARY.md`
</output>
