---
phase: 12-client-profile-sync-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/jobs/data/client_profile_sync_handler.dart
  - mobile/test/e2e/phase_12_client_profile_sync_fix_e2e_test.dart
autonomous: true
requirements: [CLNT-01]

must_haves:
  truths:
    - "Offline client profile CREATE syncs to POST /clients/{user_id}/profile (not /clients/profiles)"
    - "Offline client profile UPDATE syncs to POST /clients/{user_id}/profile (not PATCH /clients/profiles/{id})"
    - "user_id is extracted from payload['userId'], not from item.entityId (which holds the profile UUID)"
    - "E2E test verifies both CREATE and UPDATE operations route to the correct endpoint"
  artifacts:
    - path: "mobile/lib/features/jobs/data/client_profile_sync_handler.dart"
      provides: "Fixed push() method routing to correct backend endpoint"
      contains: "/clients/$userId/profile"
    - path: "mobile/test/e2e/phase_12_client_profile_sync_fix_e2e_test.dart"
      provides: "E2E tests covering CREATE/UPDATE sync push and full offline flow"
      min_lines: 60
  key_links:
    - from: "client_profile_sync_handler.dart push()"
      to: "POST /api/v1/clients/{user_id}/profile"
      via: "DioClient.pushWithIdempotency"
      pattern: "pushWithIdempotency.*clients/.*userId.*profile"
    - from: "push() payload extraction"
      to: "payload['userId']"
      via: "jsonDecode(item.payload)"
      pattern: "payload\\['userId'\\]"
---

<objective>
Fix ClientProfileSyncHandler push endpoint URLs so offline client profile edits sync correctly to the backend instead of silently parking on 404.

Purpose: INT-04 gap closure — the handler uses wrong URLs (`POST /clients/profiles` for CREATE, `PATCH /clients/profiles/{id}` for UPDATE) that don't exist on the backend. The backend only exposes `POST /clients/{user_id}/profile` with upsert semantics.

Output: Fixed sync handler + E2E tests verifying the offline-edit → sync → backend-persist flow.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-client-profile-sync-fix/12-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From mobile/lib/core/network/dio_client.dart:
```dart
Future<Response<dynamic>> pushWithIdempotency(
  String path,
  Map<String, dynamic> data,
  String idempotencyKey, {
  String method = 'POST',
})
```

From mobile/lib/core/sync/sync_handler.dart:
```dart
abstract class SyncHandler {
  String get entityType;
  Future<void> push(SyncQueueData item);
  Future<void> applyPulled(Map<String, dynamic> data);
}
```

Backend endpoint (source: backend/app/features/jobs/router.py):
```python
@router.post("/clients/{user_id}/profile", status_code=201)
async def create_or_update_client_profile(user_id: uuid.UUID, data: ClientProfileCreate, ...)
# Upsert semantics — handles both create and update
```

Sync queue payload (source: job_dao.dart insertClientProfile):
```dart
// payload keys are camelCase:
// 'id': profile UUID, 'companyId': company UUID, 'userId': user UUID
// item.entityId = profile UUID (NOT user UUID)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix push() URL and create E2E tests</name>
  <files>mobile/lib/features/jobs/data/client_profile_sync_handler.dart, mobile/test/e2e/phase_12_client_profile_sync_fix_e2e_test.dart</files>
  <behavior>
    - Test 1 (CREATE): When sync queue item has operation='CREATE' with payload containing userId, push() calls pushWithIdempotency with path '/clients/{userId}/profile' and method POST
    - Test 2 (UPDATE): When sync queue item has operation='UPDATE' with payload containing userId, push() calls pushWithIdempotency with same path '/clients/{userId}/profile' and method POST (NOT PATCH)
    - Test 3 (Full flow): Insert a client profile via Drift DAO, enqueue CREATE in sync queue, instantiate handler, call push(), verify correct URL called with correct userId in path
  </behavior>
  <action>
    **E2E test file** (`mobile/test/e2e/phase_12_client_profile_sync_fix_e2e_test.dart`):
    - Create test file following Phase 9 E2E pattern: `MockDioClient extends Mock implements DioClient`
    - Use `AppDatabase(NativeDatabase.memory())` for real Drift DB
    - Write 3 test cases matching the behavior spec above
    - For the full flow test: seed a client profile row in Drift's clientProfiles table, create a SyncQueueData item manually with the correct payload JSON, call handler.push(), verify the captured URL path
    - Use `tester.pump()` NOT `pumpAndSettle()` if any widget testing needed (per MEMORY.md)
    - Import: `import 'package:drift/drift.dart' hide isNotNull, isNull;` to avoid matcher conflicts (per MEMORY.md)

    **Handler fix** (`client_profile_sync_handler.dart`):
    - Remove the entire switch statement on `item.operation`
    - Extract `userId` from `payload['userId'] as String` (camelCase key — per job_dao.dart enqueue pattern)
    - Do NOT use `item.entityId` — that holds the profile UUID, not the user UUID (Pitfall 1 from research)
    - Route both CREATE and UPDATE to: `await _dioClient.pushWithIdempotency('/clients/$userId/profile', payload, item.id)`
    - No `method:` parameter needed — default is POST which matches the backend upsert endpoint
    - Keep the `default:` throw for unknown operations as defensive coding
    - Update the doc comment to reflect the correct endpoint: `POST /api/v1/clients/{user_id}/profile` for both CREATE and UPDATE

    **CRITICAL pitfalls (from 12-RESEARCH.md):**
    - Pitfall 1: `item.entityId` is the profile UUID, NOT userId — extract userId from payload
    - Pitfall 2: Backend has NO PATCH endpoint — both CREATE and UPDATE use POST upsert
    - Pitfall 3: Do NOT use pumpAndSettle in tests — Drift streams never settle
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/e2e/phase_12_client_profile_sync_fix_e2e_test.dart</automated>
  </verify>
  <done>
    - client_profile_sync_handler.dart push() routes both CREATE and UPDATE to POST /clients/{userId}/profile
    - userId extracted from payload['userId'], not item.entityId
    - 3 E2E tests pass: CREATE push, UPDATE push, full offline flow
    - No switch on operation — unified POST path for all operations
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no regressions in existing sync tests</name>
  <files></files>
  <action>
    Run the Phase 9 sync E2E tests and the full mobile test suite to confirm the handler change does not break any existing sync tests. No file modifications — verification only.

    Run in order:
    1. `cd mobile && flutter test test/e2e/phase_9_sync_gap_closure_e2e_test.dart` — Phase 9 sync tests that exercise the sync engine
    2. `cd mobile && dart analyze lib/features/jobs/data/client_profile_sync_handler.dart` — ensure no analyzer warnings on the modified file
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/e2e/phase_9_sync_gap_closure_e2e_test.dart && dart analyze lib/features/jobs/data/client_profile_sync_handler.dart</automated>
  </verify>
  <done>
    - Phase 9 sync E2E tests still pass
    - dart analyze reports no issues on the modified handler file
    - No regressions introduced by the URL fix
  </done>
</task>

</tasks>

<verification>
1. `cd mobile && flutter test test/e2e/phase_12_client_profile_sync_fix_e2e_test.dart` — all 3 tests green
2. `cd mobile && flutter test test/e2e/phase_9_sync_gap_closure_e2e_test.dart` — no regressions
3. `cd mobile && dart analyze lib/features/jobs/data/client_profile_sync_handler.dart` — no issues
4. Grep confirms: `grep -n "clients/profiles" mobile/lib/features/jobs/data/client_profile_sync_handler.dart` returns no matches (old broken URLs removed)
5. Grep confirms: `grep -n "clients/\$userId/profile" mobile/lib/features/jobs/data/client_profile_sync_handler.dart` returns the correct URL
</verification>

<success_criteria>
- ClientProfileSyncHandler.push() uses `POST /clients/{userId}/profile` for both CREATE and UPDATE
- userId extracted from `payload['userId']` (not `item.entityId`)
- 3 E2E tests pass covering CREATE, UPDATE, and full offline flow
- No regressions in Phase 9 sync tests
- dart analyze clean on modified file
</success_criteria>

<output>
After completion, create `.planning/phases/12-client-profile-sync-fix/12-01-SUMMARY.md`
</output>
