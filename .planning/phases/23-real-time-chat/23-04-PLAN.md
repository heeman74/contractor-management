---
phase: 23-real-time-chat
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - mobile/lib/features/chat/presentation/screens/chat_screen.dart
  - mobile/lib/features/chat/presentation/screens/chat_thread_screen.dart
  - mobile/lib/features/chat/presentation/widgets/chat_thread_tile.dart
  - mobile/lib/features/chat/presentation/widgets/message_bubble.dart
  - mobile/lib/features/chat/presentation/widgets/chat_input_bar.dart
  - mobile/lib/features/chat/presentation/widgets/typing_indicator.dart
  - mobile/lib/core/routing/route_names.dart
  - mobile/lib/core/routing/app_router.dart
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04]

must_haves:
  truths:
    - "GC can see chat thread list organized by trade scope within a project"
    - "User can tap a thread and see messages in chronological order"
    - "User can type and send a text message that appears in the thread"
    - "User can attach photos and PDFs to chat messages"
    - "Offline messages show clock icon and queue in SyncQueue"
    - "Typing indicator shows when other party is typing"
  artifacts:
    - path: "mobile/lib/features/chat/presentation/screens/chat_screen.dart"
      provides: "Thread list screen with Trade Conversations and Project Group sections"
      contains: "class ChatScreen"
    - path: "mobile/lib/features/chat/presentation/screens/chat_thread_screen.dart"
      provides: "Message list with input bar, WebSocket connection, read receipts"
      contains: "class ChatThreadScreen"
    - path: "mobile/lib/features/chat/presentation/widgets/message_bubble.dart"
      provides: "Adaptive message bubble (own/other, text/photo/PDF/annotated)"
      contains: "class MessageBubble"
    - path: "mobile/lib/features/chat/presentation/widgets/chat_input_bar.dart"
      provides: "Message input with attachment picker and @mention autocomplete"
      contains: "class ChatInputBar"
  key_links:
    - from: "mobile/lib/features/chat/presentation/screens/chat_thread_screen.dart"
      to: "mobile/lib/features/chat/domain/chat_providers.dart"
      via: "ref.watch(chatMessagesProvider(threadId))"
      pattern: "chatMessagesProvider"
    - from: "mobile/lib/features/chat/presentation/screens/chat_thread_screen.dart"
      to: "mobile/lib/features/chat/data/chat_ws_client.dart"
      via: "ChatWsClient for real-time message delivery"
      pattern: "ChatWsClient"
    - from: "mobile/lib/core/routing/app_router.dart"
      to: "mobile/lib/features/chat/presentation/screens/chat_screen.dart"
      via: "GoRouter route for chat screens"
      pattern: "chatThread"
---

<objective>
Mobile chat UI: thread list screen, message thread screen, message bubbles, input bar with attachment picker and @mention, typing indicator, and GoRouter navigation.

Purpose: Delivers the complete mobile chat experience — users can browse threads, read and send messages, share files, and see typing indicators.
Output: ChatScreen, ChatThreadScreen, 4 widgets, GoRouter routes.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-real-time-chat/23-CONTEXT.md
@.planning/phases/23-real-time-chat/23-UI-SPEC.md
@.planning/phases/23-real-time-chat/23-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->

From mobile/lib/features/chat/domain/chat_providers.dart:
```dart
final chatDaoProvider = Provider((ref) => ...);
final chatThreadsProvider = StreamProvider.autoDispose.family<List<ChatThread>, String>(...);
final chatMessagesProvider = StreamProvider.autoDispose.family<List<ChatMessage>, String>(...);
final chatWsClientProvider = Provider.autoDispose.family<ChatWsClient, String>(...);
final chatRepositoryProvider = Provider((ref) => ...);
final chatSyncServiceProvider = Provider((ref) => ...);
```

From mobile/lib/features/chat/data/chat_ws_client.dart:
```dart
class ChatWsClient {
  Stream<Map<String, dynamic>> get messages;
  void send(Map<String, dynamic> payload);
  void sendTyping();
  void sendRead(int seq);
  void dispose();
}
```

From mobile/lib/features/chat/data/chat_dao.dart:
```dart
class ChatDao extends DatabaseAccessor {
  Stream<List<ChatThread>> watchThreadsByProject(String projectId);
  Stream<List<ChatMessage>> watchMessages(String threadId);
  Future<void> insertMessage(ChatMessagesCompanion msg);
  Future<int?> getLastSeq(String threadId);
}
```

From mobile/lib/core/routing/route_names.dart:
```dart
class RouteNames {
  static const projects = '/projects';
  static const projectDetail = '/projects/:id';
  // ... existing routes
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChatScreen (thread list) and ChatThreadTile widget</name>
  <files>
    mobile/lib/features/chat/presentation/screens/chat_screen.dart,
    mobile/lib/features/chat/presentation/widgets/chat_thread_tile.dart,
    mobile/lib/core/routing/route_names.dart,
    mobile/lib/core/routing/app_router.dart
  </files>
  <read_first>
    mobile/lib/features/chat/domain/chat_providers.dart,
    mobile/lib/core/routing/route_names.dart,
    mobile/lib/core/routing/app_router.dart,
    mobile/lib/features/projects/presentation/screens/project_detail_screen.dart,
    mobile/lib/features/projects/presentation/screens/project_list_screen.dart,
    .planning/phases/23-real-time-chat/23-UI-SPEC.md
  </read_first>
  <action>
    1. **ChatScreen** (`chat_screen.dart`):
    - ConsumerStatefulWidget, takes `projectId` as parameter
    - AppBar title: "Messages" (Display, 20px, weight 600 — `titleLarge`)
    - On init: call `ref.read(chatRepositoryProvider).fetchThreads(projectId)` to sync from server
    - Watch `chatThreadsProvider(projectId)` for reactive Drift stream
    - Body: `ListView` with two sections separated by section headers:
      - "Trade Conversations" header (Label, 12px weight 400, muted foreground, left-aligned with 24px top margin)
      - Scope threads filtered by `threadType == 'scope'`, sorted by unread count desc then lastMessageAt desc
      - "Project Group" header
      - Project-wide threads filtered by `threadType == 'project_wide'`
    - Empty state: Center column with "No conversations yet" (Heading) and "Conversations are created automatically when a contractor is assigned to a trade scope." (Body, muted) per UI-SPEC
    - Tap thread → `context.push('/projects/$projectId/chat/$threadId')`
    - Long-press thread → `showModalBottomSheet` with "Mute Conversation" / "Unmute Conversation" option

    2. **ChatThreadTile** (`chat_thread_tile.dart`):
    - StatelessWidget, takes ChatThread data, onTap, onLongPress callbacks
    - Row layout, minimum 56px height:
      - Leading: thread type indicator. Scope thread: 12px colored dot (cycle through chart colors `--chart-1` through `--chart-5` based on trade scope index). Project-wide: `Icons.groups` icon in `colorScheme.secondary`
      - Title area (Expanded column):
        - Thread name: `titleMedium` (16px, w600) — bold if unread, normal if read
        - Last message preview: `bodySmall` (12px, w400), 1 line truncated, `colorScheme.onSurface.withOpacity(0.6)`
      - Trailing column (right-aligned):
        - Timestamp: `bodySmall`, muted, relative format ("2:14 PM", "Mon", "Mar 18")
        - Unread badge: Container with `colorScheme.primary` fill, 20px min-width circle, white text count. Hidden when unreadCount == 0. Shows "99+" for >99
      - If muted: bell-slash icon (`Icons.notifications_off`) next to thread name
    - Semantics: `Semantics(label: "{thread.name}, {unreadCount} unread messages, last message: {preview}")`

    3. **route_names.dart**: Add:
    ```dart
    static const chat = '/projects/:projectId/chat';
    static const chatThread = '/projects/:projectId/chat/:threadId';
    static String chatPath(String projectId) => '/projects/$projectId/chat';
    static String chatThreadPath(String projectId, String threadId) => '/projects/$projectId/chat/$threadId';
    ```

    4. **app_router.dart**: Add routes under existing project routes:
    - `GoRoute(path: 'chat', builder: (_, state) => ChatScreen(projectId: state.pathParameters['id']!))`
    - `GoRoute(path: 'chat/:threadId', builder: (_, state) => ChatThreadScreen(threadId: state.pathParameters['threadId']!, projectId: state.pathParameters['id']!))`
    - These should be child routes of the project detail route
    - Also add a "Chat" button to ProjectDetailScreen or a navigation entry point (e.g., floating action button or AppBar action)
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/chat/presentation/screens/chat_screen.dart lib/features/chat/presentation/widgets/chat_thread_tile.dart lib/core/routing/route_names.dart lib/core/routing/app_router.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ChatScreen watches `chatThreadsProvider(projectId)` from Drift
    - Two sections: "Trade Conversations" and "Project Group" headers
    - ChatThreadTile shows thread type indicator (color dot / group icon), name, preview, timestamp, unread badge
    - Unread badge hidden when 0, shows "99+" for >99
    - Route constants: `RouteNames.chat` and `RouteNames.chatThread`
    - GoRouter routes registered as children of project detail
    - Empty state shows correct copy per UI-SPEC
    - Long-press shows mute/unmute bottom sheet
    - `dart analyze` passes on all files
  </acceptance_criteria>
  <done>Thread list screen renders with sections, tiles show unread state, navigation wired to GoRouter</done>
</task>

<task type="auto">
  <name>Task 2: Create ChatThreadScreen, MessageBubble, ChatInputBar, and TypingIndicator</name>
  <files>
    mobile/lib/features/chat/presentation/screens/chat_thread_screen.dart,
    mobile/lib/features/chat/presentation/widgets/message_bubble.dart,
    mobile/lib/features/chat/presentation/widgets/chat_input_bar.dart,
    mobile/lib/features/chat/presentation/widgets/typing_indicator.dart
  </files>
  <read_first>
    mobile/lib/features/chat/domain/chat_providers.dart,
    mobile/lib/features/chat/data/chat_ws_client.dart,
    mobile/lib/features/chat/data/chat_dao.dart,
    mobile/lib/features/chat/data/chat_sync_service.dart,
    mobile/lib/features/projects/presentation/screens/task_photo_viewer_screen.dart,
    mobile/lib/features/projects/domain/annotation_schema.dart,
    .planning/phases/23-real-time-chat/23-UI-SPEC.md
  </read_first>
  <action>
    1. **ChatThreadScreen** (`chat_thread_screen.dart`):
    - ConsumerStatefulWidget, params: threadId, projectId
    - AppBar: thread name (`titleMedium`, w600) + member count for project-wide threads (`bodySmall`, "5 members")
    - WebSocket connection: on initState, get ChatWsClient from provider, call connect(threadId)
    - Listen to WS messages stream: on 'message' → insert into Drift via ChatDao; on 'typing' → show TypingIndicator; on 'read_receipt' → update read receipts; on 'reconnected' → call chatRepository.fetchMissedMessages(threadId, lastSeq)
    - Body: Watch `chatMessagesProvider(threadId)` — `ListView.builder(reverse: true)` for natural chat scroll (newest at bottom)
    - Date separators: compare message dates, insert centered date chip ("Today", "Yesterday", "Mar 18") between different days
    - Scroll to top (which is oldest in reverse list) triggers pagination: call `chatRepository.fetchMessages(threadId, beforeSeq: firstSeq)` to load older messages. Show "Load older messages" spinner.
    - On screen enter: send read receipt via WS `sendRead(lastSeq)`
    - Bottom: `ChatInputBar` with callbacks for onSend, onAttachment
    - `Scaffold(resizeToAvoidBottomInset: true)` so keyboard pushes input up
    - On dispose: ChatWsClient.dispose()

    2. **MessageBubble** (`message_bubble.dart`):
    - StatelessWidget, params: ChatMessage data, bool isOwn, String? readByText
    - Layout: Align(alignment: isOwn ? Alignment.centerRight : Alignment.centerLeft)
    - Container with:
      - Background: isOwn → `colorScheme.primary`, !isOwn → `colorScheme.surfaceVariant`
      - Text color: isOwn → `colorScheme.onPrimary`, !isOwn → `colorScheme.onSurface`
      - Padding: 16px (md spacing)
      - Border radius: 16px with one sharp corner (top-left for own, top-right for other — standard chat convention)
    - Content variants:
      - Text-only: `bodyMedium` (14px, w400)
      - Photo attachment: `ClipRRect` thumbnail (max 200px width), tap → full-screen photo viewer
      - PDF attachment: Row with PDF icon + filename, tap → url_launcher
      - Annotated photo: thumbnail + "Annotated photo" Chip below, tap → PhotoAnnotationScreen(readOnly: true)
    - Bottom-right of bubble: timestamp (`bodySmall`, 12px, muted)
    - Status icon (own messages only):
      - `status == 'pending'`: Clock icon (`Icons.schedule`, 14px)
      - `status == 'sent'`: Single check (`Icons.check`, 14px)
      - Read by someone: Double check (`Icons.done_all`, 14px, accent color)
    - Opacity 0.7 for pending messages
    - Read receipt row below own messages: `ReadReceiptRow` (if readByText non-null)
    - Sender name label above bubble for incoming messages: `bodySmall`, w600, muted foreground
    - Long-press: `showModalBottomSheet` with "Copy Text" option (both own and incoming)
    - Semantics: `Semantics(label: "{senderName}: {content}, {timestamp}")`

    3. **ChatInputBar** (`chat_input_bar.dart`):
    - StatefulWidget
    - Row layout with 16px (md) padding:
      - Left: Attachment `IconButton` (Icons.attach_file, 48px touch target). Opens `showModalBottomSheet` with 3 options: "Photo" (ImagePicker), "Document" (FilePicker — PDF only), "Annotated Photo" (navigate to picker for annotated task photos)
      - Center (Expanded): `TextField` with `minLines: 1`, `maxLines: 5`, min height 48px, `textInputAction: TextInputAction.newline` (per UI-SPEC: field workers use Enter for line breaks). Placeholder: "Message {threadName}..."
      - Right: Send `IconButton` (Icons.send, 48px touch target). Enabled (`colorScheme.primary`) when text non-empty or attachment staged. Disabled (`colorScheme.onSurface.withOpacity(0.3)`) when empty.
    - @mention autocomplete: When "@" typed, show overlay `ListView` above input with thread member suggestions. Filter as user types after "@". Tap inserts `@{name}` and adds user_id to mentions list. Typing "@all" adds mention_all flag.
    - Typing indicator emission: on text change, debounce 500ms → call `wsClient.sendTyping()`
    - onSend callback: clear text, generate UUID, write message to Drift + SyncQueue if offline (via ChatSyncService), send via WS if online
    - Staged attachment preview: show above input bar as removable chip/thumbnail

    4. **TypingIndicator** (`typing_indicator.dart`):
    - StatelessWidget, params: String? typingUserName
    - Shown when typingUserName is non-null
    - Layout: Row aligned to left (like incoming message):
      - Animated three dots: use `AnimatedBuilder` or `TweenAnimationBuilder` cycling opacity on 3 small circles (4px diameter, xs gap)
      - Text: "{firstName} is typing..." in `bodySmall` (12px, w400, muted)
    - Auto-hide: parent (ChatThreadScreen) manages a Timer that clears typingUserName after 3 seconds with no update
    - Semantics: `Semantics(label: "{firstName} is typing", liveRegion: true)`
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/chat/presentation/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ChatThreadScreen connects to WebSocket via ChatWsClient on initState
    - Messages displayed via `chatMessagesProvider` in reverse ListView
    - Date separators between messages from different days
    - MessageBubble has 4 variants: text, photo, PDF, annotated photo
    - Own messages right-aligned with primary background; incoming left-aligned with surfaceVariant
    - Status icons: clock (pending), single check (sent), double check (read)
    - ChatInputBar has attachment button (48px), expandable TextField (1-5 lines, min 48px), send button (48px)
    - @mention autocomplete overlay shows on "@" input
    - Typing indicator shows animated dots + "{name} is typing..." with 3s auto-hide
    - `dart analyze lib/features/chat/presentation/` passes
  </acceptance_criteria>
  <done>Complete mobile chat UI: thread screen with WS, message bubbles with all variants, input bar with attachments and @mentions, typing indicator with auto-hide</done>
</task>

</tasks>

<verification>
- ChatScreen shows thread list with two sections
- ChatThreadScreen connects WebSocket and shows messages reactively
- MessageBubble renders all 4 types with correct alignment and colors
- ChatInputBar sends messages via WS/SyncQueue with attachment support
- Navigation: project detail → chat → thread works via GoRouter
- All files pass dart analyze
</verification>

<success_criteria>
- `dart analyze lib/features/chat/` passes with no errors
- ChatScreen renders thread list from Drift stream
- ChatThreadScreen connects WS and displays messages
- Send button creates message in Drift and sends via WS
</success_criteria>

<output>
After completion, create `.planning/phases/23-real-time-chat/23-04-SUMMARY.md`
</output>
