---
phase: 23-real-time-chat
plan: 06
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - backend/tests/test_phase_23_e2e.py
  - mobile/test/e2e/phase_23_real_time_chat_e2e_test.dart
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05]

must_haves:
  truths:
    - "Backend E2E tests cover all 5 CHAT requirements end-to-end"
    - "Flutter E2E tests cover thread list, message send/receive, file sharing, offline queue, and FCM notification"
    - "All tests pass in CI without manual intervention"
  artifacts:
    - path: "backend/tests/test_phase_23_e2e.py"
      provides: "Backend E2E integration tests covering CHAT-01 through CHAT-05"
      contains: "test_gc_sends_message"
    - path: "mobile/test/e2e/phase_23_real_time_chat_e2e_test.dart"
      provides: "Flutter E2E widget tests covering full chat flow"
      contains: "phase_23"
  key_links:
    - from: "backend/tests/test_phase_23_e2e.py"
      to: "backend/app/features/chat/router.py"
      via: "ASGI test client hitting chat REST endpoints"
      pattern: "/api/v1/chat"
    - from: "mobile/test/e2e/phase_23_real_time_chat_e2e_test.dart"
      to: "mobile/lib/features/chat/presentation/screens/chat_screen.dart"
      via: "Widget tests pumping ChatScreen and ChatThreadScreen"
      pattern: "ChatScreen"
---

<objective>
Phase 23 E2E tests: comprehensive backend integration tests and Flutter widget E2E tests covering all CHAT-01 through CHAT-05 requirements.

Purpose: Ensures all chat features work end-to-end before phase verification. Required by CLAUDE.md: "Every phase MUST have E2E tests covering all verification items."
Output: test_phase_23_e2e.py (backend) + phase_23_real_time_chat_e2e_test.dart (Flutter).
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-real-time-chat/23-CONTEXT.md
@.planning/phases/23-real-time-chat/23-03-SUMMARY.md
@.planning/phases/23-real-time-chat/23-04-SUMMARY.md

<interfaces>
<!-- Backend endpoints to test -->

```
GET  /api/v1/chat/threads?project_id={id}
GET  /api/v1/chat/threads/{id}/messages?before_seq={n}&limit=50
POST /api/v1/chat/threads/{id}/messages
POST /api/v1/chat/threads/{id}/read
GET  /api/v1/chat/threads/{id}/receipts
POST /api/v1/chat/messages/{id}/attachment
PUT  /api/v1/chat/threads/{id}/mute
POST /api/v1/chat/threads
WS   /api/v1/ws/chat/{thread_id}?token=...
```

<!-- Flutter screens/widgets to test -->

```dart
ChatScreen(projectId: String)      // Thread list
ChatThreadScreen(threadId: String, projectId: String)  // Message view
ChatThreadTile                      // Thread list item
MessageBubble                       // Message display
ChatInputBar                        // Message input
TypingIndicator                     // Typing status
```

<!-- Provider mocking pattern -->

```dart
// From existing E2E tests:
ProviderScope(overrides: [
  chatDaoProvider.overrideWithValue(mockChatDao),
  chatThreadsProvider(projectId).overrideWith((_) => Stream.value(threads)),
  chatMessagesProvider(threadId).overrideWith((_) => Stream.value(messages)),
])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend Phase 23 E2E integration tests</name>
  <files>
    backend/tests/test_phase_23_e2e.py
  </files>
  <read_first>
    backend/tests/test_chat.py,
    backend/tests/conftest.py,
    backend/app/features/chat/router.py,
    backend/app/features/chat/service.py,
    backend/app/features/chat/schemas.py,
    backend/app/features/notifications/service.py
  </read_first>
  <action>
    Create `backend/tests/test_phase_23_e2e.py` with comprehensive E2E tests. These complement the unit/integration tests in test_chat.py (Plan 03) by testing full user flows.

    **Fixtures:**
    - `gc_user_and_token` — create GC user + JWT token
    - `contractor_user_and_token` — create contractor user + JWT token
    - `project_with_trade_scope` — create project + trade scope with contractor assigned
    - `chat_thread` — create scope thread for trade scope with GC + contractor membership

    **E2E Test Scenarios (minimum 15):**

    CHAT-01: GC sends text message to contractor
    1. `test_e2e_gc_sends_text_message` — GC creates thread, sends message via POST, verify 201 + seq assigned + content matches
    2. `test_e2e_message_appears_in_thread_history` — GC sends 3 messages, GET history, verify all 3 in correct seq order
    3. `test_e2e_message_with_mentions` — Send message with mentions=[contractor_id], verify mentions field stored

    CHAT-02: Contractor replies in real-time
    4. `test_e2e_contractor_sends_reply` — Contractor sends message to same thread, verify seq increments
    5. `test_e2e_conversation_flow` — GC sends, contractor replies, GC replies again — GET history shows all 3 in order
    6. `test_e2e_message_dedup` — Send same message_id twice, GET history shows only 1 message

    CHAT-03: Photo and file sharing
    7. `test_e2e_photo_attachment_upload` — POST message, then POST attachment (photo file), verify attachment_url set
    8. `test_e2e_pdf_attachment_upload` — POST message with PDF file, verify attachment_type='pdf'
    9. `test_e2e_annotated_photo_share` — POST message with attachment_type='annotated_photo' and annotation_data JSON

    CHAT-04: Threads per trade scope
    10. `test_e2e_scope_thread_auto_creation` — Create trade scope thread, verify thread_type='scope' and both GC + contractor are members
    11. `test_e2e_project_wide_thread` — Create project_wide thread, verify all contractors + GC are members
    12. `test_e2e_threads_isolated_per_project` — Create threads for 2 projects, GET threads for project A, verify only project A threads returned
    13. `test_e2e_thread_idempotent_creation` — Create scope thread twice for same trade_scope, verify only 1 exists

    CHAT-05: Push notifications
    14. `test_e2e_fcm_notification_on_message` — Mock NotificationService.send_chat_notification, send message, verify it was called with correct params (sender_name, trade_scope_name, preview)
    15. `test_e2e_mention_overrides_mute` — Mute thread for contractor, send message with mention, verify FCM still fires for mentioned user
    16. `test_e2e_muted_user_skipped_without_mention` — Mute thread, send message WITHOUT mention, verify FCM not fired for muted user

    Cross-cutting:
    17. `test_e2e_read_receipts_flow` — GC sends message, contractor marks read at seq=N, GC fetches receipts, verify contractor's read receipt
    18. `test_e2e_cursor_pagination_full_flow` — Insert 60 messages, paginate with before_seq in 3 batches of 20, verify complete coverage
    19. `test_e2e_rls_cross_tenant_isolation` — Company A thread inaccessible with Company B token

    Use `unittest.mock.patch` for FCM tests (mock `NotificationService._send_to_token` or `send_chat_notification`).
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run python -m pytest tests/test_phase_23_e2e.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - At least 15 test functions in test_phase_23_e2e.py
    - Tests cover all 5 CHAT requirements: CHAT-01 (message send), CHAT-02 (reply flow), CHAT-03 (file sharing), CHAT-04 (thread organization), CHAT-05 (FCM notifications)
    - FCM tests mock the notification service — no actual Firebase calls
    - RLS isolation test verifies cross-tenant protection
    - All tests pass with `pytest tests/test_phase_23_e2e.py -x`
  </acceptance_criteria>
  <done>15+ backend E2E tests pass; all CHAT requirements have test coverage; RLS and FCM mocking verified</done>
</task>

<task type="auto">
  <name>Task 2: Create Flutter Phase 23 E2E widget tests</name>
  <files>
    mobile/test/e2e/phase_23_real_time_chat_e2e_test.dart
  </files>
  <read_first>
    mobile/lib/features/chat/presentation/screens/chat_screen.dart,
    mobile/lib/features/chat/presentation/screens/chat_thread_screen.dart,
    mobile/lib/features/chat/presentation/widgets/message_bubble.dart,
    mobile/lib/features/chat/presentation/widgets/chat_input_bar.dart,
    mobile/lib/features/chat/domain/chat_providers.dart,
    mobile/lib/features/chat/data/chat_dao.dart,
    mobile/test/e2e/phase_22_task_execution_e2e_test.dart
  </read_first>
  <action>
    Create `mobile/test/e2e/phase_23_real_time_chat_e2e_test.dart` following existing E2E test patterns.

    **Setup:**
    - Import `package:drift/drift.dart hide isNotNull, isNull;` (MEMORY.md pattern)
    - Use in-memory Drift DB for ChatDao tests
    - Mock Dio for API calls (MockDioClient pattern from existing E2E tests)
    - Mock ChatWsClient to simulate WebSocket events
    - Use ProviderScope overrides for all providers
    - NEVER use `pumpAndSettle()` with Drift streams — use `pump()` (MEMORY.md)

    **Test data factory:**
    - `makeChatThread({threadType, name, unreadCount, lastMessageAt})`
    - `makeChatMessage({threadId, senderId, senderName, content, seq, status, attachmentType})`

    **E2E Test Scenarios (minimum 16):**

    CHAT-01: GC sends text message
    1. `test_chat_screen_shows_thread_list` — Seed 2 threads (1 scope + 1 project_wide), pump ChatScreen, verify both thread names visible
    2. `test_thread_tile_shows_unread_badge` — Seed thread with unreadCount=5, verify Badge shows "5"
    3. `test_thread_tile_hides_badge_when_read` — Seed thread with unreadCount=0, verify no badge widget
    4. `test_send_text_message` — Pump ChatThreadScreen, type "Hello", tap send button, verify ChatDao.insertMessage called with content "Hello"

    CHAT-02: Contractor replies
    5. `test_incoming_message_appears` — Pump ChatThreadScreen with initial messages, simulate WS message event, pump(), verify new message bubble appears
    6. `test_messages_in_seq_order` — Seed 5 messages with different seq, verify displayed in ascending seq order
    7. `test_message_dedup_on_echo` — Send message (pending), simulate server echo with same ID, verify only 1 bubble

    CHAT-03: File sharing
    8. `test_photo_message_bubble` — Seed message with attachmentType='photo', verify image thumbnail rendered
    9. `test_pdf_message_bubble` — Seed message with attachmentType='pdf', attachmentUrl with .pdf, verify PDF icon + filename shown
    10. `test_annotated_photo_bubble` — Seed message with attachmentType='annotated_photo' + annotationData, verify "Annotated photo" chip visible
    11. `test_attachment_picker_shows_options` — Tap attachment button, verify bottom sheet with "Photo", "Document" options

    CHAT-04: Thread organization
    12. `test_thread_sections` — Seed scope and project_wide threads, verify "Trade Conversations" and "Project Group" section headers
    13. `test_empty_thread_list` — No threads, verify "No conversations yet" empty state text
    14. `test_thread_sorted_unread_first` — Seed 3 threads (1 unread, 2 read), verify unread thread appears first

    CHAT-05: Offline + FCM
    15. `test_offline_message_shows_clock_icon` — Send message while offline (mock connectivity=false), verify clock icon on bubble (Icons.schedule)
    16. `test_offline_message_queued_in_sync_queue` — Send offline, verify SyncQueue entry created with entityType='chat_message'

    Additional:
    17. `test_typing_indicator_shows` — Simulate WS typing event, pump(), verify typing indicator text visible
    18. `test_typing_indicator_auto_hides` — Simulate typing event, advance fake timers by 4 seconds, verify indicator hidden
    19. `test_read_receipt_display` — Seed own message with read receipts, verify double check icon or "Read by" text
    20. `test_mute_thread_long_press` — Long-press thread tile, verify "Mute Conversation" option in bottom sheet

    Use `tester.pump()` throughout (never `pumpAndSettle`). Use `Stream.value()` for provider overrides to avoid pending timers.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/e2e/phase_23_real_time_chat_e2e_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - At least 16 test functions in phase_23_real_time_chat_e2e_test.dart
    - Tests cover all 5 CHAT requirements
    - NO `pumpAndSettle()` calls (MEMORY.md: Drift streams never settle)
    - Uses `Stream.value()` for Drift provider overrides
    - Import uses `hide isNotNull, isNull` for Drift (MEMORY.md)
    - All tests pass with `flutter test test/e2e/phase_23_real_time_chat_e2e_test.dart`
    - Mock patterns match existing E2E tests (MockDioClient, ProviderScope overrides)
  </acceptance_criteria>
  <done>16+ Flutter E2E tests pass; all CHAT requirements have mobile test coverage; no pumpAndSettle with Drift streams</done>
</task>

</tasks>

<verification>
- Backend: `pytest tests/test_phase_23_e2e.py -x` — all 15+ tests pass
- Mobile: `flutter test test/e2e/phase_23_real_time_chat_e2e_test.dart` — all 16+ tests pass
- Every CHAT requirement (01-05) has at least 2 tests across both platforms
</verification>

<success_criteria>
- All backend E2E tests pass
- All Flutter E2E tests pass
- Full CHAT-01 through CHAT-05 coverage verified
- No existing tests broken (full suite green)
</success_criteria>

<output>
After completion, create `.planning/phases/23-real-time-chat/23-06-SUMMARY.md`
</output>
