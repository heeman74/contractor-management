---
phase: 04-job-lifecycle
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/pubspec.yaml
  - mobile/lib/features/client/presentation/screens/job_request_form_screen.dart
  - mobile/lib/core/routing/route_names.dart
  - mobile/lib/core/routing/app_router.dart
  - mobile/lib/features/client/presentation/screens/client_portal_screen.dart
autonomous: true
requirements: [CLNT-04]
gap_closure: true

must_haves:
  truths:
    - "Client can tap 'Add photos' on the in-app job request form and the device gallery picker opens"
    - "Selected photos appear as file-based thumbnails in the form (not placeholder icons)"
    - "Client can navigate to the job request form from the client portal screen"
    - "GoRouter resolves /client/request to JobRequestFormScreen"
  artifacts:
    - path: "mobile/pubspec.yaml"
      provides: "image_picker dependency"
      contains: "image_picker:"
    - path: "mobile/lib/features/client/presentation/screens/job_request_form_screen.dart"
      provides: "Functional photo picker using ImagePicker"
      contains: "ImagePicker"
    - path: "mobile/lib/core/routing/route_names.dart"
      provides: "jobRequestForm route constant"
      contains: "jobRequestForm"
    - path: "mobile/lib/core/routing/app_router.dart"
      provides: "GoRoute for JobRequestFormScreen"
      contains: "JobRequestFormScreen"
    - path: "mobile/lib/features/client/presentation/screens/client_portal_screen.dart"
      provides: "Navigation button to job request form"
      contains: "jobRequestForm"
  key_links:
    - from: "mobile/lib/features/client/presentation/screens/client_portal_screen.dart"
      to: "mobile/lib/features/client/presentation/screens/job_request_form_screen.dart"
      via: "context.go(RouteNames.jobRequestForm)"
      pattern: "context\\.go.*jobRequestForm"
    - from: "mobile/lib/core/routing/app_router.dart"
      to: "mobile/lib/features/client/presentation/screens/job_request_form_screen.dart"
      via: "GoRoute builder"
      pattern: "JobRequestFormScreen"
---

<objective>
Close two verification gaps in Phase 4 that prevent CLNT-04 (client-initiated job requests) from being fully satisfied on mobile.

Gap 1: The photo picker in JobRequestFormScreen is a SnackBar stub -- image_picker is not in pubspec.yaml. Replace with functional ImagePicker integration showing real thumbnails.

Gap 2: JobRequestFormScreen has no GoRoute registration. Clients cannot navigate to the in-app request form. Add route constant, register GoRoute under the client branch, and add a navigation entry point from ClientPortalScreen.

Purpose: Complete the in-app client job request flow so CLNT-04 is fully satisfied (backend + web form + admin review already verified).
Output: Functional photo picker and navigable route to the job request form.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-job-lifecycle/04-VERIFICATION.md
@mobile/lib/features/client/presentation/screens/job_request_form_screen.dart
@mobile/lib/core/routing/app_router.dart
@mobile/lib/core/routing/route_names.dart
@mobile/lib/features/client/presentation/screens/client_portal_screen.dart
@mobile/pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement functional photo picker in JobRequestFormScreen</name>
  <files>
    mobile/pubspec.yaml
    mobile/lib/features/client/presentation/screens/job_request_form_screen.dart
  </files>
  <action>
    1. Add `image_picker: ^1.1.2` to the `dependencies` section of `mobile/pubspec.yaml` (after `flutter_secure_storage`).

    2. In `job_request_form_screen.dart`, add the import:
       `import 'dart:io';`
       `import 'package:image_picker/image_picker.dart';`

    3. Replace the entire `_pickPhoto()` method body. Remove the SnackBar stub. New implementation:
       - Create `final picker = ImagePicker();`
       - Call `final file = await picker.pickImage(source: ImageSource.gallery, maxWidth: 1200, maxHeight: 1200, imageQuality: 80);`
       - If `file != null`, call `setState(() => _photoPaths.add(file.path));`
       - If `file == null`, do nothing (user cancelled picker).
       - Do NOT wrap in try/catch with empty catch -- if an error occurs, let it propagate or show a SnackBar with the error message (`debugPrint` at minimum per CLAUDE.md).

    4. Replace the photo thumbnail display. Currently the `itemBuilder` in the horizontal ListView renders a plain Container with an Icon. Change it to:
       - Use `ClipRRect(borderRadius: BorderRadius.circular(8), child: Image.file(File(_photoPaths[index]), width: 80, height: 80, fit: BoxFit.cover))`.
       - Keep the existing red circle delete button (Positioned X icon) exactly as-is on top of the thumbnail.
       - Keep the existing 80x80 size.

    5. Do NOT change any other form fields, validation, or submission logic. Only the photo picker and thumbnails change.
  </action>
  <verify>
    `grep -q "image_picker:" mobile/pubspec.yaml && grep -q "ImagePicker" mobile/lib/features/client/presentation/screens/job_request_form_screen.dart && grep -q "Image.file" mobile/lib/features/client/presentation/screens/job_request_form_screen.dart && echo "PASS" || echo "FAIL"`
  </verify>
  <done>
    - image_picker is in pubspec.yaml dependencies
    - _pickPhoto() calls ImagePicker().pickImage(source: ImageSource.gallery)
    - Photo thumbnails render via Image.file() with the actual file path
    - No SnackBar stub remains in _pickPhoto()
  </done>
</task>

<task type="auto">
  <name>Task 2: Register GoRoute for JobRequestFormScreen and add navigation entry point</name>
  <files>
    mobile/lib/core/routing/route_names.dart
    mobile/lib/core/routing/app_router.dart
    mobile/lib/features/client/presentation/screens/client_portal_screen.dart
  </files>
  <action>
    1. In `route_names.dart`, add a new constant under the "Client-only routes" section:
       `/// In-app job request form -- client submits a request for admin review.`
       `static const jobRequestForm = '/client/request';`

    2. In `app_router.dart`, add the import for JobRequestFormScreen:
       `import '../../features/client/presentation/screens/job_request_form_screen.dart';`

    3. In `app_router.dart`, add a GoRoute for the job request form inside Branch 6 (Client - Portal). The branch currently has only the `/client/portal` route. Add the new route as a sibling GoRoute within the same StatefulShellBranch `routes` list:
       ```
       GoRoute(
         path: RouteNames.jobRequestForm,
         builder: (context, state) => const JobRequestFormScreen(),
       ),
       ```
       Place it AFTER the existing clientPortal GoRoute within the Branch 6 routes list.

    4. In `client_portal_screen.dart`:
       - Add imports: `import 'package:go_router/go_router.dart';` and `import '../../../../core/routing/route_names.dart';`
       - Add a prominent "Submit a Job Request" FilledButton below the existing placeholder text. Use `onPressed: () => context.go(RouteNames.jobRequestForm)`.
       - Keep the existing placeholder content (icon, title, subtitle) -- just add the button below it with a SizedBox(height: 24) spacer.
       - The button should have an icon: `FilledButton.icon(icon: const Icon(Icons.add_task), label: const Text('Submit a Job Request'), onPressed: ...)`.

    5. The `/client/request` path starts with `/client` so the existing `_checkRoleAccess` function in app_router.dart will automatically gate it to users with the `client` role. No role guard changes needed.
  </action>
  <verify>
    `grep -q "jobRequestForm" mobile/lib/core/routing/route_names.dart && grep -q "JobRequestFormScreen" mobile/lib/core/routing/app_router.dart && grep -q "jobRequestForm" mobile/lib/features/client/presentation/screens/client_portal_screen.dart && echo "PASS" || echo "FAIL"`
  </verify>
  <done>
    - RouteNames.jobRequestForm constant exists with value '/client/request'
    - GoRoute for /client/request is registered in app_router.dart Branch 6
    - ClientPortalScreen has a "Submit a Job Request" button that navigates to RouteNames.jobRequestForm
    - Role gating works automatically via _checkRoleAccess (client prefix)
  </done>
</task>

</tasks>

<verification>
1. `grep "image_picker:" mobile/pubspec.yaml` returns a match
2. `grep "ImagePicker" mobile/lib/features/client/presentation/screens/job_request_form_screen.dart` returns a match
3. `grep "Image.file" mobile/lib/features/client/presentation/screens/job_request_form_screen.dart` returns a match
4. `grep "jobRequestForm" mobile/lib/core/routing/route_names.dart` returns a match
5. `grep "JobRequestFormScreen" mobile/lib/core/routing/app_router.dart` returns a match
6. `grep "jobRequestForm" mobile/lib/features/client/presentation/screens/client_portal_screen.dart` returns a match
7. No SnackBar stub text remains: `grep -c "add image_picker to pubspec.yaml" mobile/lib/features/client/presentation/screens/job_request_form_screen.dart` returns 0
</verification>

<success_criteria>
- CLNT-04 gap 1 (photo picker stub) is closed: image_picker dependency added, _pickPhoto() launches gallery, thumbnails show real images
- CLNT-04 gap 2 (missing route) is closed: /client/request resolves to JobRequestFormScreen, client can navigate from portal
- No regressions: existing form fields, validation, offline-first submission logic unchanged
- All 5 VERIFICATION.md truths can now be marked VERIFIED (truths 1-3 were already verified; truths 4-5 blocked only by these two gaps)
</success_criteria>

<output>
After completion, create `.planning/phases/04-job-lifecycle/04-09-SUMMARY.md`
</output>
