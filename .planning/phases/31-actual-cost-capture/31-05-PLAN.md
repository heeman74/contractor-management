---
phase: 31-actual-cost-capture
plan: 05
type: execute
wave: 4
depends_on: ["31-04"]
autonomous: true
requirements: [COST-01, COST-02, COST-03]
files_modified:
  - mobile/lib/features/finance/presentation/providers/cost_providers.dart
  - mobile/lib/features/finance/presentation/widgets/add_cost_sheet.dart
  - mobile/lib/features/finance/presentation/widgets/cost_entry_card.dart
  - mobile/lib/features/finance/presentation/widgets/cost_list_section.dart
  - mobile/lib/features/jobs/presentation/screens/job_detail_screen.dart
  - mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart
  - mobile/lib/features/projects/presentation/screens/project_detail_screen.dart
  - mobile/test/e2e/phase_31_cost_capture_e2e_test.dart
must_haves:
  truths:
    - "Owner/PM sees a Costs section + Add cost action on the mobile job detail and trade-scope detail screens"
    - "Owner/PM can add a materials/subcontractor/other cost entry via a bottom sheet, offline, and it appears in the list"
    - "Owner/PM can capture a receipt from camera or gallery when adding a cost entry"
    - "The project detail screen shows an aggregated Costs rollup section"
    - "A user without finance.view sees no Costs section / Add cost action on any screen"
  artifacts:
    - path: "mobile/lib/features/finance/presentation/widgets/add_cost_sheet.dart"
      provides: "offline cost-entry form with camera/gallery receipt capture"
      contains: "ImagePicker"
    - path: "mobile/test/e2e/phase_31_cost_capture_e2e_test.dart"
      provides: "offline create + receipt upload E2E"
      contains: "cost-entries"
  key_links:
    - from: "mobile/lib/features/jobs/presentation/screens/job_detail_screen.dart"
      to: "CostListSection"
      via: "gated section using cost providers"
      pattern: "CostListSection"
    - from: "mobile/lib/features/finance/presentation/widgets/add_cost_sheet.dart"
      to: "CostEntryDao + CostReceiptDao"
      via: "insert entry then insert local receipt (pending_upload)"
      pattern: "insertCostEntry"
---

<objective>
Build the mobile cost-capture UI on top of the 31-04 data layer: Riverpod providers, an
`AddCostSheet` bottom sheet with camera/gallery receipt capture, a cost list section wired
into the job detail and trade-scope detail screens, and an aggregated Costs rollup section
on the project detail screen — all gated by finance permission. Ship the phase mobile E2E
in the same change.

Purpose: COST-01/02/03 on the field surface (D-01/D-02). Receipt capture was explicitly
called out as first-class field capture (camera + gallery), offline-first. D-06 requires
the Costs entry points to be hidden without finance permission.

Output: A finance presentation layer wired into three screens + a green
`phase_31_cost_capture_e2e_test.dart` proving offline create + receipt upload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-actual-cost-capture/31-CONTEXT.md
@.planning/phases/31-actual-cost-capture/31-RESEARCH.md
@.planning/phases/31-actual-cost-capture/31-VALIDATION.md
@CLAUDE.md

<interfaces>
From Plan 31-04 (already built): CostEntryDao (insertCostEntry, softDeleteCostEntry,
watchByJob, watchByTradeScope, watchByProject, upsertFromRemote), CostReceiptDao
(insertReceipt, watchByCostEntry, getPendingUploads...), CostEntrySyncHandler (registered),
CostReceiptUploadService (wired into SyncEngine.pullDelta), FinanceRepository (on-demand
fetchers), Drift v16 CostEntries/CostReceipts tables.

Patterns to mirror (read them):
- mobile/lib/features/projects/presentation/widgets/task_photo_grid.dart — receipt thumbnail:
  resolveMediaUrl(remoteUrl) + headers: mediaAuthHeaders(), fallback Image.file(localPath) until uploaded
- mobile/lib/core/network/media_url.dart — resolveMediaUrl, mediaAuthHeaders()
- An existing AsyncNotifier/StreamProvider in features/projects for the watchByX -> provider pattern
- An existing permission gate on mobile (how the app reads roles/finance perms client-side —
  find the existing gating helper; hide the section when finance.view is absent)
- ImagePicker usage already in the app (chat/task photo capture) for camera+gallery
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cost providers + AddCostSheet (offline create + camera/gallery receipt) + cost widgets</name>
  <files>mobile/lib/features/finance/presentation/providers/cost_providers.dart, mobile/lib/features/finance/presentation/widgets/add_cost_sheet.dart, mobile/lib/features/finance/presentation/widgets/cost_entry_card.dart, mobile/lib/features/finance/presentation/widgets/cost_list_section.dart</files>
  <read_first>
    - mobile/lib/features/finance/data/cost_entry_dao.dart + cost_receipt_dao.dart + finance_repository.dart (the data layer from 31-04)
    - mobile/lib/features/projects/presentation/widgets/task_photo_grid.dart (thumbnail: resolveMediaUrl + mediaAuthHeaders, local-file fallback)
    - mobile/lib/core/network/media_url.dart (resolveMediaUrl, mediaAuthHeaders)
    - an existing bottom-sheet form in mobile (e.g. a task note / attachment sheet) for the input + ImagePicker capture pattern
    - CLAUDE.md (Riverpod: AsyncNotifier for async init; no bare `as` casts; no silent catch)
  </read_first>
  <action>
    Create mobile/lib/features/finance/presentation/providers/cost_providers.dart: StreamProviders
    `costEntriesForJobProvider(jobId)` / `costEntriesForTradeScopeProvider(scopeId)` /
    `costRollupForProjectProvider(projectId)` backed by the DAO watch streams; a
    `costCategoriesProvider` (FutureProvider from FinanceRepository.fetchCostCategories with Drift
    fallback); and a `financePermissionProvider` (or reuse an existing client-side permission read)
    exposing whether the user has finance.view / finance.manage. On screen open, trigger the on-demand
    FinanceRepository fetch (Future.microtask) so lists refresh from the server while showing cached Drift data.
    Create mobile/lib/features/finance/presentation/widgets/add_cost_sheet.dart: a modal bottom sheet
    with amount (numeric > 0), category (dropdown from costCategoriesProvider), incurredDate (defaults
    to today), vendor, note, and a receipt capture row using ImagePicker (camera AND gallery). On save:
    call CostEntryDao.insertCostEntry (queues sync), then for each captured image call
    CostReceiptDao.insertReceipt(localPath, uploadStatus 'pending_upload'); the SyncEngine drains the
    entry push + receipt upload on next sync. Takes an anchor (jobId XOR tradeScopeId) as a param.
    Create mobile/lib/features/finance/presentation/widgets/cost_entry_card.dart (category, amount
    formatted via toStringAsFixed(2), date, vendor; receipt thumbnails via task_photo_grid pattern;
    edit/delete actions only when finance.manage) and cost_list_section.dart (renders a list from a
    provider + an "Add cost" button shown only with finance.manage; empty state).
  </action>
  <verify>
    <automated>cd mobile && dart analyze lib/features/finance/presentation</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "insertCostEntry" mobile/lib/features/finance/presentation/widgets/add_cost_sheet.dart` succeeds
    - `grep -q "ImagePicker" mobile/lib/features/finance/presentation/widgets/add_cost_sheet.dart` succeeds (camera+gallery)
    - `grep -q "resolveMediaUrl" mobile/lib/features/finance/presentation/widgets/cost_entry_card.dart` succeeds (auth'd thumbnail)
    - `grep -q "toStringAsFixed(2)" mobile/lib/features/finance/presentation/widgets/cost_entry_card.dart` succeeds (Pitfall 5 — display rounding)
    - `dart analyze lib/features/finance/presentation` reports no errors
  </acceptance_criteria>
  <done>Providers, an offline-capable add-cost sheet with camera/gallery receipt capture, and cost list/card widgets exist and analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Costs sections into job/trade-scope/project detail screens, permission-gated</name>
  <files>mobile/lib/features/jobs/presentation/screens/job_detail_screen.dart, mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart, mobile/lib/features/projects/presentation/screens/project_detail_screen.dart</files>
  <read_first>
    - mobile/lib/features/jobs/presentation/screens/job_detail_screen.dart (the file being modified — where a section mounts)
    - mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart (the file being modified)
    - mobile/lib/features/projects/presentation/screens/project_detail_screen.dart (the file being modified — rollup section)
    - mobile/lib/features/finance/presentation/providers/cost_providers.dart + widgets (from Task 1)
  </read_first>
  <action>
    In job_detail_screen.dart: add a `CostListSection` bound to `costEntriesForJobProvider(jobId)`
    with an "Add cost" action opening `AddCostSheet(jobId: job.id)` — wrap the whole section so it
    renders only when the finance permission provider reports finance.view (and the Add action only
    with finance.manage).
    In trade_scope_detail_screen.dart: same, bound to `costEntriesForTradeScopeProvider(scopeId)` and
    `AddCostSheet(tradeScopeId: scope.id)`.
    In project_detail_screen.dart: add a read-only aggregated Costs rollup section bound to
    `costRollupForProjectProvider(projectId)` showing the total (display-only, rounded) + the itemized
    list; gated behind finance.view. Per CONTEXT Claude's-Discretion, an anchor-picker create path here
    is optional — a read-only rollup is the minimum; keep create on job/scope screens.
    Follow the existing GoRouter/navigation + section-composition conventions on each screen; no magic
    strings (use RouteNames if navigating).
  </action>
  <verify>
    <automated>cd mobile && dart analyze lib/features/jobs/presentation/screens/job_detail_screen.dart lib/features/projects/presentation/screens/trade_scope_detail_screen.dart lib/features/projects/presentation/screens/project_detail_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "CostListSection" mobile/lib/features/jobs/presentation/screens/job_detail_screen.dart` succeeds
    - `grep -q "costEntriesForTradeScopeProvider" mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart` succeeds
    - `grep -q "costRollupForProjectProvider" mobile/lib/features/projects/presentation/screens/project_detail_screen.dart` succeeds
    - Each screen gates the section on finance.view (grep for the finance permission provider/helper name)
    - `dart analyze` on the three screens reports no errors
  </acceptance_criteria>
  <done>All three screens show a permission-gated Costs section; job/scope screens offer offline Add cost, project screen shows the rollup.</done>
</task>

<task type="auto">
  <name>Task 3: Mobile phase E2E (offline create + receipt upload on reconnect)</name>
  <files>mobile/test/e2e/phase_31_cost_capture_e2e_test.dart</files>
  <read_first>
    - mobile/test/unit/features/finance/cost_receipt_upload_service_test.dart (from 31-04 — reuse the MockDio setup)
    - mobile/test/e2e (an existing phase_*_e2e_test.dart for the full-flow ProviderScope-override + MockDioClient pattern)
    - .claude/skills/e2e-feature-tests/SKILL.md (mobile E2E: real in-memory Drift, mock Dio + plugins, assert captured request + payload)
    - mobile/lib/features/finance/presentation/widgets/add_cost_sheet.dart (the flow under test)
  </read_first>
  <action>
    Create mobile/test/e2e/phase_31_cost_capture_e2e_test.dart. Use a real in-memory AppDatabase and
    MockDioClient. Cover: (1) offline create — drive AddCostSheet (or the DAO+provider path) to add a
    cost entry with a local receipt while "offline" (Dio not called), assert the entry is persisted in
    Drift and a sync_queue row (entityType 'cost_entry') exists and the receipt is uploadStatus
    'pending_upload'; (2) reconnect drain — run the sync-queue drain + SyncEngine.pullDelta with a
    MockDio that accepts POST /cost-entries and POST /cost-entries/{id}/receipts, assert the captured
    request paths + payload (category_id, amount, anchor) and that the receipt transitions to
    'uploaded' with a remoteUrl; (3) gating — a widget test asserting the Costs section is absent when
    the finance permission provider reports no finance.view. Mock ImagePicker at the plugin level.
    Register any custom fallback values (mocktail) in setUpAll as needed (STATE.md pattern).
  </action>
  <verify>
    <automated>cd mobile && flutter test test/e2e/phase_31_cost_capture_e2e_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/test/e2e/phase_31_cost_capture_e2e_test.dart` exists
    - The E2E asserts a captured `POST /cost-entries` and `POST /cost-entries/{id}/receipts` (grep for both paths)
    - The E2E asserts the receipt transitions to `uploaded` after drain (grep for `uploaded`)
    - `flutter test test/e2e/phase_31_cost_capture_e2e_test.dart` exits 0
    - `dart analyze test/e2e/phase_31_cost_capture_e2e_test.dart` reports no errors
  </acceptance_criteria>
  <done>The offline-create-then-upload-on-reconnect flow and the permission-gating case are covered by green mobile E2E.</done>
</task>

</tasks>

<verification>
- `cd mobile && dart analyze lib/features/finance lib/features/jobs lib/features/projects test` clean.
- `cd mobile && flutter test` — full mobile suite green (phase E2E + unit tests + no sibling regressions).
</verification>

<success_criteria>
- COST-01/02/03 on mobile: offline add materials/subcontractor/other cost entries with camera/gallery receipt capture on job + trade-scope screens; project screen shows the rollup.
- Receipts upload on reconnect via the working retry/backoff service (proven by E2E).
- D-06: Costs sections hidden without finance.view.
</success_criteria>

<output>
After completion, create `.planning/phases/31-actual-cost-capture/31-05-SUMMARY.md`.
</output>
