---
phase: 31-actual-cost-capture
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
autonomous: true
requirements: [COST-03]
files_modified:
  - backend/app/features/finance/router.py
  - backend/app/features/finance/service.py
  - backend/app/features/finance/repository.py
  - backend/app/features/files/serve_router.py
  - backend/tests/test_phase_31_e2e.py
must_haves:
  truths:
    - "Owner/PM can upload a receipt file to a cost entry and fetch it back through /files/cost-receipts/..."
    - "A cost entry can hold multiple receipts (zero-to-many)"
    - "Tenant B cannot fetch Tenant A's receipt file — returns 404, not the bytes"
    - "A user without finance.manage gets 403 on upload and delete receipt endpoints; without finance.view gets 403 on list receipts"
  artifacts:
    - path: "backend/app/features/files/serve_router.py"
      provides: "authenticated, RLS-scoped serving of /files/cost-receipts/{cost_entry_id}/{filename}"
      contains: "cost-receipts"
    - path: "backend/app/features/finance/router.py"
      provides: "receipt upload/list/delete endpoints gated by finance.*"
      contains: "receipts"
  key_links:
    - from: "backend/app/features/files/serve_router.py"
      to: "CostReceipt"
      via: "RLS-scoped existence query on remote_url"
      pattern: "CostReceipt.remote_url"
    - from: "backend/app/features/finance/router.py"
      to: "_save_cost_receipt_file"
      via: "multipart UploadFile saved under uploads/cost-receipts/{cost_entry_id}"
      pattern: "cost-receipts"
---

<objective>
Add receipt capture to the backend: a multipart upload endpoint that stores files under
`uploads/cost-receipts/{cost_entry_id}/`, list + delete receipt endpoints, and a new
`cost-receipts` branch in the authenticated `serve_router` so receipts are served only to
same-tenant callers. Ship the receipt backend E2E in the same change.

Purpose: COST-03 requires Owner/PM to attach receipt photos (optional, multiple) to a cost
entry, retrievable only within their tenant. This mirrors the working task-attachment upload
mechanics on the SERVER side (`_save_task_attachment_file` + the `task-attachments` serve
branch) — the RLS-scoped existence check is what enforces cross-tenant 404.

Output: `POST/GET/DELETE /api/v1/cost-entries/{id}/receipts` gated by finance.*, a
`cost-receipts` serve branch, and passing receipt tests in `test_phase_31_e2e.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-actual-cost-capture/31-CONTEXT.md
@.planning/phases/31-actual-cost-capture/31-RESEARCH.md
@.planning/phases/31-actual-cost-capture/31-VALIDATION.md
@CLAUDE.md

<interfaces>
From Plan 31-01 (already built): CostReceipt model (cost_receipts table, cost_entry_id,
remote_url, caption), CostReceiptResponse schema, FinanceService/FinanceRepository,
finance router with cost-entry endpoints, test_phase_31_e2e.py cost-entry tests.

serve_router.py existing `task-attachments` branch (the exact template to copy):
```python
elif category == "task-attachments":
    result = await db.execute(
        select(TaskAttachment.id).where(TaskAttachment.remote_url == f"/files/{file_path}").limit(1))
    if result.scalars().first() is None:
        raise _not_found()
```
Upload template — backend/app/features/projects/router.py:
```python
_MAX_ATTACHMENT_BYTES = 25 * 1024 * 1024   # line ~584
async def _save_task_attachment_file(task_id, file: UploadFile) -> str: ...  # line ~600
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Receipt upload/list/delete endpoints + service/repository receipt methods</name>
  <files>backend/app/features/finance/router.py, backend/app/features/finance/service.py, backend/app/features/finance/repository.py</files>
  <read_first>
    - backend/app/features/projects/router.py (lines ~584-670: `_MAX_ATTACHMENT_BYTES`, `_save_task_attachment_file`, `upload_task_attachment` — mirror exactly for receipts)
    - backend/app/features/finance/router.py (the file being modified — add receipt handlers below cost-entry handlers)
    - backend/app/features/finance/service.py (the file being modified — add add_receipt/list_receipts/delete_receipt)
    - backend/app/features/finance/repository.py (the file being modified — add receipt queries with deleted_at filter where applicable)
    - backend/app/features/billing_milestones/router.py (inline require_permission gating pattern)
  </read_first>
  <action>
    In backend/app/features/finance/router.py add a module-level helper
    `async def _save_cost_receipt_file(cost_entry_id: uuid.UUID, file: UploadFile) -> str:` copied
    from `_save_task_attachment_file` but writing to `Path("uploads")/"cost-receipts"/str(cost_entry_id)`,
    reusing the 25 MB limit (import or redefine `_MAX_COST_RECEIPT_BYTES = 25 * 1024 * 1024`; raise
    HTTPException(413) when exceeded), and returning `f"/files/cost-receipts/{cost_entry_id}/{unique_filename}"`
    (unique_filename = `f"{uuid.uuid4()}{suffix}"`). Add endpoints:
    - `POST /cost-entries/{cost_entry_id}/receipts` (multipart `file: UploadFile`,
      `caption: Annotated[str|None, Form()] = None`) -> require_permission("finance.manage");
      first `get_entry_or_404(cost_entry_id)` so a forged/foreign id 404s under RLS; save file;
      `FinanceService.add_receipt(cost_entry_id, remote_url, caption)`; 201 CostReceiptResponse.
    - `GET /cost-entries/{cost_entry_id}/receipts` -> require_permission("finance.view");
      list receipts for the entry (exclude deleted_at); 200 list[CostReceiptResponse].
    - `DELETE /cost-entries/{cost_entry_id}/receipts/{receipt_id}` ->
      require_permission("finance.manage"); soft-delete the receipt row (set deleted_at); 204.
    In service.py add `add_receipt`, `list_receipts`, `delete_receipt`; in repository.py add
    `list_receipts_for_entry` (filter `CostReceipt.deleted_at.is_(None)`) and `get_receipt_or_404`.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -c "from app.features.finance.router import router; paths=[r.path for r in router.routes]; assert any('receipts' in p for p in paths), paths; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "_save_cost_receipt_file" backend/app/features/finance/router.py` succeeds
    - `grep -q '/cost-entries/{cost_entry_id}/receipts' backend/app/features/finance/router.py` succeeds
    - Receipt write endpoints gated: `grep -c 'require_permission("finance.manage")' backend/app/features/finance/router.py` increased vs 31-01 (upload + delete)
    - `grep -q "CostReceipt.deleted_at.is_(None)" backend/app/features/finance/repository.py` succeeds
    - `grep -q "413" backend/app/features/finance/router.py` succeeds (size guard)
    - The python route-inspection verify prints `ok`
  </acceptance_criteria>
  <done>Upload/list/delete receipt endpoints exist, gated by finance.*, storing files under uploads/cost-receipts and returning /files/cost-receipts URLs.</done>
</task>

<task type="auto">
  <name>Task 2: serve_router cost-receipts branch (RLS-scoped existence check)</name>
  <files>backend/app/features/files/serve_router.py</files>
  <read_first>
    - backend/app/features/files/serve_router.py (the file being modified — the `task-attachments` elif is the exact template; also its import block)
    - backend/app/features/finance/models.py (CostReceipt — the model to import + query)
    - backend/tests/test_file_serving_auth_e2e.py (cross-tenant serving test pattern, e.g. test_other_tenant_cannot_fetch_task_photo)
  </read_first>
  <action>
    In backend/app/features/files/serve_router.py: add `from app.features.finance.models import CostReceipt`
    to the imports. In `serve_upload`, add a new branch copied EXACTLY from the `task-attachments`
    branch (do NOT use a bare UUID-format check — the RLS-scoped row query is what makes cross-tenant
    requests resolve to zero rows -> 404, per research Pitfall 6):
    ```python
    elif category == "cost-receipts":
        result = await db.execute(
            select(CostReceipt.id).where(CostReceipt.remote_url == f"/files/{file_path}").limit(1))
        if result.scalars().first() is None:
            raise _not_found()
    ```
    Place it alongside the other `elif` branches, before the final `else: raise _not_found()`.
    No other changes — `_safe_resolve` path-traversal guard and `get_current_user` auth are shared.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -c "import app.features.files.serve_router as s; import inspect; src=inspect.getsource(s); assert 'cost-receipts' in src and 'CostReceipt' in src; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "cost-receipts" backend/app/features/files/serve_router.py` succeeds
    - `grep -q "from app.features.finance.models import CostReceipt" backend/app/features/files/serve_router.py` succeeds
    - `grep -q "CostReceipt.remote_url == f\"/files/{file_path}\"" backend/app/features/files/serve_router.py` succeeds (RLS-scoped, not a UUID-shape check)
    - The python verify prints `ok`
  </acceptance_criteria>
  <done>The cost-receipts serve branch performs an RLS-scoped existence query so a same-tenant caller gets the file and a cross-tenant caller gets 404.</done>
</task>

<task type="auto">
  <name>Task 3: Extend test_phase_31_e2e.py with receipt E2E (upload/fetch, multiple, cross-tenant 404, 403)</name>
  <files>backend/tests/test_phase_31_e2e.py</files>
  <read_first>
    - backend/tests/test_phase_31_e2e.py (the file being extended — reuse its `_token`/fixtures + existing cost-entry helpers)
    - backend/tests/test_file_serving_auth_e2e.py (test_other_tenant_cannot_fetch_task_photo — mirror for receipts)
    - .claude/skills/e2e-feature-tests/SKILL.md (backend E2E conventions; multipart upload via ASGI client)
    - backend/tests/conftest.py (seed_two_tenants, tenant_a_client, tenant_b_client)
  </read_first>
  <action>
    Add to backend/tests/test_phase_31_e2e.py these EXACT test names (per 31-VALIDATION.md):
    - `test_upload_and_fetch_cost_receipt`: create an entry, POST a small file to
      `/cost-entries/{id}/receipts` (multipart), assert 201 + a `/files/cost-receipts/...` remote_url,
      then GET that `/files/cost-receipts/...` path with the same tenant's client and assert 200.
    - `test_multiple_receipts_per_cost_entry`: upload two files to one entry; GET
      `/cost-entries/{id}/receipts` returns 2 rows.
    - `test_other_tenant_cannot_fetch_cost_receipt`: tenant A uploads a receipt; tenant B GETs the
      same `/files/cost-receipts/...` path and asserts 404 (mirror test_other_tenant_cannot_fetch_task_photo).
    - Extend `test_non_finance_role_403_on_every_cost_endpoint` (or add a receipt-focused assert
      block) to also assert 403 for an admin/non-finance token on POST upload, GET list receipts,
      and DELETE receipt endpoints.
    Use the ASGI client's multipart form (`files={"file": ("r.jpg", b"...", "image/jpeg")}`,
    `data={"caption": "..."}`). Clean up written files is optional (test uploads dir is disposable).
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_31_e2e.py -q -k "receipt"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def test_upload_and_fetch_cost_receipt" backend/tests/test_phase_31_e2e.py` succeeds
    - `grep -q "def test_multiple_receipts_per_cost_entry" backend/tests/test_phase_31_e2e.py` succeeds
    - `grep -q "def test_other_tenant_cannot_fetch_cost_receipt" backend/tests/test_phase_31_e2e.py` succeeds
    - `pytest tests/test_phase_31_e2e.py -q -k "receipt"` exits 0
    - Full file green: `pytest tests/test_phase_31_e2e.py -q` exits 0
    - `ruff check backend/tests/test_phase_31_e2e.py` exits 0
  </acceptance_criteria>
  <done>All COST-03 receipt behaviors (upload, fetch, multiple, cross-tenant 404, 403 gating) are covered by green backend E2E tests.</done>
</task>

</tasks>

<verification>
- `cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_31_e2e.py -q` — entire Phase 31 backend suite green (entries + receipts).
- `cd backend && source .venv/bin/activate && python -m pytest tests/test_file_serving_auth_e2e.py -q` — no regression in existing serve auth tests.
- `ruff check` / `ruff format --check` clean on modified files.
</verification>

<success_criteria>
- COST-03: receipts upload, list, delete, and serve authenticated + RLS-scoped; multiple receipts per entry supported.
- Cross-tenant receipt fetch returns 404 (Pitfall 6 closed).
- Non-finance role gets 403 on every receipt endpoint (criterion 4 extended to receipts).
</success_criteria>

<output>
After completion, create `.planning/phases/31-actual-cost-capture/31-02-SUMMARY.md`.
</output>
