---
phase: 31-actual-cost-capture
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
autonomous: true
requirements: [COST-01, COST-02, COST-03]
files_modified:
  - web/src/features/finance/api.ts
  - web/src/features/finance/types.ts
  - web/src/features/finance/hooks.ts
  - web/src/features/finance/components/AddCostDialog.tsx
  - web/src/features/finance/components/CostEntryList.tsx
  - web/src/features/finance/components/ProjectCostsCard.tsx
  - web/src/app/(dashboard)/projects/components/ProjectDetail.tsx
  - web/src/app/(dashboard)/projects/components/TradeScopeDetail.tsx
  - web/src/app/(dashboard)/jobs/[id]/page.tsx
  - web/tests/cost-capture.spec.ts
  - web/src/features/finance/__tests__/cost-entry-form.test.tsx
must_haves:
  truths:
    - "Owner/PM sees a Costs section + Add cost action on the job detail, trade-scope detail, and project detail pages"
    - "Owner/PM can add a materials/subcontractor/other cost entry via a dialog (amount, category, date, vendor, note) and it appears in the list"
    - "Owner/PM can attach a receipt file when adding a cost entry"
    - "The project Costs card shows the aggregated rollup total plus itemized entries"
    - "A user without finance.view sees no Costs section at all; without finance.manage sees no Add/edit/delete controls"
  artifacts:
    - path: "web/src/features/finance/api.ts"
      provides: "finance API client (cost entries, categories, rollup, receipt upload)"
      contains: "uploadCostReceipt"
    - path: "web/src/features/finance/hooks.ts"
      provides: "TanStack Query hooks for cost data + mutations"
      contains: "useAddCostEntry"
    - path: "web/tests/cost-capture.spec.ts"
      provides: "Playwright E2E for gated cost capture flow"
      contains: "finance"
  key_links:
    - from: "web/src/features/finance/components/AddCostDialog.tsx"
      to: "web/src/features/finance/api.ts"
      via: "useAddCostEntry mutation calls createCostEntry"
      pattern: "createCostEntry"
    - from: "web/src/app/(dashboard)/projects/components/ProjectDetail.tsx"
      to: "ProjectCostsCard"
      via: "gated render behind usePermissions().can('finance.view')"
      pattern: "finance.view"
---

<objective>
Build the web cost-capture UI as a self-contained `web/src/features/finance/` module (API
client + types + hooks + components) and wire it into the job detail page, trade-scope
detail, and project detail — all gated by `usePermissions()`. Ship Playwright + Jest tests
in the same change.

Purpose: COST-01/02/03 on the desk surface (D-01 web = desk entry/upload; D-02 both inline
placements + a project-level Costs card that shows the rollup). D-06 requires the entire
Costs section to be hidden without `finance.view` and all Add/edit/delete controls hidden
without `finance.manage`.

Output: A finance feature module + three wired-in Costs sections + a green
`cost-capture.spec.ts` and a Jest form test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-actual-cost-capture/31-CONTEXT.md
@.planning/phases/31-actual-cost-capture/31-RESEARCH.md
@.planning/phases/31-actual-cost-capture/31-VALIDATION.md
@CLAUDE.md

<interfaces>
Backend endpoints (from 31-01/31-02), prefix /api/v1:
  POST   /cost-entries/                          body: {job_id|trade_scope_id, category_id, amount(str), incurred_date, vendor?, note?} -> 201 CostEntryResponse
  GET    /cost-entries/?job_id=X | ?trade_scope_id=X -> CostEntryResponse[]
  GET    /cost-entries/{id}                       -> CostEntryResponse
  PATCH  /cost-entries/{id}                       -> CostEntryResponse
  DELETE /cost-entries/{id}                       -> 204
  GET    /projects/{project_id}/cost-entries      -> {project_id, total(str), entries: CostEntryResponse[]}
  GET    /cost-categories/                         -> {id, name, is_system}[]
  POST   /cost-entries/{id}/receipts (multipart file, caption?) -> 201 CostReceiptResponse
  GET    /cost-entries/{id}/receipts               -> CostReceiptResponse[]
CostEntryResponse: {id, job_id, trade_scope_id, category_id, category_name?, amount(string), incurred_date, vendor?, note?, created_at, updated_at}

web/src/lib/api-client.ts (confirmed exports): apiGet, apiPost, apiPatch, apiDelete,
apiUpload(path, formData: FormData)  // apiUpload streams multipart through the proxy
web/src/lib/hooks/usePermissions.ts: const { can } = usePermissions(); can("finance.view") | can("finance.manage")
Pattern to mirror: web/src/lib/api/projects.ts (client), web/src/app/(dashboard)/projects/components/AddTradeScopeSheet.tsx (dialog+form-hook), ProjectDetail.tsx (Card composition)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: finance feature module — API client, types, hooks</name>
  <files>web/src/features/finance/api.ts, web/src/features/finance/types.ts, web/src/features/finance/hooks.ts</files>
  <read_first>
    - web/src/lib/api-client.ts (confirm apiGet/apiPost/apiPatch/apiDelete/apiUpload signatures — apiUpload is the multipart helper)
    - web/src/lib/api/projects.ts (client function style to mirror exactly)
    - web/src/app/(dashboard)/projects/hooks (existing useAddTradeScope-style TanStack Query mutation + invalidation pattern; read one hook file)
    - web/src/lib/hooks/usePermissions.ts (can(key) gating hook)
  </read_first>
  <action>
    Create the module under web/src/features/finance/:
    - `types.ts`: `CostEntry` (id, jobId|null, tradeScopeId|null, categoryId, categoryName?, amount: string, incurredDate: string, vendor?, note?, createdAt, updatedAt — map snake_case from API), `CostCategory` (id, name, isSystem), `CostReceipt` (id, costEntryId, remoteUrl, caption?), `ProjectCostRollup` (projectId, total: string, entries: CostEntry[]), `CostEntryInput` (categoryId, amount, incurredDate, vendor?, note?, plus jobId? / tradeScopeId?). Keep amount as string (Decimal-as-string).
    - `api.ts`: `fetchCostEntriesForJob(jobId)`, `fetchCostEntriesForTradeScope(tradeScopeId)`,
      `fetchProjectCostRollup(projectId)`, `fetchCostCategories()`, `createCostEntry(input)`,
      `updateCostEntry(id, patch)`, `deleteCostEntry(id)`,
      `uploadCostReceipt(costEntryId, file: File)` (build FormData, append "file", call
      `apiUpload<CostReceipt>(\`/api/v1/cost-entries/${costEntryId}/receipts\`, form)`),
      `fetchReceipts(costEntryId)`. Convert snake_case API fields to the camelCase types.
    - `hooks.ts`: TanStack Query hooks — `useCostEntriesForJob`, `useCostEntriesForTradeScope`,
      `useProjectCostRollup`, `useCostCategories`, and mutations `useAddCostEntry`,
      `useUpdateCostEntry`, `useDeleteCostEntry`, `useUploadReceipt` — each mutation invalidates
      the relevant list/rollup query keys on success (mirror the existing add-trade-scope hook's
      queryClient.invalidateQueries pattern). Query keys: ["cost-entries","job",jobId] etc.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "uploadCostReceipt" web/src/features/finance/api.ts` succeeds
    - `grep -q "apiUpload" web/src/features/finance/api.ts` succeeds (multipart via existing helper, no hand-rolled fetch)
    - `grep -q "useAddCostEntry" web/src/features/finance/hooks.ts` and `useProjectCostRollup` both succeed
    - `grep -q "invalidateQueries" web/src/features/finance/hooks.ts` succeeds
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The finance module compiles and exposes typed cost-entry/category/rollup/receipt fetchers and mutation hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Cost components + wire into job/trade-scope/project detail, permission-gated</name>
  <files>web/src/features/finance/components/AddCostDialog.tsx, web/src/features/finance/components/CostEntryList.tsx, web/src/features/finance/components/ProjectCostsCard.tsx, web/src/app/(dashboard)/projects/components/ProjectDetail.tsx, web/src/app/(dashboard)/projects/components/TradeScopeDetail.tsx, web/src/app/(dashboard)/jobs/[id]/page.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/projects/components/AddTradeScopeSheet.tsx (dialog + react-hook-form pattern to mirror for AddCostDialog)
    - web/src/app/(dashboard)/projects/components/ProjectDetail.tsx (Card composition — where ProjectCostsCard mounts)
    - web/src/app/(dashboard)/projects/components/TradeScopeDetail.tsx (where a Costs section + AddCostDialog mount)
    - web/src/app/(dashboard)/jobs/[id]/page.tsx (job detail — where a Costs section mounts)
    - web/src/lib/hooks/usePermissions.ts (can() gating)
    - web/src/features/finance/hooks.ts (the hooks just created)
  </read_first>
  <action>
    Create components under web/src/features/finance/components/:
    - `AddCostDialog.tsx`: a dialog (mirror AddTradeScopeSheet) with fields amount (numeric, > 0),
      category (Select populated from `useCostCategories`), incurredDate (defaults to today), vendor,
      note, and an optional receipt file input. On submit: `useAddCostEntry` with the correct anchor
      prop (jobId OR tradeScopeId passed in), then if a file was chosen call `useUploadReceipt`
      with the new entry id. Show a success toast + error inline/toast on failure.
    - `CostEntryList.tsx`: renders CostEntry[] (category name, amount formatted, date, vendor),
      each with edit/delete controls that only render when `can("finance.manage")`. Empty state.
    - `ProjectCostsCard.tsx`: a Card using `useProjectCostRollup(projectId)` showing the rollup
      `total` prominently + a CostEntryList of the aggregated entries (read-only rollup per D-02).
    Wire in, each behind `usePermissions`:
    - ProjectDetail.tsx: render `{can("finance.view") && <ProjectCostsCard projectId={...} />}`.
    - TradeScopeDetail.tsx: `{can("finance.view") && <Costs section: CostEntryList(useCostEntriesForTradeScope) + {can("finance.manage") && <AddCostDialog tradeScopeId={...} />}>}`.
    - jobs/[id]/page.tsx: same Costs section using `useCostEntriesForJob` + `<AddCostDialog jobId={...} />`.
    All amounts displayed via a shared formatter (no magic — reuse existing currency formatting if
    present in web/src/lib). Receipt thumbnails (from GET receipts) may render as links — keep minimal.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx eslint web/src/features/finance web/src/app/\(dashboard\)/projects/components/ProjectDetail.tsx --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "can(\"finance.view\")" web/src/app/(dashboard)/projects/components/ProjectDetail.tsx` succeeds (section-level gate)
    - `grep -q "can(\"finance.manage\")" web/src/features/finance/components/CostEntryList.tsx` succeeds (edit/delete gate)
    - `grep -rq "AddCostDialog" web/src/app/(dashboard)/jobs/[id]/page.tsx` and in TradeScopeDetail.tsx both succeed
    - `grep -q "useProjectCostRollup" web/src/features/finance/components/ProjectCostsCard.tsx` succeeds
    - `npx tsc --noEmit` exits 0 and `eslint ... --max-warnings 0` exits 0
  </acceptance_criteria>
  <done>Costs sections appear on all three surfaces, fully gated, with a working add-cost + receipt-upload dialog and a project rollup card.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E (cost-capture.spec.ts) + Jest form test</name>
  <files>web/tests/cost-capture.spec.ts, web/src/features/finance/__tests__/cost-entry-form.test.tsx</files>
  <read_first>
    - web/tests/project-assignments.spec.ts (permission-gated flow: mock /api/auth/login, log in via UI, SPA-navigate by clicking sidebar — required so permissions load)
    - web/tests/create-contractor.spec.ts (mock /api/proxy request/response + assert captured payload — the base template)
    - .claude/skills/e2e-feature-tests/SKILL.md (web Playwright + Jest conventions)
    - web/src/features/finance/components/AddCostDialog.tsx (the flow under test)
  </read_first>
  <action>
    Create web/tests/cost-capture.spec.ts. Mock `/api/proxy` for: `GET /me/permissions`
    (parametrize per test — with `["finance.view","finance.manage"]` and, in a separate test,
    without finance keys), `GET /cost-categories/`, `GET /cost-entries/?...`,
    `POST /cost-entries/`, `POST /cost-entries/{id}/receipts`. Log in through the UI first
    (mock `**/api/auth/login`) then SPA-navigate to a job or project detail page. Tests:
    (1) with finance perms: open Add cost, fill amount/category/date, submit, assert the captured
    POST /cost-entries payload (category_id, amount, anchor) AND the new entry rendered in the list;
    (2) attach a file and assert a POST to `/cost-entries/{id}/receipts` was captured;
    (3) without finance.view: assert the Costs section / Add cost control is NOT visible.
    Also create web/src/features/finance/__tests__/cost-entry-form.test.tsx (Jest): render
    AddCostDialog inside a QueryClientProvider with `@/lib/api-client` mocked; assert amount<=0 and
    missing-category validation block submit, and a valid submit calls createCostEntry once.
  </action>
  <verify>
    <automated>cd web && npx jest src/features/finance && npx playwright test tests/cost-capture.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web/tests/cost-capture.spec.ts` exists and `npx playwright test tests/cost-capture.spec.ts` exits 0
    - The spec asserts the captured `POST /cost-entries` payload (grep the file for `cost-entries` and `expect`)
    - A test asserts the Costs control is hidden without finance.view (grep for `finance.view` or a not-visible assertion)
    - `npx jest src/features/finance` exits 0
    - `npx eslint web/tests/cost-capture.spec.ts web/src/features/finance --max-warnings 0` exits 0
  </acceptance_criteria>
  <done>Playwright covers the gated happy path + receipt upload + hidden-without-permission case; Jest covers form validation — all green.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` clean; `npx eslint . --max-warnings 0` clean on modified files.
- `cd web && npm run test-e2e` — full Playwright suite green (no regression).
- `cd web && npx jest` — component suite green.
</verification>

<success_criteria>
- COST-01/02/03 on web: add materials/subcontractor/other cost entries + optional receipt on job, trade-scope, and project surfaces.
- D-02: project Costs card shows aggregated rollup total + itemized entries.
- D-06: Costs section hidden without finance.view; Add/edit/delete hidden without finance.manage — proven by Playwright.
</success_criteria>

<output>
After completion, create `.planning/phases/31-actual-cost-capture/31-03-SUMMARY.md`.
</output>
