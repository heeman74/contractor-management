---
phase: 13-web-foundation-and-auth
plan: 04
type: execute
wave: 3
depends_on:
  - 13-03
files_modified:
  - web/src/app/(auth)/login/page.tsx
  - web/src/app/(dashboard)/layout.tsx
  - web/src/app/(dashboard)/page.tsx
  - web/src/components/layout/sidebar.tsx
  - web/src/components/layout/topbar.tsx
  - web/src/components/layout/dashboard-shell.tsx
  - web/src/components/shared/status-badge.tsx
  - web/src/components/shared/kpi-card.tsx
  - web/src/app/not-found.tsx
  - web/src/app/error.tsx
  - web/proxy.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "Admin can log in with email and password and land on the dashboard home"
    - "Global sidebar is visible on every dashboard page with all 8 module links"
    - "Sidebar collapses to icon-only mode and persists state to localStorage"
    - "Topbar shows breadcrumbs on left and company name + avatar dropdown on right"
    - "Dashboard home shows KPI cards (Active Jobs, Pending Quotes, Overdue Invoices, Today's Schedule)"
    - "Login errors display as inline red alert banner above form fields"
    - "Global server/network errors show toast notifications in bottom-right"
    - "Custom 404 and 500 error pages display with Go to Dashboard button"
  artifacts:
    - path: "web/src/app/(auth)/login/page.tsx"
      provides: "Split-screen login page with form"
      min_lines: 80
    - path: "web/src/components/layout/sidebar.tsx"
      provides: "Collapsible dark sidebar with 8 module nav items"
      min_lines: 80
    - path: "web/src/components/layout/topbar.tsx"
      provides: "Breadcrumbs + user dropdown"
      min_lines: 40
    - path: "web/src/app/(dashboard)/page.tsx"
      provides: "Dashboard home with KPI cards and activity feed"
      min_lines: 60
    - path: "web/src/components/shared/status-badge.tsx"
      provides: "Reusable status badge with semantic colors"
      exports: ["StatusBadge"]
    - path: "web/src/app/not-found.tsx"
      provides: "Custom 404 page"
    - path: "web/src/app/error.tsx"
      provides: "Custom 500 error boundary"
  key_links:
    - from: "web/src/app/(auth)/login/page.tsx"
      to: "web/src/app/api/auth/login/route.ts"
      via: "fetch POST /api/auth/login"
      pattern: "fetch.*api/auth/login"
    - from: "web/src/app/(dashboard)/layout.tsx"
      to: "web/src/components/layout/sidebar.tsx"
      via: "import and render"
      pattern: "Sidebar"
    - from: "web/src/app/(dashboard)/page.tsx"
      to: "web/src/lib/api-client.ts"
      via: "apiClient for KPI data"
      pattern: "apiClient|apiGet"
    - from: "web/src/components/layout/sidebar.tsx"
      to: "web/src/store/slices/ui-slice.ts"
      via: "Redux dispatch toggleSidebar"
      pattern: "toggleSidebar|sidebarCollapsed"
---

<objective>
Build the login page, dashboard shell (sidebar + topbar), dashboard home page with KPI cards, error pages, and status badge component.

Purpose: This is the user-facing surface of Phase 13. Admin logs in via the split-screen login page, lands on the dashboard home with real KPI data, and can navigate via the persistent sidebar. Error handling is wired globally (toasts) and per-form (inline). This plan delivers the complete visual and functional experience for AUTH-01, AUTH-05, and AUTH-06.

Output: Working login flow, dashboard shell with sidebar/topbar, KPI dashboard home, error pages, status badge system.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-web-foundation-and-auth/13-CONTEXT.md
@.planning/phases/13-web-foundation-and-auth/13-RESEARCH.md
@.planning/phases/13-web-foundation-and-auth/13-03-SUMMARY.md

<interfaces>
<!-- From 13-03: auth infrastructure -->

From web/src/lib/api-client.ts:
```typescript
export class ApiError extends Error { status: number; detail: string; }
export function apiClient<T>(path: string, init?: RequestInit, retry?: boolean): Promise<T>;
export function apiGet<T>(path: string): Promise<T>;
export function apiPost<T>(path: string, body: unknown): Promise<T>;
```

From web/src/lib/auth.ts:
```typescript
export function getServerUser(): Promise<AuthUser | null>;
```

From web/src/types/api.ts:
```typescript
export interface AuthUser { user_id: string; company_id: string; roles: string[]; }
export interface LoginRequest { email: string; password: string; }
```

From web/src/store/slices/ui-slice.ts:
```typescript
// State: { sidebarCollapsed: boolean }
// Actions: toggleSidebar(), setSidebarCollapsed(boolean)
```

From web/src/store/slices/auth-slice.ts:
```typescript
// Actions: setAuthUser(payload), clearAuth()
// State: { displayName, companyName, roles, isAuthenticated }
```

Route Handlers (from 13-03):
- POST /api/auth/login — body: {email, password} → {user_id, company_id, roles}
- POST /api/auth/logout — no body → 200
- POST /api/auth/refresh — no body → {user_id, company_id, roles}

shadcn/ui components available (from 13-02):
Button, Input, Label, Card, Avatar, DropdownMenu, Breadcrumb, Separator, Sheet, Badge, Skeleton, Sonner (Toaster)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page and error pages</name>
  <files>web/src/app/(auth)/login/page.tsx, web/src/app/not-found.tsx, web/src/app/error.tsx</files>
  <action>
**Login Page** (`web/src/app/(auth)/login/page.tsx`):
Must be a "use client" component. Per user decisions (LOCKED):

Layout:
- Split-screen: left panel (hidden on mobile <768px) with blue gradient (indigo-600 to blue-500) + ContractorHub logo text + tagline ("Manage your contracting business with confidence")
- Right panel: centered login form card

Form (use react-hook-form + zod):
- Email field (Input + Label) with inline validation (red text below field on invalid email)
- Password field (Input + Label) with show/hide toggle (Eye/EyeOff icon from lucide-react)
- "Forgot password?" link below password — on click, show toast "Contact your administrator" (per user decision: stubbed)
- Sign In button: `disabled` during submit + spinner icon + text "Signing in..."
- Login error: inline red alert banner (with AlertCircle icon) above form fields — clears when user starts typing

Submit handler:
1. POST to `/api/auth/login` with `{ email, password }` (the Route Handler, not FastAPI directly)
2. On success: dispatch `setAuthUser()` to Redux with returned user_id/company_id/roles, then `router.push("/")` to dashboard
3. On 401/422: show error in inline banner ("Invalid email or password")
4. On 500/network: show error in inline banner ("Something went wrong. Please try again.")
5. Check URL for `?reason=session_expired` on mount — if present, show banner "Your session expired. Please log in again."

Read `?redirectTo` param from URL. After successful login, if redirectTo exists, navigate there instead of "/".

**404 Page** (`web/src/app/not-found.tsx`):
- Centered content: large "404" heading, "Page not found" subtext
- "Go to Dashboard" button (Link to "/")
- Branded with indigo accent

**500 Error Page** (`web/src/app/error.tsx`):
- Must be "use client" (Next.js requirement for error.tsx)
- Centered content: "Something went wrong" heading, error.message if available
- "Go to Dashboard" button (Link to "/")
- "Try Again" button calling `reset()` prop
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/web && npx tsc --noEmit</automated>
  </verify>
  <done>Login page renders split-screen layout with form; form validates email/password; submit calls /api/auth/login; error banner displays on failure; 404 and 500 pages render with Go to Dashboard button</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard shell (sidebar, topbar, layout) and dashboard home with KPI cards</name>
  <files>web/src/components/layout/sidebar.tsx, web/src/components/layout/topbar.tsx, web/src/components/layout/dashboard-shell.tsx, web/src/app/(dashboard)/layout.tsx, web/src/app/(dashboard)/page.tsx, web/src/components/shared/status-badge.tsx, web/src/components/shared/kpi-card.tsx</files>
  <action>
**Sidebar** (`web/src/components/layout/sidebar.tsx`):
"use client" component. Per user decisions (LOCKED):

- Dark background (bg-gray-900 text-white)
- Expanded width: 240px with icon + label; Collapsed width: 64px icon-only
- Collapse toggle button at top (ChevronLeft/ChevronRight icon)
- Flat module list (no groupings), each with Lucide icon:
  1. LayoutDashboard — Dashboard (href "/")
  2. Briefcase — Jobs (href "/jobs")
  3. Calendar — Schedule (href "/schedule")
  4. FileText — Quotes (href "/quotes")
  5. Receipt — Invoices (href "/invoices")
  6. Users — Clients (href "/clients")
  7. HardHat — Contractors (href "/contractors")
  8. BarChart3 — Reports (href "/reports")
- Divider (Separator) before bottom section
- Bottom: user avatar (first letter) + display name (when expanded) + dropdown with "Log out"
- Active nav item: filled bg-indigo-600/bg-indigo-700 highlight
- Use `usePathname()` from next/navigation to determine active item
- Collapse state: read from Redux `ui.sidebarCollapsed`, dispatch `toggleSidebar()` on toggle click
- Persist to localStorage: on collapse toggle, write to localStorage; on mount, read from localStorage and dispatch `setSidebarCollapsed()`
- Responsive per user decisions:
  - Desktop (>1024px): render as fixed sidebar
  - Tablet (768-1024px): auto-collapse to mini sidebar
  - Mobile (<768px): hidden by default, hamburger menu triggers Sheet (shadcn/ui) overlay

**Topbar** (`web/src/components/layout/topbar.tsx`):
"use client" component:
- Left: Breadcrumb trail using shadcn/ui Breadcrumb component. Derive from pathname (e.g., `/jobs/123` → Dashboard > Jobs > Job #123). For this phase, simple pathname-based breadcrumbs (no dynamic titles until later phases).
- Right: Company name text + Avatar dropdown (DropdownMenu) with "Log out" action
- Logout action: POST to `/api/auth/logout`, dispatch `clearAuth()`, router.push("/login")
- Mobile (<768px): add hamburger button (Menu icon) on left that opens the sidebar Sheet

**Dashboard Shell** (`web/src/components/layout/dashboard-shell.tsx`):
Server Component wrapper:
- Flexbox layout: sidebar (fixed left) + main content area (topbar + scrollable content)
- Main content area gets `ml-[240px]` when sidebar expanded, `ml-[64px]` when collapsed
- Use CSS transition for smooth sidebar collapse animation

**Dashboard Layout** (`web/src/app/(dashboard)/layout.tsx`):
- Import and render DashboardShell wrapping children
- Use `getServerUser()` to get user metadata for the shell (pass as props or via Redux hydration)

**Dashboard Home** (`web/src/app/(dashboard)/page.tsx`):
"use client" component (needs TanStack Query for data fetching):
- Top row: 4 KPI cards in responsive grid (4 cols on xl, 2 on md, 1 on sm)
  - Active Jobs: fetches from GET /api/v1/jobs?status=in_progress (count)
  - Pending Quotes: fetches from GET /api/v1/quotes?status=draft (count)
  - Overdue Invoices: fetches from GET /api/v1/invoices?status=overdue (count)
  - Today's Schedule: fetches from GET /api/v1/scheduling/bookings?date=today (count)
- Each KPI card: clickable (Link to respective module page), shows icon + label + count
- Use Skeleton component for loading states (4 skeleton cards matching KPI card shape)
- Below cards: Recent Activity feed (last 10 items) — for now, fetch from a suitable endpoint or show a placeholder section "Recent Activity" with skeleton. If no dedicated activity endpoint exists in backend, show a static "Coming soon" card.
- Use `useQuery` from TanStack Query for each data fetch via apiClient
- Handle errors with toast (via Sonner) — `toast.error("Failed to load dashboard data")`

**KPI Card** (`web/src/components/shared/kpi-card.tsx`):
- Props: `title: string`, `value: number | string`, `icon: LucideIcon`, `href: string`, `loading?: boolean`
- Uses shadcn/ui Card, renders skeleton when loading
- Clickable (wraps in Link)

**Status Badge** (`web/src/components/shared/status-badge.tsx`):
- Reusable across all phases (14-18)
- Props: `status: string`, `size?: "sm" | "md"`
- Color map per user decision:
  - green: active, paid, approved, complete
  - yellow: pending, in_progress, in-progress
  - red: overdue, declined, cancelled
  - blue: scheduled, sent
  - gray: draft
- Uses shadcn/ui Badge with variant override via cn()
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/web && npx tsc --noEmit && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Sidebar renders 8 nav items with correct icons and active highlighting; collapse toggle works and persists to localStorage; topbar shows breadcrumbs and user dropdown; dashboard home shows 4 KPI cards with real API data; status badge renders correct semantic colors; mobile responsive (Sheet sidebar on <768px)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of complete Phase 13</name>
  <files>web/src/app/(auth)/login/page.tsx, web/src/components/layout/sidebar.tsx, web/src/app/(dashboard)/page.tsx</files>
  <action>
Human verifies the complete Phase 13 web foundation. Prerequisites: Start FastAPI backend (`cd backend && uv run uvicorn app.main:app --reload`) and Next.js dev server (`cd web && npm run dev`).

Verification steps:
1. Visit http://localhost:3000 — should redirect to /login
2. Login page: verify split-screen layout (blue gradient left, form right)
3. Try submitting empty form — see inline validation errors
4. Enter invalid credentials — see red error banner above form
5. Enter valid admin credentials — redirected to dashboard home
6. Dashboard: verify 4 KPI cards visible (may show 0 if no data)
7. Sidebar: verify 8 nav items (Dashboard, Jobs, Schedule, Quotes, Invoices, Clients, Contractors, Reports)
8. Click sidebar collapse toggle — sidebar shrinks to icons only
9. Refresh page — sidebar stays collapsed (localStorage persistence)
10. Topbar: verify breadcrumb shows "Dashboard", verify company name + avatar dropdown on right
11. Click avatar dropdown > Log out — redirected to /login
12. Try navigating directly to http://localhost:3000 — should redirect to /login (session cleared)
13. Visit http://localhost:3000/nonexistent — should see 404 page with "Go to Dashboard" button
14. Resize browser to mobile width (<768px) — sidebar becomes hamburger menu overlay
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/web && npm run build</automated>
  </verify>
  <done>User has visually verified: login flow works end-to-end, sidebar renders correctly with collapse, topbar shows breadcrumbs and user menu, dashboard KPI cards load, error pages display, responsive layout functions on mobile</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` — zero type errors
- `cd web && npm run build` — succeeds
- Login flow: email/password → POST /api/auth/login → cookies set → redirect to dashboard
- Sidebar: 8 items, collapse toggle, localStorage persistence, responsive
- Topbar: breadcrumbs, user dropdown, logout
- Dashboard: 4 KPI cards with real data from backend
- Error handling: toast on server errors, inline on form validation, red banner on login failure
- 404/500 pages render correctly
</verification>

<success_criteria>
- Admin can log in and see dashboard home with KPI data
- Sidebar shows all 8 module nav items with correct Lucide icons
- Sidebar collapses to icon-only and persists via localStorage
- Topbar shows breadcrumbs and company/user dropdown
- Logout clears session and redirects to login
- Error handling: toasts for global errors, inline for form validation, banner for login errors
- 404 and 500 error pages display correctly
- Status badge component ready for reuse in phases 14-18
- Responsive: desktop full sidebar, tablet mini sidebar, mobile hamburger + Sheet
</success_criteria>

<output>
After completion, create `.planning/phases/13-web-foundation-and-auth/13-04-SUMMARY.md`
</output>
