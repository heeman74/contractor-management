---
phase: 34-budgeting-and-overrun-alerts
plan: 04
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - web/src/features/finance/types.ts
  - web/src/features/finance/api.ts
  - web/src/lib/format.ts
  - web/src/features/finance/components/MarginSummarySection.tsx
  - web/src/features/finance/components/BudgetSummarySection.tsx
  - web/src/features/finance/components/CostBreakdownSummary.tsx
  - web/src/features/finance/components/ProjectCostsCard.tsx
  - web/src/features/finance/__tests__/budget-section.test.tsx
autonomous: true
requirements: [BUDG-02]
user_setup: []

must_haves:
  truths:
    - "On the project Costs card and the trade-scope Costs section, a finance.view holder sees Budget, Spent and Remaining rows between the Total row and the Revenue/Margin section"
    - "The Spent figure reads '{amount} · {percent}%' using the backend percent — the client never divides"
    - "Between 80% and 100% an amber 'Nearing budget' chip sits left of the Remaining amount"
    - "Over budget, the Remaining figure renders as a red negative amount with the minus sign, and no chip"
    - "With no budget, or on a job breakdown, or while loading, no budget rows render at all"
  artifacts:
    - path: "web/src/features/finance/components/BudgetSummarySection.tsx"
      provides: "Pure display component for the Budget/Spent/Remaining triad"
      contains: "Nearing budget"
    - path: "web/src/features/finance/types.ts"
      provides: "BudgetVsActual type + budget field on CostBreakdown / ProjectCostRollup"
      contains: "percentUsed"
    - path: "web/src/features/finance/__tests__/budget-section.test.tsx"
      provides: "State-matrix coverage for the budget rows"
  key_links:
    - from: "web/src/features/finance/api.ts"
      to: "backend budget block"
      via: "snake_case budget → camelCase BudgetVsActual mapper"
      pattern: "percent_used"
    - from: "web/src/features/finance/components/CostBreakdownSummary.tsx"
      to: "BudgetSummarySection"
      via: "rendered between the Total row and MarginSummarySection on project/trade-scope variants"
      pattern: "<BudgetSummarySection"
---

<objective>
Render budgeted vs spent vs remaining on the web project and trade-scope Costs
surfaces — view only, exactly per the 34-UI-SPEC state matrix.

Purpose: BUDG-02's web half. Creating/editing a budget (the "Set budget" and
"Edit" affordances, states 2 and 11) ships in plan 34-07; this plan delivers the
read-only states (1, 3–10) so the display contract is settled and tested before
mutation UI lands on top of it.
Output: the budget type + parser, a pure `BudgetSummarySection`, its wiring into
`CostBreakdownSummary`, and jest coverage of every read-only state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-budgeting-and-overrun-alerts/34-CONTEXT.md
@.planning/phases/34-budgeting-and-overrun-alerts/34-UI-SPEC.md
@.planning/phases/34-budgeting-and-overrun-alerts/34-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- API contract from plan 34-02 (backend), and the shipped web pieces to reuse. -->

Backend response (snake_case), additive on `GET /trade-scopes/{id}/cost-breakdown`
and `GET /projects/{id}/cost-entries`; absent/null on `GET /jobs/{id}/cost-breakdown`:
```json
"budget": {
  "budget_id": "uuid",
  "total": "10000.00",
  "spent": "8200.00",
  "remaining": "1800.00",
  "percent_used": "82.0"
}
```
`remaining` is negative when over budget; `percent_used` carries one decimal.

web/src/features/finance/types.ts (shipped shapes to extend):
```ts
export interface CostBreakdown { categories; labor; laborTrackedAtJobLevel; grandTotal; margin }
export interface ProjectCostRollup { projectId; total; entries; categories; labor; grandTotal; margin }
```

web/src/features/finance/components/CostBreakdownSummary.tsx (shipped):
```tsx
export function CostBreakdownSummary({ breakdown, variant, isLoading, isError }:
  { breakdown: CostBreakdown | null | undefined;
    variant: "job" | "trade-scope" | "project";
    isLoading?: boolean; isError?: boolean })
// renders: labor row, category rows, <Separator/>, Total row (data-testid="breakdown-grand-total"),
// then <MarginSummarySection margin={...}/> which opens with its own <Separator/>.
// Private row primitive: function BreakdownRow({ label, children })  — label left, children right in a
// flex gap-2 span.
```

web/src/features/finance/components/MarginSummarySection.tsx (shipped, exported):
```ts
export function formatMarginPercent(percent: string): string   // drops a trailing ".0"
export function formatMarginDollars(amount: string): string    // "-$350.00" — sign before symbol
```

web/src/features/finance/components/FinanceFlagChip.tsx (shipped): the single amber
chip recipe (`bg-brand/15 text-amber-900`) — reuse it, do not restyle it.

web/src/lib/format.ts: `formatCurrency(value: string): string`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Budget types, response parsing, shared signed-currency helper</name>
  <files>web/src/features/finance/types.ts, web/src/features/finance/api.ts, web/src/lib/format.ts, web/src/features/finance/components/MarginSummarySection.tsx</files>
  <read_first>
    - web/src/features/finance/types.ts (whole file — Decimal-as-string doc conventions)
    - web/src/features/finance/api.ts (whole file — the snake_case ApiResponse interfaces and the hand-written mappers, especially toCostBreakdown / toProjectCostRollup)
    - web/src/lib/format.ts (formatCurrency implementation)
    - web/src/features/finance/components/MarginSummarySection.tsx lines 1-40 (formatMarginDollars / formatMarginPercent)
  </read_first>
  <action>
1. `types.ts` — add, with a doc comment stating money stays a string end-to-end
   and `remaining` goes negative when over budget:
```ts
export interface BudgetVsActual {
  budgetId: string;
  total: string;
  spent: string;
  remaining: string;
  /** One decimal from the backend, e.g. "82.0" — never re-derived client-side. */
  percentUsed: string;
}
```
   and `budget: BudgetVsActual | null;` on BOTH `CostBreakdown` and
   `ProjectCostRollup` (null = no budget set or an older backend).

2. `api.ts` — add a `BudgetVsActualApiResponse` snake_case interface and a
   `toBudgetVsActual(raw): BudgetVsActual | null` mapper that returns `null` when
   the key is missing or not an object, then call it from the cost-breakdown and
   project-rollup mappers. Follow the existing mapper style exactly (explicit
   field-by-field mapping, no spread casts).

3. `web/src/lib/format.ts` — add
```ts
/** "-$350.00": the minus sign leads, matching how negative money reads on
 *  every finance surface. Positive values format like formatCurrency. */
export function formatSignedCurrency(amount: string): string
```
   moving the body of `formatMarginDollars` here, and make
   `MarginSummarySection.formatMarginDollars` a one-line delegate to it so the
   shipped export (and its tests) keep working while there is exactly one
   implementation (DRY).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run lint && npm test -- margin-summary-section</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "percentUsed" web/src/features/finance/types.ts` matches inside BudgetVsActual
    - `grep -c "budget: BudgetVsActual | null" web/src/features/finance/types.ts` == 2
    - `grep -n "toBudgetVsActual" web/src/features/finance/api.ts` appears at least 3 times (definition + two call sites)
    - `grep -n "export function formatSignedCurrency" web/src/lib/format.ts` matches
    - `grep -n "formatSignedCurrency" web/src/features/finance/components/MarginSummarySection.tsx` matches (delegation, no duplicated body)
    - `cd web && npx tsc --noEmit` exits 0 and `npm run lint` exits 0 (--max-warnings 0)
  </acceptance_criteria>
  <done>The budget block is typed and parsed on both finance responses, and negative money has one formatter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BudgetSummarySection display component</name>
  <files>web/src/features/finance/components/BudgetSummarySection.tsx, web/src/features/finance/__tests__/budget-section.test.tsx</files>
  <read_first>
    - .planning/phases/34-budgeting-and-overrun-alerts/34-UI-SPEC.md § "Anatomy — budget summary section", § "State matrix", § "Test hooks (web)", § "Copywriting Contract" fixed strings
    - web/src/features/finance/components/CostBreakdownSummary.tsx (row rhythm, private BreakdownRow, Separator usage, LOADING_AMOUNT convention)
    - web/src/features/finance/components/FinanceFlagChip.tsx
    - web/src/features/finance/__tests__/margin-summary-section.test.tsx (jest + testing-library conventions used in this feature)
  </read_first>
  <behavior>
    - budget=null renders nothing (no separator, no rows) — state 1/9
    - percentUsed "42.0" renders "Budget"/"Spent"/"Remaining" rows, Spent testid text "$4,200.00 · 42%", no chip, no red — state 3
    - percentUsed "82.0" with positive remaining renders the amber chip with text "Nearing budget" left of the Remaining amount — state 4
    - percentUsed "100.0" with remaining "0.00" renders "$0.00" plain, no chip, Spent shows "· 100%" — state 5
    - remaining "-1200.00" renders "-$1,200.00" carrying the destructive text class and NO chip; Spent shows "· 112%" — state 6
    - percentUsed "82.5" renders "· 82.5%" (only a trailing ".0" is dropped)
    - The Remaining amount always carries font-semibold; Budget and Spent amounts do not
    - data-testids budget-section, budget-amount, budget-spent, budget-remaining, budget-warning-chip are present exactly as specified
  </behavior>
  <action>
Create `web/src/features/finance/components/BudgetSummarySection.tsx` — a pure
display component: no hooks, no fetching, no query client.

```tsx
interface BudgetSummarySectionProps { budget: BudgetVsActual | null | undefined }
export function BudgetSummarySection({ budget }: BudgetSummarySectionProps)
export function formatPercentUsed(percentUsed: string): string   // "82.0" -> "82", "82.5" -> "82.5"
```

- Returns `null` immediately when `budget` is null/undefined (states 1, 7, 9 —
  the caller never renders it while loading either).
- Otherwise renders `<Separator />` followed by three rows in the shipped row
  rhythm (`space-y-2`, `flex items-center justify-between text-sm`, label
  `text-gray-700`, right side a `flex items-center gap-2` span):
  - Budget: label "Budget", amount `formatCurrency(budget.total)` at plain
    `text-sm`, `data-testid="budget-amount"`.
  - Spent: label "Spent", figure `` `${formatCurrency(budget.spent)} · ${formatPercentUsed(budget.percentUsed)}%` ``
    — the separator is a space, U+00B7 middle dot, space — `data-testid="budget-spent"`.
  - Remaining: label "Remaining", optional chip then the figure at
    `text-sm font-semibold`, `data-testid="budget-remaining"`.
- Band classification derives ONLY from the backend strings: `isOverBudget =
  Number(budget.remaining) < 0`; `isNearingBudget = !isOverBudget &&
  Number(budget.percentUsed) >= WARNING_PERCENT` with
  `const WARNING_PERCENT = 80;` (named constant, no magic number). The two states
  are mutually exclusive — amber chip and red numerals never co-render.
- Over budget: render `formatSignedCurrency(budget.remaining)` with
  `text-destructive`; the label and everything else keep their normal color.
  Otherwise `formatCurrency(budget.remaining)` with no color class.
- Nearing: `<FinanceFlagChip>` with the constant `NEARING_BUDGET_CHIP_LABEL = "Nearing budget"`
  and `data-testid="budget-warning-chip"`.
- Every copy string is a named module constant (matching the shipped
  `JOB_LEVEL_LABOR_NOTE` / `UNBURDENED_TITLE` pattern). Keep the component under
  ~40 lines by extracting a small private `BudgetRow` or reusing the same
  row markup shape as `CostBreakdownSummary`'s private `BreakdownRow` — do not
  export a second competing row primitive.

Create `web/src/features/finance/__tests__/budget-section.test.tsx` covering every
`<behavior>` bullet plus a `formatPercentUsed` unit block.
  </action>
  <verify>
    <automated>cd web && npm test -- budget-section && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Nearing budget" web/src/features/finance/components/BudgetSummarySection.tsx` matches exactly once (as a named constant)
    - `grep -n "data-testid=\"budget-section\"\|budget-amount\|budget-spent\|budget-remaining\|budget-warning-chip" web/src/features/finance/components/BudgetSummarySection.tsx` returns all five testids
    - `grep -n "useQuery\|useState\|fetch(" web/src/features/finance/components/BudgetSummarySection.tsx` returns nothing (pure display)
    - `grep -n "text-destructive" web/src/features/finance/components/BudgetSummarySection.tsx` matches
    - `cd web && npm test -- budget-section` reports >= 8 passing, 0 failing
  </acceptance_criteria>
  <done>The Budget/Spent/Remaining triad renders exactly the UI-SPEC read-only states, under test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the budget rows into the project and trade-scope Costs surfaces</name>
  <files>web/src/features/finance/components/CostBreakdownSummary.tsx, web/src/features/finance/components/ProjectCostsCard.tsx, web/src/features/finance/__tests__/budget-section.test.tsx</files>
  <read_first>
    - web/src/features/finance/components/CostBreakdownSummary.tsx (whole file)
    - web/src/features/finance/components/ProjectCostsCard.tsx (whole file — the rollup→CostBreakdown object it builds)
    - web/src/features/finance/__tests__/cost-breakdown-summary.test.tsx (existing assertions that must keep passing)
    - .planning/phases/34-budgeting-and-overrun-alerts/34-UI-SPEC.md § "State matrix" rows 7 and 10
  </read_first>
  <behavior>
    - variant="project" or "trade-scope" with a budget renders the budget rows between the Total row and the margin section
    - variant="job" with a budget in the data renders NO budget rows — state 10
    - isLoading renders no budget rows even when stale data is present — state 7
    - isError renders only the inherited error line, no budget rows — state 8
    - ProjectCostsCard forwards rollup.budget into the CostBreakdownSummary it builds, so the project card shows the same triad
    - All existing cost-breakdown-summary tests still pass unchanged
  </behavior>
  <action>
In `CostBreakdownSummary.tsx`:
- Import `BudgetSummarySection` and render it between the Total row and
  `<MarginSummarySection ... />`:
  `{variant !== "job" && !isLoading && <BudgetSummarySection budget={breakdown?.budget ?? null} />}`
  Extract the condition into a named local (e.g. `const showsBudget = variant !== "job" && !isLoading;`)
  so the JSX stays readable. `MarginSummarySection` already opens with its own
  `<Separator />`, so ordering composes without editing it.
- Leave `isBreakdownEmpty` semantics alone EXCEPT: a breakdown that has a budget
  but no categories, no cost and no revenue must still render (otherwise a
  freshly-set budget on an empty scope would be invisible). Add
  `breakdown.budget != null` to the "not empty" conditions and extend that
  function's doc comment with the reason.

In `ProjectCostsCard.tsx`: add `budget: rollup.budget` to the object literal it
passes as `breakdown` (it currently maps categories/labor/grandTotal/margin).

Extend `budget-section.test.tsx` with the wiring tests from `<behavior>` (render
`CostBreakdownSummary` with each variant), and re-run the shipped
`cost-breakdown-summary` suite unchanged.
  </action>
  <verify>
    <automated>cd web && npm test -- "budget-section|cost-breakdown-summary|margin-summary-section" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "<BudgetSummarySection" web/src/features/finance/components/CostBreakdownSummary.tsx` matches once, positioned after the Total row block and before `<MarginSummarySection`
    - `grep -n "variant !== \"job\"" web/src/features/finance/components/CostBreakdownSummary.tsx` matches
    - `grep -n "budget: rollup.budget" web/src/features/finance/components/ProjectCostsCard.tsx` matches
    - `grep -n "budget != null" web/src/features/finance/components/CostBreakdownSummary.tsx` matches inside isBreakdownEmpty
    - `cd web && npm test -- "budget-section|cost-breakdown-summary"` reports 0 failures
  </acceptance_criteria>
  <done>Project and trade-scope Costs surfaces show budget-vs-actual; job surfaces provably do not.</done>
</task>

</tasks>

<verification>
- `cd web && npm run lint && npx tsc --noEmit && npm run test` — full web unit suite green with zero warnings
- Visual sanity via the jest DOM assertions: Spent reads "{amount} · {percent}%", Remaining is semibold, chip and red numerals never co-occur
- No new shadcn component was installed (UI-SPEC Registry Safety): `git status` shows no changes under web/src/components/ui/
</verification>

<success_criteria>
- BUDG-02 (web read half): budgeted vs spent vs remaining renders on project and trade-scope Costs surfaces from the backend block, with no client-side percent math
- UI-SPEC states 1, 3, 4, 5, 6, 7, 8, 9, 10 all covered by tests
- Job breakdowns and all shipped Phase 32/33 rendering are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/34-budgeting-and-overrun-alerts/34-04-SUMMARY.md`
</output>
