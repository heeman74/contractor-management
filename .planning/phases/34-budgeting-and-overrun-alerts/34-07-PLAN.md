---
phase: 34-budgeting-and-overrun-alerts
plan: 07
type: execute
wave: 4
depends_on: ["34-04"]
files_modified:
  - web/src/features/finance/api.ts
  - web/src/features/finance/hooks.ts
  - web/src/features/finance/components/SetBudgetDialog.tsx
  - web/src/features/finance/components/BudgetSummarySection.tsx
  - web/src/features/finance/components/CostBreakdownSummary.tsx
  - web/src/features/finance/components/ProjectCostsCard.tsx
  - web/src/app/(dashboard)/projects/components/ProjectDetail.tsx
  - web/src/app/(dashboard)/projects/components/TradeScopeDetail.tsx
  - web/src/app/(dashboard)/monitoring/_components/AlertPanel.tsx
  - web/src/features/finance/__tests__/budget-section.test.tsx
  - web/src/features/finance/__tests__/set-budget-dialog.test.tsx
  - web/tests/phase-34-budgets.spec.ts
autonomous: true
requirements: [BUDG-01, BUDG-02]
user_setup: []

must_haves:
  truths:
    - "An owner/PM sees a 'Set budget' affordance on a project or trade scope with no budget, and an 'Edit' affordance when one exists"
    - "A finance.view-only user sees the budget figures but no Set budget and no Edit affordance"
    - "Setting a budget closes the dialog, toasts the amount, and refreshes the rows without a page reload"
    - "Editing shows the current budget headline plus current spend, warns honestly when the entered amount is below spend, and never blocks the save"
    - "Removing a budget asks for confirmation naming what stops, then removes it"
    - "Budget alerts appear in the monitoring Alert panel under the header 'Alerts'"
  artifacts:
    - path: "web/src/features/finance/components/SetBudgetDialog.tsx"
      provides: "Create/edit/remove budget dialog"
      contains: "Set Budget"
    - path: "web/tests/phase-34-budgets.spec.ts"
      provides: "Playwright coverage of the set/edit/remove flow and the budget rows"
  key_links:
    - from: "web/src/features/finance/hooks.ts"
      to: "budget endpoints"
      via: "useSetBudget / useUpdateBudget / useDeleteBudget invalidating the cost-entries prefix"
      pattern: "invalidateAllCostEntries"
    - from: "web/src/app/(dashboard)/projects/components/TradeScopeDetail.tsx"
      to: "SetBudgetDialog"
      via: "onManageBudget callback opening the dialog with the scope anchor"
      pattern: "SetBudgetDialog"
    - from: "web/src/app/(dashboard)/monitoring/_components/AlertPanel.tsx"
      to: "budget alert types"
      via: "existing severity path; header copy corrected to 'Alerts'"
      pattern: ">Alerts<"
---

<objective>
Let owners and PMs set, edit and remove budgets from the web Costs surfaces
(BUDG-01's UI half), and stop calling the monitoring panel "AI Alerts" now that
rule-based budget alerts land in it.

Purpose: plan 34-04 shipped the read-only budget rows; this plan adds UI-SPEC
states 2 and 11 (the manage affordances), the dialog behind them, and the
end-to-end Playwright proof of the whole feature on web.
Output: three mutation hooks, `SetBudgetDialog`, the manage affordances wired
into both surfaces, one header copy fix, and a phase-34 Playwright spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-budgeting-and-overrun-alerts/34-CONTEXT.md
@.planning/phases/34-budgeting-and-overrun-alerts/34-UI-SPEC.md
@.planning/phases/34-budgeting-and-overrun-alerts/34-02-SUMMARY.md
@.planning/phases/34-budgeting-and-overrun-alerts/34-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Backend endpoints (plan 34-02) and the shipped web pieces to reuse. -->

```
POST   /api/v1/budgets/            body {project_id|trade_scope_id, total}  -> 201 BudgetResponse   finance.manage
PATCH  /api/v1/budgets/{budgetId}  body {total}                              -> 200 BudgetResponse   finance.manage
DELETE /api/v1/budgets/{budgetId}                                            -> 204                  finance.manage
409 on a duplicate active budget at the same anchor.
```

web/src/features/finance/hooks.ts (shipped patterns to copy):
```ts
function invalidateAllCostEntries(queryClient) { queryClient.invalidateQueries({ queryKey: ["cost-entries"] }); }
export function useAddCostEntry() { ... onSuccess: () => invalidateAllCostEntries(queryClient) }
export function useTradeScopeCostBreakdown(tradeScopeId)   // queryKey ["cost-entries","breakdown","trade-scope",id]
export function useProjectCostRollup(projectId)            // queryKey ["cost-entries","project-rollup",id]
```

web/src/features/finance/components/BudgetSummarySection.tsx (plan 34-04):
```tsx
export function BudgetSummarySection({ budget }: { budget: BudgetVsActual | null | undefined })
export function formatPercentUsed(percentUsed: string): string
```

web/src/features/finance/components/CostBreakdownSummary.tsx (plan 34-04):
renders `<BudgetSummarySection budget={...}/>` between the Total row and
`<MarginSummarySection/>` for the project and trade-scope variants.

Surfaces that host the dialog:
- `web/src/app/(dashboard)/projects/components/ProjectDetail.tsx:189` renders
  `{can("finance.view") && <ProjectCostsCard projectId={project.id} />}` — the
  project name lives on `project` here.
- `web/src/app/(dashboard)/projects/components/TradeScopeDetail.tsx:300-330` —
  the Costs section, already gated `can("finance.view")` with an
  `can("finance.manage")`-gated "Add cost" button and an `AddCostDialog` mounted
  below; `scope.trade_name` is in scope.

Dialog precedent: `web/src/app/(dashboard)/team/_components/rate-history-dialog.tsx`
— base-ui `dialog.tsx`, eyebrow + headline figure ("CURRENT RATE"), form reset via
an `onOpenChange` wrapper (NOT `useEffect`: react-hooks/set-state-in-effect fails
`npm run lint --max-warnings 0`, Phase 32-03 lesson).
Toasts: `import { toast } from "sonner"` (see `AddCostDialog.tsx`).
Playwright precedent: `web/tests/phase-33-margin.spec.ts` — log in through the UI
then SPA-navigate; `page.goto` directly leaves `usePermissions` disabled and
finance surfaces never render (Phase 32-04 lesson).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Budget mutation hooks + SetBudgetDialog</name>
  <files>web/src/features/finance/api.ts, web/src/features/finance/hooks.ts, web/src/features/finance/components/SetBudgetDialog.tsx, web/src/features/finance/__tests__/set-budget-dialog.test.tsx</files>
  <read_first>
    - web/src/features/finance/api.ts (mapper + apiPost/apiPatch/apiDelete conventions and the exact path prefix used)
    - web/src/features/finance/hooks.ts (whole file — mutation hook shape, invalidateAllCostEntries)
    - web/src/app/(dashboard)/team/_components/rate-history-dialog.tsx (whole file — dialog structure, eyebrow/headline, onOpenChange reset, footer layout, destructive step if any)
    - web/src/features/finance/components/AddCostDialog.tsx (toast usage, submit-disabled-while-pending pattern)
    - .planning/phases/34-budgeting-and-overrun-alerts/34-UI-SPEC.md § 3 "SetBudgetDialog" + § Copywriting Contract (every string is verbatim) + § 6 Test hooks
  </read_first>
  <behavior>
    - Create mode renders the title "Set budget — Riverside Remodel", helper "You'll get alerts at 80% and 100% of this amount.", and a "Set Budget" submit
    - Edit mode renders "Edit budget — Plumbing scope", the eyebrow "CURRENT BUDGET" over the current figure, the caption "Current spend: $8,200.00 (82%).", helper "Increasing the budget re-arms the 80% and 100% alerts.", and a "Save Budget" submit
    - Entering an amount below current spend in edit mode shows "Below current spend — the overrun alert will fire." and submit stays enabled
    - Submitting 0 or an empty amount shows "Enter a budget greater than $0." and calls no mutation
    - Submitting more than 99,999,999.99 shows "Enter an amount under $100,000,000."
    - A successful create toasts "Budget set — $10,000.00." and closes; an edit toasts "Budget updated — $12,000.00."
    - A failed save toasts "Could not save budget. Please try again." and keeps the dialog open
    - A 409 response toasts "A budget already exists here. Refresh to see it."
    - "Remove budget" swaps to the confirmation body, "Remove Budget" calls the delete mutation and toasts "Budget removed.", "Cancel" returns to the form
    - Reopening after a cancelled edit shows the original amount again (reset happens in the onOpenChange wrapper, not an effect)
  </behavior>
  <action>
1. `api.ts` — add `setBudget({projectId?, tradeScopeId?, total})`, `updateBudget(budgetId, total)`
   and `deleteBudget(budgetId)` calling the three endpoints with the file's
   existing path prefix and snake_case request bodies. Add a `Budget` response
   interface + mapper only if a caller needs the row back; the dialog only needs
   success/failure, so prefer not adding unused types (no dead code).
2. `hooks.ts` — add `useSetBudget`, `useUpdateBudget`, `useDeleteBudget`, each a
   `useMutation` whose `onSuccess` calls the existing `invalidateAllCostEntries`
   (the breakdown and rollup queries both live under the `["cost-entries"]`
   prefix, so one invalidation refreshes every budget row on screen).
3. Create `web/src/features/finance/components/SetBudgetDialog.tsx`:
```tsx
interface SetBudgetDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  /** Anchor + display name; exactly one id is set. */
  anchor: { projectId?: string; tradeScopeId?: string; name: string };
  /** Present = edit mode; null = create mode. */
  budget: BudgetVsActual | null;
}
export function SetBudgetDialog(props: SetBudgetDialogProps)
```
   Implement exactly the UI-SPEC § 3 layout and copy (every string a named module
   constant), including: the create/edit title builders, the "CURRENT BUDGET"
   eyebrow block, the current-spend caption built from
   `formatCurrency(budget.spent)` + `formatPercentUsed(budget.percentUsed)`, the
   live below-spend note, the two validation messages, the destructive
   remove-confirmation step (confirm copy verbatim, initial focus on "Cancel"),
   and the `data-testid`s from UI-SPEC § 6 (`set-budget-dialog`,
   `budget-amount-input`, `budget-remove-button`, `budget-remove-confirm`).
   Reset form state inside an `onOpenChange` wrapper — never `useEffect`.
   Keep the component decomposed: separate `BudgetAmountField`,
   `CurrentBudgetHeadline` and `RemoveBudgetConfirmation` sub-components in the
   same file rather than one 200-line render.
4. Create `web/src/features/finance/__tests__/set-budget-dialog.test.tsx`
   covering every `<behavior>` bullet, mocking `sonner` the way
   `cost-entry-form.test.tsx` does and mocking the hooks module for mutation
   assertions.
  </action>
  <verify>
    <automated>cd web && npm test -- set-budget-dialog && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useSetBudget\|useUpdateBudget\|useDeleteBudget" web/src/features/finance/hooks.ts` returns all three, each with an `invalidateAllCostEntries` call
    - `grep -n "You'll get alerts at 80% and 100% of this amount." web/src/features/finance/components/SetBudgetDialog.tsx` matches verbatim, and likewise for "Increasing the budget re-arms the 80% and 100% alerts." and "Below current spend — the overrun alert will fire."
    - `grep -n "useEffect" web/src/features/finance/components/SetBudgetDialog.tsx` returns nothing
    - `grep -n "data-testid=\"set-budget-dialog\"\|budget-amount-input\|budget-remove-button\|budget-remove-confirm" web/src/features/finance/components/SetBudgetDialog.tsx` returns all four
    - `cd web && npm test -- set-budget-dialog` reports >= 10 passing, 0 failing; `npm run lint` exits 0
  </acceptance_criteria>
  <done>Budgets can be created, edited and removed from one dialog whose copy matches the contract exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manage affordances on the project and trade-scope Costs surfaces</name>
  <files>web/src/features/finance/components/BudgetSummarySection.tsx, web/src/features/finance/components/CostBreakdownSummary.tsx, web/src/features/finance/components/ProjectCostsCard.tsx, web/src/app/(dashboard)/projects/components/ProjectDetail.tsx, web/src/app/(dashboard)/projects/components/TradeScopeDetail.tsx, web/src/features/finance/__tests__/budget-section.test.tsx</files>
  <read_first>
    - web/src/features/finance/components/BudgetSummarySection.tsx (plan 34-04 output)
    - web/src/features/finance/components/CostBreakdownSummary.tsx (current props + where BudgetSummarySection is rendered)
    - web/src/features/finance/components/ProjectCostsCard.tsx (whole file)
    - web/src/app/(dashboard)/projects/components/ProjectDetail.tsx around line 189 (how ProjectCostsCard is mounted, what project fields are available)
    - web/src/app/(dashboard)/projects/components/TradeScopeDetail.tsx lines 290-340 (Costs section, can() gating, AddCostDialog mounting pattern)
    - .planning/phases/34-budgeting-and-overrun-alerts/34-UI-SPEC.md § "State matrix" rows 2 and 11 + the "Set budget"/"Edit" affordance copy
  </read_first>
  <behavior>
    - budget=null + canManage renders a Separator and a Budget row whose trailing content is the "Set budget" ghost button (testid budget-set-button) — state 2
    - budget=null + !canManage renders nothing at all — state 1 unchanged
    - budget present + canManage renders an "Edit" ghost button with aria-label "Edit budget" (testid budget-edit-button) left of the Budget amount — state 11
    - budget present + !canManage renders the three rows with no buttons
    - Loading never renders a premature "Set budget" button — state 7
    - Clicking either button invokes the onManageBudget callback exactly once
    - The trade-scope surface opens the dialog with the scope anchor and the name "{trade name} scope"; the project surface opens it with the project anchor and the project name
  </behavior>
  <action>
1. `BudgetSummarySection` — extend the props to
   `{ budget, canManage = false, onManageBudget }` where `onManageBudget?: () => void`.
   - When `budget == null`: render nothing unless `canManage && onManageBudget`,
     in which case render `<Separator/>` + a Budget row whose trailing content is
     a ghost "Set budget" button (`data-testid="budget-set-button"`), using the
     shipped ghost-text-button recipe from the Phase 32 "Manage rate" affordance.
   - When a budget exists and `canManage`: render the "Edit" ghost button
     (`data-testid="budget-edit-button"`, `aria-label="Edit budget"`) before the
     amount in the Budget row's trailing span (8px gap, already the row's
     `gap-2`).
   - Both copy strings are named constants. Do not add any fetching here — the
     component stays pure display + callback.
2. `CostBreakdownSummary` — add optional pass-through props
   `canManageBudget?: boolean` and `onManageBudget?: () => void`, forwarded to
   `BudgetSummarySection`. Nothing else about the component changes.
3. `ProjectCostsCard` — accept a new required-ish `projectName: string` prop,
   hold `const [budgetDialogOpen, setBudgetDialogOpen] = useState(false)`, read
   `can` from `usePermissions`, pass `canManageBudget={can("finance.manage")}` and
   `onManageBudget={() => setBudgetDialogOpen(true)}` into `CostBreakdownSummary`,
   and mount `<SetBudgetDialog open={...} onOpenChange={...}
   anchor={{ projectId, name: projectName }} budget={rollup?.budget ?? null} />`.
   Update `ProjectDetail.tsx` to pass `projectName={project.name}`.
4. `TradeScopeDetail` — same shape inside the existing Costs section: local
   dialog state, `canManageBudget={can("finance.manage")}` (the `can` helper is
   already in scope), and `<SetBudgetDialog ... anchor={{ tradeScopeId: scope.id,
   name: `${scope.trade_name} scope` }} budget={breakdown?.budget ?? null} />`
   mounted next to the existing `AddCostDialog`.
5. Extend `budget-section.test.tsx` with the `<behavior>` cases (states 2, 7, 11
   and the callback assertions).
  </action>
  <verify>
    <automated>cd web && npm test -- "budget-section|set-budget-dialog|cost-breakdown-summary" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "budget-set-button\|budget-edit-button" web/src/features/finance/components/BudgetSummarySection.tsx` returns both testids
    - `grep -n "aria-label=\"Edit budget\"" web/src/features/finance/components/BudgetSummarySection.tsx` matches
    - `grep -c "SetBudgetDialog" web/src/features/finance/components/ProjectCostsCard.tsx` >= 2 and the same for TradeScopeDetail.tsx
    - `grep -n "projectName={project.name}" web/src/app/(dashboard)/projects/components/ProjectDetail.tsx` matches
    - `grep -n "scope\b.*scope\`" web/src/app/(dashboard)/projects/components/TradeScopeDetail.tsx` shows the "{trade name} scope" dialog name construction
    - `cd web && npm test -- budget-section` reports 0 failures including tests named for states 2 and 11
  </acceptance_criteria>
  <done>finance.manage holders can open the budget dialog from both Costs surfaces; view-only users see figures without affordances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AlertPanel header fix + phase-34 Playwright spec</name>
  <files>web/src/app/(dashboard)/monitoring/_components/AlertPanel.tsx, web/tests/phase-34-budgets.spec.ts</files>
  <read_first>
    - web/src/app/(dashboard)/monitoring/_components/AlertPanel.tsx (whole file — severity config, the rescheduling_suggestion special case, header block at ~line 234)
    - web/tests/phase-33-margin.spec.ts (whole file — login-through-UI + SPA-navigation, route mocking order, assertion style)
    - .planning/phases/34-budgeting-and-overrun-alerts/34-UI-SPEC.md § 4 "AlertPanel budget entries" and § 6 Test hooks
  </read_first>
  <behavior>
    - The monitoring panel header reads "Alerts"; the "{N} new" badge and the "No alerts — all projects on track" empty state are unchanged
    - A budget_warning alert renders through the warning severity path (amber) with its impact_text and no Accept/Dismiss rescheduling buttons
    - A budget_overrun alert renders through the critical severity path (red)
    - Playwright: on a trade-scope Costs section with a budget, budget-amount, budget-spent and budget-remaining are visible and budget-spent contains " · "
    - Playwright: clicking budget-set-button opens set-budget-dialog, submitting an amount closes it and the budget rows appear
    - Playwright: clicking budget-edit-button, then budget-remove-button, then budget-remove-confirm removes the budget and the rows disappear
    - Playwright: a warning-band budget shows budget-warning-chip; an over-budget one shows a leading "-" in budget-remaining and no chip
  </behavior>
  <action>
1. `AlertPanel.tsx` — change the header text "AI Alerts" to "Alerts" (the budget
   thresholds are rule-based, not AI; the old label would misattribute them).
   No structural change: the new alert types already flow through the existing
   severity path. If the severity config map is keyed by alert_type anywhere,
   verify budget types fall through to the severity-based branch and add nothing
   special-cased.
2. Create `web/tests/phase-34-budgets.spec.ts` following `phase-33-margin.spec.ts`
   exactly: log in through the login page, then SPA-navigate (never
   `page.goto` a deep route — `usePermissions` stays disabled and finance
   surfaces never render, Phase 32-04). Mock API routes most-specific-first.
   Cover the Playwright bullets in `<behavior>`, plus one assertion that the
   monitoring panel header is "Alerts" and a budget alert card renders its
   impact_text.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/phase-34-budgets.spec.ts --project=chromium --reporter=line && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AI Alerts" web/src/app/(dashboard)/monitoring/_components/AlertPanel.tsx` returns nothing
    - `grep -n ">Alerts<" web/src/app/(dashboard)/monitoring/_components/AlertPanel.tsx` matches
    - `grep -n "No alerts — all projects on track" web/src/app/(dashboard)/monitoring/_components/AlertPanel.tsx` still matches (empty state untouched)
    - `grep -n "page.goto" web/tests/phase-34-budgets.spec.ts` appears only for the login page
    - `grep -n "budget-set-button\|set-budget-dialog\|budget-remove-confirm\|budget-warning-chip" web/tests/phase-34-budgets.spec.ts` returns all four
    - `cd web && npx playwright test tests/phase-34-budgets.spec.ts --project=chromium` passes with >= 5 tests
  </acceptance_criteria>
  <done>Budget alerts read correctly in the monitoring panel and the whole web budget flow is proven end to end.</done>
</task>

</tasks>

<verification>
- `cd web && npm run lint && npx tsc --noEmit && npm run test` — zero warnings, all unit tests green
- `cd web && npx playwright test tests/phase-34-budgets.spec.ts tests/phase-33-margin.spec.ts --project=chromium` — new and prior finance specs green
- Registry safety: `git status` shows no new files under web/src/components/ui/
- Permission gating spot-check: the view-only jest cases assert the absence of both affordances
</verification>

<success_criteria>
- BUDG-01 (web): budgets can be set, edited and removed from the project and trade-scope Costs surfaces, gated finance.manage
- UI-SPEC states 2 and 11 are implemented and tested, completing the state matrix
- All dialog, affordance and toast copy is verbatim from the contract
- The monitoring panel header no longer misattributes rule-based alerts to AI
</success_criteria>

<output>
After completion, create `.planning/phases/34-budgeting-and-overrun-alerts/34-07-SUMMARY.md`
</output>
