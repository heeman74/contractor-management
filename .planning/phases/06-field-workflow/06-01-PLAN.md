---
phase: 06-field-workflow
plan: 01
type: execute
wave: 1
depends_on: [06-00]
files_modified:
  - backend/migrations/versions/0009_field_workflow_tables.py
  - backend/app/features/jobs/models.py
  - backend/app/features/jobs/schemas.py
  - backend/app/features/jobs/router.py
  - backend/app/features/jobs/service.py
  - backend/app/features/jobs/repository.py
  - backend/app/features/files/__init__.py
  - backend/app/features/files/router.py
  - backend/app/main.py
autonomous: true
requirements: [FIELD-01, FIELD-02, FIELD-04]

must_haves:
  truths:
    - "Backend can store and retrieve job notes with author and timestamp"
    - "Backend can receive file uploads and store them on local filesystem"
    - "Backend can store and retrieve time entries with clock in/out times"
    - "Backend geocodes GPS coordinates to address on job update sync"
    - "Sync endpoint returns job_notes, time_entries, and attachments in delta pull"
  artifacts:
    - path: "backend/migrations/versions/0009_field_workflow_tables.py"
      provides: "job_notes, attachments, time_entries tables + GPS columns on jobs"
      contains: "job_notes"
    - path: "backend/app/features/files/router.py"
      provides: "File upload endpoint"
      contains: "upload_attachment"
    - path: "backend/app/features/jobs/router.py"
      provides: "Note and time entry REST endpoints"
      contains: "create_note"
  key_links:
    - from: "backend/app/features/files/router.py"
      to: "uploads/ directory"
      via: "aiofiles write"
      pattern: "aiofiles\\.open"
    - from: "backend/app/features/jobs/router.py"
      to: "backend/app/features/jobs/service.py"
      via: "service method calls"
      pattern: "service\\.(create_note|create_time_entry)"
---

<objective>
Backend data foundation for field workflow: Alembic migration 0009 creating job_notes, attachments, and time_entries tables with RLS; GPS columns on jobs table; ORM models and Pydantic schemas; REST endpoints for notes CRUD, time entry CRUD, and file upload; sync endpoint extension for new entity types.

Purpose: All mobile features in Plans 02-05 depend on backend endpoints existing. This plan creates the full backend API surface.
Output: Migration 0009, ORM models, schemas, REST endpoints, file upload endpoint, sync endpoint extension.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-field-workflow/06-CONTEXT.md
@.planning/phases/06-field-workflow/06-RESEARCH.md

@backend/migrations/versions/0008_job_lifecycle_tables.py
@backend/app/features/jobs/models.py
@backend/app/features/jobs/schemas.py
@backend/app/features/jobs/router.py
@backend/app/features/jobs/service.py
@backend/app/features/jobs/repository.py
@backend/app/features/sync/router.py
@backend/app/features/sync/schemas.py
@backend/app/main.py

<interfaces>
<!-- Existing patterns to follow -->

From backend/app/features/jobs/models.py:
- Job model with TenantScopedModel base, relationships, columns
- Use same pattern for JobNote, TimeEntry models

From backend/app/features/jobs/router.py:
- APIRouter with tags, Depends(get_current_user), Depends(get_db)
- Route ordering: specific paths before /{id} catch-all
- response_model=None on 204 DELETE routes

From backend/app/features/sync/schemas.py:
- SyncResponse with entity lists defaulting to []
- Pattern for adding new entity types to sync pull

From backend/app/features/jobs/router.py (file upload pattern):
- aiofiles.open for writing uploaded files
- uploads/ directory structure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0009 + ORM models + Pydantic schemas</name>
  <files>
    backend/migrations/versions/0009_field_workflow_tables.py,
    backend/app/features/jobs/models.py,
    backend/app/features/jobs/schemas.py
  </files>
  <action>
    1. Create Alembic migration 0009_field_workflow_tables.py (depends on 0008):
       - job_notes table: id UUID PK, company_id UUID FK(companies), job_id UUID FK(jobs), author_id UUID FK(users), body TEXT NOT NULL, version INT DEFAULT 1, created_at/updated_at TIMESTAMPTZ, deleted_at TIMESTAMPTZ nullable. Add set_updated_at trigger. Enable RLS with company_id policy (same pattern as jobs table).
       - attachments table: id UUID PK, company_id UUID FK(companies), note_id UUID FK(job_notes), attachment_type TEXT CHECK IN ('photo','pdf','drawing'), remote_url TEXT nullable, caption TEXT nullable, sort_order INT DEFAULT 0, version INT DEFAULT 1, created_at/updated_at TIMESTAMPTZ, deleted_at TIMESTAMPTZ nullable. Enable RLS.
       - time_entries table: id UUID PK, company_id UUID FK(companies), job_id UUID FK(jobs), contractor_id UUID FK(users), clocked_in_at TIMESTAMPTZ NOT NULL, clocked_out_at TIMESTAMPTZ nullable, duration_seconds INT nullable, session_status TEXT CHECK IN ('active','completed','adjusted') DEFAULT 'active', adjustment_log JSONB DEFAULT '[]', version INT DEFAULT 1, created_at/updated_at TIMESTAMPTZ, deleted_at TIMESTAMPTZ nullable. Enable RLS.
       - ALTER TABLE jobs ADD COLUMN gps_latitude NUMERIC(9,6), gps_longitude NUMERIC(9,6), gps_address TEXT.
       - Use op.execute raw SQL (same pattern as 0007/0008 — Alembic autogenerate unreliable for RLS).

    2. Add ORM models to backend/app/features/jobs/models.py:
       - JobNote(TenantScopedModel): id, company_id, job_id, author_id, body, version. Relationships: job (Job), author (User), attachments (list[Attachment] via selectinload). Use lazy="raise" on relationships.
       - Attachment(TenantScopedModel): id, company_id, note_id, attachment_type, remote_url, caption, sort_order, version. Relationship: note (JobNote). Use lazy="raise".
       - TimeEntry(TenantScopedModel): id, company_id, job_id, contractor_id, clocked_in_at, clocked_out_at, duration_seconds, session_status, adjustment_log (JSONB), version. Relationships: job (Job), contractor (User). Use lazy="raise".
       - Add gps_latitude (Numeric(9,6) nullable), gps_longitude (Numeric(9,6) nullable), gps_address (Text nullable) columns to existing Job model.
       - Add job_notes relationship to Job model (one-to-many, lazy="raise").
       - Add time_entries relationship to Job model (one-to-many, lazy="raise").

    3. Add Pydantic schemas to backend/app/features/jobs/schemas.py:
       - JobNoteCreate(BaseModel): body (str, max_length=2000)
       - JobNoteResponse(BaseResponseSchema): id, job_id, author_id, body, created_at, attachments (list[AttachmentResponse])
       - AttachmentResponse(BaseResponseSchema): id, note_id, attachment_type, remote_url, caption, sort_order, created_at
       - TimeEntryCreate(BaseModel): clocked_in_at (datetime)
       - TimeEntryUpdate(BaseModel): clocked_out_at (datetime), duration_seconds (int)
       - TimeEntryAdjust(BaseModel): clocked_in_at (datetime optional), clocked_out_at (datetime optional), reason (str)
       - TimeEntryResponse(BaseResponseSchema): id, job_id, contractor_id, clocked_in_at, clocked_out_at, duration_seconds, session_status, adjustment_log, created_at
       - Add gps_latitude, gps_longitude, gps_address fields to JobResponse and JobUpdate schemas.
       - Add JobNoteResponse and TimeEntryResponse to SyncResponse in sync/schemas.py (default=[]).
       - Add AttachmentResponse to SyncResponse (default=[]).
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run ruff check app/features/jobs/models.py app/features/jobs/schemas.py && uv run python -c "from app.features.jobs.models import JobNote, TimeEntry, Attachment; print('Models import OK')" && echo "Lint + import OK"</automated>
  </verify>
  <done>Migration 0009 creates 3 new tables + GPS columns. ORM models defined with relationships. All Pydantic schemas created. Sync response extended with new entity lists.</done>
</task>

<task type="auto">
  <name>Task 2: REST endpoints for notes, time entries, file upload, and sync extension</name>
  <files>
    backend/app/features/jobs/router.py,
    backend/app/features/jobs/service.py,
    backend/app/features/jobs/repository.py,
    backend/app/features/files/__init__.py,
    backend/app/features/files/router.py,
    backend/app/main.py
  </files>
  <action>
    1. Add service methods to jobs/service.py (JobService class):
       - create_note(job_id, author_id, body) -> JobNote: Creates note, eager-loads attachments for response.
       - list_notes(job_id) -> list[JobNote]: Returns notes for job, newest first, with attachments eager-loaded.
       - create_time_entry(job_id, contractor_id, clocked_in_at) -> TimeEntry: Creates active session. If contractor has another active session (across any job), close it first (compute duration, set status=completed). If job status is 'scheduled', transition to 'in_progress' and record in status_history.
       - update_time_entry(entry_id, clocked_out_at, duration_seconds) -> TimeEntry: Clock out — set status=completed.
       - adjust_time_entry(entry_id, adjuster_id, clocked_in_at, clocked_out_at, reason) -> TimeEntry: Admin edit — append to adjustment_log JSONB, update times, set status=adjusted, recalculate duration.
       - list_time_entries(job_id) -> list[TimeEntry]: Returns time entries for job, ordered by clocked_in_at desc.
       - update_job_gps(job_id, latitude, longitude) -> Job: Store GPS coords. Call ORSGeocodingProvider.reverse_geocode(latitude, longitude) to resolve address. If geocode fails, store coords with gps_address=None (will retry on next sync).

    2. Add repository methods to jobs/repository.py if needed (follow BaseRepository pattern).

    3. Add REST endpoints to jobs/router.py (declare BEFORE /jobs/{job_id} catch-all):
       - POST /jobs/{job_id}/notes -> 201, JobNoteResponse (auth required)
       - GET /jobs/{job_id}/notes -> list[JobNoteResponse] (auth required)
       - POST /jobs/{job_id}/time-entries -> 201, TimeEntryResponse (auth required, contractor only)
       - PATCH /jobs/{job_id}/time-entries/{entry_id} -> TimeEntryResponse (clock out)
       - PATCH /jobs/{job_id}/time-entries/{entry_id}/adjust -> TimeEntryResponse (admin only)
       - GET /jobs/{job_id}/time-entries -> list[TimeEntryResponse] (auth required)

    4. Create backend/app/features/files/ package:
       - __init__.py (empty)
       - router.py: POST /files/upload (multipart, auth required). Accept UploadFile + Form fields (note_id, attachment_type, caption optional). Save to uploads/attachments/{note_id}/{uuid}{ext}. Create Attachment DB record with remote_url = /files/attachments/{note_id}/{filename}. Return 201 with AttachmentResponse.

    5. Wire in main.py:
       - Import and include files router: app.include_router(files_router, prefix="/api/v1")
       - Mount static files: app.mount("/files", StaticFiles(directory="uploads"), name="files") — add AFTER all router includes.
       - Ensure uploads/ directory exists (os.makedirs on startup).

    6. Extend sync endpoint (sync/router.py):
       - Add job_notes, time_entries, attachments to the delta pull query (filter by updated_at > cursor, same pattern as other entities).
       - Return in SyncResponse with the new fields.
       - Handle GPS geocode in the job sync push handler: when receiving a job update with gps_latitude/gps_longitude and gps_address=None, call reverse_geocode and set gps_address.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run ruff check app/features/jobs/ app/features/files/ app/features/sync/ && uv run ruff format --check app/features/jobs/ app/features/files/ app/features/sync/ && uv run python -m pytest tests/test_field_workflow.py -x --no-header 2>&1 | tail -5</automated>
  </verify>
  <done>All REST endpoints respond correctly. File upload saves to disk and creates DB record. Sync endpoint returns new entity types. GPS geocode triggers on job update with coordinates. One-job-at-a-time enforcement in create_time_entry. Backend test stubs still skip (implementation in 06-06).</done>
</task>

</tasks>

<verification>
- `ruff check` and `ruff format` pass on all modified files
- Migration 0009 has correct down_revision pointing to 0008
- All new endpoints are declared before /jobs/{job_id} catch-all to prevent route shadowing
- File upload endpoint creates uploads/ directory structure
- SyncResponse includes job_notes, time_entries, attachments with default=[]
- Backend test stub file runs without errors (all skip)
</verification>

<success_criteria>
- Migration 0009 creates job_notes, attachments, time_entries tables with RLS and GPS columns on jobs
- POST /jobs/{job_id}/notes creates a note and returns it with attachments
- POST /files/upload accepts multipart file and stores to disk
- POST /jobs/{job_id}/time-entries enforces one-active-session-per-contractor
- Sync endpoint returns new entity types in delta pull
- GPS coordinates trigger reverse geocode on sync
</success_criteria>

<output>
After completion, create `.planning/phases/06-field-workflow/06-01-SUMMARY.md`
</output>
