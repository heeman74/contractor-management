---
phase: 06-field-workflow
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - mobile/test/unit/features/jobs/note_dao_test.dart
  - mobile/test/unit/features/jobs/attachment_dao_test.dart
  - mobile/test/widget/features/jobs/add_note_bottom_sheet_test.dart
  - mobile/test/widget/features/jobs/notes_tab_test.dart
  - mobile/test/unit/features/jobs/gps_capture_test.dart
  - mobile/test/widget/features/jobs/gps_capture_widget_test.dart
  - mobile/test/widget/features/jobs/gps_overwrite_dialog_test.dart
  - mobile/test/unit/features/jobs/drawing_save_test.dart
  - mobile/test/widget/features/jobs/drawing_pad_screen_test.dart
  - mobile/test/unit/features/jobs/time_entry_dao_test.dart
  - mobile/test/unit/features/jobs/timer_notifier_test.dart
  - mobile/test/widget/features/jobs/timer_screen_test.dart
  - backend/tests/test_field_workflow.py
autonomous: true
requirements: [FIELD-01, FIELD-02, FIELD-03, FIELD-04]

must_haves:
  truths:
    - "All 13 test stub files exist before any implementation begins"
    - "Each stub has at least one skipped test describing expected behavior"
    - "Running the stubs produces no errors (all tests skip gracefully)"
  artifacts:
    - path: "mobile/test/unit/features/jobs/note_dao_test.dart"
      provides: "NoteDao test stubs"
      contains: "NoteDao"
    - path: "mobile/test/unit/features/jobs/time_entry_dao_test.dart"
      provides: "TimeEntryDao test stubs"
      contains: "TimeEntryDao"
    - path: "backend/tests/test_field_workflow.py"
      provides: "Backend integration test stubs"
      contains: "test_create_note"
  key_links: []
---

<objective>
Create all 13 Wave 0 test stub files required by VALIDATION.md before any implementation begins. Each stub contains a minimal test group with skip markers describing the expected behavior. This ensures every implementation task in Waves 1-2 has a corresponding test file to run in its verify step.

Purpose: Nyquist compliance — no implementation task should rely on lint-only verification when a test stub can confirm the test infrastructure is in place.
Output: 13 test stub files (12 Flutter + 1 backend) that compile and skip cleanly.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-field-workflow/06-CONTEXT.md
@.planning/phases/06-field-workflow/06-VALIDATION.md

@mobile/test/unit/features/users/user_dao_watch_by_role_test.dart
@mobile/test/widget/features/schedule/booking_card_interactions_test.dart
@backend/tests/conftest.py

<interfaces>
<!-- Follow existing test patterns exactly -->

Flutter unit test stub pattern:
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('FeatureName', () {
    test('expected behavior description', skip: 'Wave 0 stub — implementation in plan 06-XX', () {
      // Will test: [specific behavior]
    });
  });
}
```

Backend test stub pattern:
```python
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in plan 06-01")
def test_create_note(client, contractor_token):
    """POST /jobs/{job_id}/notes creates a note."""
    pass
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flutter test stubs (12 files)</name>
  <files>
    mobile/test/unit/features/jobs/note_dao_test.dart,
    mobile/test/unit/features/jobs/attachment_dao_test.dart,
    mobile/test/widget/features/jobs/add_note_bottom_sheet_test.dart,
    mobile/test/widget/features/jobs/notes_tab_test.dart,
    mobile/test/unit/features/jobs/gps_capture_test.dart,
    mobile/test/widget/features/jobs/gps_capture_widget_test.dart,
    mobile/test/widget/features/jobs/gps_overwrite_dialog_test.dart,
    mobile/test/unit/features/jobs/drawing_save_test.dart,
    mobile/test/widget/features/jobs/drawing_pad_screen_test.dart,
    mobile/test/unit/features/jobs/time_entry_dao_test.dart,
    mobile/test/unit/features/jobs/timer_notifier_test.dart,
    mobile/test/widget/features/jobs/timer_screen_test.dart
  </files>
  <action>
    Create 12 Flutter test stub files. Each file must:
    1. Import `package:flutter_test/flutter_test.dart`
    2. Have a `main()` with a `group()` named after the feature
    3. Contain 2-3 skipped tests (using `skip:` parameter) describing expected behaviors from VALIDATION.md
    4. Use `skip: 'Wave 0 stub — implementation in plan 06-XX'` with the correct plan number

    Specific stubs:

    note_dao_test.dart (plan 06-02/06-06):
    - test: 'insertNote creates note with sync queue entry'
    - test: 'watchNotesForJob returns newest first'
    - test: 'watchNotesForJob excludes soft-deleted'

    attachment_dao_test.dart (plan 06-02/06-06):
    - test: 'insertAttachment sets uploadStatus to pending_upload'
    - test: 'getPendingUploads returns pending and failed'
    - test: 'markUploaded sets remoteUrl and status'

    add_note_bottom_sheet_test.dart (plan 06-03):
    - test: 'renders text field and attachment buttons'
    - test: 'save disabled when body empty and no attachments'
    - test: 'save calls NoteDao.insertNote'

    notes_tab_test.dart (plan 06-03):
    - test: 'renders note entries with author and timestamp'
    - test: 'empty state shows no notes message'
    - test: 'FAB opens Add Note bottom sheet'

    gps_capture_test.dart (plan 06-04):
    - test: 'stores raw lat/lng via JobDao.updateJobGps'
    - test: 'handles permission denied gracefully'

    gps_capture_widget_test.dart (plan 06-04):
    - test: 'renders Capture Location button'
    - test: 'shows coordinates when gpsAddress is null'
    - test: 'shows geocoded address when available'

    gps_overwrite_dialog_test.dart (plan 06-04):
    - test: 'shows confirm dialog when address exists'
    - test: 'cancelling dialog preserves existing address'

    drawing_save_test.dart (plan 06-04):
    - test: 'exports PNG to app support directory'
    - test: 'returns file path via Navigator.pop'

    drawing_pad_screen_test.dart (plan 06-04):
    - test: 'renders drawing tools toolbar'
    - test: 'renders 8 color swatches'
    - test: 'grid toggle button present'

    time_entry_dao_test.dart (plan 06-05/06-06):
    - test: 'clockIn creates active entry'
    - test: 'clockIn auto-clocks out existing session'
    - test: 'clockOut computes duration'

    timer_notifier_test.dart (plan 06-05):
    - test: 'build restores active session from Drift'
    - test: 'clockIn starts Timer.periodic'
    - test: 'clockOut stops timer and resets state'

    timer_screen_test.dart (plan 06-05):
    - test: 'renders elapsed time display'
    - test: 'shows Clock In button when no active session'
    - test: 'shows session history list'

    Ensure parent directories exist (mkdir -p for test/unit/features/jobs/ and test/widget/features/jobs/).
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/unit/features/jobs/note_dao_test.dart test/unit/features/jobs/time_entry_dao_test.dart test/widget/features/jobs/notes_tab_test.dart test/widget/features/jobs/timer_screen_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <done>All 12 Flutter test stub files exist, compile cleanly, and all tests skip with descriptive messages.</done>
</task>

<task type="auto">
  <name>Task 2: Create backend test stub file</name>
  <files>
    backend/tests/test_field_workflow.py
  </files>
  <action>
    Create backend/tests/test_field_workflow.py with skipped test stubs:

    1. Import pytest
    2. Create skipped test functions covering all FIELD requirements:
       - test_create_note: POST /jobs/{job_id}/notes -> 201
       - test_list_notes: GET /jobs/{job_id}/notes returns newest first
       - test_create_note_empty_body: POST with empty body -> 422
       - test_create_note_body_too_long: POST body > 2000 chars -> 422
       - test_create_time_entry: POST /jobs/{job_id}/time-entries -> 201
       - test_clock_out_time_entry: PATCH /jobs/{job_id}/time-entries/{id} -> 200
       - test_auto_close_active_session: POST new entry auto-closes previous
       - test_adjust_time_entry_admin: PATCH .../adjust updates with audit
       - test_list_time_entries: GET /jobs/{job_id}/time-entries ordered desc
       - test_upload_file: POST /files/upload -> 201
       - test_upload_file_no_auth: POST /files/upload -> 401
       - test_rls_note_isolation: cross-tenant note access blocked
       - test_rls_time_entry_isolation: cross-tenant time entry blocked
       - test_gps_geocode_on_sync: GPS coordinates trigger reverse geocode

    3. All tests use `@pytest.mark.skip(reason="Wave 0 stub — implementation in plan 06-01/06-06")`
    4. Each test function has a docstring describing the expected behavior
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run python -m pytest tests/test_field_workflow.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>Backend test stub file exists with 14 skipped tests. All skip cleanly with pytest.</done>
</task>

</tasks>

<verification>
- All 13 test files exist on disk
- `flutter test test/unit/features/jobs/ --no-pub` runs without compile errors (all skip)
- `flutter test test/widget/features/jobs/ --no-pub` runs without compile errors (all skip)
- `uv run python -m pytest tests/test_field_workflow.py -v` shows all tests skipped
- No test uses pumpAndSettle()
</verification>

<success_criteria>
- 12 Flutter test stubs + 1 backend test stub created
- All stubs compile and skip cleanly (zero failures)
- Every stub file maps to a VALIDATION.md Wave 0 requirement
- Wave 1-2 plans can now reference these files in their verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/06-field-workflow/06-00-SUMMARY.md`
</output>
