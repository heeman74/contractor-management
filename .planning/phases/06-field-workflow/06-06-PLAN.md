---
phase: 06-field-workflow
plan: 06
type: execute
wave: 3
depends_on: [06-01, 06-03, 06-04, 06-05]
files_modified:
  - mobile/test/widget/features/jobs/notes_tab_test.dart
  - mobile/test/widget/features/jobs/add_note_bottom_sheet_test.dart
  - mobile/test/widget/features/jobs/timer_screen_test.dart
  - mobile/test/widget/features/jobs/contractor_job_card_test.dart
  - mobile/test/widget/features/jobs/drawing_pad_screen_test.dart
  - mobile/test/widget/features/jobs/gps_capture_button_test.dart
  - mobile/test/unit/features/jobs/note_dao_test.dart
  - mobile/test/unit/features/jobs/time_entry_dao_test.dart
  - mobile/test/unit/features/jobs/attachment_upload_service_test.dart
  - backend/tests/test_field_workflow.py
autonomous: true
requirements: [FIELD-01, FIELD-02, FIELD-03, FIELD-04]

must_haves:
  truths:
    - "Note creation and retrieval tested with Drift in-memory DB"
    - "Time entry clock in/out and one-job-at-a-time tested"
    - "Attachment upload service tested with mock Dio"
    - "Notes tab widget renders notes with thumbnails"
    - "Timer screen widget shows elapsed time and clock in/out"
    - "GPS capture handles permission states correctly"
    - "Backend note/time entry/file upload endpoints tested"
    - "Contractor job card renders action bar and active state"
  artifacts:
    - path: "mobile/test/unit/features/jobs/note_dao_test.dart"
      provides: "NoteDao unit tests with in-memory Drift"
      contains: "NoteDao"
    - path: "mobile/test/unit/features/jobs/time_entry_dao_test.dart"
      provides: "TimeEntryDao unit tests including one-job-at-a-time"
      contains: "auto-clocks out"
    - path: "mobile/test/widget/features/jobs/notes_tab_test.dart"
      provides: "Widget tests for notes tab rendering"
      contains: "NotesTab"
    - path: "backend/tests/test_field_workflow.py"
      provides: "Backend integration tests for all field workflow endpoints"
      contains: "test_create_note"
  key_links:
    - from: "mobile/test/unit/features/jobs/time_entry_dao_test.dart"
      to: "mobile/lib/features/jobs/data/time_entry_dao.dart"
      via: "in-memory Drift DB"
      pattern: "TimeEntryDao"
    - from: "backend/tests/test_field_workflow.py"
      to: "backend/app/features/jobs/router.py"
      via: "ASGI test client"
      pattern: "/api/v1/jobs/.*/notes"
---

<objective>
Comprehensive test suite for all Phase 6 field workflow features. Unit tests for DAOs (Drift in-memory), widget tests for all new screens and widgets, backend integration tests for all new endpoints.

Purpose: Per CLAUDE.md every new feature MUST include intensive E2E tests. This plan covers the full test surface for FIELD-01 through FIELD-04.
Output: 7 widget test files, 3 unit test files, 1 backend integration test file.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-field-workflow/06-CONTEXT.md
@.planning/phases/06-field-workflow/06-03-SUMMARY.md
@.planning/phases/06-field-workflow/06-04-SUMMARY.md
@.planning/phases/06-field-workflow/06-05-SUMMARY.md

@mobile/test/widget/features/schedule/booking_card_interactions_test.dart
@mobile/test/unit/features/users/user_dao_watch_by_role_test.dart
@mobile/test/helpers/
@backend/tests/conftest.py
@backend/tests/test_scheduling.py

<interfaces>
<!-- Test patterns established in project -->

Widget test pattern:
- ProviderScope overrides with stub notifiers
- pump() NOT pumpAndSettle() for Drift stream-backed providers
- MockDio via MockDioClient.instance for network mocks

Unit test pattern (Drift):
- In-memory database: AppDatabase.forTesting(NativeDatabase.memory())
- import 'package:drift/drift.dart' hide isNotNull, isNull;

Backend test pattern:
- ASGI client with pytest fixtures from conftest.py
- JWT bearer tokens (not X-Company-Id headers)
- clean_tables truncates all tables
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drift DAO unit tests and attachment upload service tests</name>
  <files>
    mobile/test/unit/features/jobs/note_dao_test.dart,
    mobile/test/unit/features/jobs/time_entry_dao_test.dart,
    mobile/test/unit/features/jobs/attachment_upload_service_test.dart
  </files>
  <action>
    1. Create note_dao_test.dart (follow user_dao_watch_by_role_test.dart pattern):
       - Setup: in-memory AppDatabase, NoteDao instance
       - Test: insertNote creates note in jobNotes table with correct fields
       - Test: insertNote also creates sync_queue entry with entityType='job_note' and operation='CREATE'
       - Test: watchNotesForJob returns notes ordered by createdAt DESC (newest first)
       - Test: watchNotesForJob filters by jobId (notes for other jobs not returned)
       - Test: watchNotesForJob excludes soft-deleted notes (deletedAt not null)
       - Test: upsertFromSync inserts new note from sync data
       - Test: upsertFromSync updates existing note (by id) from sync data
       - Use pump() for stream assertions, NOT pumpAndSettle()

    2. Create time_entry_dao_test.dart:
       - Setup: in-memory AppDatabase, TimeEntryDao instance
       - Test: clockIn creates active entry (clockedOutAt is null, sessionStatus='active')
       - Test: clockIn enqueues CREATE to sync_queue
       - Test: clockIn auto-clocks out existing active session for same contractor (ONE JOB AT A TIME)
       - Test: auto-clock-out computes duration correctly and sets sessionStatus='completed'
       - Test: auto-clock-out enqueues UPDATE for previous session to sync_queue
       - Test: clockOut sets clockedOutAt, computes durationSeconds, sets sessionStatus='completed'
       - Test: clockOut enqueues UPDATE to sync_queue
       - Test: watchActiveSession returns only entry with clockedOutAt=null for given contractor
       - Test: watchActiveSession returns null when no active session
       - Test: watchEntriesForJob returns entries ordered by clockedInAt DESC
       - Test: watchEntriesForJob excludes soft-deleted entries

    3. Create attachment_upload_service_test.dart:
       - Setup: MockDio (mocktail), mock AttachmentDao
       - Test: uploadPending calls getPendingUploads and uploads each via Dio POST /api/v1/files/upload
       - Test: successful upload calls markUploaded with remoteUrl from response
       - Test: failed upload (DioException) calls incrementRetry
       - Test: uploadPending sets status to 'uploading' before upload attempt
       - Test: empty pending list does nothing (no Dio calls)
       - Test: FormData includes note_id, attachment_type, and file
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/unit/features/jobs/note_dao_test.dart test/unit/features/jobs/time_entry_dao_test.dart test/unit/features/jobs/attachment_upload_service_test.dart --no-pub 2>&1 | tail -20</automated>
  </verify>
  <done>NoteDao tests verify dual-write and ordering. TimeEntryDao tests verify one-job-at-a-time enforcement and clock in/out. AttachmentUploadService tests verify upload flow with retry logic.</done>
</task>

<task type="auto">
  <name>Task 2: Widget tests and backend integration tests</name>
  <files>
    mobile/test/widget/features/jobs/notes_tab_test.dart,
    mobile/test/widget/features/jobs/add_note_bottom_sheet_test.dart,
    mobile/test/widget/features/jobs/timer_screen_test.dart,
    mobile/test/widget/features/jobs/contractor_job_card_test.dart,
    mobile/test/widget/features/jobs/drawing_pad_screen_test.dart,
    mobile/test/widget/features/jobs/gps_capture_button_test.dart,
    backend/tests/test_field_workflow.py
  </files>
  <action>
    1. Create notes_tab_test.dart:
       - Test: renders note entries with author, timestamp, body text
       - Test: renders attachment thumbnails inline with notes
       - Test: notes ordered newest first
       - Test: empty state shows "No notes yet" message
       - Test: FAB/button opens Add Note bottom sheet
       - Use ProviderScope overrides with stub notifier for notesForJobProvider
       - Use pump() not pumpAndSettle()

    2. Create add_note_bottom_sheet_test.dart:
       - Test: renders text field, camera button, gallery button, PDF button, drawing button
       - Test: save button disabled when body empty and no attachments
       - Test: save button enabled when body has text
       - Test: save calls NoteDao.insertNote with correct arguments
       - Test: max 10 attachments — buttons disabled at limit

    3. Create timer_screen_test.dart:
       - Test: renders elapsed time display (HH:MM:SS format)
       - Test: shows "Clock In" button when no active session
       - Test: shows "Clock Out" button when active session for this job
       - Test: session history list renders completed sessions with duration
       - Test: total time summary displayed correctly

    4. Create contractor_job_card_test.dart:
       - Test: renders action bar with Add Note, Camera, Clock In buttons for non-completed jobs
       - Test: active job shows highlighted border and elapsed timer
       - Test: completed job shows dimmed card with total time, no action bar
       - Test: status badge supports long-press for transitions
       - Test: Clock In button calls timerNotifier.clockIn

    5. Create drawing_pad_screen_test.dart:
       - Test: renders drawing tools toolbar (pen, eraser, shapes)
       - Test: renders color picker with 8 colors
       - Test: renders thickness selector with 3 options
       - Test: grid toggle button present
       - Test: save button present in app bar
       - Test: close button shows confirm dialog when canvas has content
       - Note: landscape lock testing may be limited in widget tests — test UI elements only

    6. Create gps_capture_button_test.dart:
       - Test: renders "Capture Location" button
       - Test: shows loading indicator during GPS fetch (mock Geolocator)
       - Test: displays coordinates when gpsLatitude/gpsLongitude set but no gpsAddress
       - Test: displays geocoded address when gpsAddress is set
       - Test: shows confirm dialog when existing address would be overwritten
       - Mock geolocator for permission states (use mocktail)

    7. Create backend/tests/test_field_workflow.py:
       - Test: POST /jobs/{job_id}/notes creates note with correct fields (201)
       - Test: GET /jobs/{job_id}/notes returns notes newest first
       - Test: POST /jobs/{job_id}/notes with empty body returns 422
       - Test: POST /jobs/{job_id}/notes body exceeding 2000 chars returns 422
       - Test: POST /jobs/{job_id}/time-entries creates active session (201)
       - Test: PATCH /jobs/{job_id}/time-entries/{id} clocks out session
       - Test: POST time-entry when another is active auto-closes previous
       - Test: PATCH /jobs/{job_id}/time-entries/{id}/adjust updates times and logs adjustment (admin only)
       - Test: GET /jobs/{job_id}/time-entries returns entries ordered by clocked_in_at desc
       - Test: POST /files/upload stores file and creates attachment record (201)
       - Test: POST /files/upload without auth returns 401
       - Test: RLS prevents cross-tenant note access
       - Test: RLS prevents cross-tenant time entry access
       - Use existing conftest.py fixtures (JWT bearer tokens, clean_tables)
       - Run migration 0009 in test setup
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/widget/features/jobs/notes_tab_test.dart test/widget/features/jobs/timer_screen_test.dart test/widget/features/jobs/contractor_job_card_test.dart --no-pub 2>&1 | tail -20</automated>
  </verify>
  <done>Widget tests cover notes tab, add note sheet, timer, contractor card, drawing pad, GPS button. Backend tests cover all new endpoints with RLS and auth verification. All tests pass.</done>
</task>

</tasks>

<verification>
- All Flutter unit tests pass: `flutter test test/unit/features/jobs/`
- All Flutter widget tests pass: `flutter test test/widget/features/jobs/`
- Backend tests pass: `cd backend && uv run python -m pytest tests/test_field_workflow.py -v`
- No `pumpAndSettle()` used in any Drift-stream-backed test
- One-job-at-a-time explicitly tested in time_entry_dao_test.dart
- RLS cross-tenant isolation tested in backend
</verification>

<success_criteria>
- NoteDao: 7+ unit tests covering dual-write, ordering, filtering, sync upsert
- TimeEntryDao: 11+ unit tests covering clock in/out, auto-close, active session watch
- AttachmentUploadService: 6+ unit tests covering upload flow, retry, error handling
- Notes tab: 5+ widget tests covering rendering, empty state, interactions
- Timer screen: 5+ widget tests covering display, buttons, session history
- Contractor job card: 5+ widget tests covering action bar, active state, completed state
- Backend: 13+ integration tests covering notes, time entries, file upload, RLS
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/06-field-workflow/06-06-SUMMARY.md`
</output>
