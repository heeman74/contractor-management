---
phase: 20
slug: dependency-engine
status: draft
shadcn_initialized: true
preset: base-nova / neutral / css-variables
created: 2026-03-21
---

# Phase 20 — UI Design Contract

> Visual and interaction contract for the Dependency Engine phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn | components.json (confirmed) |
| Style | base-nova | components.json `"style": "base-nova"` |
| Preset | neutral / css-variables | globals.css oklch tokens |
| Component library | Radix UI (via shadcn) | existing ui/ directory |
| Icon library | lucide-react (0.577.0) | components.json `"iconLibrary": "lucide"` |
| Font | Geist Sans (sans) / Geist Mono (mono) | web/src/app/layout.tsx |
| Gantt library (web) | @svar-ui/react-gantt 2.5.2 | RESEARCH.md — only MIT Gantt with FS/SS/FF/SE |
| Gantt library (mobile) | Flutter CustomPainter + InteractiveViewer | RESEARCH.md — no viable pub.dev alternative |

---

## Spacing Scale

Declared values (all multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, dependency type badge internal padding, arrow stroke gap |
| sm | 8px | Compact list item padding, Gantt lane header padding, conflict badge gap |
| md | 16px | Default element spacing, dialog body padding, form field gap |
| lg | 24px | Section padding, Gantt lane height baseline (lane top/bottom padding = 12px each) |
| xl | 32px | Layout gaps between Gantt and sidebar panels |
| 2xl | 48px | Gantt lane row minimum height (header strip + task bar + breathing room) |
| 3xl | 64px | Page-level spacing, Gantt page top padding |

Exceptions:
- Touch targets on mobile Gantt: minimum 44px height per task bar (WCAG 2.5.5 AA)
- Today line: 2px vertical stroke — not a spacing token, a visual marker
- Gantt task bar corner radius: 4px (xs token applied to border-radius)
- Mobile drag-to-connect tap target: minimum 44x44px handle at task bar right edge

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px | 400 (regular) | 1.5 | Gantt task bar labels, form field text, dialog body, zone list items |
| Label | 12px | 500 (medium) | 1.4 | Status badges, dependency type chips (FS/SS/FF/SE), conflict badge text, today line label, Gantt time-axis tick labels |
| Heading | 16px | 600 (semibold) | 1.3 | Gantt swim lane trade name headers, dialog titles, panel section headings |
| Display | 20px | 600 (semibold) | 1.2 | Gantt page title ("Project Timeline"), project overview conflict count |

Source: Extrapolated from existing ProjectTree.tsx (text-sm = 14px body, text-xs = 12px labels, font-medium/font-semibold weights) and shadcn base-nova defaults.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `oklch(1 0 0)` — white (#ffffff) | Gantt page background, dialog backgrounds, detail panels, zone list modal body |
| Secondary (30%) | `oklch(0.97 0 0)` — off-white (#f7f7f7) | Gantt swim lane row alternating background, sidebar panel background, card surfaces |
| Accent (10%) | Indigo (`#4f46e5` / indigo-600) | See reserved-for list below |
| Status: On-track | `bg-green-100 text-green-800` | Task bar fill — status complete or on-track (existing StatusBadge pattern) |
| Status: At-risk | `bg-yellow-100 text-yellow-800` | Task bar fill — status in_progress or behind schedule |
| Status: Blocked | `bg-red-100 text-red-800` | Task bar fill — status blocked; blocked badge in ProjectTree |
| Conflict warning | `bg-yellow-100 text-yellow-800` border `border-yellow-400` | Conflict badge on task detail, conflict indicator on Gantt bar |
| Cycle error | `border-2 border-red-500 bg-red-50` | SVAR Gantt task bar highlight; cycle path arrows drawn in `#ef4444` |
| Trade lane colors | TradeScope.trade_color (GC-defined hex) | Swim lane header strip, 12px dot in ProjectTree (existing pattern) |
| Today line | `#6b7280` (gray-500) | Vertical marker on Gantt time axis |
| Destructive | `oklch(0.577 0.245 27.325)` — red | Remove dependency confirmation button only |

Accent (indigo) reserved for:
1. "Add Dependency" primary button
2. "Save Zone" primary button in zone management modal
3. Selected state in ProjectTree (existing `bg-indigo-50 text-indigo-700` pattern — unchanged)
4. Folder icon in expanded project tree node (existing `text-indigo-400` — unchanged)
5. Active tab indicator on project detail Gantt tab

Dark mode tokens are defined in globals.css and apply automatically. No separate dark-mode overrides needed in this phase.

---

## Component Inventory

### New Components (Web)

| Component | File | Built From | Purpose |
|-----------|------|-----------|---------|
| GanttView | `web/src/components/gantt/GanttView.tsx` | @svar-ui/react-gantt | SVAR Gantt wrapper with swim lane config, `"use client"` required |
| ConflictBadge | `web/src/components/gantt/ConflictBadge.tsx` | shadcn Badge + lucide AlertTriangle | Inline conflict warning with trade names and date |
| CycleErrorDialog | `web/src/components/gantt/CycleErrorDialog.tsx` | shadcn Dialog | Shows cycle chain "Framing → Electrical → Plumbing → Framing" on dependency create failure |
| DependencyTypeSelect | `web/src/components/gantt/DependencyTypeSelect.tsx` | shadcn Select | FS/SS/FF/SE picker with lag_days input |
| ZoneManageModal | `web/src/components/gantt/ZoneManageModal.tsx` | shadcn Dialog + shadcn Input + shadcn Button | Inline zone list editor (add/rename/delete zones) |
| GanttPage | `web/src/app/(dashboard)/projects/[id]/gantt/page.tsx` | GanttView + layout primitives | Full Gantt route page |

### New Components (Mobile)

| Component | File | Built From | Purpose |
|-----------|------|-----------|---------|
| GanttScreen | `mobile/lib/features/projects/presentation/screens/gantt_screen.dart` | Scaffold + GanttChartWidget | Full Gantt page with AppBar and zoom controls |
| GanttChartWidget | `mobile/lib/features/projects/presentation/widgets/gantt_chart_widget.dart` | InteractiveViewer + CustomPaint | Zoom/pan wrapper for Gantt canvas |
| GanttPainter | `mobile/lib/features/projects/presentation/widgets/gantt_painter.dart` | CustomPainter | Draws swim lanes, task bars, progress fills, today line, dependency arrows |
| DependencyArrowPainter | `mobile/lib/features/projects/presentation/widgets/dependency_arrow_painter.dart` | CustomPainter | Overlay for bezier dependency arrows; red for cycle-involved arrows |
| CycleErrorDialog | Inline in gantt_screen.dart | AlertDialog | "Framing → Electrical → Plumbing → Framing" with tappable task names |

### Reused Components (Web)

| Component | File | Change |
|-----------|------|--------|
| StatusBadge | `web/src/components/shared/status-badge.tsx` | No change — `blocked` already maps to red |
| ProjectTree | `web/src/app/(dashboard)/projects/components/ProjectTree.tsx` | Add `blocked` badge next to blocked task nodes |
| shadcn Dialog | `web/src/components/ui/dialog.tsx` | Used for CycleErrorDialog and ZoneManageModal |
| shadcn Badge | `web/src/components/ui/badge.tsx` | Used for ConflictBadge and dependency type chips |
| shadcn Select | `web/src/components/ui/select.tsx` | Used for DependencyTypeSelect |
| shadcn Alert | `web/src/components/ui/alert.tsx` | Used for conflict summary warning on Gantt page |

---

## Interaction Contract

### Web Gantt Interactions

| Interaction | Trigger | Response |
|-------------|---------|----------|
| Drag task bar | Mouse drag on task bar | Reschedule task — updates `start_date` and `due_date` optimistically; PATCH to API on drop |
| Drag-connect dependency | Drag from task bar right-edge handle to another task | Ghost arrow follows cursor; on drop: opens DependencyTypeSelect popover to confirm type + lag; POST to API; if cycle: dismiss popover, open CycleErrorDialog |
| Click task bar | Single click | Opens task detail panel in right sidebar (slide-in Sheet) |
| Zoom | Mouse wheel / zoom control buttons | Day / Week / Month scale. Default: Week. Three zoom levels only |
| Conflict badge click | Click ConflictBadge | Expands inline: "Electrical and Plumbing overlap in Kitchen on Mar 25. Click tasks to reschedule." |
| Remove dependency | Right-click dependency arrow | Context menu with single option "Remove dependency" → confirmation inline popover |
| Add zone | Zone manage button (above Gantt) | Opens ZoneManageModal — inline list with text input and "Add Zone" button |

### Mobile Gantt Interactions

| Interaction | Trigger | Response |
|-------------|---------|----------|
| Pan | One-finger drag | Horizontal time scroll + vertical trade lane scroll |
| Zoom | Pinch (InteractiveViewer) | Scale 0.5x (month view) to 3.0x (day view); default 1.0x (week view) |
| Tap task bar | Single tap | Navigate to task detail screen |
| Long-press drag-connect | Long press on task bar right edge, then drag to target | Ghost arrow follows finger; release on target task bar → bottom sheet opens with FS/SS/FF/SE selector + lag input + "Create Dependency" button |
| Blocked start button | Contractor taps "Start Task" on blocked task | Button is disabled + greyed out; tapping shows snackbar: "This task is blocked. Complete [predecessor name] first." |

### Blocked State Contract

- **Web:** Blocked tasks show red StatusBadge in ProjectTree and red-filled bar in Gantt. "Start Task" button is absent from task detail panel for GC view. Tooltips on hover: "Blocked by: [predecessor task name]"
- **Mobile:** Blocked task cards show red "Blocked" chip. "Start" button is `enabled: false` with grey fill and reduced opacity (0.38). Tap shows snackbar per above. Contractors cannot override.

---

## Copywriting Contract

| Element | Copy | Notes |
|---------|------|-------|
| Primary CTA (add dependency) | "Add Dependency" | Button in task detail panel sidebar; verb + noun |
| Primary CTA (save zone) | "Add Zone" | Inside ZoneManageModal; verb + noun |
| Primary CTA (create Gantt dep via drag) | "Create Dependency" | Bottom sheet confirm button on mobile; web popover confirm button |
| Gantt page tab label | "Timeline" | Tab added to project detail page nav alongside "Overview" / "Tasks" |
| Empty state heading (no dependencies) | "No dependencies yet" | Shown in task detail sidebar "Dependencies" section |
| Empty state body (no dependencies) | "Drag from a task bar to connect tasks, or click Add Dependency." | Web only. Mobile: "Long-press a task bar to draw a connection." |
| Empty state (no tasks on Gantt) | "No tasks scheduled" | "Add tasks to trade scopes to see them on the timeline." |
| Empty state (no zones) | "No zones defined" | "Add zones like Kitchen or Master Bath to detect trade conflicts." |
| Conflict warning | "Conflict: [Trade A] and [Trade B] overlap in [Zone] on [Date]" | Example: "Conflict: Electrical and Plumbing overlap in Kitchen on Mar 25" |
| Conflict resolution guidance | "Reschedule one task to resolve." | Appended to conflict warning; not a hard block |
| Cycle error dialog title | "Dependency creates a loop" | |
| Cycle error dialog body | "This link creates a cycle: [Task A] → [Task B] → [Task C] → [Task A]. Remove one of these links to break the loop." | Task names are links/tappable on mobile |
| Cycle error CTA | "Got it" | Single dismiss button; no action taken |
| Blocked snackbar (mobile) | "Blocked by [predecessor task name]. Complete it first." | |
| Blocked tooltip (web hover) | "Blocked by: [predecessor task name]" | On blocked status badge |
| Lag field label | "Lag / Lead (days)" | Positive = delay, negative = overlap |
| Lag field hint | "Positive = delay, negative = overlap" | Below the input in DependencyTypeSelect |
| Remove dependency confirmation | "Remove this dependency?" | Inline popover below the right-click context menu item. Two buttons: "Remove" (destructive red) and "Cancel" |
| Remove dependency destructive button | "Remove" | Red button; no further confirmation after this click |
| Zone delete confirmation | "Delete [Zone Name]?" | "Tasks assigned to this zone will be unzoned. Conflicts using this zone will resolve." Buttons: "Delete Zone" (red) and "Cancel" |

---

## State Definitions

### Task Bar States (Gantt)

| State | Visual Treatment |
|-------|-----------------|
| not_started | Gray bar `bg-gray-200`, 0% progress fill |
| in_progress | Amber bar `bg-amber-200`, progress fill `bg-amber-500` at completion % |
| complete | Green bar `bg-green-200`, progress fill `bg-green-600` at 100% |
| blocked | Red bar `bg-red-200`, dashed border `border-dashed border-red-400`, lock icon at left edge |
| conflict | Yellow border `border-2 border-yellow-400`, warning icon overlay at top-right of bar |
| cycle-involved | Red border `border-2 border-red-500 bg-red-50`, applies to ALL bars in the cycle path |

### Dependency Arrow States

| State | Visual Treatment |
|-------|-----------------|
| Normal FS | Dark gray bezier arrow `#374151`, arrowhead at target |
| SS / FF / SE | Same gray, label chip on arrow midpoint showing "SS", "FF", or "SE" |
| With lag | Label chip on arrow midpoint showing "+Nd" (lag) or "-Nd" (lead) |
| Cycle-involved | Red arrow `#ef4444`, 2px stroke weight, animated dash (CSS animation, web only) |
| Ghost (in-progress drag) | Dashed gray `#9ca3af`, 1px stroke, follows cursor/finger |

---

## Zone Management UI

Zone list is managed via a modal dialog (not inline on the Gantt). GC accesses it via a "Manage Zones" button in the Gantt page toolbar.

**ZoneManageModal layout:**
- Header: "Project Zones"
- Body: scrollable list of existing zones. Each row: zone name (text) + delete icon button (trash, red on hover)
- Footer: text input ("Zone name") + "Add Zone" button (accent indigo)
- Zone names are display-only after creation — no inline rename to keep the interaction simple
- Validation: duplicate zone names show inline error "A zone with this name already exists" below the input

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Dialog, Badge, Select, Alert, Button, Input, Sheet | not required |
| @svar-ui/react-gantt | Direct npm install (not a shadcn registry block) | npm package — not a registry block; no shadcn vetting process applies. MIT license confirmed. SSR guard required: `"use client"` + `dynamic(..., { ssr: false })` fallback. |

No third-party shadcn registry blocks are used in this phase. @svar-ui/react-gantt is a standard npm dependency installed via `npm install`, not a shadcn registry block, so the registry vetting gate is not applicable.

---

## Accessibility Requirements

| Requirement | Implementation |
|-------------|---------------|
| Gantt keyboard navigation (web) | Arrow keys navigate between task bars; Enter opens task detail; focus ring visible at all times |
| Blocked button state | `disabled` attribute set on "Start" button; not just visual styling |
| Color not sole indicator | All status states pair color with a label (StatusBadge text) or icon (lock on blocked bar) |
| Conflict badge | Uses `role="alert"` and `aria-live="polite"` to announce new conflicts |
| Cycle error dialog | Focus traps inside dialog; Escape closes; `role="alertdialog"` |
| Touch target minimum | 44x44px on all mobile interactive elements including drag handles |
| Today line | Has `aria-label="Today"` on its SVG/Canvas element or adjacent DOM label |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Source Attribution

| Decision | Pre-populated From |
|----------|--------------------|
| Design system (shadcn, neutral, base-nova) | components.json (confirmed existing) |
| Font (Geist Sans/Mono) | web/src/app/layout.tsx |
| Color tokens (oklch values) | web/src/app/globals.css |
| Accent color (indigo) | web/src/app/(dashboard)/projects/components/ProjectTree.tsx — existing selection pattern |
| StatusBadge `blocked` = red | web/src/components/shared/status-badge.tsx |
| Icon library (lucide-react) | components.json |
| Gantt library (SVAR) | RESEARCH.md — library selection at Claude's discretion |
| Mobile Gantt (CustomPainter) | RESEARCH.md — library selection at Claude's discretion |
| All four dependency types (FS/SS/FF/SE) | CONTEXT.md — locked decision |
| Hard block enforcement | CONTEXT.md — locked decision |
| Conflict = warning not hard block | CONTEXT.md — locked decision |
| Cycle error = visual path highlight | CONTEXT.md — locked decision |
| Swim lanes by trade color | CONTEXT.md — locked decision |
| Zone list (project-scoped, modal) | CONTEXT.md + RESEARCH.md (modal vs inline at Claude's discretion) |
| Gantt zoom levels (3 levels: day/week/month) | Claude's discretion |
| Drag-to-connect mobile (long-press) | Claude's discretion — mobile Gantt interaction patterns |
| ZoneManageModal layout | Claude's discretion — zone list UI design |
