---
phase: 27-user-roles-rbac-expansion
plan: 02
type: execute
wave: 2
depends_on: [27-01, 27-04]
files_modified:
  - web/src/types/api.ts
  - web/src/lib/roles.ts
  - web/src/lib/permissions.ts
  - web/src/hooks/use-permissions.ts
  - web/src/components/layout/sidebar.tsx
  - web/src/app/(dashboard)/foreman/page.tsx
  - web/src/app/(dashboard)/foreman/status/[projectId]/page.tsx
  - web/src/components/shared/role-select.tsx
  - web/src/app/(dashboard)/settings/roles/page.tsx
  - web/src/app/(dashboard)/settings/roles/_components/permission-matrix.tsx
autonomous: true
requirements: [ROLE-06, ROLE-07, PERM-06]

must_haves:
  truths:
    - "RoleAssignmentRequest.role accepts all eight roles and a shared ROLES source of truth drives labels"
    - "usePermissions() exposes the current user's effective permission keys from GET /me/permissions"
    - "Sidebar and route gating are driven by effective permissions, not hardcoded role literals"
    - "Owner/admin see a Settings -> Roles page that reads and edits the per-company permission matrix"
    - "Editing a role's permissions and saving calls PUT /roles/{role}/permissions; Reset calls the reset endpoint"
    - "The matrix editor is hidden/403-safe for users lacking roles.permissions.manage"
  artifacts:
    - path: "web/src/lib/roles.ts"
      provides: "ROLE_SLUGS, Role, ROLE_LABELS, capability helpers"
      exports: ["ROLE_SLUGS", "ROLE_LABELS", "isManager", "isGc", "isOwner"]
    - path: "web/src/lib/permissions.ts"
      provides: "PermissionKey type + PERMISSION_GROUPS labels mirroring the backend catalog"
      exports: ["PERMISSION_GROUPS", "PermissionKey"]
    - path: "web/src/hooks/use-permissions.ts"
      provides: "usePermissions() -> { can(key), permissions, isLoading }"
      exports: ["usePermissions"]
    - path: "web/src/app/(dashboard)/settings/roles/_components/permission-matrix.tsx"
      provides: "Editable role x permission grid wired to the rbac endpoints"
      exports: ["PermissionMatrix"]
  key_links:
    - from: "web/src/hooks/use-permissions.ts"
      to: "backend GET /api/v1/me/permissions"
      via: "fetch effective permissions for UI gating"
      pattern: "me/permissions"
    - from: "web/src/app/(dashboard)/settings/roles/_components/permission-matrix.tsx"
      to: "backend PUT /api/v1/roles/{role}/permissions"
      via: "save edits to the per-company matrix"
      pattern: "roles/.*permissions"
    - from: "web/src/components/layout/sidebar.tsx"
      to: "web/src/hooks/use-permissions.ts"
      via: "nav visibility uses can(key)"
      pattern: "usePermissions|can\\("
---

<objective>
Web (Next.js) support for the expanded, editable role system: a shared role source of truth,
a permissions catalog mirror + `usePermissions()` hook backed by `GET /me/permissions`,
permission-driven nav/route gating, a reusable role-select, and the owner/admin
**Settings → Roles** permission-matrix editor (read/edit/reset) wired to 27-04's endpoints.

Purpose: consumes the API contract from 27-01 (roles) and 27-04 (permissions). No backend changes.
Output: `lib/roles.ts`, `lib/permissions.ts`, `hooks/use-permissions.ts`, permission-gated
sidebar + foreman pages, `RoleSelect`, and the `settings/roles` matrix editor page + component.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-user-roles-rbac-expansion/27-CONTEXT.md
@web/src/types/api.ts
@web/src/components/layout/sidebar.tsx
@web/src/app/(dashboard)/foreman/page.tsx
@web/src/app/(dashboard)/foreman/status/[projectId]/page.tsx
@web/src/store/slices/auth-slice.ts
@web/src/app/(dashboard)/clients/_components/create-client-dialog.tsx
@web/src/app/(dashboard)/contractors/_components/create-contractor-dialog.tsx

<interfaces>
From backend (27-04):
- GET  /api/v1/roles/permissions -> { catalog: {key,label,group}[], roles: {role: key[]}, defaults: {role: key[]} }
- PUT  /api/v1/roles/{role}/permissions  body { permissions: key[] }
- POST /api/v1/roles/permissions/reset   body { role?: string }
- GET  /api/v1/me/permissions -> { permissions: key[] }

From web/src/types/api.ts:
- RoleAssignmentRequest { user_id: string; role: "admin"|"contractor"|"client" }  (~line 405)
- AuthUser.roles / TokenResponse.roles: string[]

Existing calls go through /api/proxy (see how other dashboard pages call FastAPI). Reuse that
pattern for the new endpoints; do NOT call FastAPI directly from the browser.

From foreman pages: inline roles.includes("admin"|"owner"|"foreman") checks (to be replaced).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared role + permission sources of truth and typed request</name>
  <files>
    web/src/lib/roles.ts,
    web/src/lib/permissions.ts,
    web/src/types/api.ts
  </files>
  <read_first>
    web/src/types/api.ts,
    .planning/phases/27-user-roles-rbac-expansion/27-CONTEXT.md
  </read_first>
  <action>
1. Create `web/src/lib/roles.ts` (as in the original plan):
   - `ROLE_SLUGS` (eight, `as const`), `Role` type, `ROLE_LABELS` record.
   - Capability role-sets ADMIN_ROLES/MANAGER_ROLES/GC_ROLES and helpers
     `hasAnyRole(roles, allowed)`, `isOwner`, `isAdmin`, `isManager`, `isGc` (roles: string[]).
   These stay useful for coarse checks and label rendering; fine-grained gating uses permissions.
2. Create `web/src/lib/permissions.ts`:
   - `PERMISSION_KEYS` (the 21 keys) + `PermissionKey` type + `PERMISSION_GROUPS`
     (group → {key,label}[]) mirroring the backend catalog in 27-CONTEXT.md. Used to render
     the editor grid with stable labels even before the catalog fetch resolves.
3. `web/src/types/api.ts`:
   - `RoleAssignmentRequest.role` → `Role`.
   - Add `RolePermissionsResponse`, `RolePermissionsUpdate`, `MyPermissionsResponse` interfaces
     matching 27-04's shapes. Keep AuthUser/TokenResponse.roles as string[].
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ROLE_SLUGS" web/src/lib/roles.ts
    - grep -q "isManager" web/src/lib/roles.ts
    - grep -q "PERMISSION_GROUPS" web/src/lib/permissions.ts
    - grep -q "RolePermissionsResponse" web/src/types/api.ts
  </acceptance_criteria>
  <done>
    roles.ts + permissions.ts are the shared sources; RoleAssignmentRequest.role is Role; rbac response types added. tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: usePermissions() hook backed by GET /me/permissions</name>
  <files>
    web/src/hooks/use-permissions.ts
  </files>
  <read_first>
    web/src/lib/permissions.ts,
    web/src/store/slices/auth-slice.ts,
    web/src/hooks
  </read_first>
  <action>
1. Create `web/src/hooks/use-permissions.ts`:
   - `usePermissions()` fetches `GET /api/proxy/... /me/permissions` (via the existing proxy/
     data-fetching pattern — SWR/React Query/whatever the app already uses; match neighbors).
   - Returns `{ permissions: Set<PermissionKey>, can: (key: PermissionKey) => boolean, isLoading }`.
   - `can(key)` returns true if the key is present. Cache for the session; refetch on login.
   - Handle the loading state so gated UI doesn't flash (treat unknown as "not allowed" until loaded).
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/web && npx tsc --noEmit && npx eslint src/hooks/use-permissions.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "usePermissions" web/src/hooks/use-permissions.ts
    - grep -q "me/permissions" web/src/hooks/use-permissions.ts
  </acceptance_criteria>
  <done>
    usePermissions() exposes can()/permissions/isLoading from the effective-permissions endpoint. tsc + eslint clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Permission-driven sidebar + foreman gating; RoleSelect</name>
  <files>
    web/src/components/layout/sidebar.tsx,
    web/src/app/(dashboard)/foreman/page.tsx,
    web/src/app/(dashboard)/foreman/status/[projectId]/page.tsx,
    web/src/components/shared/role-select.tsx
  </files>
  <read_first>
    web/src/hooks/use-permissions.ts,
    web/src/lib/roles.ts,
    web/src/components/layout/sidebar.tsx,
    web/src/app/(dashboard)/foreman/page.tsx,
    web/src/app/(dashboard)/foreman/status/[projectId]/page.tsx,
    web/src/components/ui/select.tsx
  </read_first>
  <action>
1. `sidebar.tsx`: gate nav items on `can(key)` from usePermissions instead of role literals:
   - Contractors/Reports/company areas → `can("clients.manage")` / `can("company.settings.manage")` as fits each item.
   - Projects/Jobs/Schedule/Quotes/Invoices → the matching `*.manage` key.
   - Foreman area → `can("tasks.manage") || can("foreman.assign")`.
   - Add a **Settings → Roles** nav item gated on `can("roles.permissions.manage")`.
   Keep a coarse role fallback (isAdmin) only for the brief loading window if needed.
2. `foreman/page.tsx:34`: replace the inline `isAdmin` with `can("foreman.assign")` (or
   `isManager(roles)` fallback). `foreman/status/[projectId]/page.tsx:38`: replace with
   `can("tasks.manage") || roles.includes("foreman")`.
3. Create `web/src/components/shared/role-select.tsx`: `RoleSelect({ value, onChange, exclude })`
   over `ROLE_SLUGS`/`ROLE_LABELS` (unchanged from original plan; reusable admin role picker).
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/web && npx tsc --noEmit && npx eslint src/components/layout/sidebar.tsx src/components/shared/role-select.tsx "src/app/(dashboard)/foreman/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "usePermissions\|can(" web/src/components/layout/sidebar.tsx
    - grep -q "roles.permissions.manage" web/src/components/layout/sidebar.tsx
    - grep -q "RoleSelect" web/src/components/shared/role-select.tsx
  </acceptance_criteria>
  <done>
    Sidebar + foreman pages gate on effective permissions; a permission-gated Settings→Roles link exists; RoleSelect renders all eight roles. tsc + eslint clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: Settings -> Roles permission-matrix editor (owner/admin)</name>
  <files>
    web/src/app/(dashboard)/settings/roles/page.tsx,
    web/src/app/(dashboard)/settings/roles/_components/permission-matrix.tsx
  </files>
  <read_first>
    web/src/lib/roles.ts,
    web/src/lib/permissions.ts,
    web/src/hooks/use-permissions.ts,
    web/src/types/api.ts,
    web/src/components/ui/checkbox.tsx,
    web/src/app/(dashboard)/reports/page.tsx
  </read_first>
  <action>
1. `settings/roles/page.tsx`: server/client page that gates on `can("roles.permissions.manage")`
   (redirect or render an "insufficient access" state otherwise — the backend also 403s, so this
   is UX only). Fetches `GET /api/proxy/.../roles/permissions` and renders `<PermissionMatrix/>`.
2. `_components/permission-matrix.tsx` (`PermissionMatrix`):
   - Grid: rows = permission keys grouped by PERMISSION_GROUPS, columns = the eight roles.
   - Each cell = a checkbox (checked if the role holds the key). `owner` column is all-checked
     and **disabled** (locked). Keys not editable for owner.
   - Track dirty state per role; **Save** a changed role → `PUT /roles/{role}/permissions`
     with the full key list for that role. Show success/error toast.
   - **Reset to defaults** button → `POST /roles/permissions/reset` (all roles), then refetch.
     Optional per-role reset.
   - Disable Save if an edit would remove the last `roles.permissions.manage` holder (mirror the
     backend guard) and show a hint; rely on the backend 409/400 as the hard stop.
   - Use `defaults` from the response to show a "modified from default" indicator per role (nice-to-have).
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/web && npx tsc --noEmit && npx eslint "src/app/(dashboard)/settings/roles/page.tsx" "src/app/(dashboard)/settings/roles/_components/permission-matrix.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PermissionMatrix" web/src/app/(dashboard)/settings/roles/_components/permission-matrix.tsx
    - grep -q "roles/.*permissions\|roles/permissions" web/src/app/(dashboard)/settings/roles/_components/permission-matrix.tsx
    - grep -q "reset" web/src/app/(dashboard)/settings/roles/_components/permission-matrix.tsx
    - grep -q "roles.permissions.manage" "web/src/app/(dashboard)/settings/roles/page.tsx"
  </acceptance_criteria>
  <done>
    Owner/admin can view and edit the role x permission grid; owner column locked; Save PUTs per role; Reset restores defaults; page gated on roles.permissions.manage. tsc + eslint clean.
  </done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` is clean.
- `cd web && npx eslint src/lib/roles.ts src/lib/permissions.ts src/hooks/use-permissions.ts src/components/layout/sidebar.tsx "src/app/(dashboard)/settings/roles/page.tsx"` is clean.
- Manual/E2E: as an admin, toggling a permission in Settings→Roles and saving changes what a user of that role can access; a non-permission user cannot see the Settings→Roles link and is 403'd by the API.
</verification>

<success_criteria>
- roles.ts + permissions.ts are the shared web sources of truth; RoleAssignmentRequest.role is the eight-role Role type.
- usePermissions()/can() drives nav + route gating from GET /me/permissions.
- Settings → Roles editor reads the matrix, saves per-role edits (PUT), and resets to defaults, gated on roles.permissions.manage with owner locked.
- tsc + eslint clean.
</success_criteria>

<output>
After completion, create `.planning/phases/27-user-roles-rbac-expansion/27-02-SUMMARY.md`.
</output>
