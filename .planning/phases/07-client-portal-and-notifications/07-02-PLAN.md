---
phase: 07-client-portal-and-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart
  - mobile/lib/features/client/presentation/screens/photo_viewer_screen.dart
  - mobile/lib/features/client/presentation/screens/client_portal_screen.dart
  - mobile/lib/features/client/presentation/providers/client_providers.dart
  - mobile/lib/features/client/presentation/widgets/job_progress_stepper.dart
  - mobile/lib/features/client/presentation/widgets/delay_banner.dart
  - mobile/lib/features/client/presentation/widgets/photo_timeline.dart
  - mobile/lib/features/client/presentation/widgets/client_notes_tab.dart
  - mobile/lib/features/jobs/data/job_dao.dart
  - mobile/lib/core/routing/app_router.dart
  - mobile/lib/core/routing/route_names.dart
  - mobile/pubspec.yaml
autonomous: true
requirements: [CLNT-03, CLNT-05]

must_haves:
  truths:
    - "Client sees a step progress bar showing the current lifecycle stage of their job"
    - "Client can scroll through a chronological photo timeline of uploaded progress photos"
    - "Client can tap a photo to open a full-screen viewer with pinch-to-zoom and swipe navigation"
    - "Client sees an orange delay banner with reason and original vs new ETA when a job is delayed"
    - "Client sees a read-only activity log with contractor notes, delay events, and status transitions"
    - "Client portal list shows pending requests section with status badges"
    - "Completed/invoiced jobs appear dimmed with green checkmark in the portal list"
    - "Job cards show ETA date and delay warning icon when applicable"
  artifacts:
    - path: "mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart"
      provides: "Client-specific job detail with progress stepper and 3 tabs (Photos, Notes, Details)"
      min_lines: 100
    - path: "mobile/lib/features/client/presentation/screens/photo_viewer_screen.dart"
      provides: "Full-screen photo viewer with pinch-to-zoom and swipe between photos"
      min_lines: 40
    - path: "mobile/lib/features/client/presentation/widgets/job_progress_stepper.dart"
      provides: "Horizontal step progress bar widget for job lifecycle stages"
      min_lines: 30
    - path: "mobile/lib/features/client/presentation/widgets/delay_banner.dart"
      provides: "Orange warning banner with delay reason and ETA comparison"
      min_lines: 30
  key_links:
    - from: "mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart"
      to: "mobile/lib/features/jobs/domain/job_entity.dart"
      via: "JobEntity.statusHistory parsed for delay entries"
      pattern: "statusHistory.*delay"
    - from: "mobile/lib/features/client/presentation/screens/client_portal_screen.dart"
      to: "mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart"
      via: "GoRouter navigation from job card tap"
      pattern: "clientJobDetail"
    - from: "mobile/lib/features/client/presentation/screens/client_portal_screen.dart"
      to: "mobile/lib/features/jobs/data/job_dao.dart"
      via: "watchRequestsForClient streams pending/declined requests for client portal"
      pattern: "watchRequestsForClient"
    - from: "mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart"
      to: "mobile/lib/features/jobs/presentation/providers/note_providers.dart"
      via: "notesForJobProvider for notes and attachments"
      pattern: "notesForJob"
---

<objective>
Client portal UI: job detail screen with progress stepper, photo timeline, activity log, delay visibility, and enhanced portal list.

Purpose: Gives clients a transparent view of their job's progress (CLNT-03) with delay visibility (CLNT-05). The client-specific detail screen has a step progress bar, Photos tab, Notes tab, and Details tab. The portal list is enhanced with pending requests, ETA display, and delay indicators.

Output: ClientJobDetailScreen, PhotoViewerScreen, enhanced ClientPortalScreen, supporting widgets, route registration.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-client-portal-and-notifications/07-CONTEXT.md
@.planning/phases/07-client-portal-and-notifications/07-RESEARCH.md

@mobile/lib/features/client/presentation/screens/client_portal_screen.dart
@mobile/lib/features/jobs/domain/job_entity.dart
@mobile/lib/features/jobs/domain/job_status.dart
@mobile/lib/features/jobs/domain/note_entity.dart
@mobile/lib/features/jobs/domain/attachment_entity.dart
@mobile/lib/features/jobs/presentation/providers/note_providers.dart
@mobile/lib/features/jobs/presentation/providers/crm_providers.dart
@mobile/lib/features/jobs/data/job_dao.dart
@mobile/lib/core/routing/app_router.dart
@mobile/lib/core/routing/route_names.dart

<interfaces>
<!-- Key types the executor needs -->

From mobile/lib/features/jobs/domain/job_entity.dart:
```dart
abstract class JobEntity with _$JobEntity {
  const factory JobEntity({
    required String id,
    required String companyId,
    required String description,
    required String status,
    required String tradeType,
    String? clientId,
    String? contractorId,
    String? address,
    String? scheduledDate,
    String? completionDate,
    required List<Map<String, dynamic>> statusHistory,
    // ... other fields
  }) = _JobEntity;
}
```

From mobile/lib/features/jobs/domain/job_status.dart:
```dart
enum JobStatus {
  quote, scheduled, inProgress, complete, invoiced, cancelled;
  String get displayLabel => ...;
  static JobStatus fromString(String s) => ...;
}
```

From mobile/lib/features/jobs/domain/note_entity.dart:
```dart
abstract class NoteEntity with _$NoteEntity {
  const factory NoteEntity({
    required String id, required String jobId, required String content,
    required String noteType, required String createdAt,
    String? authorId, String? authorName,
    required List<AttachmentEntity> attachments,
  }) = _NoteEntity;
}
```

From mobile/lib/features/jobs/domain/attachment_entity.dart:
```dart
abstract class AttachmentEntity with _$AttachmentEntity {
  const factory AttachmentEntity({
    required String id, required String noteId,
    required String fileName, required String attachmentType,
    String? localPath, String? remoteUrl,
    required String uploadStatus, required String createdAt,
    String? caption,
  }) = _AttachmentEntity;
}
```

From mobile/lib/features/jobs/presentation/providers/note_providers.dart:
- notesForJobProvider: StreamProvider.autoDispose.family<List<NoteEntity>, String>
- noteCountProvider: Provider.autoDispose.family

From mobile/lib/features/jobs/presentation/providers/crm_providers.dart:
- clientJobHistoryNotifierProvider: streams client's jobs from Drift

From mobile/lib/features/jobs/data/job_dao.dart:
- watchPendingRequestsByCompany(companyId): existing method, filters by companyId + status='pending'
- NOTE: No watchRequestsForClient method exists yet — must be added (filters by clientId column)

From mobile/lib/core/database/tables/job_requests.dart:
- TextColumn get clientId => text().nullable()(); (line 20)

GoRouter Branch 6 currently has: /client/portal and /client/request
Need to add: /client/jobs/:id for client job detail
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClientJobDetailScreen with progress stepper, tabs, delay banner, and supporting widgets</name>
  <files>
    mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart
    mobile/lib/features/client/presentation/screens/photo_viewer_screen.dart
    mobile/lib/features/client/presentation/providers/client_providers.dart
    mobile/lib/features/client/presentation/widgets/job_progress_stepper.dart
    mobile/lib/features/client/presentation/widgets/delay_banner.dart
    mobile/lib/features/client/presentation/widgets/photo_timeline.dart
    mobile/lib/features/client/presentation/widgets/client_notes_tab.dart
    mobile/pubspec.yaml
  </files>
  <action>
    1. Add photo_view package to pubspec.yaml: `photo_view: ^0.15.0`

    2. Create client_providers.dart:
       - photosForJobProvider: StreamProvider.autoDispose.family<List<AttachmentEntity>, String> that watches notesForJobProvider, expands attachments, filters by uploadStatus=='uploaded' AND remoteUrl!=null AND (attachmentType=='photo' OR attachmentType=='drawing'), sorts chronologically
       - clientJobProvider: StreamProvider.autoDispose.family<JobEntity?, String> that watches a single job by ID from JobDao (reuse existing jobStreamProvider or create client-specific one from JobDao)

    3. Create job_progress_stepper.dart:
       - JobProgressStepper(JobStatus currentStatus) StatelessWidget
       - Horizontal Row of 5 stages: Quote, Scheduled, In Progress, Complete, Invoiced
       - Each stage: circle with number/check icon + label below + connecting line between stages
       - Completed stages: primary color filled circle with check icon
       - Current stage: primary color filled circle with number
       - Future stages: grey outlined circle with number
       - Connecting lines: primary color for completed, grey for future
       - For cancelled status: don't render stepper — the parent screen shows a "Cancelled" banner instead

    4. Create delay_banner.dart:
       - DelayBanner widget accepting List<Map<String, dynamic>> delayEntries (filtered from statusHistory where type=='delay')
       - Shows latest delay by default: orange/amber Container with icon, delay reason text, "Original ETA: X -> New ETA: Y" comparison
       - If multiple delays exist, shows "View previous delays (N)" TextButton that expands to show full history
       - Uses ExpansionTile or AnimatedCrossFade for expand/collapse
       - Styled: amber.shade50 background, amber.shade800 text, warning icon

    5. Create photo_timeline.dart:
       - PhotoTimeline widget: ConsumerWidget that watches photosForJobProvider(jobId)
       - GridView.builder with crossAxisCount=3 showing photo thumbnails
       - Each thumbnail: ClipRRect with Image.network(remoteUrl), caption overlay at bottom if exists, timestamp
       - Tap navigates to PhotoViewerScreen with the list of photos and initial index
       - Empty state: Camera icon + "No progress photos yet -- photos will appear here as work progresses."
       - Photo count badge: expose count via photosForJobProvider.length

    6. Create client_notes_tab.dart:
       - ClientNotesTab: ConsumerWidget that watches notesForJobProvider(jobId)
       - Merges three entry types into chronological list:
         a. Contractor notes: noteType field, show content, attachment count badge, author name, timestamp
         b. Delay events: parsed from JobEntity.statusHistory where type=='delay' — orange icon, "Delay reported" header, reason text, new ETA
         c. Status transitions: parsed from JobEntity.statusHistory where type=='transition' — blue icon, "Status changed to X" text, timestamp
       - All entries sorted by timestamp, newest first
       - Read-only — no interaction beyond scrolling
       - Each entry styled as a Card with leading icon (note icon for notes, warning icon for delays, info icon for transitions)

    7. Create client_job_detail_screen.dart:
       - ClientJobDetailScreen(String jobId): ConsumerWidget
       - Watches clientJobProvider(jobId) for job data
       - Layout:
         - AppBar with job description as title
         - If job.status == 'cancelled': show red "Cancelled" banner at top instead of stepper
         - Else: JobProgressStepper(currentStatus) at top
         - Below stepper: DelayBanner if job has delay entries in statusHistory AND status is not 'complete'/'invoiced'
         - TabBar with 3 tabs: Photos (with count badge), Notes, Details
         - Photos tab: PhotoTimeline(jobId)
         - Notes tab: ClientNotesTab(jobId, jobEntity) — pass job for statusHistory parsing
         - Details tab: Card showing job fields — description, address, trade type, contractor name, scheduled date/time window, expected completion date (ETA), status. No pricing info per CONTEXT.md decision.
       - "Last updated: X ago" text at bottom — relative time for <24h, absolute date for >24h
       - Pull-to-refresh via SyncEngine.syncNow()

    8. Create photo_viewer_screen.dart:
       - PhotoViewerScreen(List<AttachmentEntity> photos, int initialIndex)
       - Uses photo_view package: PhotoViewGallery.builder for swipe between photos
       - AppBar with "X of Y" counter, share/download button
       - Download button: save image to device gallery (use image_gallery_saver or gal package, or use the simpler approach of showing a snackbar with "Long press to save" since gallery saving requires additional platform setup)
       - Pinch-to-zoom via PhotoView default behavior
       - Caption overlay at bottom when photo has caption
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/client/presentation/ 2>&1 | head -20</automated>
  </verify>
  <done>
    - ClientJobDetailScreen renders with step progress bar, 3 tabs (Photos, Notes, Details), delay banner
    - PhotoViewerScreen shows full-screen photos with pinch-to-zoom and swipe
    - Delay banner parses statusHistory for delay entries and shows original vs new ETA
    - Activity log merges contractor notes, delay events, and status transitions chronologically
    - Photo timeline shows only uploaded attachments with valid remoteUrl
    - Cancelled jobs show banner instead of stepper
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced ClientPortalScreen with pending requests, ETA, delay indicators, and route wiring</name>
  <files>
    mobile/lib/features/client/presentation/screens/client_portal_screen.dart
    mobile/lib/features/jobs/data/job_dao.dart
    mobile/lib/core/routing/app_router.dart
    mobile/lib/core/routing/route_names.dart
  </files>
  <action>
    1. Add watchRequestsForClient method to JobDao (job_dao.dart):
       - Method signature: Stream<List<JobRequestEntity>> watchRequestsForClient(String clientId)
       - Query: select from jobRequests where clientId equals the given clientId AND requestStatus is IN ('pending', 'declined', 'request_more_info') AND deletedAt is null, ordered by createdAt desc
       - This is needed because the existing watchPendingRequestsByCompany filters by companyId + 'pending' status only — the client portal needs to show pending, declined, and request_more_info requests scoped to the specific client user

    2. Update RouteNames in route_names.dart:
       - Add: `static const clientJobDetail = '/client/jobs/:id';`
       - Add helper: `static String clientJobDetailPath(String jobId) => '/client/jobs/$jobId';`

    3. Update app_router.dart Branch 6 (Client Portal):
       - Add GoRoute for clientJobDetail under Branch 6:
         ```
         GoRoute(
           path: '/client/jobs/:id',
           builder: (context, state) {
             final jobId = state.pathParameters['id']!;
             return ClientJobDetailScreen(jobId: jobId);
           },
         ),
         ```
       - Import ClientJobDetailScreen and PhotoViewerScreen
       - Add PhotoViewerScreen as a top-level push route (not in Branch 6 — it's a full-screen overlay):
         ```
         GoRoute(
           path: '/photo-viewer',
           builder: (context, state) {
             final extra = state.extra as Map<String, dynamic>;
             return PhotoViewerScreen(
               photos: extra['photos'] as List<AttachmentEntity>,
               initialIndex: extra['initialIndex'] as int,
             );
           },
         ),
         ```
       - Add RouteNames.photoViewer = '/photo-viewer' constant

    4. Enhance ClientPortalScreen:
       - Add "Pending Requests" section at the TOP of the list (above active jobs):
         - Create a clientPendingRequestsProvider in client_providers.dart that watches watchRequestsForClient(currentUserId) from JobDao
         - Show section only when pending/declined requests exist; hidden when empty
         - Each request card: description, preferred dates, urgency badge, submission date, status chip (Pending=amber, Accepted=green, Declined=red)
         - Declined requests: red badge with admin's decline reason; dismissible via Dismissible widget or a close button
         - "Request More Info" status: show admin's message text
         - Accepted requests: don't show (they become jobs in the main list)

       - Enhance _JobCard widget:
         - Add ETA date beneath the status chip (from job.completionDate or parsed from statusHistory)
         - For completed/invoiced jobs: dim the card (0.6 opacity), add green checkmark trailing icon instead of chevron
         - For jobs with delay entries in statusHistory: small orange warning Icon next to status chip, show updated ETA
         - onTap: navigate to RouteNames.clientJobDetailPath(job.id) instead of RouteNames.jobDetailPath (client-specific detail, not admin)

       - Add "Last updated: X ago" indicator:
         - Show relative time ("5 min ago", "2 hours ago") for < 24 hours
         - Show absolute date ("Mar 12") for > 24 hours
         - Position at the top of the screen below AppBar, or as a subtitle in AppBar

       - Keep existing "Request Job" FAB unchanged
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/client/ lib/features/jobs/data/job_dao.dart lib/core/routing/ 2>&1 | head -20</automated>
  </verify>
  <done>
    - watchRequestsForClient method added to JobDao, querying by clientId for pending/declined/request_more_info requests
    - Client portal list shows "Pending Requests" section at top when requests exist
    - Job cards show ETA date and orange delay warning icon when applicable
    - Completed/invoiced jobs appear dimmed with green checkmark
    - Declined requests show red badge with decline reason
    - Tapping a job card navigates to ClientJobDetailScreen (not admin JobDetailScreen)
    - /client/jobs/:id route registered in GoRouter Branch 6
    - PhotoViewerScreen route registered as top-level push route
  </done>
</task>

</tasks>

<verification>
- `cd mobile && dart analyze lib/features/client/ lib/core/routing/` passes with no errors
- ClientPortalScreen renders job list with enhanced cards (ETA, delay indicators)
- ClientJobDetailScreen renders with stepper, 3 tabs, delay banner
- Navigation flow: portal list -> client job detail -> photo viewer works
- Cancelled jobs show banner instead of stepper
</verification>

<success_criteria>
- Client opening the app sees their jobs with current status, ETA, and delay indicators
- Client can tap a job to see progress stepper, photos, activity log, and details
- Delay reason and original vs new ETA comparison is visible in both the list and detail views
- Photo timeline shows only uploaded photos/drawings with full-screen viewer
- Pending job requests are visible at the top of the portal list
- Client portal uses client-specific routes (not admin routes)
</success_criteria>

<output>
After completion, create `.planning/phases/07-client-portal-and-notifications/07-02-SUMMARY.md`
</output>
