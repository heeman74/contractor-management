---
phase: 07-client-portal-and-notifications
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - backend/tests/test_phase_7_e2e.py
  - backend/tests/test_notification_service.py
  - mobile/test/e2e/phase_7_client_portal_e2e_test.dart
  - backend/tests/conftest.py
autonomous: true
requirements: [CLNT-02, CLNT-03, CLNT-05]

must_haves:
  truths:
    - "Backend tests verify notification dispatch on all four job milestone transitions"
    - "Backend tests verify sync delta filtering by client role"
    - "Backend tests verify device token CRUD (register, cleanup on invalid)"
    - "Flutter E2E tests verify client portal list with pending requests, ETA, delay indicators"
    - "Flutter E2E tests verify client job detail screen with progress stepper, photo timeline, notes tab, delay banner"
    - "Flutter E2E tests verify role gating (client cannot access admin routes)"
  artifacts:
    - path: "backend/tests/test_phase_7_e2e.py"
      provides: "Integration tests for notification dispatch, sync filtering, and token management"
      min_lines: 100
    - path: "backend/tests/test_notification_service.py"
      provides: "Unit tests for NotificationService with mocked Firebase"
      min_lines: 50
    - path: "mobile/test/e2e/phase_7_client_portal_e2e_test.dart"
      provides: "E2E widget tests for client portal screens"
      min_lines: 150
  key_links:
    - from: "backend/tests/test_phase_7_e2e.py"
      to: "backend/app/features/jobs/service.py"
      via: "Tests call transition_status and verify notification was sent"
      pattern: "transition_status.*scheduled"
    - from: "mobile/test/e2e/phase_7_client_portal_e2e_test.dart"
      to: "mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart"
      via: "Widget test pumps ClientJobDetailScreen with mock data"
      pattern: "ClientJobDetailScreen"
---

<objective>
E2E and unit tests for all Phase 7 requirements.

Purpose: Verifies all CLNT-02 (notifications), CLNT-03 (client portal), and CLNT-05 (delay visibility) requirements are met with automated tests. Per CLAUDE.md, every feature MUST include E2E tests in the same change.

Output: Backend integration tests, NotificationService unit tests, Flutter E2E widget tests.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-client-portal-and-notifications/07-CONTEXT.md
@.planning/phases/07-client-portal-and-notifications/07-RESEARCH.md
@.planning/phases/07-client-portal-and-notifications/07-01-SUMMARY.md
@.planning/phases/07-client-portal-and-notifications/07-02-SUMMARY.md

@backend/tests/conftest.py
@mobile/test/e2e/phase_6_field_workflow_e2e_test.dart

<interfaces>
<!-- From Plan 01 -->
NotificationService.send_job_notification(user_id, job_description, event, job_id)
NotificationService.upsert_token(user_id, token, platform)
POST /api/v1/notifications/token — { token, platform }
Sync delta endpoint filters by client_id for client role

<!-- From Plan 02 -->
ClientJobDetailScreen(jobId) — progress stepper, 3 tabs, delay banner
ClientPortalScreen — enhanced with pending requests, ETA, delay indicators
PhotoTimeline, DelayBanner, JobProgressStepper, ClientNotesTab widgets
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend integration and unit tests for notifications and sync filtering</name>
  <files>
    backend/tests/test_phase_7_e2e.py
    backend/tests/test_notification_service.py
    backend/tests/conftest.py
  </files>
  <action>
    1. Update conftest.py clean_tables fixture to include device_tokens table in the TRUNCATE list.

    2. Create test_notification_service.py — unit tests with mocked Firebase:
       - Mock firebase_admin.messaging.send to capture calls
       - test_send_job_notification_success: verify FCM message constructed with correct title, body, data, token
       - test_send_notification_unregistered_error: mock UnregisteredError, verify token is deleted from device_tokens
       - test_send_notification_generic_error: mock Exception, verify token is NOT deleted (fire-and-forget)
       - test_upsert_token_creates_new: register token, verify it's in DB
       - test_upsert_token_updates_existing: register same token twice, verify single row with updated last_used_at
       - test_upsert_token_multiple_devices: same user registers two different tokens, verify both stored
       - Use the test DB (real AsyncSession) for repository operations, mock only firebase_admin.messaging

    3. Create test_phase_7_e2e.py — integration tests via ASGI client:
       - Test notification dispatch on status transitions:
         a. test_notification_on_scheduled: create job with client, transition to scheduled, verify NotificationService.send was called (mock at the firebase_admin.messaging level)
         b. test_notification_on_in_progress: transition to in_progress, verify notification sent
         c. test_notification_on_complete: transition to complete, verify notification sent
         d. test_notification_on_delay: call report_delay, verify notification sent with delay event
         e. test_no_notification_without_client: transition job without client_id, verify no notification sent

       - Test token registration endpoint:
         a. test_register_token: POST /api/v1/notifications/token with valid data, expect 204
         b. test_register_token_unauthenticated: POST without auth, expect 401
         c. test_register_token_invalid_platform: POST with platform='web', expect 422

       - Test sync delta client role filtering:
         a. test_sync_client_sees_own_jobs: create 2 jobs (one for client A, one for client B), sync as client A, verify only client A's job returned
         b. test_sync_client_sees_own_notes: create notes on both jobs, sync as client A, verify only notes for client A's job
         c. test_sync_admin_sees_all_jobs: sync as admin, verify both jobs returned
         d. test_sync_client_role_filter: verify the WHERE clause filters correctly by client_id

       - Use existing conftest fixtures: authenticated_client, test_company, seed_users
       - Create a client user fixture with client role for client-scoped tests
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run python -m pytest tests/test_notification_service.py tests/test_phase_7_e2e.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - NotificationService unit tests pass: send success, unregistered cleanup, generic error tolerance, token CRUD
    - Integration tests pass: notification dispatch on 4 transitions, token endpoint auth/validation, sync role filtering
    - All tests use real DB with mocked Firebase (no real FCM calls)
  </done>
</task>

<task type="auto">
  <name>Task 2: Flutter E2E widget tests for client portal and job detail</name>
  <files>
    mobile/test/e2e/phase_7_client_portal_e2e_test.dart
  </files>
  <action>
    1. Create phase_7_client_portal_e2e_test.dart following existing phase E2E patterns (see phase_6_field_workflow_e2e_test.dart for structure).

    2. Test setup:
       - Use in-memory Drift DB seeded with test data
       - Mock DioClient via MockDioClient pattern
       - Create ProviderScope overrides for all providers used by client screens
       - Seed jobs with various statuses (quote, scheduled, in_progress, complete, cancelled)
       - Seed job with delay entries in statusHistory
       - Seed notes with uploaded attachments (photos/drawings with remoteUrl)
       - Mock auth state as client role
       - IMPORTANT: Use pump() not pumpAndSettle() for Drift stream-backed widgets (per MEMORY.md)

    3. ClientPortalScreen E2E tests:
       - test_portal_shows_active_jobs: seed 3 jobs, verify all appear in list
       - test_portal_shows_eta_on_cards: seed job with completionDate, verify ETA text visible
       - test_portal_shows_delay_indicator: seed job with delay in statusHistory, verify orange warning icon visible
       - test_portal_completed_jobs_dimmed: seed completed job, verify green checkmark and dimmed appearance
       - test_portal_pending_requests_section: seed pending job request, verify "Pending Requests" header and request card visible
       - test_portal_declined_request_shows_reason: seed declined request with reason, verify red badge and reason text
       - test_portal_tap_navigates_to_detail: tap job card, verify navigation to ClientJobDetailScreen

    4. ClientJobDetailScreen E2E tests:
       - test_detail_shows_progress_stepper: seed scheduled job, verify stepper shows Quote completed, Scheduled active
       - test_detail_cancelled_shows_banner: seed cancelled job, verify "Cancelled" banner visible and stepper NOT present
       - test_detail_delay_banner: seed job with delay entry, verify orange banner with reason and ETA comparison
       - test_detail_delay_banner_hidden_when_complete: seed complete job with delay history, verify banner NOT shown
       - test_detail_multiple_delays_expandable: seed job with 2+ delays, verify "View previous delays" button expands history
       - test_detail_photos_tab_count_badge: seed job with 3 uploaded photos, verify "(3)" badge on Photos tab
       - test_detail_photos_tab_empty_state: seed job with no photos, verify empty state message
       - test_detail_photos_tab_only_uploaded: seed mix of uploaded and pending attachments, verify only uploaded shown
       - test_detail_notes_tab_activity_log: seed job with notes + delays + transitions, verify chronological list with correct icons
       - test_detail_details_tab: seed job, verify description, address, contractor name, scheduled date, ETA all visible
       - test_detail_no_pricing_info: verify no price/cost text visible (deferred to Phase 8)

    5. Role gating E2E tests:
       - test_client_cannot_access_admin_routes: mock client auth, attempt to navigate to /admin/team, verify redirect to unauthorized
       - test_client_cannot_access_contractor_routes: attempt /contractor/jobs, verify redirect

    6. Use `pump()` for all assertions after state changes (not pumpAndSettle — Drift streams never settle).
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/e2e/phase_7_client_portal_e2e_test.dart 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All E2E tests pass for client portal list (active jobs, ETA, delay indicators, pending requests, completed dimming)
    - All E2E tests pass for client job detail (stepper, delay banner, photo timeline, notes tab, details tab)
    - Role gating tests verify client cannot access admin/contractor routes
    - Tests use pump() instead of pumpAndSettle() for Drift-backed widgets
    - No manual UAT needed for functional behavior (visual checks only)
  </done>
</task>

</tasks>

<verification>
- `cd backend && uv run python -m pytest tests/test_notification_service.py tests/test_phase_7_e2e.py -x -q` — all pass
- `cd mobile && flutter test test/e2e/phase_7_client_portal_e2e_test.dart` — all pass
- All existing tests still pass: `cd backend && uv run python -m pytest tests/ -x -q -m 'not slow'`
</verification>

<success_criteria>
- Backend: notification dispatch verified on all 4 milestone transitions plus delay
- Backend: sync client role filtering verified (own jobs only)
- Backend: token CRUD fully tested (register, update, cleanup)
- Flutter: client portal list fully tested (jobs, requests, indicators)
- Flutter: client job detail fully tested (stepper, tabs, delay, photos)
- Flutter: role gating verified
- All tests automated — minimal manual UAT needed
</success_criteria>

<output>
After completion, create `.planning/phases/07-client-portal-and-notifications/07-04-SUMMARY.md`
</output>
