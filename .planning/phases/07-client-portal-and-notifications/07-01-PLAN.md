---
phase: 07-client-portal-and-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/versions/0010_device_tokens.py
  - backend/app/features/notifications/__init__.py
  - backend/app/features/notifications/models.py
  - backend/app/features/notifications/repository.py
  - backend/app/features/notifications/schemas.py
  - backend/app/features/notifications/service.py
  - backend/app/features/notifications/router.py
  - backend/app/features/jobs/service.py
  - backend/app/features/sync/service.py
  - backend/app/features/sync/router.py
  - backend/app/main.py
  - backend/.gitignore
autonomous: true
requirements: [CLNT-02, CLNT-05]

must_haves:
  truths:
    - "NotificationService.send_job_notification() dispatches FCM messages to all device tokens for a given user"
    - "JobService.transition_status() calls NotificationService on scheduled, in_progress, and complete transitions for the job's client"
    - "JobService.report_delay() calls NotificationService for the job's client"
    - "Sync delta endpoint filters jobs by client_id when the requesting user has client role"
    - "POST /api/v1/notifications/token upserts a device token for the authenticated user"
    - "Invalid FCM tokens are cleaned up on UnregisteredError from Firebase"
  artifacts:
    - path: "backend/migrations/versions/0010_device_tokens.py"
      provides: "device_tokens table with user_id, token, platform, created_at, last_used_at"
      contains: "device_tokens"
    - path: "backend/app/features/notifications/service.py"
      provides: "NotificationService with send_job_notification and upsert_token"
      exports: ["NotificationService"]
    - path: "backend/app/features/notifications/router.py"
      provides: "POST /api/v1/notifications/token endpoint"
      exports: ["router"]
    - path: "backend/app/features/notifications/models.py"
      provides: "DeviceToken SQLAlchemy model"
      exports: ["DeviceToken"]
  key_links:
    - from: "backend/app/features/jobs/service.py"
      to: "backend/app/features/notifications/service.py"
      via: "NotificationService.send_job_notification() called in transition_status and report_delay"
      pattern: "notification.*send"
    - from: "backend/app/features/sync/service.py"
      to: "Job.client_id filter"
      via: "client role check adds WHERE Job.client_id == user_id"
      pattern: "client_id"
---

<objective>
Backend notification infrastructure and sync role filtering.

Purpose: Enables FCM push notification dispatch on job milestones (CLNT-02) and ensures the sync delta endpoint filters data by client_id for client-role users (CLNT-05). This is the backend foundation that mobile plans depend on.

Output: Alembic migration 0010 (device_tokens table), NotificationService, token registration endpoint, notification dispatch calls wired into JobService, role-filtered sync.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-client-portal-and-notifications/07-CONTEXT.md
@.planning/phases/07-client-portal-and-notifications/07-RESEARCH.md

@backend/app/features/jobs/service.py
@backend/app/features/sync/service.py
@backend/app/features/sync/router.py
@backend/app/main.py
@backend/app/core/base_service.py
@backend/app/core/base_repository.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/features/jobs/service.py:
- class JobService(TenantScopedService[Job])
- async def transition_status(self, job_id, new_status, ...) — line ~199
- async def report_delay(self, job_id, reason, new_eta, ...) — line ~369

From backend/app/features/sync/service.py:
- SyncService handles get_jobs_since, get_users_since, etc.
- Sync router calls service methods and assembles SyncResponse

From backend/app/core/base_service.py:
- BaseService[T] base class — for non-tenant-scoped services
- TenantScopedService[T] base class — for tenant-scoped services
- Constructor takes AsyncSession

From backend/app/core/base_repository.py:
- BaseRepository[T] base class — for non-tenant-scoped repositories
- TenantScopedRepository[T] base class — for tenant-scoped repositories

Alembic migration naming: 0010_device_tokens.py (next after 0009_field_workflow_tables.py)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Device tokens migration, NotificationService, and token endpoint</name>
  <files>
    backend/migrations/versions/0010_device_tokens.py
    backend/app/features/notifications/__init__.py
    backend/app/features/notifications/models.py
    backend/app/features/notifications/repository.py
    backend/app/features/notifications/schemas.py
    backend/app/features/notifications/service.py
    backend/app/features/notifications/router.py
    backend/app/main.py
    backend/.gitignore
  </files>
  <action>
    1. Create Alembic migration 0010_device_tokens.py with raw SQL (consistent with scheduling/job migrations):
       - Table: device_tokens (id UUID PK default gen_random_uuid(), user_id UUID NOT NULL FK users(id), token TEXT NOT NULL, platform VARCHAR(10) NOT NULL CHECK (platform IN ('android', 'ios')), created_at TIMESTAMPTZ DEFAULT now(), last_used_at TIMESTAMPTZ DEFAULT now())
       - UNIQUE constraint on (user_id, token) — same device registers once per user
       - Index on user_id for efficient lookup
       - RLS: ENABLE ROW LEVEL SECURITY, policy allowing access where user_id matches app.current_user_id OR via company tenant
       - down_revision = '0009' (field_workflow_tables)

    2. Create SQLAlchemy model DeviceToken in models.py:
       - Inherits from Base (not TenantScopedModel — device_tokens is user-scoped, not tenant-scoped)
       - Columns: id, user_id (FK users.id), token, platform, created_at, last_used_at
       - lazy="raise" on any relationships

    3. Create NotificationRepository in repository.py:
       - Inherits from BaseRepository[DeviceToken] (user-scoped, not tenant-scoped — per CLAUDE.md OOP rules)
       - Set model = DeviceToken
       - get_tokens_for_user(user_id) -> list[DeviceToken]
       - upsert_token(user_id, token, platform) — INSERT ON CONFLICT (user_id, token) DO UPDATE SET last_used_at=now()
       - delete_token(token) — remove invalid tokens

    4. Create Pydantic schemas in schemas.py:
       - TokenRegisterRequest: token (str), platform (Literal['android', 'ios'])

    5. Create NotificationService in service.py:
       - Inherits from BaseService[DeviceToken] (user-scoped, not tenant-scoped — per CLAUDE.md OOP rules)
       - Set repository_class = NotificationRepository
       - Constructor takes AsyncSession (call super().__init__(db))
       - upsert_token(user_id, token, platform) — delegates to repository
       - send_job_notification(user_id, job_description, event, job_id) — looks up all tokens for user_id, sends FCM message to each
       - Guard firebase_admin init: `if not firebase_admin._apps: firebase_admin.initialize_app(cred)` where cred comes from GOOGLE_APPLICATION_CREDENTIALS env var
       - Fire-and-forget on FCM failure: log error, continue. On UnregisteredError: delete the invalid token.
       - If GOOGLE_APPLICATION_CREDENTIALS is not set, log warning and skip FCM (graceful degradation for dev/test environments without Firebase)

    6. Create router.py with POST /api/v1/notifications/token (status_code=204):
       - Depends(get_current_user) — all authenticated users can register tokens
       - Calls NotificationService.upsert_token()

    7. Register notification router in main.py: app.include_router(notification_router, prefix="/api/v1/notifications")

    8. Add `firebase-service-account.json` to backend/.gitignore

    9. Add firebase-admin to backend dependencies: `cd backend && uv add firebase-admin`
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run python -c "from app.features.notifications.service import NotificationService; from app.features.notifications.models import DeviceToken; from app.features.notifications.router import router; print('imports OK')"</automated>
  </verify>
  <done>
    - device_tokens table created via migration 0010
    - NotificationService inherits from BaseService[DeviceToken] and handles FCM dispatch with graceful degradation
    - NotificationRepository inherits from BaseRepository[DeviceToken]
    - POST /api/v1/notifications/token endpoint registered and functional
    - firebase-service-account.json in .gitignore
    - firebase-admin installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire notification dispatch into JobService and add sync role filtering</name>
  <files>
    backend/app/features/jobs/service.py
    backend/app/features/sync/service.py
    backend/app/features/sync/router.py
  </files>
  <action>
    1. In JobService.transition_status():
       - After successful status transition, check if the job has a client_id
       - If client_id exists and new_status is one of [scheduled, in_progress, complete], create NotificationService(db) and call send_job_notification(client_id, job.description, event_name, job.id)
       - event_name maps: scheduled -> "scheduled", in_progress -> "started", complete -> "completed"
       - Notification body format: "Your job '{description}' has been {event_past_tense}." with appropriate detail per event type (e.g., scheduled includes date)
       - Do NOT let notification failure affect the status transition — wrap in try/except with logging

    2. In JobService.report_delay():
       - After successful delay recording, if job has client_id, send notification with event="delayed"
       - Body: "Your job '{description}' has been delayed. Updated completion: {new_eta}."
       - Same fire-and-forget pattern

    3. In SyncService (sync/service.py):
       - Add optional parameter client_user_id: str | None = None to get_jobs_since() and all entity-fetching methods
       - When client_user_id is provided, add .where(Job.client_id == client_user_id) to the query
       - For notes/attachments: filter by job_id IN (client's jobs) — join through Job table
       - For bookings: filter by job_id IN (client's jobs)
       - Users, companies, user_roles: no additional filtering (needed for display)

    4. In sync router (sync/router.py):
       - Extract current user's role from the JWT/auth dependency
       - If role is 'client', pass client_user_id=current_user.user_id to all SyncService methods
       - If role is 'admin' or 'contractor', pass client_user_id=None (no additional filtering)
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run ruff check app/features/jobs/service.py app/features/sync/service.py app/features/sync/router.py && uv run ruff format --check app/features/jobs/service.py app/features/sync/service.py app/features/sync/router.py</automated>
  </verify>
  <done>
    - JobService.transition_status() sends push notification to client on scheduled/started/complete transitions
    - JobService.report_delay() sends push notification to client on delay
    - Sync delta endpoint filters jobs/notes/attachments/bookings by client_id for client-role users
    - Notification failures do not block job operations
    - Code passes ruff check and format
  </done>
</task>

</tasks>

<verification>
- `cd backend && uv run ruff check app/ && uv run ruff format --check app/` passes
- `cd backend && uv run python -c "from app.features.notifications.service import NotificationService"` succeeds
- NotificationService gracefully degrades when GOOGLE_APPLICATION_CREDENTIALS is unset
- Migration 0010 creates device_tokens table with correct schema
</verification>

<success_criteria>
- Backend can receive and store FCM device tokens via POST /api/v1/notifications/token
- Job status transitions (scheduled, in_progress, complete) and delay reports trigger FCM notifications to the job's client
- Sync delta endpoint returns only the client's own jobs, notes, and attachments when accessed by a client-role user
- All notification failures are fire-and-forget — they never block job operations
</success_criteria>

<output>
After completion, create `.planning/phases/07-client-portal-and-notifications/07-01-SUMMARY.md`
</output>
