---
phase: 25-per-trade-billing
plan: 05
type: execute
wave: 3
depends_on: ["25-03", "25-04"]
files_modified:
  - backend/tests/test_phase_25_e2e.py
  - backend/tests/test_billing_milestones.py
  - mobile/test/e2e/phase_25_per_trade_billing_e2e_test.dart
  - mobile/test/features/billing_milestones/billing_milestone_dao_test.dart
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-04
  - BILL-05

must_haves:
  truths:
    - "Backend E2E tests cover all 5 BILL requirements end-to-end"
    - "Backend unit tests cover BillingMilestoneService CRUD and double-billing prevention"
    - "Flutter E2E tests cover TradeScopeDetailScreen billing UI and ProjectDetailScreen summaries"
    - "Flutter DAO tests cover BillingMilestoneDao with in-memory Drift DB"
    - "All tests pass (pytest green, flutter test green)"
  artifacts:
    - path: "backend/tests/test_phase_25_e2e.py"
      provides: "Backend E2E integration tests for BILL-01 through BILL-05"
      min_lines: 150
    - path: "backend/tests/test_billing_milestones.py"
      provides: "BillingMilestoneService unit tests"
      contains: "test_double_billing_prevented"
    - path: "mobile/test/e2e/phase_25_per_trade_billing_e2e_test.dart"
      provides: "Flutter E2E widget tests for billing UI"
      min_lines: 100
    - path: "mobile/test/features/billing_milestones/billing_milestone_dao_test.dart"
      provides: "Drift DAO tests for BillingMilestoneDao"
      contains: "watchByScope"
  key_links:
    - from: "backend/tests/test_phase_25_e2e.py"
      to: "backend/app/features/quotes/router.py"
      via: "ASGI client requests to /api/trade-scopes/{scope_id}/quotes"
      pattern: "trade-scopes.*quotes"
    - from: "backend/tests/test_phase_25_e2e.py"
      to: "backend/app/features/invoices/router.py"
      via: "ASGI client requests to /api/trade-scopes/{scope_id}/invoices"
      pattern: "trade-scopes.*invoices"
---

<objective>
Comprehensive E2E and unit tests for all BILL requirements — backend integration tests via ASGI client and Flutter widget/DAO tests.

Purpose: Ensures all 5 BILL requirements are verified by automated tests. Backend tests exercise the full HTTP -> service -> DB path. Flutter tests verify UI rendering and DAO behavior with in-memory Drift DB.

Output: 4 test files covering BILL-01 through BILL-05 with all tests passing.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-per-trade-billing/25-CONTEXT.md
@.planning/phases/25-per-trade-billing/25-RESEARCH.md
@.planning/phases/25-per-trade-billing/25-03-SUMMARY.md
@.planning/phases/25-per-trade-billing/25-04-SUMMARY.md

<interfaces>
<!-- Endpoints to test (from Plan 03) -->
POST   /api/trade-scopes/{scope_id}/quotes          — create trade-scoped quote
GET    /api/trade-scopes/{scope_id}/quotes          — list quotes for scope
GET    /api/projects/{project_id}/quote-summary     — aggregated project quote view
POST   /api/trade-scopes/{scope_id}/invoices/generate — generate from completed tasks
POST   /api/trade-scopes/{scope_id}/invoices/progress  — generate progress invoice (body: {milestone_id})
GET    /api/trade-scopes/{scope_id}/invoices        — list invoices for scope
GET    /api/trade-scopes/{scope_id}/milestones      — list milestones
POST   /api/trade-scopes/{scope_id}/milestones      — create milestone
PUT    /api/trade-scopes/{scope_id}/milestones/{id} — update milestone
DELETE /api/trade-scopes/{scope_id}/milestones/{id} — delete milestone
GET    /api/projects/{project_id}/invoice-summary   — aggregated invoice view

<!-- Widgets to test (from Plan 04) -->
TradeScopeDetailScreen — billing section with Create Quote, Generate Invoice, MilestoneListCard
ProjectDetailScreen — BillingSummaryCard for quotes and invoices
MilestoneListCard — milestone rows with invoiced badges and generate invoice buttons
CreateMilestoneSheet — form validation and submission
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend integration tests (E2E + unit)</name>
  <files>
    backend/tests/test_phase_25_e2e.py,
    backend/tests/test_billing_milestones.py
  </files>
  <read_first>
    backend/tests/conftest.py,
    backend/tests/test_phase_24_e2e.py,
    backend/app/features/quotes/router.py,
    backend/app/features/invoices/router.py,
    backend/app/features/billing_milestones/router.py,
    backend/app/features/projects/router.py
  </read_first>
  <action>
    1. Create `backend/tests/test_phase_25_e2e.py` — integration tests using ASGI client:
       - Use existing conftest.py fixtures (async_client, admin_token, test_company, clean_tables)
       - Setup helper: create a project with 2 trade scopes (plumbing, electrical), each with 3 tasks (2 complete, 1 in_progress)

       Test cases (minimum):
       - **test_create_trade_scope_quote** (BILL-01): POST /api/trade-scopes/{scope_id}/quotes with line_items, assert 201, response has trade_scope_id, no job_id
       - **test_trade_scope_quote_independent** (BILL-01): Create quotes on two different scopes, verify they are independent (editing one doesn't affect the other)
       - **test_project_quote_summary** (BILL-02): Create quotes on both scopes, GET /api/projects/{project_id}/quote-summary, assert per-trade breakdown and grand total match
       - **test_generate_scope_invoice** (BILL-03): POST /api/trade-scopes/{scope_id}/invoices/generate, assert 201, verify line items match completed tasks (2 items), invoice has trade_scope_id
       - **test_generate_scope_invoice_inherits_quote_tax** (BILL-03): Create and approve a quote with tax_rate=10, generate invoice, verify invoice.tax_rate=10
       - **test_project_invoice_summary** (BILL-04): Generate invoices on both scopes, GET /api/projects/{project_id}/invoice-summary, assert totals across trades
       - **test_create_milestone** (BILL-05): POST milestone on scope, verify 201
       - **test_progress_billing_milestone** (BILL-05): Create milestone (40%), approve quote, POST /api/trade-scopes/{scope_id}/invoices/progress with milestone_id, verify invoice amount = 40% of quote total
       - **test_double_billing_prevented** (BILL-05): Create progress invoice for milestone, attempt again, assert 409 response
       - **test_milestone_crud** (BILL-05): Create, update (change percentage), delete milestone — verify each operation
       - **test_legacy_job_scoped_quote_still_works**: Create a traditional job-scoped quote (job_id set, no trade_scope_id), verify it still works (backwards compatibility)

    2. Create `backend/tests/test_billing_milestones.py` — focused unit/integration tests:
       - **test_create_milestone_valid**: Create with valid data, verify stored correctly
       - **test_create_milestone_invalid_percentage**: percentage=0 or percentage=101, verify 422
       - **test_update_milestone**: Update name and percentage, verify changes persisted
       - **test_delete_milestone**: Soft delete, verify deleted_at set, not returned in list
       - **test_list_milestones_by_scope**: Create 3 milestones, verify all returned ordered by sort_order
       - **test_mark_invoiced_atomic**: Mark as invoiced, verify is_invoiced=True
       - **test_double_billing_prevented**: Mark invoiced, attempt again, verify 409
       - **test_milestone_rls**: Create milestone with company A token, attempt to read with company B token, verify 404 or empty
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run python -m pytest tests/test_phase_25_e2e.py tests/test_billing_milestones.py -x -v --timeout=60 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "test_create_trade_scope_quote" backend/tests/test_phase_25_e2e.py` succeeds
    - `grep -q "test_project_quote_summary" backend/tests/test_phase_25_e2e.py` succeeds
    - `grep -q "test_generate_scope_invoice" backend/tests/test_phase_25_e2e.py` succeeds
    - `grep -q "test_project_invoice_summary" backend/tests/test_phase_25_e2e.py` succeeds
    - `grep -q "test_progress_billing_milestone" backend/tests/test_phase_25_e2e.py` succeeds
    - `grep -q "test_double_billing_prevented" backend/tests/test_phase_25_e2e.py` succeeds
    - `grep -q "test_double_billing_prevented" backend/tests/test_billing_milestones.py` succeeds
    - All pytest tests pass (exit code 0)
  </acceptance_criteria>
  <done>11+ backend E2E integration tests and 8+ unit tests all passing. Every BILL requirement has at least 2 test cases. Double-billing prevention verified. Legacy backwards compatibility confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Flutter E2E widget tests and DAO tests</name>
  <files>
    mobile/test/e2e/phase_25_per_trade_billing_e2e_test.dart,
    mobile/test/features/billing_milestones/billing_milestone_dao_test.dart
  </files>
  <read_first>
    mobile/test/e2e/phase_24_inspection_workflow_e2e_test.dart,
    mobile/test/features/invoices/data/invoice_dao_test.dart,
    mobile/lib/features/billing_milestones/data/billing_milestone_dao.dart,
    mobile/lib/features/billing_milestones/presentation/widgets/milestone_list_card.dart,
    mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart,
    mobile/lib/features/projects/presentation/screens/project_detail_screen.dart
  </read_first>
  <action>
    1. Create `mobile/test/features/billing_milestones/billing_milestone_dao_test.dart`:
       - Use Drift in-memory DB (NativeDatabase.memory())
       - Seed company and trade scope data
       - **test_create_milestone**: Create milestone, verify stored in DB and sync_queue has entry
       - **test_watch_by_scope**: Create 3 milestones for scope A, 1 for scope B, verify watchByScope(scopeA) returns 3 ordered by sortOrder
       - **test_update_milestone**: Create, update name, verify updated via watch stream
       - **test_delete_milestone**: Create, delete, verify watch stream excludes soft-deleted record
       - **test_upsert_from_sync**: Call upsertFromSync with raw JSON, verify milestone row exists; call again with updated data, verify updated (no duplicate)
       - **test_upsert_from_sync_no_sync_queue**: Verify upsertFromSync does NOT write to sync_queue (important: sync pull must not re-enqueue)
       - Use `pump()` not `pumpAndSettle()` for stream assertions per project rules

    2. Create `mobile/test/e2e/phase_25_per_trade_billing_e2e_test.dart`:
       - Use in-memory Drift DB, mock DioClient via MockDioClient.instance
       - Seed: company, project, 2 trade scopes, tasks (some complete), quotes, invoices, milestones

       Widget test cases:
       - **test_trade_scope_billing_section_visible**: Render TradeScopeDetailScreen, verify "Billing" section header, "Create Quote" button, "Generate Invoice" button present
       - **test_create_quote_button_navigates**: Tap "Create Quote", verify navigation triggered (mock GoRouter)
       - **test_generate_invoice_button_posts**: Tap "Generate Invoice", verify Dio POST to /api/trade-scopes/{scopeId}/invoices/generate was called
       - **test_milestone_list_renders**: Seed 2 milestones, render MilestoneListCard, verify both milestone names visible
       - **test_invoiced_milestone_shows_badge**: Seed milestone with isInvoiced=true, verify "Invoiced" chip visible
       - **test_invoiced_milestone_no_generate_button**: Seed invoiced milestone, verify "Generate Invoice" button NOT present for that row
       - **test_create_milestone_sheet_validation**: Open CreateMilestoneSheet, submit empty, verify "required" validation error
       - **test_create_milestone_sheet_submits**: Fill name="Rough-in", percentage="40", submit, verify DAO write called
       - **test_project_quote_summary_renders**: Mock quote-summary API response, render BillingSummaryCard(type: quote), verify per-trade rows and grand total
       - **test_project_invoice_summary_renders**: Mock invoice-summary API response, render BillingSummaryCard(type: invoice), verify total_billed, total_paid, total_outstanding displayed
       - **test_project_detail_shows_billing_summaries**: Render ProjectDetailScreen with mocked providers, verify both BillingSummaryCard widgets present

       Use `pump()` everywhere (never `pumpAndSettle()`) per project Drift stream rule.
       Use `tester.runAsync()` for Drift one-shot queries if needed.
       Use `Stream.value()` for fake DAO providers to avoid pending timers.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/features/billing_milestones/billing_milestone_dao_test.dart test/e2e/phase_25_per_trade_billing_e2e_test.dart --no-pub 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "test_trade_scope_billing_section_visible" mobile/test/e2e/phase_25_per_trade_billing_e2e_test.dart` succeeds
    - `grep -q "test_milestone_list_renders" mobile/test/e2e/phase_25_per_trade_billing_e2e_test.dart` succeeds
    - `grep -q "test_project_quote_summary_renders" mobile/test/e2e/phase_25_per_trade_billing_e2e_test.dart` succeeds
    - `grep -q "test_project_invoice_summary_renders" mobile/test/e2e/phase_25_per_trade_billing_e2e_test.dart` succeeds
    - `grep -q "test_watch_by_scope" mobile/test/features/billing_milestones/billing_milestone_dao_test.dart` succeeds
    - `grep -q "test_upsert_from_sync_no_sync_queue" mobile/test/features/billing_milestones/billing_milestone_dao_test.dart` succeeds
    - All flutter tests pass (exit code 0)
    - No use of `pumpAndSettle` in test files (grep returns empty)
  </acceptance_criteria>
  <done>6+ DAO tests and 11+ E2E widget tests all passing. Every BILL requirement covered by at least one Flutter test. No pumpAndSettle usage. All tests use pump() and Stream.value() patterns.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run python -m pytest tests/test_phase_25_e2e.py tests/test_billing_milestones.py -x` — all pass
- `cd mobile && flutter test test/features/billing_milestones/ test/e2e/phase_25_per_trade_billing_e2e_test.dart` — all pass
- Full suite: `cd backend && uv run python -m pytest` and `cd mobile && flutter test` — no regressions
</verification>

<success_criteria>
- Backend: 11+ E2E tests + 8+ unit tests, all passing, covering BILL-01 through BILL-05
- Mobile: 6+ DAO tests + 11+ widget E2E tests, all passing
- Double-billing prevention tested from both backend and UI perspectives
- Legacy backwards compatibility confirmed (job-scoped quote test)
- No pumpAndSettle in Flutter tests
- Full test suites pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/25-per-trade-billing/25-05-SUMMARY.md`
</output>
