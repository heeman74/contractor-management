---
phase: 25-per-trade-billing
plan: 04
type: execute
wave: 2
depends_on: ["25-02"]
files_modified:
  - mobile/lib/features/billing_milestones/presentation/providers/billing_milestone_providers.dart
  - mobile/lib/features/billing_milestones/presentation/widgets/milestone_list_card.dart
  - mobile/lib/features/billing_milestones/presentation/widgets/create_milestone_sheet.dart
  - mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart
  - mobile/lib/features/projects/presentation/screens/project_detail_screen.dart
  - mobile/lib/features/projects/presentation/providers/billing_summary_providers.dart
  - mobile/lib/features/projects/presentation/widgets/billing_summary_card.dart
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-04
  - BILL-05

must_haves:
  truths:
    - "GC can see Create Quote and Generate Invoice buttons on TradeScopeDetailScreen"
    - "GC can view and manage billing milestones on TradeScopeDetailScreen"
    - "GC can see project-level quote and invoice summaries on ProjectDetailScreen"
    - "Milestone list shows is_invoiced badge and disables Generate Invoice for already-invoiced milestones"
  artifacts:
    - path: "mobile/lib/features/billing_milestones/presentation/providers/billing_milestone_providers.dart"
      provides: "Riverpod providers for milestone CRUD"
      contains: "billingMilestonesByScope"
    - path: "mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart"
      provides: "Extended screen with billing sections"
      contains: "Create Quote"
    - path: "mobile/lib/features/projects/presentation/screens/project_detail_screen.dart"
      provides: "Extended screen with aggregation summaries"
      contains: "quote-summary"
    - path: "mobile/lib/features/projects/presentation/widgets/billing_summary_card.dart"
      provides: "Project-level billing aggregation widget"
      contains: "BillingSummaryCard"
  key_links:
    - from: "mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart"
      to: "mobile/lib/features/billing_milestones/presentation/providers/billing_milestone_providers.dart"
      via: "ref.watch(billingMilestonesByScope)"
      pattern: "billingMilestonesByScope"
    - from: "mobile/lib/features/projects/presentation/screens/project_detail_screen.dart"
      to: "mobile/lib/features/projects/presentation/providers/billing_summary_providers.dart"
      via: "ref.watch(projectQuoteSummary/projectInvoiceSummary)"
      pattern: "projectQuoteSummary|projectInvoiceSummary"
---

<objective>
Mobile UI: billing sections on TradeScopeDetailScreen (quote/invoice/milestone actions) and project-level aggregation summaries on ProjectDetailScreen.

Purpose: Delivers the GC-facing mobile experience for trade-scoped billing — creating quotes per trade, generating invoices from completed work, managing milestones for progress billing, and viewing project-level financial summaries.

Output: Extended TradeScopeDetailScreen with billing actions, MilestoneListCard, BillingSummaryCard on ProjectDetailScreen, Riverpod providers.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-per-trade-billing/25-CONTEXT.md
@.planning/phases/25-per-trade-billing/25-RESEARCH.md
@.planning/phases/25-per-trade-billing/25-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->

From mobile/lib/features/billing_milestones/data/billing_milestone_dao.dart (created in Plan 02):
```dart
class BillingMilestoneDao extends DatabaseAccessor<AppDatabase> with _$BillingMilestoneDaoMixin {
  Stream<List<BillingMilestoneEntity>> watchByScope(String scopeId)
  Future<String> createMilestone(BillingMilestoneEntity entity)
  Future<void> updateMilestone(BillingMilestoneEntity entity)
  Future<void> deleteMilestone(String id)
}
```

From mobile/lib/features/billing_milestones/domain/billing_milestone_entity.dart (created in Plan 02):
```dart
class BillingMilestoneEntity {
  final String id, companyId, tradeScopeId, name;
  final double percentage;
  final String? description;
  final bool isInvoiced;
  final int sortOrder, version;
  final DateTime createdAt, updatedAt;
  final DateTime? deletedAt;
}
```

From mobile/lib/features/invoices/data/invoice_dao.dart (extended in Plan 02):
```dart
Stream<List<InvoiceEntity>> watchInvoicesForScope(String scopeId)
```

From mobile/lib/features/quotes/data/quote_dao.dart (extended in Plan 02):
```dart
Stream<List<QuoteEntity>> watchQuotesForScope(String scopeId)
```

From mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart (existing):
```dart
class TradeScopeDetailScreen extends ConsumerWidget {
  final String scopeId;
  final String projectId;
  Widget build(BuildContext context, WidgetRef ref)
  // Currently shows: scope header, task list, punch items
}
```

From mobile/lib/features/projects/presentation/screens/project_detail_screen.dart (existing):
```dart
class ProjectDetailScreen extends ConsumerWidget {
  final String projectId;
  // Currently shows: project info, trade scope cards
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Riverpod providers for billing milestones and aggregation summaries</name>
  <files>
    mobile/lib/features/billing_milestones/presentation/providers/billing_milestone_providers.dart,
    mobile/lib/features/projects/presentation/providers/billing_summary_providers.dart
  </files>
  <read_first>
    mobile/lib/features/billing_milestones/data/billing_milestone_dao.dart,
    mobile/lib/features/invoices/data/invoice_dao.dart,
    mobile/lib/features/quotes/data/quote_dao.dart,
    mobile/lib/features/projects/presentation/providers/project_providers.dart,
    mobile/lib/core/database/app_database.dart
  </read_first>
  <action>
    1. Create `mobile/lib/features/billing_milestones/presentation/providers/billing_milestone_providers.dart`:
       - `billingMilestonesByScope` — StreamProvider.autoDispose.family<List<BillingMilestoneEntity>, String>(scopeId) that watches `db.billingMilestoneDao.watchByScope(scopeId)` where db comes from GetIt or a ref.watch(databaseProvider)
       - `createMilestoneAction` — a provider or method that calls `billingMilestoneDao.createMilestone(entity)`
       - `deleteMilestoneAction` — calls `billingMilestoneDao.deleteMilestone(id)`

    2. Create `mobile/lib/features/projects/presentation/providers/billing_summary_providers.dart`:
       - `scopeQuotesProvider` — StreamProvider.autoDispose.family<List<QuoteEntity>, String>(scopeId) that watches `db.quoteDao.watchQuotesForScope(scopeId)`
       - `scopeInvoicesProvider` — StreamProvider.autoDispose.family<List<InvoiceEntity>, String>(scopeId) that watches `db.invoiceDao.watchInvoicesForScope(scopeId)`
       - `projectQuoteSummaryProvider` — FutureProvider.autoDispose.family<Map<String, dynamic>, String>(projectId) that fetches from backend GET /api/projects/{projectId}/quote-summary via DioClient
       - `projectInvoiceSummaryProvider` — FutureProvider.autoDispose.family<Map<String, dynamic>, String>(projectId) that fetches from backend GET /api/projects/{projectId}/invoice-summary via DioClient
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/billing_milestones/presentation/providers/ lib/features/projects/presentation/providers/billing_summary_providers.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "billingMilestonesByScope" mobile/lib/features/billing_milestones/presentation/providers/billing_milestone_providers.dart` succeeds
    - `grep -q "scopeQuotesProvider" mobile/lib/features/projects/presentation/providers/billing_summary_providers.dart` succeeds
    - `grep -q "projectQuoteSummaryProvider" mobile/lib/features/projects/presentation/providers/billing_summary_providers.dart` succeeds
    - `grep -q "projectInvoiceSummaryProvider" mobile/lib/features/projects/presentation/providers/billing_summary_providers.dart` succeeds
    - `dart analyze` reports no errors on provider files
  </acceptance_criteria>
  <done>Riverpod providers for milestone CRUD, scope-level quote/invoice streams, and project-level aggregation summaries. All providers follow autoDispose.family pattern.</done>
</task>

<task type="auto">
  <name>Task 2: UI — TradeScopeDetailScreen billing section and ProjectDetailScreen summary</name>
  <files>
    mobile/lib/features/billing_milestones/presentation/widgets/milestone_list_card.dart,
    mobile/lib/features/billing_milestones/presentation/widgets/create_milestone_sheet.dart,
    mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart,
    mobile/lib/features/projects/presentation/screens/project_detail_screen.dart,
    mobile/lib/features/projects/presentation/widgets/billing_summary_card.dart
  </files>
  <read_first>
    mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart,
    mobile/lib/features/projects/presentation/screens/project_detail_screen.dart,
    mobile/lib/features/billing_milestones/presentation/providers/billing_milestone_providers.dart,
    mobile/lib/features/projects/presentation/providers/billing_summary_providers.dart,
    mobile/lib/features/invoices/presentation/screens/invoice_detail_screen.dart,
    mobile/lib/features/quotes/presentation/screens/quote_detail_screen.dart
  </read_first>
  <action>
    1. Create `mobile/lib/features/billing_milestones/presentation/widgets/milestone_list_card.dart`:
       - `MilestoneListCard` — ConsumerWidget taking `scopeId`
       - Watches `billingMilestonesByScope(scopeId)`
       - Renders Card with "Billing Milestones" header + "Add" IconButton
       - Each milestone row shows: name, percentage badge, description (if any), "Invoiced" chip if isInvoiced=true
       - Non-invoiced milestones show "Generate Invoice" TextButton that calls POST /api/trade-scopes/{scopeId}/invoices/progress with milestone_id via DioClient; on success show SnackBar "Progress invoice created"; on 409 show "Already invoiced"
       - Swipe-to-delete on non-invoiced milestones (calls deleteMilestone)
       - "Add" button opens CreateMilestoneSheet

    2. Create `mobile/lib/features/billing_milestones/presentation/widgets/create_milestone_sheet.dart`:
       - `CreateMilestoneSheet` — StatefulWidget modal bottom sheet
       - Form fields: name (TextFormField, required), percentage (TextFormField, numeric, 0-100 validation), description (TextFormField, optional)
       - Submit: creates BillingMilestoneEntity with generated UUID, calls createMilestoneAction
       - Validation: name not empty, percentage > 0 and <= 100
       - Close sheet on success

    3. Update `mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart`:
       - Add billing section after existing task list / punch items (visible only for admin/GC role)
       - Section header "Billing" with divider
       - Row of action buttons: "Create Quote" (navigates to quote creation with trade_scope_id), "Generate Invoice" (POST to /api/trade-scopes/{scopeId}/invoices/generate; on success navigate to invoice detail)
       - Below buttons: show MilestoneListCard(scopeId: scopeId)
       - Below milestones: show list of existing scope quotes (from scopeQuotesProvider) as tappable rows navigating to quote detail
       - Show list of existing scope invoices (from scopeInvoicesProvider) as tappable rows navigating to invoice detail
       - Per D-02: "Create Quote" pre-populates with trade scope name in the line item editor
       - Per D-05: "Generate Invoice" only visible when scope has completed tasks

    4. Create `mobile/lib/features/projects/presentation/widgets/billing_summary_card.dart`:
       - `BillingSummaryCard` — ConsumerWidget taking `projectId` and `type` enum (quote or invoice)
       - For quote summary: watches projectQuoteSummaryProvider(projectId), renders Card with "Quote Summary" title, per-trade breakdown table (trade name, quote count, subtotal), grand total row
       - For invoice summary: watches projectInvoiceSummaryProvider(projectId), renders Card with "Invoice Summary" title, per-trade breakdown (trade name, billed, paid, outstanding), grand total row with color coding (outstanding in red if > 0)
       - Loading state: shimmer/skeleton
       - Error state: error message with retry button

    5. Update `mobile/lib/features/projects/presentation/screens/project_detail_screen.dart`:
       - Add BillingSummaryCard(projectId: projectId, type: BillingSummaryType.quote) and BillingSummaryCard(projectId: projectId, type: BillingSummaryType.invoice) as new sections below trade scope cards
       - Per D-09: accessible as new sections below existing content (not tabs)
       - Only visible to admin/GC role
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/billing_milestones/presentation/ lib/features/projects/presentation/screens/trade_scope_detail_screen.dart lib/features/projects/presentation/screens/project_detail_screen.dart lib/features/projects/presentation/widgets/billing_summary_card.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Create Quote" mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart` succeeds
    - `grep -q "Generate Invoice" mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart` succeeds
    - `grep -q "MilestoneListCard" mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart` succeeds
    - `grep -q "BillingSummaryCard" mobile/lib/features/projects/presentation/screens/project_detail_screen.dart` succeeds
    - `grep -q "class MilestoneListCard" mobile/lib/features/billing_milestones/presentation/widgets/milestone_list_card.dart` succeeds
    - `grep -q "class CreateMilestoneSheet" mobile/lib/features/billing_milestones/presentation/widgets/create_milestone_sheet.dart` succeeds
    - `grep -q "class BillingSummaryCard" mobile/lib/features/projects/presentation/widgets/billing_summary_card.dart` succeeds
    - `dart analyze` reports no errors
  </acceptance_criteria>
  <done>TradeScopeDetailScreen has Create Quote, Generate Invoice, milestone management. ProjectDetailScreen has quote and invoice summary cards with per-trade breakdowns. All widgets follow ConsumerWidget pattern with proper loading/error states.</done>
</task>

</tasks>

<verification>
- `dart analyze` passes on all new and modified files
- TradeScopeDetailScreen has billing section with quote/invoice/milestone controls
- ProjectDetailScreen has aggregation summary cards
- Providers connect DAOs to UI reactively
</verification>

<success_criteria>
- GC sees Create Quote and Generate Invoice buttons on trade scope detail (admin/GC only)
- GC can add/view/delete billing milestones per trade scope
- Non-invoiced milestones show Generate Invoice button; invoiced show badge
- ProjectDetailScreen shows per-trade quote and invoice summaries
- All UI follows existing widget patterns (ConsumerWidget, loading/error states)
- dart analyze passes
</success_criteria>

<output>
After completion, create `.planning/phases/25-per-trade-billing/25-04-SUMMARY.md`
</output>
