---
phase: 29-client-contracts-and-esignature
plan: "03"
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - web/src/types/api.ts
  - web/src/lib/api/contracts.ts
  - web/src/app/(dashboard)/settings/company/page.tsx
  - web/src/app/(dashboard)/settings/company/_components/contract-terms-editor.tsx
  - web/src/app/(dashboard)/quotes/[id]/_components/quote-contract-card.tsx
  - web/src/app/(dashboard)/contracts/page.tsx
  - web/src/app/(dashboard)/contracts/[id]/page.tsx
  - web/src/components/layout/sidebar.tsx
autonomous: true
requirements: [CONTRACT-UI-01, CONTRACT-UI-02]

must_haves:
  truths:
    - "Owner/admin can set the CSLB license number and edit the contract-terms template (with an attorney-review banner + merge-field help)"
    - "From an approved quote, admin can Create Contract then Send for Signature and see live status"
    - "A Contracts list + detail shows status and downloads the signed PDF once signed"
    - "Sidebar exposes Contracts (permission-gated)"
  artifacts:
    - path: "web/src/app/(dashboard)/settings/company/_components/contract-terms-editor.tsx"
      provides: "Terms template editor with review banner + merge-field reference"
      exports: ["ContractTermsEditor"]
    - path: "web/src/app/(dashboard)/quotes/[id]/_components/quote-contract-card.tsx"
      provides: "Create/Send contract actions + status on the quote detail"
      exports: ["QuoteContractCard"]
  key_links:
    - from: "web/src/app/(dashboard)/quotes/[id]/_components/quote-contract-card.tsx"
      to: "backend POST /api/v1/contracts + /contracts/{id}/send"
      via: "generate then send for signature"
      pattern: "contracts"
---

<objective>
Web admin surfaces for contracts: company settings (CSLB license # + editable terms template with
the attorney-review banner), quote → Create/Send-contract actions with live signing status, and a
Contracts list/detail with signed-PDF download. Consumes 29-02's API.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-client-contracts-and-esignature/29-CONTEXT.md
@web/src/lib/api-client.ts
@web/src/lib/hooks/usePermissions.ts
@web/src/app/(dashboard)/quotes/[id]/_hooks/use-quote-detail.ts
@web/src/components/layout/sidebar.tsx
@web/src/lib/roles.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Types + contracts API hooks</name>
  <action>
`types/api.ts`: add `Contract`, `ContractStatus`, `ContractTemplate` interfaces mirroring the
backend. `lib/api/contracts.ts` (react-query, via apiGet/apiPost/apiPut): `useContracts`,
`useContract(id)`, `useContractForQuote(quoteId)`, `useGenerateContract`, `useSendContract`,
`useContractTemplate`, `useUpdateContractTemplate`, `useCompanySettings`/`useUpdateCompany`
(license_number). Signed-PDF download links point at `/api/v1/contracts/{id}/signed.pdf` through
the proxy (or a direct /files link once available).
  </action>
  <verify><automated>cd web && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - grep -q "useSendContract" web/src/lib/api/contracts.ts
    - grep -q "ContractStatus" web/src/types/api.ts
  </acceptance_criteria>
  <done>Typed contracts client + company-settings hooks exist; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Company settings — license # + terms editor</name>
  <action>
`settings/company/page.tsx` (gated `can("company.settings.manage")`): a form for `license_number`
+ `<ContractTermsEditor/>`. The editor is a large textarea/rich editor bound to the template
`body`, with: (a) a persistent **amber "Attorney review required — placeholder legal text, not
legal advice"** banner, (b) a merge-field reference list ({{client_name}}, {{company_license_number}},
…), (c) Save (PUT template) + a "Reset to default" action. Follow the Job Ticket design system.
  </action>
  <verify><automated>cd web && npx tsc --noEmit && npx eslint "src/app/(dashboard)/settings/company/_components/contract-terms-editor.tsx"</automated></verify>
  <acceptance_criteria>
    - grep -qi "attorney review" web/src/app/(dashboard)/settings/company/_components/contract-terms-editor.tsx
    - grep -q "license_number" web/src/app/(dashboard)/settings/company/page.tsx
  </acceptance_criteria>
  <done>Owner/admin edit the license # and terms template with a prominent review banner; tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Quote → contract actions + Contracts list/detail + nav</name>
  <action>
1. `quote-contract-card.tsx` on the quote detail (shown when quote status == approved): "Create
   contract" → generate; then "Send for signature" → send; show status chip (draft/sent/viewed/
   signed/declined) with sent/signed timestamps and a "Download signed PDF" button when signed.
   Wire into the quote detail page next to the existing quote-actions card.
2. `contracts/page.tsx` (list, gated on a contracts permission — reuse `quotes.manage` or add a
   dedicated key) + `contracts/[id]/page.tsx` (detail: status timeline, signer, download).
3. `sidebar.tsx`: add a **Contracts** nav item gated on `can("quotes.manage")` (or a new
   `contracts.manage` key if 29-02 adds one).
  </action>
  <verify><automated>cd web && npx tsc --noEmit && npx eslint "src/app/(dashboard)/quotes/[id]/_components/quote-contract-card.tsx" "src/app/(dashboard)/contracts/page.tsx" src/components/layout/sidebar.tsx</automated></verify>
  <acceptance_criteria>
    - grep -q "Send for signature\|useSendContract" web/src/app/(dashboard)/quotes/[id]/_components/quote-contract-card.tsx
    - grep -q "Contracts" web/src/components/layout/sidebar.tsx
  </acceptance_criteria>
  <done>Admin can generate + send a contract from an approved quote and manage/download contracts; nav exposed; tsc + eslint clean.</done>
</task>
</tasks>

<verification>
- `npx tsc --noEmit` + `npx eslint` clean on all changed files.
- Manual: approve a quote → Create → Send → status advances; signed PDF downloads once the webhook lands.
</verification>

<success_criteria>
- Company settings edit license # + terms template (with attorney-review banner).
- Quote → Create/Send contract with live status; Contracts list/detail + signed download; nav gated.
- tsc + eslint clean.
</success_criteria>

<output>After completion, create `.planning/phases/29-client-contracts-and-esignature/29-03-SUMMARY.md`.</output>
