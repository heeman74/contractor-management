---
phase: 29-client-contracts-and-esignature
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/versions/0029_contracts_and_license.py
  - backend/app/features/companies/models.py
  - backend/app/features/contracts/__init__.py
  - backend/app/features/contracts/models.py
  - backend/app/features/contracts/repository.py
  - backend/app/features/contracts/schemas.py
  - backend/app/features/contracts/templates_default.py
  - backend/app/features/contracts/service.py
  - backend/app/features/quotes/service.py
  - backend/app/features/pdf/service.py
  - backend/app/features/pdf/templates/quote.html
  - backend/app/features/pdf/templates/contract.html
  - backend/app/main.py
  - backend/tests/test_contracts_generation.py
  - backend/tests/test_quote_validity.py
autonomous: true
requirements: [CONTRACT-01, CONTRACT-02, QUOTE-VALIDITY-01]

must_haves:
  truths:
    - "Company has a nullable license_number (CSLB) field"
    - "contract_templates + contracts tables exist with RLS; a CA-structured default template is seeded per company"
    - "send_quote defaults expiry_date to today and the quote PDF states 'valid today only'"
    - "The quote PDF shows the real client name (no longer hardcoded None)"
    - "Generating a contract from an approved quote merges terms into a frozen snapshot and produces a persisted unsigned PDF"
    - "contract.html carries signature text-tag anchors for the e-sign provider"
  artifacts:
    - path: "backend/migrations/versions/0029_contracts_and_license.py"
      provides: "license_number + contract_templates + contracts tables, RLS, default-template backfill"
      contains: "contracts"
    - path: "backend/app/features/contracts/service.py"
      provides: "ContractService.generate_from_quote (merge -> render -> persist)"
      exports: ["ContractService"]
    - path: "backend/app/features/pdf/templates/contract.html"
      provides: "Contract PDF template with terms + signature text-tags"
      contains: "sig|req|signer1"
  key_links:
    - from: "backend/app/features/contracts/service.py"
      to: "backend/app/features/pdf/service.py"
      via: "renders contract.html to a persisted PDF via WeasyPrint"
      pattern: "generate_contract_pdf|contract.html"
    - from: "backend/app/features/quotes/service.py"
      to: "backend/app/features/quotes/models.py"
      via: "send_quote sets expiry_date = today (valid today only)"
      pattern: "expiry_date"
---

<objective>
Backend data model + document generation for contracts: the CSLB license field, editable
contract-terms templates (seeded with California-structured placeholders), the contracts table,
the "valid today only" quote statement, a real client name on the quote PDF, and generation of a
persisted unsigned contract PDF (with e-sign signature anchors) from an approved quote.

Purpose: the data + PDF foundation that 29-02 (e-sign) and the frontends consume. No provider
integration yet.
Output: migration 0029, the `contracts` feature module (model/repo/schema/service + default
template), quote-validity change, quote/contract PDF templates, and tests.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-client-contracts-and-esignature/29-CONTEXT.md
@backend/app/features/quotes/models.py
@backend/app/features/quotes/service.py
@backend/app/features/pdf/service.py
@backend/app/features/pdf/templates/quote.html
@backend/app/features/companies/models.py
@backend/app/features/users/models.py
@backend/app/core/base_models.py
@backend/app/core/base_repository.py
@backend/app/core/base_service.py
@backend/migrations/versions/0028_project_assignment_roles.py
@backend/migrations/versions/0001_initial.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0029 + Company.license_number + contract models/repo</name>
  <action>
1. `companies/models.py`: add `license_number: Mapped[str | None] = mapped_column(String, nullable=True)`.
2. Migration `0029_contracts_and_license.py` (down_revision "0028_project_assignment_roles"):
   - `ALTER TABLE companies ADD COLUMN license_number TEXT`.
   - CREATE `contract_templates` (id, company_id FK, name TEXT, body TEXT, is_default BOOL,
     version, timestamps, deleted_at) + RLS (enable+force, tenant_isolation policy) — mirror the
     0027 pattern.
   - CREATE `contracts` (all fields from 29-CONTEXT §Data model: quote_id FK quotes ON DELETE
     CASCADE, job_id, client_user_id, template_id, status TEXT CHECK IN
     ('draft','sent','viewed','signed','declined','voided') DEFAULT 'draft', terms_snapshot TEXT,
     validity_statement TEXT, unsigned_pdf_url TEXT, signed_pdf_url TEXT, provider TEXT,
     provider_request_id TEXT, provider_metadata JSONB DEFAULT '{}', signer_name TEXT,
     signer_email TEXT, sent_at/viewed_at/signed_at/declined_at TIMESTAMPTZ, version, timestamps,
     deleted_at) + indexes (company_id, quote_id), (company_id, status) + RLS.
   - Backfill: seed one default `contract_templates` row per existing company from
     `templates_default.DEFAULT_CONTRACT_BODY` (insert before enabling RLS, like 0027's backfill).
3. `contracts/models.py`: `ContractTemplate(TenantScopedModel)` + `Contract(TenantScopedModel)`
   with `lazy="raise"`, matching the migration. `contracts/repository.py`:
   `ContractTemplateRepository` (get_default, upsert) + `ContractRepository` (get_by_quote,
   list_for_company, get_by_request_id).
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=$(grep -m1 '^DATABASE_URL=' .env|cut -d= -f2-) uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run python -c "from app.features.contracts.models import Contract, ContractTemplate; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "license_number" backend/app/features/companies/models.py
    - grep -q "contracts" backend/migrations/versions/0029_contracts_and_license.py
    - grep -q "class Contract" backend/app/features/contracts/models.py
  </acceptance_criteria>
  <done>Migration 0029 creates the license column + both tables with RLS and seeds default templates; models + repos import and round-trip.</done>
</task>

<task type="auto">
  <name>Task 2: Default CA-structured terms template</name>
  <action>
Create `contracts/templates_default.py` with `DEFAULT_CONTRACT_BODY` — an HTML/markdown string
containing the California home-improvement sections from 29-CONTEXT (contractor name/license/
address, work description, start/completion dates, total price + payment schedule, down-payment
limit notice, three-day right-to-cancel, mechanics'-lien warning, change-order/arbitration).
Every section body is placeholder text prefixed with `[ATTORNEY REVIEW REQUIRED]` and uses the
merge-field placeholders (`{{company_license_number}}`, `{{client_name}}`, etc.). Add
`DEFAULT_TEMPLATE_NAME = "California Home Improvement Contract (draft)"`.
  </action>
  <acceptance_criteria>
    - grep -q "ATTORNEY REVIEW REQUIRED" backend/app/features/contracts/templates_default.py
    - grep -q "right to cancel" backend/app/features/contracts/templates_default.py
    - grep -q "license" backend/app/features/contracts/templates_default.py
  </acceptance_criteria>
  <done>Default template ships CA-structured placeholder sections with merge fields and review markers.</done>
</task>

<task type="auto">
  <name>Task 3: Quote "valid today only" + client name on quote PDF</name>
  <read_first>backend/app/features/quotes/service.py, backend/app/features/pdf/service.py, backend/app/features/pdf/templates/quote.html</read_first>
  <action>
1. `quotes/service.py` `send_quote`: if `quote.expiry_date is None`, set it to
   `datetime.now(UTC).date()` (valid today only). Keep the existing status/sent_at logic.
2. `pdf/service.py` `generate_quote_pdf` (~lines 134-139): populate the client context from the
   quote's job → client User (name, address) instead of hardcoded `None`. Add a
   `validity_statement` context value: "This quote is valid only for the day it was issued
   ({{expiry_date}}). Prices are not guaranteed after this date."
3. `pdf/templates/quote.html`: render the validity statement prominently near the totals, and
   show the wired client name in the "Prepared For" block.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import app.features.pdf.service, app.features.quotes.service; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "expiry_date" backend/app/features/quotes/service.py
    - grep -qi "valid" backend/app/features/pdf/templates/quote.html
  </acceptance_criteria>
  <done>Sent quotes default to same-day expiry and the PDF states validity + shows the real client name.</done>
</task>

<task type="auto">
  <name>Task 4: contract.html template + ContractService.generate_from_quote</name>
  <read_first>backend/app/features/pdf/service.py, backend/app/features/pdf/templates/quote.html, backend/app/features/quotes/models.py</read_first>
  <action>
1. `pdf/templates/contract.html`: company header (name, address, **license #**), client block,
   project/scope + line-items summary (from the quote), the merged `terms_snapshot` (rendered as
   safe HTML), the validity statement, and a **signature block with provider text-tag anchors**
   (`[sig|req|signer1]`, `[date|req|signer1]`, and a printed-name line). Keep WeasyPrint-safe CSS.
2. `pdf/service.py`: add `generate_contract_pdf(context) -> bytes` rendering contract.html
   (mirror `generate_quote_pdf`).
3. `contracts/schemas.py`: `ContractResponse`, `ContractTemplateResponse`,
   `ContractTemplateUpdate`, `GenerateContractRequest {quote_id}`.
4. `contracts/service.py` `ContractService(TenantScopedService)`:
   - `generate_from_quote(quote_id)`: load the quote (must be `approved`; else 409) + its job +
     client + company + default template; resolve merge fields → `terms_snapshot`; build the
     validity statement; render contract PDF; persist to
     `uploads/contracts/{company_id}/{contract_id}/unsigned.pdf`; create the `Contract` row
     (status `draft`, unsigned_pdf_url, signer_name/email from client). Return ContractResponse.
   - `get_template()` / `update_template(body)` for the editor (29-03).
5. `main.py`: register a `contracts_router` placeholder (endpoints land in 29-02) — or defer
   router to 29-02. Generation is exercised via the service in tests here.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.features.contracts.service import ContractService; from app.features.pdf.service import pdf_service; print(hasattr(pdf_service,'generate_contract_pdf'))"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "sig|req|signer1" backend/app/features/pdf/templates/contract.html
    - grep -q "def generate_from_quote" backend/app/features/contracts/service.py
    - grep -q "terms_snapshot" backend/app/features/contracts/service.py
  </acceptance_criteria>
  <done>A California-structured contract PDF is generated from an approved quote, terms frozen into a snapshot, and persisted with signature anchors.</done>
</task>

<task type="auto">
  <name>Task 5: Tests</name>
  <action>
1. `tests/test_quote_validity.py`: send a draft quote → expiry_date == today; approving the same
   quote after simulating a past date → 409/expired (reuse approve_quote's expiry path). Quote
   PDF bytes render without error and are non-empty.
2. `tests/test_contracts_generation.py`: create job→quote→approve; `generate_from_quote` returns
   a Contract with status `draft`, non-null `unsigned_pdf_url`, a `terms_snapshot` containing the
   merged license number + client name; generating from a non-approved quote → 409; template
   default is seeded for a new company.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_quote_validity.py tests/test_contracts_generation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "generate_from_quote" backend/tests/test_contracts_generation.py
  </acceptance_criteria>
  <done>Validity + contract-generation flows are covered and green.</done>
</task>

</tasks>

<verification>
- `alembic upgrade/downgrade/upgrade` round-trips 0029.
- `pytest tests/test_quote_validity.py tests/test_contracts_generation.py` green.
- `ruff check app tests` + `ruff format --check` clean; `from app.main import app` builds.
- conftest truncate list updated for `contracts` + `contract_templates` (FK to companies).
</verification>

<success_criteria>
- Company.license_number exists; contract_templates + contracts tables with RLS + seeded CA default.
- Sent quotes are "valid today only" with a PDF statement; quote PDF shows the real client name.
- generate_from_quote produces a persisted, CA-structured unsigned contract PDF with signature anchors and a frozen terms snapshot.
- Tests green; ruff clean.
</success_criteria>

<output>After completion, create `.planning/phases/29-client-contracts-and-esignature/29-01-SUMMARY.md`.</output>
