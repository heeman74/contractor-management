---
phase: 29-client-contracts-and-esignature
plan: "05"
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - mobile/lib/features/contracts/data/contract_repository.dart
  - mobile/lib/features/contracts/domain/contract.dart
  - mobile/lib/features/contracts/presentation/providers/contract_providers.dart
  - mobile/lib/features/contracts/presentation/screens/contract_sign_screen.dart
  - mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart
  - mobile/lib/core/routing/app_router.dart
  - mobile/pubspec.yaml
  - mobile/test/unit/features/contracts/contract_repository_test.dart
autonomous: true
requirements: [ESIGN-MOBILE-CLIENT-01]

must_haves:
  truths:
    - "A client sees a contract card (status) on the job detail once a contract is sent"
    - "Review & Sign opens the provider's embedded ceremony in a WebView using the sign_url"
    - "After signing, status refreshes to signed and the signed PDF is viewable/downloadable"
  artifacts:
    - path: "mobile/lib/features/contracts/presentation/screens/contract_sign_screen.dart"
      provides: "WebView-hosted embedded signing screen"
    - path: "mobile/lib/features/contracts/data/contract_repository.dart"
      provides: "fetch contract, sign-url, signed-pdf via Dio"
---

<objective>
Mobile client signing: surface a contract on the client job detail, open the provider's embedded
ceremony in a WebView (authenticated `sign-url`), and reflect signed status + signed-PDF access.
Consumes 29-02's API. (Follows the flutter layered-architecture skill.)
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-client-contracts-and-esignature/29-CONTEXT.md
@mobile/lib/features/quotes/presentation/screens/quote_detail_screen.dart
@mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart
@mobile/lib/core/network/dio_client.dart
@mobile/lib/core/routing/app_router.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Domain + repository + providers</name>
  <action>
1. `domain/contract.dart`: immutable `Contract` (id, quoteId, status enum draft/sent/viewed/
   signed/declined/voided, sentAt, signedAt, signedPdfUrl) + `fromJson`.
2. `data/contract_repository.dart` (Dio): `getContractForJob(jobId)` /
   `getSignUrl(contractId)` (GET /contracts/{id}/sign-url) / `signedPdfUrl(contractId)` (resolve
   via mediaHost helper). Type-safe parsing (no bare `as`), DioException handling per CLAUDE.md.
3. `presentation/providers/contract_providers.dart`: Riverpod provider for the current job's
   contract + a signUrl future provider.
  </action>
  <verify><automated>cd mobile && dart analyze lib/features/contracts</automated></verify>
  <acceptance_criteria>
    - grep -q "getSignUrl" mobile/lib/features/contracts/data/contract_repository.dart
    - grep -q "enum ContractStatus\|status" mobile/lib/features/contracts/domain/contract.dart
  </acceptance_criteria>
  <done>Contract domain/repository/providers parse the API and expose the sign URL; analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Sign screen (WebView) + client job-detail card + route</name>
  <action>
1. Add `webview_flutter` to `pubspec.yaml` (if absent).
2. `screens/contract_sign_screen.dart`: loads the embedded `sign_url` in a WebView; detects the
   provider's completion (URL/JS callback) → pops with success → refresh status. Handle load
   errors + a signed/expired state.
3. `client_job_detail_screen.dart`: add a **Contract** card near the existing Quote tab — shows
   status; when `sent/viewed`, a "Review & Sign" button → contract_sign_screen; when `signed`, a
   "View signed contract" action (open signedPdfUrl).
4. `app_router.dart`: register the contract-sign route (client-gated).
  </action>
  <verify><automated>cd mobile && dart analyze lib/features/contracts lib/features/client/presentation/screens/client_job_detail_screen.dart</automated></verify>
  <acceptance_criteria>
    - grep -q "WebView" mobile/lib/features/contracts/presentation/screens/contract_sign_screen.dart
    - grep -q "Review & Sign\|contract" mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart
  </acceptance_criteria>
  <done>Client can open the embedded ceremony in a WebView from the job detail and see signed status; analyze clean.</done>
</task>

<task type="auto">
  <name>Task 3: Repository unit test</name>
  <action>
`test/unit/features/contracts/contract_repository_test.dart` with MockDio: getContractForJob
parses status/dates; getSignUrl returns the URL; malformed payloads throw FormatException (no
silent bad casts).
  </action>
  <verify><automated>cd mobile && flutter test test/unit/features/contracts/contract_repository_test.dart</automated></verify>
  <acceptance_criteria>
    - grep -q "getSignUrl\|getContractForJob" mobile/test/unit/features/contracts/contract_repository_test.dart
  </acceptance_criteria>
  <done>Repository parsing + sign-url covered; green.</done>
</task>
</tasks>

<verification>
- `dart analyze lib/features/contracts …` clean; `flutter test` for the repository green.
- Manual: as a client, open a job with a sent contract → Review & Sign → complete → status signed.
</verification>

<success_criteria>
- Client job detail shows a contract card; Review & Sign hosts the embedded ceremony in a WebView; signed status + signed PDF accessible.
- analyze clean; repository test green.
</success_criteria>

<output>After completion, create `.planning/phases/29-client-contracts-and-esignature/29-05-SUMMARY.md`.</output>
