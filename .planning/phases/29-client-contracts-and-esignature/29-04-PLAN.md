---
phase: 29-client-contracts-and-esignature
plan: "04"
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - web/src/app/sign/[token]/page.tsx
  - web/src/app/sign/[token]/_components/embedded-signer.tsx
  - web/src/app/api/public-contract/[token]/route.ts
autonomous: true
requirements: [ESIGN-WEB-CLIENT-01]

must_haves:
  truths:
    - "A public, unauthenticated /sign/[token] page shows the contract + terms + quote summary"
    - "The client signs via the provider's embedded ceremony hosted on that page"
    - "On completion the page reflects a signed state; the token is the only capability (no login)"
  artifacts:
    - path: "web/src/app/sign/[token]/page.tsx"
      provides: "Public tokenized contract-sign page"
    - path: "web/src/app/sign/[token]/_components/embedded-signer.tsx"
      provides: "Dropbox Sign embedded ceremony host"
---

<objective>
The emailed magic-link destination: a public, token-scoped web page where a client reviews the
contract and completes the embedded e-signature ceremony — no ContractorHub login. Consumes the
tokenized public endpoint from 29-02.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-client-contracts-and-esignature/29-CONTEXT.md
@web/src/app/api/proxy/route.ts
@web/src/app/(auth)/login/page.tsx
</context>

<interfaces>
- Backend: `GET /api/v1/public/contracts/{token}` → { contract summary, terms_snapshot, quote
  totals, sign_url }. Dropbox Sign embedded is loaded via their hellosign-embedded JS client with
  the client_id + the per-signer sign_url; on `sign` event, mark complete.
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Public route handler (token → backend, no cookie)</name>
  <action>
`app/api/public-contract/[token]/route.ts`: a Next route handler that forwards to
`GET {FASTAPI}/api/v1/public/contracts/{token}` **without** the access_token cookie (the token is
the capability). Returns the contract view JSON. This keeps the public page off the authenticated
`/api/proxy` (which requires the cookie).
  </action>
  <verify><automated>cd web && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - grep -q "public/contracts" web/src/app/api/public-contract/[token]/route.ts
  </acceptance_criteria>
  <done>A cookie-free public route proxies the tokenized contract view.</done>
</task>

<task type="auto">
  <name>Task 2: /sign/[token] page + embedded signer</name>
  <action>
1. `app/sign/[token]/page.tsx`: a standalone (outside the (dashboard) group) public page. Fetches
   the contract view via the Task-1 route; renders company/client header, the quote summary, the
   `terms_snapshot` (sanitized HTML), the validity statement, and `<EmbeddedSigner sign_url=…/>`.
   Handle invalid/expired token (friendly "link expired — contact your contractor") and an
   already-signed state (show "Signed on <date>" + a download link if provided).
2. `_components/embedded-signer.tsx`: loads the Dropbox Sign embedded client (self-hosted script,
   CSP-safe) with `clientId` + `sign_url`; opens the ceremony; on the `sign` event shows a success
   state and re-fetches status. Keep it provider-encapsulated so a DocuSign swap is localized.
   Respect reduced-motion and mobile widths.
  </action>
  <verify><automated>cd web && npx tsc --noEmit && npx eslint "src/app/sign/[token]/page.tsx" "src/app/sign/[token]/_components/embedded-signer.tsx"</automated></verify>
  <acceptance_criteria>
    - grep -q "EmbeddedSigner\|sign_url" web/src/app/sign/[token]/_components/embedded-signer.tsx
    - grep -q "terms_snapshot\|validity" web/src/app/sign/[token]/page.tsx
  </acceptance_criteria>
  <done>The public page hosts the embedded ceremony and handles expired/already-signed states; tsc + eslint clean.</done>
</task>
</tasks>

<verification>
- `npx tsc --noEmit` + `eslint` clean.
- Manual: open a magic-link with a valid token → review terms → sign → success; expired token → friendly error.
</verification>

<success_criteria>
- Public /sign/[token] page renders the contract + terms + validity and hosts the embedded signature ceremony with no login.
- Invalid/expired/already-signed states handled; provider embed encapsulated.
</success_criteria>

<output>After completion, create `.planning/phases/29-client-contracts-and-esignature/29-04-SUMMARY.md`.</output>
