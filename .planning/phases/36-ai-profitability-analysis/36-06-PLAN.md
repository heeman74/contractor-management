---
phase: 36-ai-profitability-analysis
plan: 06
type: execute
wave: 3
depends_on: ["36-04"]
files_modified:
  - web/tests/phase-36-ai-findings.spec.ts
autonomous: true
requirements: [FINAI-02]

must_haves:
  truths:
    - "A finance user navigating to /financials/[projectId] sees the finding card with its narrative and suggested action"
    - "A user without finance.view sees the deny panel and the browser issues zero requests to the finding path"
    - "A 500 on the finding path leaves the tiles and all three charts rendered"
  artifacts:
    - path: "web/tests/phase-36-ai-findings.spec.ts"
      provides: "Playwright coverage of the D-08 render and the SC2 zero-request keystone"
      min_lines: 150
  key_links:
    - from: "web/tests/phase-36-ai-findings.spec.ts"
      to: "/financials/[projectId]"
      via: "log in through the UI then SPA-navigate — never page.goto the drill-down"
      pattern: "getByRole"
---

<objective>
Ship the Playwright spec that proves the two user-visible halves of Phase 36's web
surface: a finance user sees the AI finding in context on the project financials page,
and a non-finance user sees the deny panel **while the browser issues zero requests to
the finding endpoint**.

Purpose: the render gate and the fetch gate are two different guards. A spec that only
asserts the deny panel would still pass with `FinanceGate` deleted, and a spec that only
counts requests would pass with the hook's `enabled` deleted. Both halves, or neither
proves anything (the shipped 35-11 lesson).
Output: `web/tests/phase-36-ai-findings.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-ai-profitability-analysis/36-UI-SPEC.md
@.claude/skills/e2e-feature-tests/SKILL.md
@CLAUDE.md

<interfaces>
The shipped Phase 35 spec is the template — it already solves login, permission
seeding, proxy mocking and SPA navigation for exactly this route.

From web/tests/phase-35-financials.spec.ts (read it in full):
- `page.route("**/api/proxy**", ...)` handlers ordered MOST SPECIFIC FIRST
- log in through the login form, then SPA-navigate via the sidebar / projects list —
  a hard `page.goto` resets Redux `isAuthenticated`, disables `usePermissions`, and
  every finance surface is correctly denied, so the test would pass with the gate deleted
- the deny-panel test pairs the panel assertion with a captured zero-financial-request
  counter, with an in-file comment explaining why the panel alone is not enough

Test ids the card exposes (UI-SPEC § Test Hooks):
`profitability-finding`, `-severity`, `-date`, `-narrative`, `-action`,
`-basis-note`, `-labor-note`, `-disclosure`, `-empty`, `-error`, `-skeleton`

The finding endpoint path the hook calls:
`/api/v1/projects/{projectId}/financials/finding`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec scaffold, proxy route table, and the finance-user render test</name>
  <files>web/tests/phase-36-ai-findings.spec.ts</files>
  <read_first>
    - web/tests/phase-35-financials.spec.ts (whole file — copy the login helper, the permission-response mock, the proxy route ordering and the SPA-navigation path to /financials/[projectId])
    - web/tests/phase-34-budgets.spec.ts (the AlertPanel assertions style, for the alert-copy test in Task 2)
    - .claude/skills/e2e-feature-tests/SKILL.md § "Web — Playwright E2E"
    - web/playwright.config.ts
  </read_first>
  <action>
Create `web/tests/phase-36-ai-findings.spec.ts`.

Copy the Phase 35 spec's login + proxy-mock scaffolding. Add a route handler for the
finding path BEFORE the more general `**/financials**` handler (most specific first —
the shipped ordering rule), returning a fixture:
```ts
const FINDING_FIXTURE = {
  id: "…", project_id: PROJECT_ID, severity: "warning",
  narrative: "Cumulative margin has fallen to 12.4% as materials on the plumbing scope ran $3,200 past the approved quote allowance.",
  corrective_action: "Rebill the plumbing change order or renegotiate supplier pricing before drywall starts — $3,200 is currently absorbed.",
  revenue_basis: "mixed", labor_included: true,
  found_on: "2026-07-22", last_confirmed_on: "2026-07-29",
};
```

Test 1 — `finance user sees the AI profitability finding on the project drill-down`:
1. Log in through the UI as a `finance.view` holder.
2. SPA-navigate to `/financials`, then click through to the project row —
   **never `page.goto("/financials/<id>")`**.
3. Assert `getByTestId("profitability-finding")` is visible.
4. Assert the severity chip reads `Margin warning`.
5. Assert the date line reads exactly `Found Jul 22, 2026 · Last confirmed Jul 29, 2026`.
6. Assert the narrative element contains the fixture's `narrative` string (reference the
   fixture constant, never a retyped sentence).
7. Assert the literal eyebrow `SUGGESTED ACTION` is visible and the action element
   contains the fixture's `corrective_action`.
8. Assert the basis note reads `Includes revenue from approved quotes — not yet invoiced.`
   and the labor note reads
   `Unburdened labor: Wage cost only — excludes payroll tax, insurance, overhead.`
9. Assert the disclosure line is present and is the last caption.
10. Assert the finding card sits ABOVE the Margin Trend chart card — compare
    `boundingBox().y` of `profitability-finding` against the trend chart card's.

Test 2 — `a critical finding renders the red chip and unchanged card chrome`: same
navigation with `severity: "critical"`, asserting the chip text `Margin critical` and
that the card root's class list contains no `border-red` and no `bg-red` token.

Test 3 — `no open finding shows the honest empty state`: the finding route returns
`null` and the financials mock reports `margin.incomplete === true`; assert
`profitability-finding-empty` renders the heading `Not analyzed — incomplete cost data`.
Repeat with `incomplete === false` asserting `No margin erosion flagged`.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/phase-36-ai-findings.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web/tests/phase-36-ai-findings.spec.ts` exists
    - `! grep -q 'page.goto("/financials/' web/tests/phase-36-ai-findings.spec.ts`
    - `grep -q "Found Jul 22, 2026 · Last confirmed Jul 29, 2026" <spec>`
    - `grep -q "SUGGESTED ACTION" <spec>`
    - `grep -q "Not analyzed — incomplete cost data" <spec>` and `grep -q "No margin erosion flagged" <spec>`
    - `grep -q "boundingBox" <spec>` (the placement assertion)
    - the narrative/action assertions reference `FINDING_FIXTURE.narrative` / `.corrective_action`, not retyped sentences
    - `npx playwright test tests/phase-36-ai-findings.spec.ts` exits 0
  </acceptance_criteria>
  <done>A finance user's render path is asserted end to end including placement, both empty-state variants and the critical-band chrome rule, with AI prose referenced from the fixture rather than retyped.</done>
</task>

<task type="auto">
  <name>Task 2: The SC2 keystone — deny panel paired with a zero-request counter, and the outage test</name>
  <files>web/tests/phase-36-ai-findings.spec.ts</files>
  <read_first>
    - web/tests/phase-35-financials.spec.ts (the shipped SC3 denial test: the deny panel + captured request counter pairing and the in-file comment explaining why both halves are needed)
    - .planning/phases/36-ai-profitability-analysis/36-UI-SPEC.md § State Matrix rows 1 and 19
  </read_first>
  <action>
Test 4 — `non-finance user sees the deny panel and issues zero finding requests`:
1. Install a request listener BEFORE navigation that counts every request whose URL
   contains `/financials/finding`.
2. Log in through the UI as a user whose `/me/permissions` mock omits `finance.view`.
3. SPA-navigate toward `/financials`.
4. Assert the shipped deny panel is visible AND the finding-request counter is `0`.
5. Add an in-file comment stating both halves and why: a hard `page.goto` resets Redux,
   so the deny panel alone renders for permitted users too and would keep passing with
   `FinanceGate` deleted; the zero-request half is what proves the hook's `enabled` gate.
   Note the break-it-once check: removing `FinanceGate` must fail the panel half and
   removing `enabled` must fail the counter half.

Test 5 — `a findings outage never blanks the money dashboard`:
1. Route the finding path to `fulfill({ status: 500 })` while every other financial
   route stays healthy.
2. SPA-navigate to the drill-down.
3. Assert `profitability-finding-error` shows
   `Couldn't load the profitability finding. Refresh to try again.`
4. Assert in the SAME test that the revenue/cost/margin tiles, the Margin Trend card,
   the Budget vs Actual by Trade Scope card and the Cost Category Mix card are all
   still visible. This is the UI half of the keystone set.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/phase-36-ai-findings.spec.ts && npm run test-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "/financials/finding" <spec>` inside a request listener
    - `grep -qE "toBe\(0\)|toHaveLength\(0\)" <spec>` (the zero-request assertion)
    - `grep -q "profitability-finding-error" <spec>`
    - `grep -q "Couldn't load the profitability finding. Refresh to try again." <spec>`
    - the outage test asserts >= 4 other surfaces still visible in the same `test(...)` block
    - `npx playwright test tests/phase-36-ai-findings.spec.ts` exits 0
    - `npm run test-e2e` exits 0 (no shipped spec regressed)
  </acceptance_criteria>
  <done>The SC2 keystone pairs the deny panel with a zero-request counter and says in-file why both halves are load-bearing; the outage test proves a findings failure leaves the rest of the dashboard intact.</done>
</task>

</tasks>

<verification>
- `cd web && npx playwright test tests/phase-36-ai-findings.spec.ts` — green
- `npm run test-e2e` — full spec suite green
- `npm run lint && npx tsc --noEmit` — clean
</verification>

<success_criteria>
- No `page.goto` at the drill-down route anywhere in the spec
- The denial test asserts BOTH the panel and a zero finding-request count
- The outage test asserts the in-card error AND at least four surviving surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/36-ai-profitability-analysis/36-06-SUMMARY.md`
</output>
