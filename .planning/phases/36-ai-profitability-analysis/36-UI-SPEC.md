---
phase: 36
slug: ai-profitability-analysis
status: draft
shadcn_initialized: true
preset: base-nova (detected via existing web/components.json — not re-run this phase)
created: 2026-07-29
---

# Phase 36 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope note:** Phase 36 is **web-only** and **read-only**. Mobile receives the FCM push text and nothing else (D-08, Phase 34 precedent). It ships **one new card** and **two text contracts**:

1. **`ProfitabilityFindingCard`** on `/financials/[projectId]` — the latest open AI finding: severity band, dates, narrative paragraph, structured corrective action, honest-data captions, AI disclosure (D-08).
2. **`AlertPanel` entry for `ai_profitability`** — no component change; this document locks the two frame strings that wrap the AI text so the alert declares its own provenance.
3. **FCM push title/body/data templates** — locked here, byte-identical body to the dashboard `impact_text` (the Phase 34 rule).

Everything inherits the **"Job Ticket"** design system unchanged from the Phase 30/32/33/34/35 contracts. **Zero new hex values, zero new type sizes, zero new weights, zero new spacing values, zero new npm or shadcn dependencies.**

**Pre-populated from:** 36-CONTEXT.md (D-01..D-10), 36-RESEARCH.md (Patterns 6, 8, 9; Pitfalls 2, 6, 7, 8, 9; Open Question 1), 35-UI-SPEC.md (palette, red rule, chip recipes, empty-state component, caption voice), 34-UI-SPEC.md (alert-copy + FCM template conventions), 33-UI-SPEC.md via shipped constants, ROADMAP Phase 36 SC1–SC3, and direct reads of `project-financials-dashboard.tsx`, `finance-summary-tiles.tsx`, `attention-list.tsx`, `financials-skeleton.tsx`, `financials-format.ts`, `chart-card.tsx`, `chart-empty-state.tsx`, `FinanceFlagChip.tsx`, `MarginSummarySection.tsx`, `CostBreakdownSummary.tsx`, `AlertPanel.tsx`, `hooks.ts`, `types.ts`, `lib/format.ts`, `budget_math.py`, `budget_service.py`, `components.json` (2026-07-29). **User input required: none** — every contract question was answered by a locked decision, a research recommendation this document adopts, or a shipped convention.

**Checker corrections applied 2026-07-29** (1 blocker + 5 recommendations): the `SUGGESTED ACTION` eyebrow moved from `text-gray-500` (3.98:1 on `bg-secondary`, sub-AA at 12px/600 — WCAG's bold exemption starts at 18.66px) to `text-gray-700` (8.50:1); `text-red-800`-on-white corrected to 8.31:1; the `FINANCE_ALERT_CHIP_CLASS` extraction re-described as a class-**set** equivalence with the orphaned `BADGE_BASE_CLASS` deleted in the same edit; the 500/600 weight split annotated; the empty-state honesty variant's coverage bound stated.

**The one thing this contract cannot lock:** the narrative, the corrective action and the alert summary are **AI-written**. Phase 34's alert copy is byte-for-byte template-locked and tested byte-for-byte; **that is impossible here** (36-RESEARCH Pattern 6). This document therefore locks the **frame** — card title, severity labels, eyebrow, date line, captions, disclosure, empty/error copy, alert prefixes, push titles — byte-for-byte, and bounds the AI text by **length and grounding only**. No plan task may assert byte-identity on AI-authored strings.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `web/components.json` present; **no gate action, no new blocks installed**) |
| Preset | `style: base-nova`, `baseColor: neutral`, `iconLibrary: lucide`, `cssVariables: true`, `registries: {}` — read verbatim from `web/components.json` 2026-07-29 |
| Component library | base-ui (`@base-ui/react`) via existing wrappers — this phase uses only `card.tsx` and `skeleton.tsx`. No new primitives, no dialog, no popover, no button. |
| Chart library | **none this phase** — a finding is prose, not a plot |
| Icon library | lucide-react — **`Bot`**, the codebase's established AI-provenance glyph (`ChatBubble.tsx`, `TypingIndicator.tsx`, the AI intake affordances on `projects/page.tsx` and `quotes/page.tsx`). Reusing it means "this text came from the AI" already reads as itself. `Sparkles` is deliberately **not** introduced — a second AI icon would give one meaning two glyphs. |
| Font | Geist Sans (body) per `globals.css`. No display face, no mono — matching every shipped finance surface. |
| Platform | **Web only.** Mobile's channel is the FCM push (D-08). No Flutter widget, no mobile string beyond the push templates below. |

---

## Spacing Scale

Declared values (all multiples of 4) — unchanged from the Phase 32/33/34/35 contracts. This phase authors **no new value** and uses five of them:

| Token | Value | Usage this phase |
|-------|-------|------------------|
| xs | 4px | Title→date gap (`mt-1`), caption stack rhythm (`space-y-1`) |
| sm | 8px | Header row gap (`gap-2`), icon-box↔title gap |
| md | 16px | Card interior top padding (`pt-4` — the shipped `ChartCard`/tile convention), narrative top offset (`mt-4`), action block padding (`p-4`), captions block top offset (`mt-4`) |
| lg | 24px | Page section rhythm (`space-y-6`) — inherited, the card is one child of the drill-down stack |
| xl | 32px | In-card error block vertical padding (`py-8`) |
| 3xl | 64px | In-card empty-state vertical padding (`py-16`) — the shipped `ChartEmptyState`, reused unchanged |

**Exceptions (one, inherited, unchanged):** `py-0.5` — **2px** vertical padding inside the chip recipes (`FINANCE_FLAG_CHIP_CLASS` and its red sibling). This phase reuses those class strings byte-for-byte rather than forking the one chip recipe every finance surface shares. No new sub-4px value is authored.

---

## Typography

**No new size and no new weight.** The project-wide declared set stays **12 / 14 / 20 / 24 / 30** at authored weights **400 and 600** (500 and 700 are inherited — see the weight declaration below). Phase 36 uses **two** sizes — 12 and 14.

| Role | Size | Weight | Line Height | Phase-36 usage |
|------|------|--------|-------------|----------------|
| Caption / meta | 12px (`text-xs`) | 400 · **600 for the eyebrow** | 1.5 | Date line, revenue-basis caption, unburdened-labor caption, AI disclosure line, severity chip label — all 400, all `text-gray-500` on the white card surface. The **`SUGGESTED ACTION` eyebrow** is the **600 (`font-semibold`) + `uppercase tracking-wide` + `text-gray-700`** exception (the shipped `CURRENT RATE` / `CURRENT BUDGET` / table-header convention). It is `text-gray-700`, **not** `text-gray-500`, because it sits on the `bg-secondary` action block — see the contrast note below. |
| Body | 14px (`text-sm`) | 400 | 1.5 | Card title (`text-sm text-gray-500`), narrative paragraph (`text-gray-700`), corrective action (`text-gray-900`), empty-state body |
| Body / emphasis | 14px (`text-sm`) | **500 (`font-medium`)** | 1.5 | Empty-state heading (`text-sm font-medium text-muted-foreground` — the shipped `ChartEmptyState` recipe, reused byte-for-byte) and the in-card error line (`text-sm font-medium text-red-800` — the shipped inline-finance-error weight) |
| Page heading | 20px (`text-xl`) | 400 | 1.2 | **Inherited, not authored here** — the drill-down `<h1>` already exists |

**Weight declaration — read this before writing a jest weight assertion.** The **authored** set is still **400 and 600**: 600 appears exactly once, on the `SUGGESTED ACTION` eyebrow (`font-semibold`). **500 (`font-medium`) is an inherited weight, not a new one** — it arrives with the shipped `ChartEmptyState` and inline-finance-error recipes, which this phase reuses byte-for-byte rather than forking. 700 remains the inherited display weight and appears nowhere on this card. In short: **eyebrow → `font-semibold`; empty-state heading and error line → `font-medium`; everything else → default 400.**

**Eyebrow contrast (locked).** `text-gray-500` (#6b7280) on `bg-secondary` (#ece9e1) measures **3.98:1** — below AA. WCAG's large-text exemption for bold type starts at **18.66px**, so a 12px/600 eyebrow is held to the full **4.5:1** and `text-gray-500` fails there. The eyebrow is therefore **`text-gray-700` (#374151) on `bg-secondary` = 8.50:1 ✓**. This applies to the **eyebrow only**: every other caption on this card sits on the white card surface, where `text-gray-500` measures 4.83:1 and is unchanged.

**Deliberately absent: the 30px KPI figure.** Every other card on this page opens with a big number. The finding card does not, because a finding has no single headline figure — inventing one (a margin percent, a dollar gap) would place an *un-grounded* number in the most prominent slot on the card, which is exactly what SC3 exists to prevent. The severity chip carries the at-a-glance signal instead. This is the same rationale that gave `FinanceSummaryTiles` no icon box and no export button: **borrow the family's typography, not its furniture.**

**Money and percent inside AI text (hard rule):** the narrative, corrective action and alert summary arrive as **already-formatted strings from the backend**, grounded against the payload by the D-05 validator. The client **never** parses, re-formats, re-rounds or re-derives a figure out of them, and **never** runs `formatCurrency` over AI prose. A client-side reformat would break the grounding audit trail (the stored `payload` JSONB proves what the *published* text said) and could silently change a number after validation. `parseFloat` appears nowhere in this phase.

**Date formatting (locked):** `formatFindingDate("2026-07-29") === "Jul 29, 2026"` — implemented in `web/src/features/finance/financials-format.ts` by **splitting the `"YYYY-MM-DD"` string** against that module's existing `MONTH_NAMES` table, sliced to the shipped `ABBREVIATION_LENGTH`. **Never `new Date("2026-07-29")`** — a date-only string shifts a day across timezones (Phase 32-03 lesson, and the reason `monthParts` already exists in that file). `team/_components/rate-history-dialog.tsx` holds a private `formatEffectiveDate` doing the same job; **folding it onto the new helper is explicitly OUT OF SCOPE** — it belongs to whichever phase next touches Team, exactly as 35 held the Reports `EmptyState` boundary.

---

## Color

**Zero new hex values.** Every value below is a Job Ticket CSS token or a Tailwind step already used by `attention-list.tsx` / `status-badge.tsx`.

| Role | Value | Usage this phase |
|------|-------|------------------|
| Dominant (60%) | `--background` `#f7f6f2`, `--foreground` `#0e1726` | Page background, body ink (inherited) |
| Secondary (30%) | `--card` `#ffffff`, `--secondary` `#ece9e1`, `--border` `#e4e1d8` | Card surface, the icon box (`bg-secondary`), **the corrective-action block (`bg-secondary`)**, card hairline |
| Accent (10%) | `--brand` `#f5a623` | **One new usage: the `Margin warning` severity chip**, via the shipped `FINANCE_FLAG_CHIP_CLASS` (`bg-brand/15 text-amber-900`). Nothing else on this card is amber. |
| Destructive | `--destructive` `#d64545` | **Not used this phase.** See the red rule below. |

**Accent reserved for:** checked permission toggles, active nav item + its 4px rail, focus rings, links, primary CTAs, the unrated-hours chip (32), the incomplete-data chip (33), the "Nearing budget" chip (34), the warning-band bullet-bar fill and the active trend-window button (35), and — new this phase — the **`Margin warning` severity chip**. Nothing else. There are **no** amber backgrounds, borders or headings on the finding card.

### The red rule (inherited, size-based)

`#d64545` (`--destructive`) clears AA only at ≥24px text or as a non-text mark. **Every red glyph this phase authors is 12–14px**, so every one of them uses the `red-800` family:

| Red | Value | Applies to | Contrast |
|-----|-------|------------|----------|
| `text-red-800` on `bg-red-100` | `#991b1b` / `#fee2e2` | The **`Margin critical` severity chip** (12px) | ≈6.8:1 ✓ |
| `text-red-800` on white | `#991b1b` | The in-card error line (14px, 500) | **8.31:1** ✓ |
| `text-destructive` | `#d64545` | **Nothing this phase** — no 30px money numeral is authored here | n/a |

### Severity band → visual treatment (locked)

Two bands, adopted from 36-RESEARCH Open Question 1. **The band name is inside the fingerprint (D-06), so these names are load-bearing: renaming a band silently re-alerts every open finding.**

| Band | `DashboardAlert.severity` | Chip label | Chip class | Rationale |
|------|---------------------------|------------|------------|-----------|
| `warning` | `warning` | **`Margin warning`** | `FINANCE_FLAG_CHIP_CLASS` — `rounded-full bg-brand/15 px-2 py-0.5 text-xs text-amber-900` | Amber already means "caution, nothing lost yet" on every shipped finance surface. Same hue, same meaning, same class string. |
| `critical` | `critical` | **`Margin critical`** | `FINANCE_ALERT_CHIP_CLASS` — `rounded-full bg-red-100 px-2 py-0.5 text-xs text-red-800` | The `Over budget` tier badge's exact class set. Money is being lost, or the erosion is severe. |

**Band name = severity value = push title, one name per band.** The 35 rule ("the same condition must never have two names") applies across all four surfaces: the chip, the `aria-label`, the FCM title and the `DashboardAlert.severity` all read from one map.

**Why two bands, not three:** the band lives inside the fingerprint, so band count directly sets the re-alert cadence. Three bands means a number drifting across two boundaries re-pushes twice — PITFALLS #8's "alerts that over-fire get ignored wholesale". Two bands is also the only count that needs no `dashboard_alerts_severity_check` migration (`info|warning|critical` already allows both).

**Band boundaries (backend-owned, named `Decimal` constants — recommendation, tunable in planning; the names and the count above are NOT tunable):** `critical` when the margin is negative (D-03 signal 2 — money is already lost), or when the decline reaches `CRITICAL_DECLINE_POINTS = Decimal("10")`, or when the quote-implied gap reaches `CRITICAL_QUOTE_GAP_POINTS = Decimal("10")` — i.e. double the D-03 trigger. Everything else is `warning`.

**Card chrome never changes with severity.** No red left border, no tinted card background, no severity-colored title. Only the chip changes. A finding card must never be mistakable for the page-level error panel — and a warning finding must not read as "the page is broken".

**Grays:** `text-gray-900` for the corrective action, `text-gray-700` for the narrative and the eyebrow, `text-gray-500` for the card title and every card-surface caption, `text-muted-foreground` for the empty state — matching shipped conventions exactly.

---

## Copywriting Contract

### Standard elements

| Element | Copy |
|---------|------|
| Primary CTA | **None — this phase has no mutation and no navigation affordance.** The finding *is* the payload; the corrective action is prose, not a button, because no feature exists to execute it. Inventing a CTA that only dismisses would be an affordance for the wrong verb. |
| Empty state heading | `No margin erosion flagged` (default) · `Not analyzed — incomplete cost data` (the honesty variant) |
| Empty state body | `A nightly AI review posts a finding here when this project's margin starts to slip.` (default) · `AI skips projects with missing or unrated costs, so a finding can never rest on understated numbers.` (honesty variant) |
| Error state | `Couldn't load the profitability finding. Refresh to try again.` |
| Destructive confirmation | **None — this phase ships no destructive action, no dismiss control and no mutation of any kind.** |

### Fixed strings — the finding card (verbatim; jest and Playwright assert these)

| String | Where |
|--------|-------|
| `Profitability Finding` | Card title (`text-sm text-gray-500`); card `aria-label="Profitability Finding card"` |
| `Margin warning` / `Margin critical` | Severity chip label — one per band, per the map above |
| `Found {date}` | Date line when `lastConfirmedOn === foundOn` (e.g. `Found Jul 29, 2026`) |
| `Found {date} · Last confirmed {date}` | Date line when the finding has been re-confirmed on a later night (e.g. `Found Jul 22, 2026 · Last confirmed Jul 29, 2026`). Separator is space–middle-dot–space, the Phase 33 figure convention. **Both dates render** — a finding silently restated for a week is a *different fact* from one found last night, and the D-06 upsert makes that distinction available for free. |
| `SUGGESTED ACTION` | Eyebrow above the corrective action (12/600 `uppercase tracking-wide text-gray-700` — the `text-gray-700` is load-bearing, see the eyebrow contrast note) — the literal-uppercase eyebrow convention of `CURRENT RATE` / `CURRENT BUDGET` |
| `AI-written from this project's recorded figures — every number is from your data, never an AI estimate.` | Disclosure line, **always rendered**, last in the caption stack (12/400 `text-gray-500`). This is SC3 stated to the user; the D-05 validator is what makes it true rather than a claim. |
| `Unburdened labor: Wage cost only — excludes payroll tax, insurance, overhead.` | Labor caveat caption. **Composed from the two shipped constants** — `` `${UNBURDENED_TITLE}: ${UNBURDENED_BODY}` `` from `CostBreakdownSummary.tsx` (both promoted to `export`, values unchanged) — so PITFALLS #2's caveat can never carry two texts. |
| `Based on approved quotes — not yet invoiced.` | Revenue-basis caption when the finding's `revenueBasis === "quoted"` — **byte-identical to the shipped Phase 33 string**, reached through `finance-summary-tiles.tsx`'s `QUOTED_BASIS_CAPTION` (promoted to `export`, value unchanged) |
| `Includes revenue from approved quotes — not yet invoiced.` | Revenue-basis caption when `revenueBasis === "mixed"`. The shipped mixed caption names an amount (`Includes {amount} from…`); the finding response carries no such amount and **must not fabricate one**, so this is the amount-free sibling in the same voice. |
| `No margin erosion flagged` / `A nightly AI review posts a finding here when this project's margin starts to slip.` | Default empty state (heading / body) |
| `Not analyzed — incomplete cost data` / `AI skips projects with missing or unrated costs, so a finding can never rest on understated numbers.` | Empty state when the project's own `breakdown.margin.incomplete === true`. This is Pitfall 9 made visible: the page says *why* the AI is silent instead of implying all-clear. |
| `Couldn't load the profitability finding. Refresh to try again.` | In-card error line — the shipped `Couldn't load … Refresh to try again.` voice, scoped to the card |

**Why the empty state has two variants:** a single "nothing flagged" line would read as an all-clear on a project the AI never looked at — the exact false comfort D-01 exists to avoid. The variant reads **one shipped boolean field** (`breakdown.margin.incomplete`, already on the page) and states a rule that is unconditionally true (D-01 skips incomplete-data projects). It **re-derives nothing**: it never claims the converse, so it cannot drift from the backend eligibility gate if the other three D-01 clauses change.

**The bound on that variant (stated so a later phase does not mistake it for full D-01 coverage):** only the **incomplete-data** skip clause gets the honesty variant. A project skipped for being **non-active**, having **no revenue source**, or having **no cost data** falls into the plain empty state. That is an accepted bound, not an oversight — each of those three conditions is already legible from the tiles directly above the card (the `StatusBadge` beside the `<h1>`, the `No revenue recorded yet.` caption, a zero cost figure), whereas an incomplete-data skip is invisible without being said. Widening the variant would mean re-deriving the full D-01 gate client-side, which is the drift this contract forbids; if a later phase wants exact skip reasons on screen, the honest route is a skip-reason field on the endpoint, not more client-side inference.

### Fixed strings — the `AlertPanel` entry (`alert_type: "ai_profitability"`)

`AlertPanel.tsx` needs **no code change** (36-RESEARCH Pattern 9): it renders `impact_text` and optional `remediation_text` generically, and its severity map already covers `warning` and `critical`. The contract is therefore entirely in what the backend writes:

| Field | Value |
|-------|-------|
| `impact_text` | **`AI finding — {alert_summary}`** — a locked frame prefix (`AI_FINDING_PREFIX = "AI finding — "`) around the AI-written summary |
| `remediation_text` | **`Suggested action: {corrective_action}`** — locked prefix (`SUGGESTED_ACTION_PREFIX = "Suggested action: "`) around the AI-written action; renders 12px italic `text-gray-500` |
| `severity` | `warning` \| `critical` — the band, identity-mapped |
| `days_behind` | `null` (suppresses the schedule line) |
| `remediation_text` present | always — every published finding carries a corrective action (D-09) |
| `affected_scope_ids` | `[]` — findings are project-level this milestone (36-RESEARCH Open Question 2) |
| `rescheduling_payload` / `rescheduling_accepted` | `null` / `null` — suppresses the Accept/Dismiss buttons, the `Applied` line and the `Dismissed` line |

**Why the `AI finding — ` prefix exists:** Phase 34 renamed the panel header from "AI Alerts" to **`Alerts`** precisely because budget alerts are rule-based, not AI. This phase puts genuinely AI-authored prose into that panel, where the card's disclosure line cannot follow it. The prefix is the only place provenance can live in that channel, and it is a backend-composed frame string — testable byte-for-byte even though what follows is not. The panel header stays **`Alerts`**; its empty state (`No alerts — all projects on track`) and `{N} new` badge are unchanged.

### FCM push (locked)

| Field | `warning` band | `critical` band |
|-------|----------------|-----------------|
| title | **`Margin warning`** | **`Margin critical`** |
| body | the `impact_text` string, **byte-identical** (`AI finding — {alert_summary}`) | same rule |
| data | `{"type": "ai_profitability_finding", "alert_type": "ai_profitability", "alert_id": <uuid str>, "finding_id": <uuid str>, "project_id": <uuid str>, "severity": "warning"}` — every value a `str` (FCM constraint, the `_push_data` precedent) | same shape, `"severity": "critical"` |

Tap behavior: **default notification-open, no in-app routing** — mobile has no findings surface this milestone (deferred). `finding_id` travels so a future mobile screen can deep-link to the audit trail without a second push contract. Push goes only to `finance.view` holders resolved from the **live permission matrix** (D-07) — a UI-invisible contract restated here because keystone test 3 asserts it.

### Length contracts (bounds on AI text — the only assertion available)

| String | Bound | Enforced by |
|--------|-------|-------------|
| `alert_summary` | **≤ 280 characters** | prompt contract **and** a DB `CHECK` — over-length is **rejected, never silently truncated** (a truncated grounded sentence can lose the very figure that justifies it) |
| `corrective_action` | **≤ 280 characters** | same — D-09's "≤280 chars in the alert" applies to this string because it renders in the alert as `remediation_text` |
| `narrative` | **≤ 600 characters** | prompt contract **and** a DB `CHECK`. ~4–5 lines at 14px/1.5 across a full-width card — a paragraph, not an essay. Rendered **verbatim, never truncated, no clamp, no "read more"**. |

The locked prefixes are **additional** to these bounds — the 280 applies to the AI-authored string alone.

**Copy rules honored:** no lorem ipsum; no "No data" / "Something went wrong"; the error line states the recovery step; both empty states name why the card is empty rather than asserting an all-clear; every honesty caption states the *consequence*, not just the fact. Sentence case with terminal periods on captions, bodies and notes; the title, chip labels and eyebrow carry no terminal period — the Phase 32/33/34/35 convention.

---

## Component & Screen Contract

### Placement on `/financials/[projectId]` (locked)

```
FinancialsLayout  ->  FinanceGate
  <div class="space-y-6">
    <Link>← Financials</Link>
    <h1>{project name}</h1> <StatusBadge/>
    <p>Margin, budget and cost detail.</p>

    <FinanceSummaryTiles>                      [Revenue] [Cost] [Margin]

    ProfitabilityFindingCard                   ◀── NEW, full width
                                                   (Bot)

    ChartCard  "Margin Trend"        (TrendingUp)  full width

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      ChartCard  "Budget vs Actual by Trade Scope"  (Gauge)
      ChartCard  "Cost Category Mix"                (PieChart)
```

**Immediately below the summary tiles, above the Margin Trend.** Reading order becomes **figures → interpretation → evidence**: the tiles are the ground truth, the finding interprets them, the trend chart underneath is the evidence the narrative describes. Placing it above the tiles would open a money page with AI prose and push the audited figures below it; placing it lower would bury the one item on the page that names an action.

The card is **full width and always mounted** (finding, empty state, loading or error) so the page never reflows when a finding appears or resolves overnight.

### Card anatomy

```
Card  aria-label="Profitability Finding card"  data-testid="profitability-finding"
  CardContent pt-4
    ── header row ──────────────────────────  flex items-start justify-between gap-2
       left:  [icon box: rounded-md bg-secondary p-2 > Bot h-4 w-4 text-foreground/70]
              <p text-sm text-gray-500>Profitability Finding</p>
       right: [severity chip]                 12px, per the band map

    <p mt-1 text-xs text-gray-500>            Found Jul 22, 2026 · Last confirmed Jul 29, 2026

    <p mt-4 text-sm text-gray-700>            {narrative — verbatim, wraps, never truncated}

    ── action block ────────────────────────  mt-4 rounded-lg bg-secondary p-4
       <p text-xs font-semibold uppercase tracking-wide text-gray-700>SUGGESTED ACTION
       <p mt-1 text-sm text-gray-900>         {correctiveAction — verbatim}

    ── caption stack ───────────────────────  mt-4 space-y-1 text-xs text-gray-500
       [revenue-basis caption]?               quoted | mixed only
       [unburdened-labor caption]?            laborIncluded only
       [AI disclosure line]                   always, last
```

**Why not `ChartCard`:** `ChartCard` mandates a `kpiValue` and an Export-CSV button. A finding has no headline figure (see Typography) and nothing tabular to export — the narrative *is* the artifact. The card therefore borrows `ChartCard`'s **icon-box recipe and title typography verbatim** so it reads as a member of the same family, and nothing else. This is exactly the `FinanceSummaryTiles` precedent.

**Why the action sits in a `bg-secondary` block:** the corrective action is the only actionable sentence on the page and must survive a skim. Weight is bought with the 30% surface token, not with accent color — amber here would collide with the warning band's meaning, and a colored action block would make every critical finding's action look like a second severity signal. The block's darker surface is also why its eyebrow steps to `text-gray-700`.

### Response contract (what the card renders)

`GET /api/v1/projects/{projectId}/financials/finding` → the **latest open finding**, or `null`.

```ts
interface ProfitabilityFinding {
  id: string;
  projectId: string;
  severity: "warning" | "critical";   // the band — chip, aria-label and push title read from one map
  narrative: string;                  // ≤600, full paragraph, rendered verbatim
  correctiveAction: string;           // ≤280, rendered verbatim
  revenueBasis: RevenueBasis;         // the basis the finding was GROUNDED against
  laborIncluded: boolean;             // true when the analyzed payload carried rated labor cost
  foundOn: string;                    // "YYYY-MM-DD"
  lastConfirmedOn: string;            // "YYYY-MM-DD"
}
```

- **`alertSummary` is deliberately absent** from this response — the card does not render it, and a field nothing reads is dead code (CLAUDE.md).
- **`revenueBasis` and `laborIncluded` come from the finding's stored payload, not from today's figures.** A finding written three nights ago must carry *its own* data caveats; reading today's basis off the tiles beside it would caption a stale claim with fresh facts.
- Money and percent stay **strings end-to-end** and never appear as bare fields here — every figure the user sees is inside the grounded prose.

### Hook contract

```ts
// web/src/features/finance/hooks.ts — beside the three shipped financial hooks
export function useProjectProfitabilityFinding(projectId: string) {
  const { can } = usePermissions();
  return useQuery({
    queryKey: ["cost-entries", "financials", "finding", projectId],
    queryFn: () => fetchProjectProfitabilityFinding(projectId),
    enabled: can(FINANCE_VIEW_PERMISSION) && !!projectId,
  });
}
```

- **`enabled` is not optional.** `FinanceGate` stops the render; `enabled` stops the request. Without it an unauthorized visit still issues the call, money-adjacent text crosses the wire, and the SC2 "zero finding requests" assertion passes for the wrong reason. The spec file must carry a comment saying so (the shipped `hooks.ts` docstring precedent).
- Key sits under the shared `["cost-entries", …]` prefix so `invalidateAllCostEntries` refreshes it for free.
- **Own key, own failure surface.** The card's query is **excluded from the page-level loading gate**: `project-financials-dashboard.tsx`'s early return stays `financials.isLoading || trend.isLoading`. A slow or failing finding query must never delay or blank the money dashboard (the shipped 35-10 rule: "two queries, two keys, two failure surfaces" — here, three).

### Files and extractions

| File | Change | Note |
|------|--------|------|
| `financials/[projectId]/_components/profitability-finding-card.tsx` | **new** | Presentational only — takes parsed props, owns no hook |
| `financials/[projectId]/_components/project-financials-dashboard.tsx` | edit | Owns the third hook, mounts the card (the file's own docstring: "the only hook-owning component on `/financials/[projectId]`") |
| `features/finance/hooks.ts`, `api.ts`, `types.ts` | edit | Hook, fetcher, `ProfitabilityFinding` type |
| `features/finance/financials-format.ts` | edit | `formatFindingDate` beside the shipped month formatters — the never-`new Date()` rule stays in one module |
| `features/finance/components/FinanceFlagChip.tsx` | edit | Export **`FINANCE_ALERT_CHIP_CLASS = "rounded-full bg-red-100 px-2 py-0.5 text-xs text-red-800"`** beside the amber recipe |
| `financials/_components/attention-list.tsx` | edit | Import `FINANCE_ALERT_CHIP_CLASS` in place of the private `OVER_BUDGET_BADGE_CLASS`, **and delete the now-orphaned `BADGE_BASE_CLASS` in the same edit** — it has no other consumer, and an unused constant fails `npm run lint --max-warnings 0`. The shipped constant is *composed* (`` `${BADGE_BASE_CLASS} bg-red-100 text-red-800` ``), so the new literal is the same class **set** in a different token order, not a byte-identical string. Phase 35's assertions stay green because they are per-token `toHaveClass` checks, not string identity — the planner should confirm that by running the shipped suite, not by assuming byte equality. Two red tier chips in one feature must not be authored twice (the 33-04 precedent that created `FinanceFlagChip` in the first place). |
| `financials/_components/finance-summary-tiles.tsx` | edit | Promote `QUOTED_BASIS_CAPTION` to `export` — value unchanged |
| `features/finance/components/CostBreakdownSummary.tsx` | edit | Promote `UNBURDENED_TITLE` / `UNBURDENED_BODY` to `export` — values unchanged |
| `financials/_components/financials-skeleton.tsx` | **no change** | The card is not part of the page skeleton — it mounts with its own loading state after the page renders |
| `monitoring/_components/AlertPanel.tsx` | **no change** | Verified by test, not by edit (36-RESEARCH Pattern 9) |

### Implementation shape (clean-code / SRP)

- **The card never fetches**, never branches on permissions, and holds no state. Loading / empty / error / present is decided by the dashboard and passed as props — which is what makes the state matrix below trivial to test.
- **Intention-revealing names:** `ProfitabilityFindingCard`, `useProjectProfitabilityFinding`, `formatFindingDate`, `findingDateLine`, `severityChip`, `revenueBasisCaption`, `emptyStateCopy`. No `data`, `info`, `temp`, `card2`.
- **One map per concept.** `SEVERITY_CHIP: Record<FindingSeverity, {label, className}>` mirrors `attention-list.tsx`'s `TIER_BADGE` — a band's copy and its color can never drift apart. The backend holds the same map once for `severity` + push title.
- **Functions under ~20 lines**, 0–2 args. `findingDateLine`, `revenueBasisCaption` and `emptyStateCopy` are pure, exported, and unit-tested without React.
- **Named constants, no magic values.** Every copy string, class string, prefix, length bound and query-key segment is a named constant — the shipped `INCOMPLETE_CHIP_LABEL` / `FINANCE_FLAG_CHIP_CLASS` pattern.
- **No client-side money math.** `parseFloat` appears nowhere in this phase; there is no geometry to compute.
- **Lint traps:** no reset-on-open `useEffect` (`react-hooks/set-state-in-effect`); no orphaned constant left behind by the chip extraction; both gates run at `--max-warnings 0`.

---

## State Matrix

Test-asserted; the executor implements exactly these.

| # | State | Condition | Rendering |
|---|-------|-----------|-----------|
| 1 | Permissions loading / denied | `FinanceGate` short-circuits | No card. **Zero** requests to `/api/v1/projects/*/financials/finding` — the hook's `enabled` gate, not just the render gate (SC2) |
| 2 | Page loading | `financials.isLoading \|\| trend.isLoading` | `FinancialsSkeleton variant="project"` — **unchanged, no finding block added**. The card is not mounted yet |
| 3 | Finding loading | page rendered, finding query in flight | Card shell renders (icon, title) with two `Skeleton` bars (`h-4 w-full`, `mt-2 h-4 w-2/3`) in the body. **The page does not wait for it** |
| 4 | Finding present — warning band | `severity === "warning"` | `Margin warning` chip in `FINANCE_FLAG_CHIP_CLASS`; card chrome unchanged |
| 5 | Finding present — critical band | `severity === "critical"` | `Margin critical` chip in `FINANCE_ALERT_CHIP_CLASS`; card chrome unchanged — no red border, no tinted card |
| 6 | Narrative + action | any finding | Narrative rendered **verbatim and in full** (wraps, no truncation, no clamp); `SUGGESTED ACTION` eyebrow (`font-semibold text-gray-700`) + action in the `bg-secondary` block |
| 7 | First night | `lastConfirmedOn === foundOn` | Date line `Found {date}` |
| 8 | Re-confirmed on a later night | `lastConfirmedOn > foundOn` | Date line `Found {date} · Last confirmed {date}` — both dates, never just the newer one |
| 9 | Revenue basis `quoted` | finding's `revenueBasis === "quoted"` | Caption `Based on approved quotes — not yet invoiced.` (shipped string) |
| 10 | Revenue basis `mixed` | finding's `revenueBasis === "mixed"` | Caption `Includes revenue from approved quotes — not yet invoiced.` — **no amount is rendered**, because the response carries none and inventing one would violate SC3 |
| 11 | Revenue basis `invoiced` | finding's `revenueBasis === "invoiced"` | No basis caption — there is nothing to qualify |
| 12 | Labor in the analysis | `laborIncluded === true` | Caption `Unburdened labor: Wage cost only — excludes payroll tax, insurance, overhead.` (PITFALLS #2) |
| 13 | No labor in the analysis | `laborIncluded === false` | No labor caption |
| 14 | Disclosure | every finding | `AI-written from this project's recorded figures — every number is from your data, never an AI estimate.` — **always last, never conditional** |
| 15 | No finding, data complete | response `null`, `breakdown.margin.incomplete === false` | `ChartEmptyState`: `No margin erosion flagged` / `A nightly AI review posts a finding here when this project's margin starts to slip.` Also the rendering for a project skipped as non-active, revenue-less or cost-less — see the variant bound above |
| 16 | No finding, incomplete data | response `null`, `breakdown.margin.incomplete === true` | `ChartEmptyState`: `Not analyzed — incomplete cost data` / `AI skips projects with missing or unrated costs, so a finding can never rest on understated numbers.` — never an all-clear (Pitfall 9) |
| 17 | AI dismissed the candidate | nothing persisted (D-02) | Indistinguishable from state 15 — a dismissal is not a finding and has no UI |
| 18 | Finding resolved overnight | the condition cleared (D-06) | Endpoint returns `null` → state 15. **No "resolved" presentation, no history list, no dismiss control** — the card shows the current truth, and resolution is automatic, so a manual affordance would imply a power the user does not have |
| 19 | Finding query error ★ | finding query `isError` | In-card line `Couldn't load the profitability finding. Refresh to try again.` (`py-8 text-center text-sm font-medium text-red-800`, no nested panel chrome). **Tiles, trend, scope bars and category mix all render normally** — the keystone that a findings outage never blanks the money dashboard |
| 20 | Project not found | page-level 404 | The drill-down's `Project not found.` panel replaces the whole body; the card is not mounted and issues nothing |
| 21 | Long narrative | narrative at the 600-char bound | Renders in full across the card width; the card grows. No ellipsis, no scroll box, no "read more" |
| 22 | AlertPanel — warning | `alert_type: "ai_profitability"`, `severity: "warning"` | Existing amber `AlertTriangle` + `border-l-amber-400`; `AI finding — {summary}` as `impact_text`; `Suggested action: {action}` in italic. **No Accept/Dismiss buttons, no `Applied`/`Dismissed` line** |
| 23 | AlertPanel — critical | same, `severity: "critical"` | Existing red `AlertOctagon` + `border-l-red-400`; same text shape |
| 24 | Non-finance user, alerts | user lacks `finance.view` | The `ai_profitability` entry is absent from `GET /alerts` (`FINANCIAL_ALERT_TYPES` membership) and no FCM push is addressed to them (SC2) |

★ = the UI half of the keystone set.

---

## Test Hooks

| `data-testid` | Element |
|---------------|---------|
| `profitability-finding` | Card wrapper (also `aria-label="Profitability Finding card"`) |
| `profitability-finding-severity` | Severity chip |
| `profitability-finding-date` | Date line |
| `profitability-finding-narrative` | Narrative paragraph |
| `profitability-finding-action` | Corrective-action text (the eyebrow is reachable by its literal string) |
| `profitability-finding-basis-note` | Revenue-basis caption |
| `profitability-finding-labor-note` | Unburdened-labor caption |
| `profitability-finding-disclosure` | AI disclosure line |
| `profitability-finding-empty` | Either empty-state variant (heading text distinguishes them) |
| `profitability-finding-error` | In-card error line |
| `profitability-finding-skeleton` | In-card loading block |

---

## Accessibility Notes

- **Meaning is never carried by color alone** (WCAG 1.4.1). The severity band is a **text label** (`Margin warning` / `Margin critical`), not a hue; the chip's color is redundant reinforcement. The finding's actual claim is prose. Nothing on this card requires color perception to read.
- **Contrast:** `text-amber-900` on `bg-brand/15` ✓ (shipped, unchanged); `text-red-800` on `bg-red-100` ≈6.8:1 ✓ and on white **8.31:1** ✓; `text-gray-500` (#6b7280) on the white card surface 4.83:1 ✓; `text-gray-700` / `text-gray-900` ✓; the `bg-secondary` (#ece9e1) action block carries `text-gray-900` ✓ and **`text-gray-700` on its `SUGGESTED ACTION` eyebrow ≈8.50:1 ✓** — `text-gray-500` measures only 3.98:1 there and is not used on that surface.
- **Reading order matches visual order**: title → severity → dates → narrative → suggested action → caveats → disclosure. The disclosure is last because it qualifies everything above it.
- **The card is a landmarkless region with an `aria-label`**, reachable by accessible name exactly like the `ChartCard` family that Playwright already targets on this page. The empty state uses the shipped `ChartEmptyState`'s `role="status"`, so an overnight change announces itself to a screen reader on reload.
- **No focus traps, no interactive elements**: the card contains no button, link, popover or dialog. There is nothing to tab to and nothing to dismiss.
- **AI provenance is announced, not implied**: the `Bot` icon is decorative, so the spoken signal is the disclosure line's text — which is why it is always rendered and never collapsed behind a popover (unlike the shipped unburdened-labor `Info` popover, which qualifies a figure the user can see; here the caveat qualifies prose the user cannot audit).
- **Financial text never renders outside a `finance.view`-gated mount**, and never before permissions resolve — `FinanceGate` short-circuits the render and `enabled` short-circuits the fetch.
- **Playwright must log in through the UI then SPA-navigate**, never `page.goto` the drill-down: a hard navigation resets Redux `isAuthenticated`, disables `usePermissions`, and every finance surface is correctly denied — a test that would pass with the gate deleted (Phase 32-04 / 35-11 lesson).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | **none** — no new component installed. The card composes the existing `card.tsx` and `skeleton.tsx` wrappers plus one lucide icon already in use | not required |
| Third-party | **none declared** (`components.json` `registries: {}` — verified empty 2026-07-29) | not applicable |

No `npx shadcn add`, no `--registry` flag, no new npm dependency, no new Python package.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
