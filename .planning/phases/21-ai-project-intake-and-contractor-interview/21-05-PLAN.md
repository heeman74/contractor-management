---
phase: 21-ai-project-intake-and-contractor-interview
plan: 05
type: execute
wave: 4
depends_on: [21-03, 21-04]
files_modified:
  - mobile/test/e2e/phase_21_ai_intake_e2e_test.dart
  - mobile/test/e2e/phase_21_ai_interview_e2e_test.dart
  - web/e2e/ai-intake.spec.ts
  - web/e2e/ai-interview.spec.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03]

must_haves:
  truths:
    - "Flutter E2E tests cover intake chat flow: empty state, send message, streaming display, preview card, create project"
    - "Flutter E2E tests cover interview flow: start, send message, task preview, accept plan, restart interview"
    - "Playwright E2E tests cover web intake flow: SSE streaming, trade scope preview, create project"
    - "Playwright E2E tests cover web interview flow: SSE streaming, task preview, accept plan"
    - "All E2E tests mock the Anthropic API (no real API calls)"
  artifacts:
    - path: "mobile/test/e2e/phase_21_ai_intake_e2e_test.dart"
      provides: "Flutter E2E tests for GC intake chat"
      contains: "test_intake"
    - path: "mobile/test/e2e/phase_21_ai_interview_e2e_test.dart"
      provides: "Flutter E2E tests for contractor interview"
      contains: "test_interview"
    - path: "web/e2e/ai-intake.spec.ts"
      provides: "Playwright E2E tests for web intake"
      contains: "ai-intake"
    - path: "web/e2e/ai-interview.spec.ts"
      provides: "Playwright E2E tests for web interview"
      contains: "ai-interview"
  key_links:
    - from: "mobile/test/e2e/phase_21_ai_intake_e2e_test.dart"
      to: "mobile/lib/features/ai/presentation/screens/intake_chat_screen.dart"
      via: "Widget test with ProviderScope overrides"
      pattern: "ProviderScope|IntakeChatScreen"
---

<objective>
Create comprehensive E2E tests for both web (Playwright) and mobile (Flutter widget tests) covering all three AI requirements. Tests mock the AI/SSE layer to verify the full UI flow without real API calls.

Purpose: CLAUDE.md mandates E2E tests before phase completion. These tests verify the chat UI → provider → SSE client → state update → UI render loop for both platforms.
Output: Passing E2E test suites for both platforms covering AI-01 (intake produces scopes), AI-02 (clarifying questions), AI-03 (interview produces tasks).
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-ai-project-intake-and-contractor-interview/21-CONTEXT.md
@.planning/phases/21-ai-project-intake-and-contractor-interview/21-03-SUMMARY.md
@.planning/phases/21-ai-project-intake-and-contractor-interview/21-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Flutter E2E tests for AI intake and interview</name>
  <files>
    mobile/test/e2e/phase_21_ai_intake_e2e_test.dart,
    mobile/test/e2e/phase_21_ai_interview_e2e_test.dart
  </files>
  <read_first>
    mobile/lib/features/ai/presentation/screens/intake_chat_screen.dart,
    mobile/lib/features/ai/presentation/screens/interview_chat_screen.dart,
    mobile/lib/features/ai/presentation/providers/intake_chat_provider.dart,
    mobile/lib/features/ai/presentation/providers/interview_chat_provider.dart,
    mobile/lib/features/ai/data/ai_sse_client.dart,
    mobile/test/e2e/phase_19_projects_e2e_test.dart,
    mobile/test/e2e/phase_20_dependency_engine_e2e_test.dart
  </read_first>
  <behavior>
    - test_intake_empty_state: screen shows "Tell me about your project" and bot icon
    - test_intake_send_message: user types and sends, user bubble appears right-aligned
    - test_intake_streaming_display: mock SSE tokens, AI bubble appears and grows with each token
    - test_intake_typing_indicator: typing indicator shows before first token, disappears after
    - test_intake_trade_scope_preview: mock tool_call events, preview card appears with trade names
    - test_intake_edit_trade_scope: tap trade name in preview, edit inline, verify update
    - test_intake_create_project: tap "Create Project" button, verify navigation
    - test_intake_clarifying_question: mock ask_clarifying_question tool, verify question appears in chat
    - test_intake_offline_banner: mock offline state, verify banner shows and input disabled
    - test_intake_error_display: mock error SSE event, verify error message in AI bubble
    - test_interview_empty_state: screen shows "Let's build your task plan"
    - test_interview_streaming: mock SSE tokens, AI bubble streams
    - test_interview_task_preview: mock create_task tool_calls, task cards appear
    - test_interview_edit_task: tap task title, edit, verify update
    - test_interview_accept_plan: tap "Accept Plan", verify navigation
    - test_interview_restart_confirmation: tap "Restart Interview", verify dialog with destructive confirmation
  </behavior>
  <action>
1. Create `mobile/test/e2e/phase_21_ai_intake_e2e_test.dart`:

   Setup pattern (follow existing E2E test structure):
   ```dart
   import 'package:flutter_test/flutter_test.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:mocktail/mocktail.dart';
   // Import screens, providers, widgets

   // Mock the AiSseClient to return controlled SSE events
   class MockAiSseClient extends Mock implements AiSseClient {}

   void main() {
     late MockAiSseClient mockSseClient;

     setUp(() {
       mockSseClient = MockAiSseClient();
       // Register in GetIt or override provider
     });
   ```

   Helper: `Stream<SseEvent> fakeTokenStream(List<String> tokens, {List<Map<String, dynamic>>? toolCalls})` — yields token events for each string, then tool_call events, then done event.

   Tests (10 minimum):

   **test_intake_empty_state:**
   - Pump IntakeChatScreen with ProviderScope overrides
   - Use `pump()` (NOT pumpAndSettle — streams never settle)
   - Verify: find.text("Tell me about your project") is found
   - Verify: find.byIcon(Icons.smart_toy) is found

   **test_intake_send_message:**
   - Mock SSE to return simple text tokens
   - Enter text in TextField, tap send button
   - pump() several times
   - Verify: user message bubble appears (find text)
   - Verify: user bubble is right-aligned (check Align or MainAxisAlignment)

   **test_intake_streaming_display:**
   - Mock SSE to yield tokens ["Hello", " world", " from AI"]
   - Send message, pump between each token
   - Verify: AI bubble text grows: first "Hello", then "Hello world", then "Hello world from AI"

   **test_intake_typing_indicator:**
   - Mock SSE with delay before first token
   - Send message, pump once
   - Verify: TypingIndicator widget is visible
   - Pump until first token arrives
   - Verify: TypingIndicator is gone, AI bubble is visible

   **test_intake_trade_scope_preview:**
   - Mock SSE to return tool_call events: create_trade_scope with {trade_name: "Electrical", sort_order: 0} and {trade_name: "Plumbing", sort_order: 1}
   - Send message, pump through stream
   - Verify: find.text("Electrical") and find.text("Plumbing") in preview card
   - Verify: find.text("Create Project") button exists and is enabled

   **test_intake_clarifying_question:**
   - Mock SSE to return tool_call for ask_clarifying_question with {question: "How many bedrooms?"}
   - Send message, pump
   - Verify: "How many bedrooms?" appears in AI bubble or as a distinct message

   **test_intake_offline_banner:**
   - Override connectivity to return offline
   - Pump screen
   - Verify: find.text("AI features require an internet connection.") is found
   - Verify: send button is disabled

   **test_intake_create_project:**
   - Set up state with tradeScopes pre-populated
   - Mock the complete endpoint to return success
   - Tap "Create Project"
   - Verify navigation occurred (mock navigator or check route)

   **test_intake_error_display:**
   - Mock SSE to yield error event: {message: "Something went wrong. Please try again."}
   - Send message, pump
   - Verify: error text appears in chat

   **test_intake_edit_trade_scope:**
   - Pre-populate tradeScopes in state
   - Tap trade name text
   - Enter new name in TextField
   - Verify state updated

2. Create `mobile/test/e2e/phase_21_ai_interview_e2e_test.dart`:

   Same structure but for interview flow (6+ tests):

   **test_interview_empty_state:** verify "Let's build your task plan"
   **test_interview_streaming:** mock tokens, verify AI bubble
   **test_interview_task_preview:** mock create_task tool_calls, verify task cards with titles and estimated hours
   **test_interview_edit_task:** tap task title, edit, verify
   **test_interview_accept_plan:** tap "Accept Plan", verify navigation
   **test_interview_restart_confirmation:** tap "Restart Interview", verify AlertDialog with "This will replace all {N} existing tasks for {tradeName}. This cannot be undone.", tap "Keep Current Plan" → dialog dismisses, tap "Restart Interview" → restarts

   IMPORTANT: Use `pump()` NOT `pumpAndSettle()` throughout — SSE streams and Drift streams never settle.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/e2e/phase_21_ai_intake_e2e_test.dart test/e2e/phase_21_ai_interview_e2e_test.dart 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - mobile/test/e2e/phase_21_ai_intake_e2e_test.dart contains at least 8 `testWidgets` calls
    - mobile/test/e2e/phase_21_ai_intake_e2e_test.dart contains `MockAiSseClient` or equivalent mock
    - mobile/test/e2e/phase_21_ai_intake_e2e_test.dart contains `Tell me about your project`
    - mobile/test/e2e/phase_21_ai_intake_e2e_test.dart contains `Create Project`
    - mobile/test/e2e/phase_21_ai_intake_e2e_test.dart does NOT contain `pumpAndSettle`
    - mobile/test/e2e/phase_21_ai_interview_e2e_test.dart contains at least 6 `testWidgets` calls
    - mobile/test/e2e/phase_21_ai_interview_e2e_test.dart contains `Accept Plan`
    - mobile/test/e2e/phase_21_ai_interview_e2e_test.dart contains `Restart Interview`
    - `flutter test test/e2e/phase_21_ai_intake_e2e_test.dart` exits 0
    - `flutter test test/e2e/phase_21_ai_interview_e2e_test.dart` exits 0
  </acceptance_criteria>
  <done>Flutter E2E tests cover all three requirements: AI-01 (intake produces trade scopes), AI-02 (clarifying questions), AI-03 (interview produces tasks). Tests mock SSE client and use pump() for stream-based UI. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create Playwright E2E tests for web AI intake and interview</name>
  <files>
    web/e2e/ai-intake.spec.ts,
    web/e2e/ai-interview.spec.ts
  </files>
  <read_first>
    web/src/app/projects/new/ai-intake/page.tsx,
    web/src/app/projects/[id]/interview/[scopeId]/page.tsx,
    web/src/features/ai/hooks/useIntakeChat.ts,
    web/e2e/projects.spec.ts,
    web/playwright.config.ts
  </read_first>
  <action>
1. Create `web/e2e/ai-intake.spec.ts`:

   Use Playwright route mocking to intercept API calls (same pattern as existing E2E tests):

   ```typescript
   import { test, expect } from "@playwright/test";

   // Helper: mock SSE stream response
   function mockSSEResponse(events: Array<{event: string, data: string}>) {
     const body = events.map(e => `event: ${e.event}\ndata: ${e.data}\n\n`).join('');
     return {
       status: 200,
       headers: { "Content-Type": "text/event-stream; charset=utf-8" },
       body,
     };
   }
   ```

   Setup: mock auth cookies, mock backend endpoints.

   Tests (8 minimum):

   **test("intake page loads with empty state"):**
   - Mock /ai/intake/start to return conversation
   - Navigate to /projects/new/ai-intake
   - Expect: heading "Tell me about your project" visible

   **test("user can send a message and see response stream"):**
   - Mock /ai/intake/start
   - Mock /ai/intake/message to return SSE with token events: ["I'll help", " you plan", " your project"]
   - Navigate, type "Build a house", click Send
   - Expect: user bubble visible with "Build a house"
   - Expect: AI bubble visible with "I'll help you plan your project"

   **test("typing indicator shows while streaming"):**
   - Mock with delayed SSE response
   - Send message
   - Expect: "ContractorHub AI is typing" aria-label visible
   - Wait for first token
   - Expect: typing indicator not visible

   **test("trade scope preview appears after tool_call"):**
   - Mock SSE with token events + tool_call events for Electrical and Plumbing
   - Send message, wait for stream
   - Expect: "Electrical" and "Plumbing" visible in preview card
   - Expect: "Create Project" button visible and enabled

   **test("clarifying question appears in chat"):**
   - Mock SSE with ask_clarifying_question tool_call: "How many stories is the building?"
   - Send message
   - Expect: "How many stories is the building?" visible in chat

   **test("create project saves and navigates"):**
   - Pre-mock preview with trade scopes
   - Mock /ai/intake/complete to return {project_id: "xxx"}
   - Click "Create Project"
   - Expect: navigation to /projects/xxx

   **test("offline banner shows when disconnected"):**
   - Set navigator.onLine to false via page context
   - Expect: "AI features require an internet connection." visible
   - Expect: send button disabled

   **test("error state shows in chat"):**
   - Mock SSE with error event: "Something went wrong. Please try again."
   - Send message
   - Expect: error message visible in chat area

2. Create `web/e2e/ai-interview.spec.ts`:

   Tests (6 minimum):

   **test("interview page loads with empty state"):**
   - Mock start, navigate to /projects/xxx/interview/yyy
   - Expect: "Let's build your task plan" visible

   **test("interview streaming works"):**
   - Mock SSE with tokens
   - Send message, verify AI bubble

   **test("task preview appears after tool_call"):**
   - Mock SSE with create_task tool_calls (3 tasks)
   - Verify task cards visible with titles and estimated hours

   **test("accept plan saves tasks and navigates"):**
   - Mock /ai/interview/complete
   - Click "Accept Plan"
   - Verify navigation

   **test("restart interview shows confirmation dialog"):**
   - Click "Restart Interview"
   - Expect: dialog with "This will replace all" text
   - Click "Keep Current Plan" — dialog dismisses
   - Click "Restart Interview" again, confirm — verify restart

   **test("editing tasks in preview works"):**
   - Click task title, edit, verify update reflected

   Route mock ordering: most-specific first (exact paths for AI endpoints before general project patterns), matching the Phase 20 lesson.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/web && npx playwright test e2e/ai-intake.spec.ts e2e/ai-interview.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/ai-intake.spec.ts contains at least 8 `test(` calls
    - web/e2e/ai-intake.spec.ts contains `text/event-stream` in mock setup
    - web/e2e/ai-intake.spec.ts contains `Tell me about your project`
    - web/e2e/ai-intake.spec.ts contains `Create Project`
    - web/e2e/ai-interview.spec.ts contains at least 6 `test(` calls
    - web/e2e/ai-interview.spec.ts contains `Accept Plan`
    - web/e2e/ai-interview.spec.ts contains `Restart Interview`
    - `npx playwright test e2e/ai-intake.spec.ts` exits 0
    - `npx playwright test e2e/ai-interview.spec.ts` exits 0
  </acceptance_criteria>
  <done>Playwright E2E tests cover all three requirements for web: intake with streaming, clarifying questions, trade scope preview, and create project flow; interview with streaming, task preview, accept plan, and restart interview. All tests mock SSE responses and pass.</done>
</task>

</tasks>

<verification>
- `cd mobile && flutter test test/e2e/phase_21_ai_intake_e2e_test.dart test/e2e/phase_21_ai_interview_e2e_test.dart` — all pass
- `cd web && npx playwright test e2e/ai-intake.spec.ts e2e/ai-interview.spec.ts` — all pass
- No real Anthropic API calls made in any test
- All three requirements (AI-01, AI-02, AI-03) have test coverage on both platforms
</verification>

<success_criteria>
- All Flutter E2E tests pass (16+ tests across 2 files)
- All Playwright E2E tests pass (14+ tests across 2 files)
- Full backend test suite still passes: `cd backend && uv run python -m pytest -x`
- Full mobile test suite still passes: `cd mobile && flutter test`
</success_criteria>

<output>
After completion, create `.planning/phases/21-ai-project-intake-and-contractor-interview/21-05-SUMMARY.md`
</output>
