---
phase: 35-web-financial-dashboard
plan: 08
type: execute
wave: 5
depends_on: ["35-05", "35-06", "35-07"]
files_modified:
  - backend/tests/test_phase_35_e2e.py
  - backend/app/features/finance/portfolio_repository.py
autonomous: true
requirements: [MARG-04]

must_haves:
  truths:
    - "The company rollup issues the same number of SQL statements for a 25-project company as for a 5-project one"
    - "The company rollup's median latency at ~5,000 financial records is under a stated, committed ceiling"
    - "All three financial endpoints return 403 for a token without finance.view"
    - "The measured median is recorded, and the committed ceiling is tightened to match it"
  artifacts:
    - path: "backend/tests/test_phase_35_e2e.py"
      provides: "D-03 evidence (query-count invariance + wall-clock ceiling) and the SC3 backend gating tests"
      contains: "test_company_rollup_query_count_is_constant_in_project_count"
  key_links:
    - from: "backend/tests/test_phase_35_e2e.py"
      to: "_count_sql_statements"
      via: "statement count around one GET /financials/company at two project counts"
      pattern: "_count_sql_statements"
    - from: "backend/tests/test_phase_35_e2e.py"
      to: "require_permission(finance.view)"
      via: "admin token receives 403 on all three endpoints"
      pattern: "Missing permission: finance.view"
---

<objective>
Close D-03 with evidence rather than assumption, and prove the SC3 guard on the
server where it actually lives.

Purpose: D-03 states the phase MUST include a measured performance check so the
Phase 33 D-11 deferral is settled by data. The query-count invariant is the real
N+1 guarantee — a wall-clock number alone cannot enforce it, and a wall-clock
number alone would either flake or have no teeth. The Playwright deny test is a
false green by construction (a hard `page.goto` resets Redux), so the backend
403 assertions are the honest half of SC3.
Output: two D-03 tests, two gating tests, and a recorded measurement with the
ceiling tightened to match it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-web-financial-dashboard/35-VALIDATION.md
@.planning/phases/35-web-financial-dashboard/35-RESEARCH.md
@.planning/phases/35-web-financial-dashboard/35-CONTEXT.md
@CLAUDE.md

<interfaces>
From plan 35-02, already in `backend/tests/test_phase_35_e2e.py`:
```python
@contextlib.contextmanager
def _count_sql_statements() -> Iterator[list[str]]      # before_cursor_execute listener, removed in finally
async def _seed_company_portfolio(client, headers, company_id, *, project_count: int) -> list[str]
def _pm_headers(company_id) -> dict                     # project_manager -> HAS finance.view
def _admin_headers(company_id) -> dict                  # admin -> deliberately EXCLUDED from finance.* (Phase 30)
_COMPANY_FINANCIALS_URL / _project_financials_url(id) / _project_trend_url(id)
```

Fixtures: `tenant_a_client`, `tenant_b_client`, `seed_two_tenants` (two separate
companies — the natural 5-project vs 25-project pair).

Gate response shape (`app/core/security.py::require_permission`):
`403 {"detail": "Missing permission: finance.view"}`.

**D-03 budget (from 35-VALIDATION.md — state it in code):**
- Seed: 25 projects × (4 scopes, 2 jobs, 20 cost entries, 50 time entries,
  2 invoices, 2 approved quotes) + 25 project budgets + 100 scope budgets
  ≈ 5,000 financial rows.
- Assertion 1 — query-count invariance: statement count at 25 projects EQUALS the
  count at 5 projects (expected ~9-11 with the scope-label and scope-spend queries).
- Assertion 2 — wall-clock: discard one warm-up request, time 5, assert the MEDIAN
  is under the committed ceiling. Initial ceiling **1500 ms**.
- **Follow-up rule:** if the first measured median is well under 1500 ms, tighten
  the committed constant to ~2× the measured median so the test keeps its teeth.
  If it exceeds 1500 ms, D-03 says the cache/snapshot decision REOPENS as a
  follow-up — it is never silently added in this phase.
- Honest caveat to record in the SUMMARY: an in-process ASGI + local-Postgres
  measurement is evidence that computed-on-read does not blow up at this data
  scale; it is not a production SLO.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query-count invariance — the primary N+1 guard</name>
  <files>backend/tests/test_phase_35_e2e.py</files>
  <read_first>
    - backend/tests/test_phase_35_e2e.py (`_count_sql_statements`, `_seed_company_portfolio`, the existing company-rollup tests)
    - backend/app/features/finance/portfolio_service.py (`_fetch_portfolio_inputs` — the queries being counted)
    - .planning/phases/35-web-financial-dashboard/35-VALIDATION.md § D-03 Latency Budget
  </read_first>
  <action>
    Add `test_company_rollup_query_count_is_constant_in_project_count`:
    - Seed tenant A with `project_count=_SMALL_PORTFOLIO_PROJECTS` (5) and tenant B
      with `project_count=_LARGE_PORTFOLIO_PROJECTS` (25) — both named module
      constants, no bare numbers at the call site.
    - Warm each tenant with one `GET /financials/company` OUTSIDE the counter, so
      connection setup, RLS session variables and permission caching are not counted.
    - Then, inside `_count_sql_statements()`, issue exactly one
      `GET /financials/company` per tenant and capture both counts.
    - Assert `small_count == large_count` with a failure message that names the
      likely cause: "company rollup issued N more statements at 25 projects than at 5
      — a per-project query has been reintroduced (CLAUDE.md N+1 rule)".
    - Assert `small_count <= _MAX_COMPANY_ROLLUP_STATEMENTS` (a named constant set to
      the observed count + 2 headroom) so the batching cannot silently balloon while
      staying equal.
    - Record the observed count in a module comment above the constant.

    Add a docstring on the test stating WHY this is the primary guard: a wall-clock
    ceiling generous enough not to flake on CI is also generous enough to hide a
    reintroduced loop, whereas the statement count is deterministic.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_35_e2e.py::test_company_rollup_query_count_is_constant_in_project_count -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_SMALL_PORTFOLIO_PROJECTS = 5\|_LARGE_PORTFOLIO_PROJECTS = 25\|_MAX_COMPANY_ROLLUP_STATEMENTS" backend/tests/test_phase_35_e2e.py` matches all three
    - The named pytest command exits 0
    - `grep -n "warm" backend/tests/test_phase_35_e2e.py` matches inside the test (a warm-up request precedes counting)
    - Deliberately breaking the batching (e.g. temporarily calling `rollup_for_project` per project) makes this test fail — verified once during execution and noted in the SUMMARY, then reverted
  </acceptance_criteria>
  <done>The batched company rollup is provably constant in project count, with the observed statement count recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Wall-clock latency evidence and the ceiling-tightening rule</name>
  <files>backend/tests/test_phase_35_e2e.py</files>
  <read_first>
    - backend/tests/test_phase_35_e2e.py (`_seed_company_portfolio`, the constants added in Task 1)
    - .planning/phases/35-web-financial-dashboard/35-VALIDATION.md § D-03 Latency Budget (the follow-up rule)
    - .planning/phases/35-web-financial-dashboard/35-CONTEXT.md D-03 (caching reopens only if the budget is exceeded)
  </read_first>
  <action>
    Add `test_company_rollup_latency_budget`:
    - Seed tenant A with `_LARGE_PORTFOLIO_PROJECTS` (25) projects.
    - Issue one warm-up `GET /financials/company` and DISCARD its timing.
    - Time `_LATENCY_SAMPLE_COUNT` (5) further requests with
      `time.perf_counter()`, collect the durations in milliseconds, and assert
      `statistics.median(durations) < _COMPANY_ROLLUP_LATENCY_BUDGET_MS`.
    - `_COMPANY_ROLLUP_LATENCY_BUDGET_MS` starts at `1500` with a comment stating the
      rationale (a constant ~10-query endpoint dominated by Python Decimal aggregation
      over ~5,000 rows; a reintroduced per-project loop of 25 × 7 round trips blows
      straight through it; deliberately generous so shared CI hardware does not
      produce red builds on unrelated load).
    - On failure, the assertion message must state: "D-03: the measured median exceeds
      the committed budget — per 35-CONTEXT D-03 the cache/snapshot decision reopens as
      a follow-up; do NOT add caching inside this phase."
    - Print the measured median via `pytest`-visible output (a plain `print` is fine
      with `-s`, or attach it with `pytest`'s `record_property`) so the executor can
      read it.

    **Then apply the follow-up rule in the same task:** run the test, read the measured
    median, and if it is well under 1500 ms, lower
    `_COMPANY_ROLLUP_LATENCY_BUDGET_MS` to approximately `2 ×` the measured median
    (rounded up to the nearest 50 ms), updating the comment with the measured value
    and the date. Re-run to confirm green at the tightened ceiling. Record both the
    measured median and the committed ceiling in the plan SUMMARY, together with the
    caveat that an in-process ASGI + local-Postgres measurement is evidence, not a
    production SLO.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_35_e2e.py::test_company_rollup_latency_budget -x -q -s</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_COMPANY_ROLLUP_LATENCY_BUDGET_MS" backend/tests/test_phase_35_e2e.py` matches, and its comment contains a measured median value and a date (proving the tightening rule was applied)
    - `grep -n "statistics.median\|_LATENCY_SAMPLE_COUNT" backend/tests/test_phase_35_e2e.py` matches both
    - `grep -n "warm" backend/tests/test_phase_35_e2e.py` shows the warm-up request is excluded from the timed samples
    - The named pytest command exits 0 at the tightened ceiling
    - The SUMMARY records: measured median (ms), committed ceiling (ms), sample count, seed scale, and the not-a-production-SLO caveat
  </acceptance_criteria>
  <done>D-03 is closed with a recorded measurement and a ceiling tightened to keep the test meaningful; no caching was added.</done>
</task>

<task type="auto">
  <name>Task 3: SC3 backend gating — 403 for admin, RLS isolation on all three endpoints</name>
  <files>backend/tests/test_phase_35_e2e.py, backend/app/features/finance/portfolio_repository.py</files>
  <read_first>
    - backend/tests/test_phase_34_e2e.py `test_budget_crud_forbidden_without_finance_manage` (the 403 assertion shape)
    - backend/app/core/permissions.py (`_FINANCE_ONLY_KEYS` — admin is deliberately excluded from finance.*)
    - backend/tests/test_phase_35_e2e.py (existing tests, `_admin_headers`)
    - .planning/phases/35-web-financial-dashboard/35-RESEARCH.md Pitfall 6 (why the backend is the real guard test)
  </read_first>
  <action>
    1. Add `test_financial_endpoints_forbidden_without_finance_view`: for each of the
       three URLs (`_COMPANY_FINANCIALS_URL`, `_project_financials_url(project_id)`,
       `_project_trend_url(project_id)`), assert an `_admin_headers` request returns
       `403` with detail exactly `"Missing permission: finance.view"`. Loop over a
       list of URLs with `pytest.mark.parametrize` or an in-test list so the three
       assertions cannot drift apart. Add a docstring noting the admin token is the
       correct negative fixture because Phase 30 subtracts `finance.*` from admin.
    2. Add `test_company_rollup_is_tenant_isolated`: seed 2 projects into tenant A and
       1 into tenant B; assert tenant B's `GET /financials/company` contains only its
       own project ids and that its `portfolio.cost` excludes tenant A's spend
       entirely.
    3. In `portfolio_repository.py`, append one line to the module docstring recording
       the D-03 outcome so the constraint is discoverable from the code:
       "Constant in project count — pinned by
       test_company_rollup_query_count_is_constant_in_project_count and measured by
       test_company_rollup_latency_budget (backend/tests/test_phase_35_e2e.py)."
       Change nothing else in that file.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_35_e2e.py -q -k "forbidden or tenant_isolated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^async def test_financial_endpoints_forbidden_without_finance_view\|^async def test_company_rollup_is_tenant_isolated" backend/tests/test_phase_35_e2e.py` == 2
    - `grep -n "Missing permission: finance.view" backend/tests/test_phase_35_e2e.py` matches
    - The test body references all three URL helpers (`grep -n "_COMPANY_FINANCIALS_URL\|_project_financials_url\|_project_trend_url"` all appear inside the forbidden test)
    - `grep -n "test_company_rollup_query_count_is_constant_in_project_count" backend/app/features/finance/portfolio_repository.py` matches
    - Named pytest command reports 2 passed, 0 failed
  </acceptance_criteria>
  <done>All three financial endpoints are proven to 403 without finance.view and to stay tenant-isolated; the N+1 constraint is documented at the code it constrains.</done>
</task>

</tasks>

<verification>
- `cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_35_e2e.py -q` — the whole Phase 35 backend suite green
- `python -m pytest -q` — full backend suite green (phase gate)
- `ruff check . && ruff format --check .` — clean
</verification>

<success_criteria>
- Query count at 25 projects equals the count at 5 projects
- The measured median latency is recorded and the committed ceiling is ~2× it
- All three endpoints 403 for an admin token and stay tenant-isolated
- No cache or snapshot table was introduced
</success_criteria>

<output>
After completion, create `.planning/phases/35-web-financial-dashboard/35-08-SUMMARY.md`
</output>
