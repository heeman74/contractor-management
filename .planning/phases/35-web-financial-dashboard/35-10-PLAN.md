---
phase: 35-web-financial-dashboard
plan: 10
type: execute
wave: 3
depends_on: ["35-03", "35-04", "35-09"]
files_modified:
  - web/src/app/(dashboard)/financials/[projectId]/page.tsx
  - web/src/app/(dashboard)/financials/[projectId]/_components/project-financials-dashboard.tsx
  - web/src/app/(dashboard)/financials/[projectId]/_components/margin-trend-chart.tsx
  - web/src/app/(dashboard)/financials/[projectId]/_components/trend-window-filter.tsx
  - web/src/app/(dashboard)/financials/[projectId]/_components/scope-budget-bars.tsx
  - web/src/app/(dashboard)/financials/[projectId]/_components/category-mix-chart.tsx
  - web/src/app/(dashboard)/financials/__tests__/project-financials.test.tsx
autonomous: true
requirements: [MARG-04]

must_haves:
  truths:
    - "Owner/PM opens /financials/{projectId} and sees a monthly margin trend with revenue and cost context lines"
    - "A month with no revenue renders a gap in the line, never a $0 point"
    - "Switching the trend window refetches only the trend and keeps the previous chart on screen"
    - "Per-scope budget-vs-actual renders with the labor-exclusion caption always visible"
    - "Cost category mix caps at six slices with an Other rollup whose CSV is never rolled up"
  artifacts:
    - path: "web/src/app/(dashboard)/financials/[projectId]/page.tsx"
      provides: "Drill-down route shell (ssr:false + skeleton)"
    - path: "web/src/app/(dashboard)/financials/[projectId]/_components/margin-trend-chart.tsx"
      provides: "Three-series LineChart with break-even reference and null gaps"
      contains: "connectNulls"
    - path: "web/src/app/(dashboard)/financials/[projectId]/_components/category-mix-chart.tsx"
      provides: "Pie with MAX_PIE_SLICES rollup and unrolled CSV"
  key_links:
    - from: "web/src/app/(dashboard)/financials/[projectId]/_components/project-financials-dashboard.tsx"
      to: "useProjectFinancials + useProjectMarginTrend"
      via: "two independent query keys so the window never restates the tiles"
      pattern: "useProjectMarginTrend\\("
    - from: "web/src/app/(dashboard)/financials/[projectId]/_components/margin-trend-chart.tsx"
      to: "web/src/features/finance/financials-format.ts"
      via: "formatMonthLabel + formatAxisThousands"
      pattern: "formatMonthLabel"
---

<objective>
Build `/financials/[projectId]` — the drill-down: project summary tiles, the
monthly margin trend with its window selector, per-trade-scope budget-vs-actual,
and the cost category mix.

Purpose: SC1 — margin trend and budget-vs-actual charts for any project. This is
where the honesty rules bite hardest: a null margin must render as a gap, the
window must visibly slice buckets rather than records, and a scope bar must
declare that it excludes labor.
Output: the route shell plus five components and jest coverage of the null,
window and rollup behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-web-financial-dashboard/35-UI-SPEC.md
@.planning/phases/35-web-financial-dashboard/35-CONTEXT.md
@CLAUDE.md

<interfaces>
**From plan 35-03:**
```ts
// chart-theme.ts
CHART_TOOLTIP_STYLE, CHART_TICK, CHART_HEIGHT (280), TREND_SERIES, BREAK_EVEN_STROKE,
BUDGET_TIER_FILL, BULLET_ROW_HEIGHT, BULLET_BAR_SIZE, BULLET_AXIS_WIDTH,
BULLET_VIEWPORT_MAX_CLASS, PERCENT_AXIS_CLAMP, CATEGORY_FILL, CUSTOM_CATEGORY_FILLS,
OTHER_CATEGORY_NAME, MAX_PIE_SLICES (6), PIE_LABEL_MIN_PERCENT (5)
// financials-format.ts
formatMonthLabel("2026-03") -> "Mar 26"     // string split, never new Date()
formatAxisThousands(-4000) -> "-$4k"
truncateLabel, clampPercentForAxis, axisMaxDomain, budgetTierFill, rollUpCategories, bulletChartHeight
// chart-empty-state.tsx  ChartEmptyState({ heading, body })
// financials-skeleton.tsx  FinancialsSkeleton({ variant: "project" })
```

**From plan 35-04:**
```ts
useProjectFinancials(projectId): UseQueryResult<ProjectFinancials>
useProjectMarginTrend(projectId, window): UseQueryResult<MarginTrend>   // placeholderData keeps the previous chart
type ProjectFinancials = { projectId; name; status; breakdown: CostBreakdown; scopes: ScopeBudgetRow[] }
type ScopeBudgetRow = { tradeScopeId; tradeName; spent: string; budget: BudgetVsActual | null }
type MarginTrend = { projectId; window: TrendWindow; buckets: TrendBucket[] }
type TrendBucket = { month: string; cost: string; margin: MarginSummary }   // margin.revenue / margin.margin nullable
TREND_WINDOWS: readonly TrendWindow[]; DEFAULT_TREND_WINDOW = "12m"
```

**From plan 35-09 (already on disk):**
```tsx
// web/src/app/(dashboard)/financials/_components/bullet-bar-chart.tsx
export interface BulletBarRow {
  id: string; label: string; percentUsed: string; percentUsedClamped: number;
  spentLabel: string; budgetLabel: string; statusLabel?: string;
  isInactive?: boolean; isOverClamp?: boolean;
}
export function BulletBarChart({ rows, testId, onRowClick }: {
  rows: BulletBarRow[]; testId: string; onRowClick?: (id: string) => void;
})
// omitting onRowClick leaves cursor default and attaches no handler
// web/src/app/(dashboard)/financials/_components/finance-summary-tiles.tsx
export function FinanceSummaryTiles({ revenueTitle, costTitle, marginTitle, ... })
```

**Shipped pieces to reuse:** `ChartCard` (title, kpiValue, icon, csvFilename,
csvRows, ariaLabel), `formatCurrency` / `formatSignedCurrency`,
`formatMarginPercent` / `formatMarginDollars`, `NO_REVENUE_NOTE` (exported by
plan 35-03), `FinanceFlagChip`, `StatusBadge`, the `DateRangeFilter` preset
button recipe (`Button variant="outline" size="sm"`, active =
`bg-brand/10 text-foreground border-brand font-semibold`, `aria-pressed`),
`quote-conversion-chart.tsx` (the Pie composition to copy), and
`FinanceSummaryTiles` from plan 35-09.

**Verbatim copy (35-UI-SPEC):** `← Financials` (aria-label `Back to Financials`) ·
`Margin, budget and cost detail.` · `Revenue`/`Cost`/`Margin` tile titles ·
`Margin Trend` · `{N} months` / `1 month` · `Last 3m`·`Last 6m`·`Last 12m`·`All time` ·
`Cumulative from project start — the window only changes how far back the chart
shows.` · `Break-even` · `No revenue recorded — margin will appear once an invoice
or approved quote exists.` · `Budget vs Actual by Trade Scope` · `{N} of {M} scopes
budgeted` · `Scope spend excludes labor — labor is tracked at job level.` ·
`Cost Category Mix` · `{amount} total cost` · `Project not found.` ·
`Back to Financials` · `Couldn't load financials. Refresh to try again.` ·
empty states: `No dated financial records yet` / `Costs, tracked time, invoices and
approved quotes appear here once they carry dates.`; `No scope budgets set` / `Set a
budget on a trade scope to track its spend.`; `No costs recorded` / `Add a cost entry
or tracked time to see where the money goes.`

**Test hooks:** `margin-trend-chart`, `trend-window-3m|-6m|-12m|-all`,
`trend-window-note`, `trend-no-revenue-note`, `scope-budget-bars`,
`scope-labor-note`, `category-mix-chart`, `project-financials-not-found`,
`financials-error`, `financials-skeleton`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drill-down shell, container and header states</name>
  <files>web/src/app/(dashboard)/financials/[projectId]/page.tsx, web/src/app/(dashboard)/financials/[projectId]/_components/project-financials-dashboard.tsx, web/src/app/(dashboard)/financials/__tests__/project-financials.test.tsx</files>
  <read_first>
    - web/src/app/(dashboard)/reports/page.tsx (the ssr:false shell)
    - .planning/phases/35-web-financial-dashboard/35-UI-SPEC.md § Page layout /financials/[projectId], § State Matrix rows 21-23
    - web/src/app/(dashboard)/financials/_components/finance-summary-tiles.tsx (plan 35-09 — reused here with different labels)
    - web/src/app/(dashboard)/projects (an existing dynamic `[id]` route for the `useParams` convention)
  </read_first>
  <behavior>
    - While either query is loading, `FinancialsSkeleton variant="project"` renders.
    - When `useProjectFinancials` returns a 404, an inline panel renders `Project not found.` with `data-testid="project-financials-not-found"` and a `Back to Financials` link — no toast, no crash.
    - On any other error, `Couldn't load financials. Refresh to try again.` renders with `data-testid="financials-error"`.
    - On success, the header renders the back link, the project name as `<h1>`, a `StatusBadge` for its status, and the subtitle `Margin, budget and cost detail.`
    - The tiles reuse `FinanceSummaryTiles` with titles `Revenue` / `Cost` / `Margin`, fed from `breakdown.margin` and `breakdown.grandTotal`.
  </behavior>
  <action>
    1. `[projectId]/page.tsx` — `"use client"`, mirrors `reports/page.tsx`:
       `dynamic(() => import("./_components/project-financials-dashboard"),
       { ssr: false, loading: () => <FinancialsSkeleton variant="project" /> })`,
       reading the id via `useParams`. Wrapper `space-y-6`.
    2. `project-financials-dashboard.tsx` — `"use client"`, default export, the ONLY
       hook owner: `useProjectFinancials(projectId)` plus
       `useProjectMarginTrend(projectId, window)` with `window` in local `useState`
       initialised to `DEFAULT_TREND_WINDOW`. Two hooks, two query keys — a window
       change must never restate the tiles (D-10).
       Detect the 404 with a small named helper `isNotFoundError(error)` reading the
       shipped api-client error shape (read `web/src/lib/api-client.ts` first) — no
       bare `as` cast, no string matching on a message if a status code is available.
       Layout: header → `FinanceSummaryTiles` (`grid-cols-1 md:grid-cols-3 gap-8`) →
       full-width `Margin Trend` ChartCard → `grid grid-cols-1 md:grid-cols-2 gap-8`
       with the scope bars and category mix cards.
    3. Jest spec `.../financials/__tests__/project-financials.test.tsx` covering each
       `<behavior>` bullet with `jest.mock("@/features/finance/hooks")`.
  </action>
  <verify>
    <automated>cd web && npx jest "src/app/(dashboard)/financials/__tests__/project-financials.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ssr: false" "web/src/app/(dashboard)/financials/[projectId]/page.tsx"` matches
    - `grep -rn "useProjectFinancials\|useProjectMarginTrend" "web/src/app/(dashboard)/financials/[projectId]/_components/"` matches ONLY in `project-financials-dashboard.tsx`
    - `grep -n "project-financials-not-found" "web/src/app/(dashboard)/financials/[projectId]/_components/project-financials-dashboard.tsx"` matches
    - `grep -n "FinanceSummaryTiles" "web/src/app/(dashboard)/financials/[projectId]/_components/project-financials-dashboard.tsx"` matches (one tiles component, not a near-copy)
    - `grep -n "as Error\|as any" "web/src/app/(dashboard)/financials/[projectId]/_components/project-financials-dashboard.tsx"` returns no matches
    - `npx jest ".../project-financials.test.tsx"` reports >= 5 passing tests
  </acceptance_criteria>
  <done>The drill-down route renders through an ssr:false shell with two independent queries, the not-found panel, and the shared tiles component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MarginTrendChart and the window selector</name>
  <files>web/src/app/(dashboard)/financials/[projectId]/_components/margin-trend-chart.tsx, web/src/app/(dashboard)/financials/[projectId]/_components/trend-window-filter.tsx, web/src/app/(dashboard)/financials/__tests__/project-financials.test.tsx</files>
  <read_first>
    - .planning/phases/35-web-financial-dashboard/35-UI-SPEC.md § Chart 4 (every property row) and § State Matrix rows 24-29
    - web/src/app/(dashboard)/reports/_components/revenue-chart.tsx (the shipped cartesian composition, tooltip and axis conventions)
    - web/src/app/(dashboard)/reports/_components/date-range-filter.tsx (the preset button recipe and aria-pressed usage)
    - web/src/features/finance/financials-format.ts (formatMonthLabel, formatAxisThousands — plan 35-03)
  </read_first>
  <behavior>
    - Three `<Line>` series render: Margin (ink, strokeWidth 2.5, solid), Revenue (slate 1.5 solid), Cost (slate 1.5 dashed `4 3`), all `dot={false}` with `activeDot={{ r: 4 }}`.
    - A bucket whose `margin.margin` is `null` maps to `margin: null` in the chart datum and renders a gap — `connectNulls` stays at its `false` default and no `?? 0` appears anywhere.
    - The X axis formats `"2026-03"` as `Mar 26` via `formatMonthLabel`, with `interval="preserveStartEnd"` and `minTickGap={24}`.
    - The Y axis formats `-4000` as `-$4k` via `formatAxisThousands`.
    - A `<ReferenceLine y={0}>` renders in `BREAK_EVEN_STROKE` with the label `Break-even`.
    - The window selector renders four buttons with `data-testid="trend-window-{value}"` and `aria-pressed` true on the active one; clicking one calls `onWindowChange` with that value and nothing else.
    - The caption `Cumulative from project start — the window only changes how far back the chart shows.` renders directly beneath the selector with `data-testid="trend-window-note"`.
    - While a window refetch is in flight the previous chart stays mounted at `opacity-60` with `aria-busy="true"` — no skeleton flash.
    - When every bucket's `revenueBasis === "none"`, the caption `trend-no-revenue-note` renders the shipped `NO_REVENUE_NOTE` string.
    - With zero buckets, `ChartEmptyState` renders `No dated financial records yet` / `Costs, tracked time, invoices and approved quotes appear here once they carry dates.`
  </behavior>
  <action>
    1. `trend-window-filter.tsx` — `"use client"`, pure props
       `{ window: TrendWindow; onWindowChange: (w: TrendWindow) => void }`. Renders the
       four buttons from a `TREND_WINDOW_LABELS: Record<TrendWindow, string>` constant
       (`Last 3m`, `Last 6m`, `Last 12m`, `All time`) using the shipped preset recipe.
       No `useEffect`, no local state mirroring the prop
       (`react-hooks/set-state-in-effect` fails the lint gate).
    2. `margin-trend-chart.tsx` — `"use client"`, pure props
       `{ buckets: TrendBucket[]; isRefetching: boolean }`. Chart data comes from one
       pure mapper `toTrendData(buckets)` producing
       `{ month, margin: number | null, revenue: number | null, cost: number,
          marginLabel: string | null, revenueLabel: string | null, costLabel: string,
          marginPercentLabel: string | null, revenueBasis: string }`
       — `parseFloat` lives ONLY here and returns `null` for a null string; every
       rendered figure (tooltip, CSV) formats from the `*Label` companion field.
       Composition inside `<ResponsiveContainer width="100%" height={CHART_HEIGHT}>`:
       `<LineChart accessibilityLayer>` + `CartesianGrid strokeDasharray="3 3"
       className="stroke-border"` + the three `<Line type="monotone">` series spread
       from `TREND_SERIES` + `<ReferenceLine y={0} …/>` + `<Legend wrapperStyle={{
       fontSize: 11 }} />` + `<Tooltip contentStyle={CHART_TOOLTIP_STYLE} …/>` whose
       label is the full month (`March 2026`) and which appends the bucket's
       `revenueBasis` when it is `quoted` or `mixed`. Wrap in
       `<div data-testid="margin-trend-chart" className={isRefetching ? "opacity-60" : ""}
       aria-busy={isRefetching}>`.
       CSV builder `trendCsvRows(buckets)` → `["Month","Revenue","Cost","Margin","Margin %","Revenue basis"]`
       with EMPTY cells for nulls, never `0`.
       The dashboard container renders the selector, then the caption, then the plot,
       inside the `Margin Trend` ChartCard (`icon={TrendingUp}`,
       `ariaLabel="Margin Trend chart"`, `kpiValue` = `{N} months` / `1 month`).
    3. Extend the jest spec with one `it()` per `<behavior>` bullet — in particular a
       datum with `margin: null` asserted to produce no rendered point and no `$0.00`
       text anywhere in the card, and the CSV row for that month asserted to carry
       empty cells.
  </action>
  <verify>
    <automated>cd web && npx jest "src/app/(dashboard)/financials/__tests__/project-financials.test.tsx" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "?? 0\|connectNulls={true}\|connectNulls" "web/src/app/(dashboard)/financials/[projectId]/_components/margin-trend-chart.tsx"` shows no `?? 0` and no explicit `connectNulls` override
    - `grep -n "new Date(" "web/src/app/(dashboard)/financials/[projectId]/_components/margin-trend-chart.tsx"` returns no matches
    - `grep -c "parseFloat" "web/src/app/(dashboard)/financials/[projectId]/_components/margin-trend-chart.tsx"` <= 3 and all occurrences are inside `toTrendData`
    - `grep -n "aria-pressed" "web/src/app/(dashboard)/financials/[projectId]/_components/trend-window-filter.tsx"` matches
    - `grep -n "useEffect" "web/src/app/(dashboard)/financials/[projectId]/_components/trend-window-filter.tsx"` returns no matches
    - `grep -n "Cumulative from project start" "web/src/app/(dashboard)/financials/[projectId]/_components/" -r` matches exactly once
    - `npm run lint` passes at `--max-warnings 0`
  </acceptance_criteria>
  <done>The trend renders three series with a labelled break-even line, real gaps for null margins, a window selector that refetches only the trend, and the Pitfall-2 caption made visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ScopeBudgetBars and CategoryMixChart</name>
  <files>web/src/app/(dashboard)/financials/[projectId]/_components/scope-budget-bars.tsx, web/src/app/(dashboard)/financials/[projectId]/_components/category-mix-chart.tsx, web/src/app/(dashboard)/financials/__tests__/project-financials.test.tsx</files>
  <read_first>
    - .planning/phases/35-web-financial-dashboard/35-UI-SPEC.md § Chart 5, § Chart 6, § Color (category ramp + rollup honesty), § State Matrix rows 30-34
    - web/src/app/(dashboard)/financials/_components/project-budget-bars.tsx (plan 35-09 — the bullet-bar form this reuses)
    - web/src/app/(dashboard)/reports/_components/quote-conversion-chart.tsx (the Pie composition to copy, including the label and formatter shapes)
    - web/src/features/finance/financials-format.ts (`rollUpCategories`) and chart-theme.ts (`CATEGORY_FILL`, `MAX_PIE_SLICES`, `PIE_LABEL_MIN_PERCENT`)
  </read_first>
  <behavior>
    - Scope bars use the identical form, colour bands, sort, clamp and geometry as the project bars, keyed by `tradeName`, with NO click-through (`cursor` stays default).
    - The caption `Scope spend excludes labor — labor is tracked at job level.` renders whenever at least one scope bar renders, with `data-testid="scope-labor-note"`.
    - With no scope budget at all, `ChartEmptyState` renders `No scope budgets set` / `Set a budget on a trade scope to track its spend.` and the labor caption is NOT shown.
    - The pie composes labor plus every cost category; with 7+ distinct categories the 7th onward roll into `Other`, capping at six slices, and `Other` always sorts last.
    - The `Other` slice's tooltip lists the rolled-up category names.
    - The CSV carries one row per REAL category, never rolled up: `["Category","Amount","Percent of total"]`.
    - Slice labels are suppressed (empty string) below 5%; the legend still lists every rendered slice.
    - With no cost entries and no tracked labor, `ChartEmptyState` renders `No costs recorded` / `Add a cost entry or tracked time to see where the money goes.`
  </behavior>
  <action>
    1. `scope-budget-bars.tsx` — `"use client"`, pure props `{ scopes: ScopeBudgetRow[] }`.
       CONSUME the shared `BulletBarChart` from plan 35-09 — do NOT copy its JSX and do
       NOT modify `project-budget-bars.tsx` or `bullet-bar-chart.tsx` (they belong to
       plan 35-09; the two charts are specified as identical apart from the key, the
       click-through and the caption). This file only maps `ScopeBudgetRow[]` to
       `BulletBarRow[]` in one pure `toScopeBarRows(scopes)` (keyed by `tradeName`,
       `parseFloat` confined there), renders
       `<BulletBarChart rows={rows} testId="scope-budget-bars" />` with NO `onRowClick`,
       owns the `scope-labor-note` caption, and builds the CSV rows.
       If `BulletBarChart` turns out to be missing a prop this chart needs, add the prop
       there in a follow-up rather than forking the component — and say so in the SUMMARY.
       Rendered by the container inside a ChartCard titled
       `Budget vs Actual by Trade Scope` (`icon={Gauge}`,
       `ariaLabel="Budget vs Actual by Trade Scope chart"`,
       `kpiValue` = `{N} of {M} scopes budgeted`).
    2. `category-mix-chart.tsx` — `"use client"`, pure props
       `{ categories: CategoryTotal[]; labor: LaborCostSummary | null }`.
       Build slices in one pure function `toCategorySlices(categories, labor)`:
       prepend a `Labor` slice from `labor.total` when present, sort descending by
       amount, then apply the shipped `rollUpCategories` for the `MAX_PIE_SLICES` cap
       with `Other` last. Fill per slice from `CATEGORY_FILL` by lower-cased name,
       falling back to `CUSTOM_CATEGORY_FILLS` in alphabetical order of the custom
       category names — every hue used at most once.
       Composition copied from `quote-conversion-chart.tsx`: `PieChart` + `Pie`
       (`outerRadius={90}`, `cx/cy="50%"`, one `Cell` per slice) + `Tooltip`
       (`contentStyle={CHART_TOOLTIP_STYLE}`) + `Legend`. Slice label function returns
       `""` below `PIE_LABEL_MIN_PERCENT`. The `Other` tooltip appends the rolled-up
       names.
       CSV builder `categoryMixCsvRows(categories, labor)` — the UNROLLED list.
       Rendered inside a ChartCard titled `Cost Category Mix` (`icon={PieChartIcon}`,
       `ariaLabel="Cost Category Mix chart"`, `kpiValue` = `{amount} total cost`).
    3. Extend the jest spec with one `it()` per `<behavior>` bullet, including a
       7-category fixture asserting exactly six rendered slices, `Other` last, and a
       CSV containing all seven rows.
  </action>
  <verify>
    <automated>cd web && npx jest "src/app/(dashboard)/financials" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Scope spend excludes labor — labor is tracked at job level." "web/src/app/(dashboard)/financials/[projectId]/_components/scope-budget-bars.tsx"` matches exactly once
    - `grep -n "onClick\|router.push\|onRowClick" "web/src/app/(dashboard)/financials/[projectId]/_components/scope-budget-bars.tsx"` returns no matches (no per-scope route exists)
    - `grep -n "BulletBarChart" "web/src/app/(dashboard)/financials/[projectId]/_components/scope-budget-bars.tsx"` matches, and `grep -n "ResponsiveContainer\|BarChart" ` on the same file returns no matches (the form is consumed, not copied)
    - `git status --porcelain "web/src/app/(dashboard)/financials/_components/"` shows no modification to plan 35-09's files
    - `grep -n "rollUpCategories" "web/src/app/(dashboard)/financials/[projectId]/_components/category-mix-chart.tsx"` matches (the cap is not re-implemented inline)
    - `grep -n "#0e1726\|#59637a\|#8a93a6\|#b6bcc9" "web/src/app/(dashboard)/financials/[projectId]/_components/category-mix-chart.tsx"` returns no matches (fills come from `CATEGORY_FILL`)
    - `npx jest "src/app/(dashboard)/financials"` — both route specs green, >= 20 tests total
    - `npm run lint && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Scope budget bars reuse the project bullet-bar form with the honest labor caption, and the category pie caps at six slices while its export keeps every real category.</done>
</task>

</tasks>

<verification>
- `cd web && npx jest "src/app/(dashboard)/financials" src/features/finance` — green
- `cd web && npm run lint && npx tsc --noEmit` — clean at `--max-warnings 0`
- `grep -rn "?? 0" "web/src/app/(dashboard)/financials/"` — no money or margin coercion anywhere
</verification>

<success_criteria>
- `/financials/{projectId}` renders the trend, scope bars and category mix from two independent queries
- A null margin month renders a gap, never a $0 point, in the chart and as an empty CSV cell
- The window selector changes only the trend and states that it slices buckets, not records
</success_criteria>

<output>
After completion, create `.planning/phases/35-web-financial-dashboard/35-10-SUMMARY.md`
</output>
