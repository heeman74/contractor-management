---
phase: 22-task-execution-and-photo-annotation
plan: 05
type: execute
wave: 3
depends_on: [22-01, 22-03, 22-04]
files_modified:
  - mobile/lib/features/projects/presentation/widgets/trade_progress_card.dart
  - mobile/lib/features/projects/presentation/screens/project_detail_screen.dart
  - web/src/features/tasks/components/TradeProgressCard.tsx
  - web/src/features/projects/components/ProjectDetail.tsx
  - mobile/test/e2e/phase_22_task_execution_e2e_test.dart
autonomous: true
requirements: [TASK-07, TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06]

must_haves:
  truths:
    - "GC sees trade scope cards with real progress bars and task counts on ProjectDetailScreen"
    - "GC can tap into a scope to see read-only task list with photo thumbnails"
    - "Web project detail shows TradeProgressCard with progress bars and last activity"
    - "All TASK requirements covered by automated E2E tests"
  artifacts:
    - path: "mobile/lib/features/projects/presentation/widgets/trade_progress_card.dart"
      provides: "GC progress card with live task counts and progress bar"
      contains: "class TradeProgressCard"
    - path: "web/src/features/tasks/components/TradeProgressCard.tsx"
      provides: "Web trade progress card"
      contains: "TradeProgressCard"
    - path: "mobile/test/e2e/phase_22_task_execution_e2e_test.dart"
      provides: "Full E2E tests covering TASK-01 through TASK-07"
      min_lines: 150
  key_links:
    - from: "mobile/lib/features/projects/presentation/screens/project_detail_screen.dart"
      to: "mobile/lib/features/projects/presentation/widgets/trade_progress_card.dart"
      via: "Replaces TradeScopeCard with TradeProgressCard showing real data"
      pattern: "TradeProgressCard"
    - from: "mobile/lib/features/projects/presentation/widgets/trade_progress_card.dart"
      to: "mobile/lib/features/projects/data/task_dao.dart"
      via: "countTasksByScope and countCompletedTasksByScope for progress"
      pattern: "countTasksByScope|countCompletedTasksByScope"
---

<objective>
GC progress monitoring (mobile + web) and comprehensive E2E tests: TradeProgressCard with real task counts and progress bars, ProjectDetailScreen upgrade, web TradeProgressCard, and full E2E test suite covering all TASK requirements.

Purpose: Gives GCs at-a-glance visibility into project health across all trades. E2E tests validate the entire phase is working correctly.
Output: TradeProgressCard (mobile + web), ProjectDetailScreen upgrade, E2E test suite.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-task-execution-and-photo-annotation/22-CONTEXT.md
@.planning/phases/22-task-execution-and-photo-annotation/22-RESEARCH.md
@.planning/phases/22-task-execution-and-photo-annotation/22-UI-SPEC.md
@.planning/phases/22-task-execution-and-photo-annotation/22-01-SUMMARY.md
@.planning/phases/22-task-execution-and-photo-annotation/22-02-SUMMARY.md
@.planning/phases/22-task-execution-and-photo-annotation/22-03-SUMMARY.md
@.planning/phases/22-task-execution-and-photo-annotation/22-04-SUMMARY.md

<interfaces>
<!-- From Plan 02 providers -->
```dart
final taskDaoProvider = Provider<TaskDao>(...);
// TaskDao has: countTasksByScope, countCompletedTasksByScope, watchTasksByScope
```

<!-- From existing ProjectDetailScreen -->
```dart
// Currently uses TradeScopeCard with hardcoded completedTasks: 0, totalTasks: 0
// Must be replaced with TradeProgressCard using live data
```

<!-- Web project detail pattern -->
```typescript
// web/src/features/projects/components/ProjectDetail.tsx exists
// Shows trade scope cards — needs upgrade to show progress
```

<!-- UI-SPEC progress bar thresholds -->
```
- 0-33%: --chart-5 (purple)
- 34-66%: --chart-2 (blue)
- 67-99%: --chart-1 (sky blue)
- 100%: green-600
```

<!-- Trade scope color dots: chart-1 through chart-5 cycle -->
```css
--chart-1: oklch(0.809 0.105 251.813) /* sky blue */
--chart-2: oklch(0.623 0.214 259.815) /* blue */
--chart-3: oklch(0.546 0.245 262.881) /* indigo */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TradeProgressCard (mobile + web) + ProjectDetailScreen upgrade</name>
  <files>
    mobile/lib/features/projects/presentation/widgets/trade_progress_card.dart,
    mobile/lib/features/projects/presentation/screens/project_detail_screen.dart,
    web/src/features/tasks/components/TradeProgressCard.tsx,
    web/src/features/projects/components/ProjectDetail.tsx
  </files>
  <read_first>
    mobile/lib/features/projects/presentation/screens/project_detail_screen.dart,
    mobile/lib/features/projects/presentation/widgets/trade_scope_card.dart,
    mobile/lib/features/projects/data/task_dao.dart,
    mobile/lib/features/projects/presentation/providers/project_providers.dart,
    web/src/features/projects/components/ProjectDetail.tsx,
    web/src/features/projects/types.ts,
    web/src/lib/api-client.ts,
    mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart
  </read_first>
  <action>
1. Create `mobile/lib/features/projects/presentation/widgets/trade_progress_card.dart`:
   - ConsumerWidget
   - Parameters: scopeId (String), tradeName (String), tradeColor (String hex), contractorName (String?), onTap (VoidCallback)
   - Internally: use FutureProvider or direct calls to TaskDao for counts:
     - Watch a provider that streams total + completed counts per scope
     - Create `tradeScopeProgressProvider = StreamProvider.autoDispose.family` in this file or project_providers.dart that combines countTasksByScope stream + countCompletedTasksByScope stream. Simplest: create a FutureProvider that calls both count methods.
   - Layout: Card with md (16px) padding:
     - Row: colored dot (Container 12x12 rounded, color from tradeColor), SizedBox(width: 8), trade name (titleMedium, w600), Spacer, "X/Y tasks" (bodySmall)
     - SizedBox(height: 8)
     - LinearProgressIndicator(value: completed/total, color based on threshold):
       - 0-33%: Color(0xFF424299) (purple equivalent for chart-5)
       - 34-66%: Color(0xFF2563EB) (blue for chart-2)
       - 67-99%: Color(0xFF38BDF8) (sky blue for chart-1)
       - 100%: Color(0xFF388E3C) (green-600)
     - semanticsLabel: "$completed of $total tasks complete"
     - SizedBox(height: 4)
     - Row: contractor name (bodySmall, w600) abbreviated as "John D.", Spacer, last activity relative time (bodySmall, muted text)
     - If contractorName null: "No contractor assigned" (bodySmall, italic)
   - No activity state: "No activity yet" when no tasks exist
   - Complete state: green checkmark icon badge when 100%
   - Wrap entire card in InkWell with Semantics(label: "$tradeName: $completed of $total tasks complete")
   - onTap: navigates to read-only scope task list

2. Update `mobile/lib/features/projects/presentation/screens/project_detail_screen.dart`:
   - Replace existing TradeScopeCard instantiation with TradeProgressCard
   - For each trade scope: pass scopeId, tradeName, tradeColor, contractorName
   - onTap: context.push(RouteNames.tradeScopeDetailPath(projectId, scopeId))
   - Remove the hardcoded `completedTasks: 0, totalTasks: 0` stub
   - Remove the `_showTaskDetailStub()` method if it exists

3. Create `web/src/features/tasks/components/TradeProgressCard.tsx`:
   - Props: `{ tradeName: string, tradeColor: string, completedTasks: number, totalTasks: number, contractorName?: string, lastActivity?: string, onClick: () => void }`
   - Uses shadcn Card, Progress components
   - Layout matching mobile:
     - Row: color dot (w-3 h-3 rounded-full with background-color from tradeColor), trade name (text-base font-semibold), "X/Y tasks" (text-xs text-muted-foreground)
     - Progress bar (shadcn Progress component with value={percentage})
     - Row: contractor name (text-xs), last activity (text-xs text-muted-foreground)
   - Progress bar color thresholds via className:
     - 0-33%: bg-purple-600
     - 34-66%: bg-blue-600
     - 67-99%: bg-sky-500
     - 100%: bg-green-600
   - Complete state: green check icon next to trade name

4. Update `web/src/features/projects/components/ProjectDetail.tsx`:
   - Import TradeProgressCard
   - Replace existing trade scope display with TradeProgressCard components
   - Fetch task counts per scope from API: GET /api/v1/tasks?trade_scope_id={scopeId} and count client-side, OR add a batch endpoint. Simplest: fetch all tasks for the project and group/count client-side
   - Pass real completedTasks, totalTasks, contractorName, lastActivity to each card

5. Ensure shadcn Progress component is installed (should have been done in Plan 04 Task 2). If not: `npx shadcn add progress` from web/.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/projects/presentation/widgets/trade_progress_card.dart lib/features/projects/presentation/screens/project_detail_screen.dart && cd /Users/heechung/AndroidStudioProjects/contractormanagement/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class TradeProgressCard" mobile/lib/features/projects/presentation/widgets/trade_progress_card.dart
    - grep -q "LinearProgressIndicator" mobile/lib/features/projects/presentation/widgets/trade_progress_card.dart
    - grep -q "TradeProgressCard" mobile/lib/features/projects/presentation/screens/project_detail_screen.dart
    - grep -rL "completedTasks: 0, totalTasks: 0" mobile/lib/features/projects/presentation/screens/project_detail_screen.dart (should NOT match — removed)
    - grep -q "TradeProgressCard" web/src/features/tasks/components/TradeProgressCard.tsx
    - grep -q "TradeProgressCard" web/src/features/projects/components/ProjectDetail.tsx
    - dart analyze and tsc --noEmit pass
  </acceptance_criteria>
  <done>TradeProgressCard shows real task counts, progress bar with color thresholds, contractor name, and last activity. ProjectDetailScreen on mobile uses TradeProgressCard instead of stub TradeScopeCard. Web ProjectDetail shows same data via TradeProgressCard component.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 22 E2E tests (mobile + backend)</name>
  <files>
    mobile/test/e2e/phase_22_task_execution_e2e_test.dart
  </files>
  <read_first>
    mobile/test/e2e/phase_21_ai_chat_e2e_test.dart,
    mobile/test/e2e/phase_19_projects_e2e_test.dart,
    mobile/lib/features/projects/presentation/screens/my_tasks_screen.dart,
    mobile/lib/features/projects/presentation/screens/task_detail_screen.dart,
    mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart,
    mobile/lib/features/projects/presentation/widgets/trade_progress_card.dart,
    mobile/lib/features/projects/domain/annotation_schema.dart,
    mobile/lib/features/projects/data/task_note_dao.dart,
    mobile/lib/features/projects/data/task_attachment_dao.dart
  </read_first>
  <action>
Create `mobile/test/e2e/phase_22_task_execution_e2e_test.dart`:

Follow existing E2E test patterns from phase 19/21 tests. Use in-memory Drift DB, mock Dio at plugin level, ProviderScope overrides for all providers.

**Setup:**
- Create in-memory AppDatabase
- Seed: company, user (contractor role), project, trade scope, 3 tasks (one with photo_required=true, one overdue, one blocked)
- Override providers: taskDaoProvider, taskNoteDaoProvider, taskAttachmentDaoProvider, myTasksProvider, etc.
- Mock ImagePicker plugin (returns fake file path)
- Mock FilePicker plugin (returns fake PDF path)
- CRITICAL: Use `pump()` NOT `pumpAndSettle()` for Drift stream assertions

**TASK-01 Tests (Contractor sees tasks grouped by scope):**
- `test('MyTasksScreen shows tasks grouped by trade scope')` — seed tasks across 2 scopes, verify both scope group headers appear, task cards render within correct groups
- `test('MyTasksScreen shows overdue tasks with amber background')` — seed task with past due_date, verify amber background indicator
- `test('MyTasksScreen shows empty state when no tasks')` — no tasks seeded, verify "No tasks assigned" text

**TASK-02 Tests (Checkbox completes task):**
- `test('tapping checkbox marks task complete')` — find checkbox, tap, pump, verify task status changed in DB
- `test('tapping checkbox on completed task marks in_progress')` — seed completed task, tap checkbox, verify status = in_progress
- `test('blocked task has disabled checkbox')` — seed blocked task, verify checkbox is disabled

**TASK-03 Tests (Progress notes):**
- `test('adding a note creates timestamped entry')` — navigate to task detail, enter text in note field, tap add button, pump, verify note appears in list with timestamp

**TASK-04 Tests (Photo capture):**
- `test('photo capture attaches to task')` — mock ImagePicker, tap Add Photo, verify attachment appears in photo grid
- `test('photo gate blocks completion until photo added')` — task with photo_required=true, no photos, verify Mark Done button disabled. Add photo, verify button enabled

**TASK-05 Tests (Annotation):**
- `test('annotation JSON round-trip')` — create AnnotationLayer with arrow + measurement, serialize to JSON, deserialize, verify fields match (unit test, no widget needed)

**TASK-06 Tests (PDF attachment):**
- `test('PDF picker attaches document to task')` — mock FilePicker, tap Add Attachment, verify document appears in attachment list

**TASK-07 Tests (GC progress view):**
- `test('TradeProgressCard shows real task counts')` — seed 3 tasks (2 complete, 1 incomplete), render TradeProgressCard, verify "2/3 tasks" text and progress bar value
- `test('ProjectDetailScreen shows TradeProgressCard for each scope')` — seed project with 2 scopes, render ProjectDetailScreen, verify 2 TradeProgressCard widgets

**Count enforcement tests:**
- `test('photo limit enforced at 10')` — seed 10 photo attachments, verify Add Photo button disabled with tooltip text "Maximum 10 photos per task reached."
- `test('PDF limit enforced at 5')` — seed 5 document attachments, verify add button disabled

Total: minimum 15 tests covering all 7 TASK requirements.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/e2e/phase_22_task_execution_e2e_test.dart -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TASK-01" mobile/test/e2e/phase_22_task_execution_e2e_test.dart
    - grep -q "TASK-02" mobile/test/e2e/phase_22_task_execution_e2e_test.dart
    - grep -q "TASK-03" mobile/test/e2e/phase_22_task_execution_e2e_test.dart
    - grep -q "TASK-04" mobile/test/e2e/phase_22_task_execution_e2e_test.dart
    - grep -q "TASK-05" mobile/test/e2e/phase_22_task_execution_e2e_test.dart
    - grep -q "TASK-06" mobile/test/e2e/phase_22_task_execution_e2e_test.dart
    - grep -q "TASK-07" mobile/test/e2e/phase_22_task_execution_e2e_test.dart
    - grep -c "testWidgets\|test(" mobile/test/e2e/phase_22_task_execution_e2e_test.dart returns >= 15
    - All E2E tests pass
  </acceptance_criteria>
  <done>15+ E2E tests covering all 7 TASK requirements pass. Tests use in-memory Drift DB with pump() (not pumpAndSettle). Photo gate, checkbox completion, notes, photo attachment, annotation round-trip, PDF attachment, and GC progress view all verified.</done>
</task>

</tasks>

<verification>
- `cd mobile && flutter test test/e2e/phase_22_task_execution_e2e_test.dart -v` all pass
- `cd mobile && dart analyze` clean
- `cd web && npx tsc --noEmit` clean
- `cd mobile && flutter test` full suite passes (no regressions)
- `cd backend && uv run python -m pytest tests/integration/test_phase_22_e2e.py -x` all pass
</verification>

<success_criteria>
- GC sees trade progress cards with real counts and color-threshold progress bars
- Web project detail shows same progress data
- 15+ E2E tests covering TASK-01 through TASK-07 all pass
- No regressions in existing test suite
- Phase 22 is feature-complete
</success_criteria>

<output>
After completion, create `.planning/phases/22-task-execution-and-photo-annotation/22-05-SUMMARY.md`
</output>
