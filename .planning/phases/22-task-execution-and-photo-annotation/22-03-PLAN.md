---
phase: 22-task-execution-and-photo-annotation
plan: 03
type: execute
wave: 2
depends_on: [22-02]
files_modified:
  - mobile/lib/features/projects/presentation/screens/my_tasks_screen.dart
  - mobile/lib/features/projects/presentation/screens/task_detail_screen.dart
  - mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart
  - mobile/lib/features/projects/presentation/widgets/task_scope_group_header.dart
  - mobile/lib/features/projects/presentation/widgets/task_note_item.dart
  - mobile/lib/features/projects/presentation/widgets/task_photo_grid.dart
  - mobile/lib/core/routing/app_router.dart
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-06]

must_haves:
  truths:
    - "Contractor sees all incomplete tasks grouped by trade scope on MyTasksScreen"
    - "Tapping checkbox on a task with photo_required=false toggles it to complete"
    - "Tapping camera icon on a photo_required task opens ImagePicker"
    - "TaskDetailScreen shows notes section with add-note TextField and timestamped entries"
    - "TaskDetailScreen shows 3-column photo grid with annotated-photo pencil badge"
    - "TaskDetailScreen shows PDF attachments with tap to open in system viewer"
    - "Photo/PDF count limits enforced in UI (10 photos, 5 PDFs)"
  artifacts:
    - path: "mobile/lib/features/projects/presentation/screens/my_tasks_screen.dart"
      provides: "Cross-scope contractor task checklist"
      contains: "class MyTasksScreen"
    - path: "mobile/lib/features/projects/presentation/screens/task_detail_screen.dart"
      provides: "Full task detail with notes, photos, attachments"
      contains: "class TaskDetailScreen"
    - path: "mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart"
      provides: "Task card with priority border, checkbox/camera gate"
      contains: "class TaskChecklistCard"
  key_links:
    - from: "mobile/lib/features/projects/presentation/screens/my_tasks_screen.dart"
      to: "mobile/lib/features/projects/presentation/providers/project_providers.dart"
      via: "ref.watch(myTasksProvider(userId))"
      pattern: "myTasksProvider"
    - from: "mobile/lib/features/projects/presentation/screens/task_detail_screen.dart"
      to: "mobile/lib/features/projects/presentation/providers/project_providers.dart"
      via: "ref.watch(taskNotesProvider), ref.watch(taskAttachmentsProvider)"
      pattern: "taskNotesProvider|taskAttachmentsProvider"
    - from: "mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart"
      to: "mobile/lib/features/projects/data/task_dao.dart"
      via: "ref.read(taskDaoProvider).updateTask for status toggle"
      pattern: "updateTask"
---

<objective>
Mobile contractor UI: MyTasksScreen (cross-scope checklist with grouped tasks), TaskDetailScreen (notes, photos, PDF attachments), TaskChecklistCard (checkbox with photo gate), and all supporting widgets. Wires into existing GoRouter and providers.

Purpose: Delivers the core contractor task execution workflow — view tasks, complete them, add notes, capture photos, attach PDFs. This is the primary daily tool for contractors.
Output: MyTasksScreen, TaskDetailScreen, TaskChecklistCard, TaskScopeGroupHeader, TaskNoteItem, TaskPhotoGrid, GoRouter registration.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-task-execution-and-photo-annotation/22-CONTEXT.md
@.planning/phases/22-task-execution-and-photo-annotation/22-RESEARCH.md
@.planning/phases/22-task-execution-and-photo-annotation/22-UI-SPEC.md
@.planning/phases/22-task-execution-and-photo-annotation/22-02-SUMMARY.md

<interfaces>
<!-- Providers from Plan 02 -->
```dart
// From project_providers.dart (created in Plan 02)
final myTasksProvider = StreamProvider.autoDispose.family<List<ProjectTask>, String>(...);
final taskNotesProvider = StreamProvider.autoDispose.family<List<TaskNote>, String>(...);
final taskAttachmentsProvider = StreamProvider.autoDispose.family<List<TaskAttachment>, String>(...);
final taskPhotoCountProvider = StreamProvider.autoDispose.family<int, String>(...);
final taskDocCountProvider = StreamProvider.autoDispose.family<int, String>(...);
final taskDaoProvider = Provider<TaskDao>(...);
final taskNoteDaoProvider = Provider<TaskNoteDao>(...);
final taskAttachmentDaoProvider = Provider<TaskAttachmentDao>(...);
```

<!-- Route names from Plan 02 -->
```dart
// From route_names.dart (added in Plan 02)
static const myTasks = '/my-tasks';
static const taskDetail = '/tasks/:taskId';
static const photoAnnotation = '/tasks/:taskId/photos/:attachmentId/annotate';
static String taskDetailPath(String taskId) => '/tasks/$taskId';
static String photoAnnotationPath(String taskId, String attachmentId) => ...;
```

<!-- Existing patterns -->
```dart
// From trade_scope_detail_screen.dart — priority color pattern:
Color _priorityColor(String priority) {
  switch (priority) {
    case 'urgent': return const Color(0xFFD32F2F);
    case 'high': return const Color(0xFFF57C00);
    case 'medium': return const Color(0xFF1565C0);
    case 'low': return const Color(0xFF9E9E9E);
    default: return const Color(0xFF9E9E9E);
  }
}

// From add_note_bottom_sheet.dart — photo capture pattern:
// Uses image_picker (ImagePicker().pickImage) + flutter_image_compress (compressPhoto())
// + file_picker (FilePicker.platform.pickFiles for PDF)
```

<!-- TaskAttachment Drift generated type -->
```dart
// TaskAttachment generated class has:
// id, companyId, taskId, attachmentType, remoteUrl, localPath, caption, sortOrder, annotationData, version, createdAt, updatedAt, deletedAt
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MyTasksScreen with grouped checklist cards</name>
  <files>
    mobile/lib/features/projects/presentation/screens/my_tasks_screen.dart,
    mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart,
    mobile/lib/features/projects/presentation/widgets/task_scope_group_header.dart,
    mobile/lib/core/routing/app_router.dart
  </files>
  <read_first>
    mobile/lib/features/projects/presentation/providers/project_providers.dart,
    mobile/lib/features/projects/presentation/screens/trade_scope_detail_screen.dart,
    mobile/lib/features/projects/data/task_dao.dart,
    mobile/lib/features/projects/data/task_attachment_dao.dart,
    mobile/lib/core/routing/app_router.dart,
    mobile/lib/core/routing/route_names.dart,
    mobile/lib/features/jobs/presentation/widgets/add_note_bottom_sheet.dart,
    mobile/lib/features/auth/presentation/providers/auth_providers.dart
  </read_first>
  <action>
1. Create `mobile/lib/features/projects/presentation/widgets/task_scope_group_header.dart`:
   - StatefulWidget (manages collapsed state in-session, no Drift persistence)
   - Parameters: tradeName (String), completedCount (int), totalCount (int), isCollapsed (bool), onToggle (VoidCallback)
   - Layout: Row with trade name (titleMedium, fontWeight w600), Spacer, "{X}/{Y} complete" label (bodySmall), expand/collapse icon
   - Semantics: `Semantics(label: isCollapsed ? "Expand $tradeName tasks" : "Collapse $tradeName tasks")`

2. Create `mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart`:
   - ConsumerWidget
   - Parameters: task (ProjectTask), onTap (VoidCallback to navigate to detail)
   - Layout: Card with 4px left border using _priorityColor(task.priority)
   - Content Row: Checkbox or Camera Icon (leading) + Column[title (titleMedium), priority Badge, time estimate (bodySmall)] + trailing chevron
   - Checkbox logic:
     - If task.photoRequired == true: watch taskPhotoCountProvider(task.id)
       - If count == 0: show IconButton with Icons.camera_alt, tapping opens ImagePicker (camera source)
       - If count > 0: show regular Checkbox
     - If task.photoRequired == false: show regular Checkbox
   - Checkbox onChanged: call `ref.read(taskDaoProvider).updateTask(task.id, ProjectTasksCompanion(status: Value(task.status == 'complete' ? 'in_progress' : 'complete'), updatedAt: Value(DateTime.now())))`
   - Completed task: title with strikethrough TextDecoration, card opacity 0.7
   - Overdue: if task.dueDate != null && task.dueDate!.isBefore(DateTime.now()) → amber background Color(0xFFFFF8E1)
   - Blocked: if task.status == 'blocked' → lock icon, checkbox disabled
   - Photo-required indicator: small Chip "Photo required" when task.photoRequired == true
   - All touch targets minimum 48px height
   - Camera icon capture: use ImagePicker().pickImage(source: ImageSource.camera), then compressPhoto(), then save to local path, then TaskAttachmentDao.insertAttachment

3. Create `mobile/lib/features/projects/presentation/screens/my_tasks_screen.dart`:
   - ConsumerStatefulWidget
   - AppBar title: "My Tasks" (titleLarge, fontWeight w600)
   - Get current user ID from auth provider (ref.watch(currentUserProvider) or similar)
   - Watch myTasksProvider(userId) for task list
   - Group tasks by tradeScopeId client-side using Map<String, List<ProjectTask>>
   - For each scope group: need trade scope name — add a lookup. Use ref.watch(tradeScopeDaoProvider) or include scope name in a join-based provider. Simplest: create a helper provider that fetches trade scope names by ID from TradeScopeDao.
   - Within each group: filter to show incomplete tasks. Sort by: (1) overdue first, (2) priority order (urgent=0, high=1, medium=2, low=3), (3) dueDate ASC nulls last
   - Overdue tasks within a group: highlighted with amber background
   - Layout: ListView with TaskScopeGroupHeader + TaskChecklistCard items per group
   - Collapsed state: Map<String, bool> in StatefulWidget state
   - Empty state: Center column with "No tasks assigned" heading + "You have no pending tasks..." body text
   - On task card tap: context.push(RouteNames.taskDetailPath(task.id))

4. Register routes in `mobile/lib/core/routing/app_router.dart`:
   - Add GoRoute for RouteNames.myTasks → MyTasksScreen()
   - Add GoRoute for RouteNames.taskDetail with pathParam 'taskId' → TaskDetailScreen(taskId: state.pathParameters['taskId']!)
   - Place myTasks route under authenticated shell (same level as projects)
   - Add a way for contractors to navigate to MyTasksScreen — either a new bottom nav tab item or a button on the projects screen. Use a FloatingActionButton on projects screen labeled "My Tasks" or add myTasks as a tab in the bottom nav for contractor role.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/projects/presentation/screens/my_tasks_screen.dart lib/features/projects/presentation/widgets/task_checklist_card.dart lib/features/projects/presentation/widgets/task_scope_group_header.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class MyTasksScreen" mobile/lib/features/projects/presentation/screens/my_tasks_screen.dart
    - grep -q "myTasksProvider" mobile/lib/features/projects/presentation/screens/my_tasks_screen.dart
    - grep -q "class TaskChecklistCard" mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart
    - grep -q "photoRequired" mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart
    - grep -q "camera_alt" mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart
    - grep -q "class TaskScopeGroupHeader" mobile/lib/features/projects/presentation/widgets/task_scope_group_header.dart
    - grep -q "myTasks" mobile/lib/core/routing/app_router.dart
    - grep -q "taskDetail" mobile/lib/core/routing/app_router.dart
    - dart analyze reports no errors
  </acceptance_criteria>
  <done>MyTasksScreen shows all incomplete tasks grouped by scope with collapsible headers. TaskChecklistCard shows photo gate (camera icon when photo_required + no photo). Checkbox toggles task completion status via Drift + sync queue. Routes registered for contractor navigation.</done>
</task>

<task type="auto">
  <name>Task 2: TaskDetailScreen with notes, photos, and PDF attachments</name>
  <files>
    mobile/lib/features/projects/presentation/screens/task_detail_screen.dart,
    mobile/lib/features/projects/presentation/widgets/task_note_item.dart,
    mobile/lib/features/projects/presentation/widgets/task_photo_grid.dart
  </files>
  <read_first>
    mobile/lib/features/projects/presentation/providers/project_providers.dart,
    mobile/lib/features/projects/data/task_note_dao.dart,
    mobile/lib/features/projects/data/task_attachment_dao.dart,
    mobile/lib/features/jobs/presentation/widgets/add_note_bottom_sheet.dart,
    mobile/lib/features/jobs/presentation/screens/photo_viewer_screen.dart,
    mobile/lib/core/routing/route_names.dart,
    mobile/lib/features/projects/presentation/screens/my_tasks_screen.dart,
    mobile/lib/features/projects/presentation/widgets/task_checklist_card.dart
  </read_first>
  <action>
1. Create `mobile/lib/features/projects/presentation/widgets/task_note_item.dart`:
   - StatelessWidget
   - Parameters: note (TaskNote object — has body, authorId, createdAt)
   - Layout: Column with Row[author name (bodySmall, w600), Spacer, relative timestamp (bodySmall, muted)] + body text (bodyMedium)
   - Timestamp: use relative time — "2h ago", "Yesterday", or date string for older
   - Immutable — no edit/delete buttons (per D-11)

2. Create `mobile/lib/features/projects/presentation/widgets/task_photo_grid.dart`:
   - ConsumerWidget
   - Parameters: taskId (String)
   - Watch taskAttachmentsProvider(taskId), filter to attachmentType == 'photo'
   - Layout: GridView.builder with crossAxisCount: 3, childAspectRatio: 1.0, mainAxisSpacing: 8, crossAxisSpacing: 8
   - Each cell: Stack with ClipRRect photo thumbnail (from localPath or remoteUrl) + positioned pencil badge if annotationData != null (Positioned bottom-right, Icon(Icons.edit, size: 16) in CircleAvatar)
   - Tap thumbnail: navigate to full-screen photo viewer with "Annotate" button
     - Push to photo viewer (existing PhotoViewer pattern or new route)
     - "Annotate" button: context.push(RouteNames.photoAnnotationPath(taskId, attachment.id))
   - Empty state: "No photos yet. Tap 'Add Photo' to document your progress."

3. Create `mobile/lib/features/projects/presentation/screens/task_detail_screen.dart`:
   - ConsumerStatefulWidget
   - Parameter: taskId (String)
   - AppBar: task title (truncated to 1 line, titleLarge)
   - Body: CustomScrollView with SliverToBoxAdapter sections:

   **Section 1 — Header:**
   - Task title (titleMedium), status badge (Chip with status color), priority badge (colored Chip)
   - If photo_required: "Photo required" indicator chip

   **Section 2 — Details:**
   - Description text (bodyMedium) if not null
   - Estimated hours + estimated cost if not null
   - Materials needed list if not empty (from task.materialsNeeded JSONB — rendered as bullet list)

   **Section 3 — Notes:**
   - Header: "Notes" (titleMedium, w600)
   - Inline TextField with placeholder "Add a progress note..." and IconButton "+" to submit
   - On submit: create TaskNotesCompanion with companyId from auth, taskId, authorId from auth, body from controller, createdAt/updatedAt = DateTime.now(). Call ref.read(taskNoteDaoProvider).insertNote(...)
   - Clear TextField after submit
   - Below input: watch taskNotesProvider(taskId), render as ListView of TaskNoteItem widgets
   - Empty state: "No notes yet. Tap to add a progress note."

   **Section 4 — Photos:**
   - Header: "Photos ({count})" (titleMedium, w600)
   - TaskPhotoGrid(taskId: taskId)
   - "Add Photo" button at top of section — offers camera or gallery via showModalBottomSheet with two ListTiles
   - On capture/select: compress via compressPhoto() (from existing utility), save to local path, then TaskAttachmentDao.insertAttachment with attachmentType='photo'
   - Disable "Add Photo" when photo count >= 10 with tooltip "Maximum 10 photos per task reached."

   **Section 5 — Attachments:**
   - Header: "Attachments ({count})" (titleMedium, w600)
   - Watch taskAttachmentsProvider(taskId), filter to attachmentType == 'document'
   - ListView of ListTile: leading Icon(Icons.picture_as_pdf), title = filename (truncated 40 chars), subtitle = file size
   - Tap: launch in system viewer. Use `url_launcher` package — add to pubspec.yaml if not present. `launchUrl(Uri.file(attachment.localPath!))` for local files
   - "Add Attachment" button — opens FilePicker for PDF. On select: copy to app directory, insert via TaskAttachmentDao with attachmentType='document'
   - Disable when doc count >= 5 with "Maximum 5 attachments per task reached."

   **Bottom Bar:**
   - BottomAppBar with two buttons:
     - Left: "Add Photo" OutlinedButton (secondary action) — same picker as Section 4
     - Right: "Mark Done" ElevatedButton (primary, accent color) when task incomplete
       - If task.photoRequired and photo count == 0: disabled with text "Add photo first", "Add Photo" button has pulsing animation
       - When task complete: show "Mark Incomplete" OutlinedButton instead
   - Minimum 48px height buttons
   - "Mark Done" calls taskDaoProvider.updateTask(taskId, status: 'complete')
   - "Mark Incomplete" calls taskDaoProvider.updateTask(taskId, status: 'in_progress')
   - Semantics labels on all buttons per UI-SPEC

4. Add `url_launcher` to `mobile/pubspec.yaml` if not already present (check first — research said it's not there).
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/projects/presentation/screens/task_detail_screen.dart lib/features/projects/presentation/widgets/task_note_item.dart lib/features/projects/presentation/widgets/task_photo_grid.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class TaskDetailScreen" mobile/lib/features/projects/presentation/screens/task_detail_screen.dart
    - grep -q "taskNotesProvider" mobile/lib/features/projects/presentation/screens/task_detail_screen.dart
    - grep -q "taskAttachmentsProvider" mobile/lib/features/projects/presentation/screens/task_detail_screen.dart
    - grep -q "Mark Done" mobile/lib/features/projects/presentation/screens/task_detail_screen.dart
    - grep -q "Add Photo" mobile/lib/features/projects/presentation/screens/task_detail_screen.dart
    - grep -q "class TaskNoteItem" mobile/lib/features/projects/presentation/widgets/task_note_item.dart
    - grep -q "class TaskPhotoGrid" mobile/lib/features/projects/presentation/widgets/task_photo_grid.dart
    - grep -q "annotationData" mobile/lib/features/projects/presentation/widgets/task_photo_grid.dart
    - grep -q "url_launcher" mobile/pubspec.yaml
    - dart analyze reports no errors
  </acceptance_criteria>
  <done>TaskDetailScreen shows full task detail with notes section (add+list), 3-column photo grid with annotation badges, PDF attachments with system viewer launch. Bottom bar has "Add Photo" + "Mark Done"/"Mark Incomplete" with photo gate enforcement. All count limits enforced in UI.</done>
</task>

</tasks>

<verification>
- `cd mobile && dart analyze` clean
- `cd mobile && flutter test` existing tests still pass (no regressions)
- MyTasksScreen, TaskDetailScreen, TaskChecklistCard, TaskScopeGroupHeader, TaskNoteItem, TaskPhotoGrid all exist and compile
</verification>

<success_criteria>
- Contractor can see MyTasksScreen with all incomplete tasks grouped by trade scope
- Contractor can toggle task completion via checkbox
- Photo gate prevents completion when photo_required=true and no photos attached
- TaskDetailScreen shows notes, photos, PDF attachments with add/view capabilities
- Photo limit (10) and PDF limit (5) enforced in UI
- All navigation routes registered and working
</success_criteria>

<output>
After completion, create `.planning/phases/22-task-execution-and-photo-annotation/22-03-SUMMARY.md`
</output>
