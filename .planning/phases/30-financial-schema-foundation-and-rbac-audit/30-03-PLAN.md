---
phase: 30-financial-schema-foundation-and-rbac-audit
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - backend/app/core/finance_scrub.py
  - backend/app/features/dashboard/service.py
  - backend/app/features/dashboard/router.py
  - backend/tests/unit/test_finance_scrub.py
autonomous: true
requirements: [FINSEC-04]
must_haves:
  truths:
    - "A shared finance-scrub helper exists that strips finance-only fields from a dict when the caller lacks finance access, and is a no-op when access is granted"
    - "The dashboard alerts endpoint filters out financial alert types for users without finance.view, and returns all alerts for users with it"
    - "The alert filter is provably inert today (FINANCIAL_ALERT_TYPES is empty) but ready for Phase 36 to populate"
  artifacts:
    - path: "backend/app/core/finance_scrub.py"
      provides: "FINANCE_FIELD_NAMES constant + scrub_finance_fields helper"
      contains: "def scrub_finance_fields"
    - path: "backend/app/features/dashboard/service.py"
      provides: "FINANCIAL_ALERT_TYPES + permission-aware get_alerts"
      contains: "FINANCIAL_ALERT_TYPES"
    - path: "backend/tests/unit/test_finance_scrub.py"
      provides: "Unit coverage of the scrub helper"
  key_links:
    - from: "dashboard/router.py get_alerts"
      to: "DashboardService.get_alerts(has_finance_view=...)"
      via: "effective_permissions(current_user, db) membership check for finance.view"
      pattern: "has_finance_view"
---

<objective>
Ship the two pieces of reusable authorization plumbing that FINSEC-04 requires and that
Phases 34/36 will consume rather than rebuild: (1) a standalone finance-field-scrubbing
helper for AI tool/prompt context, and (2) a permission-aware filter in the dashboard
alerts endpoint. Both ship with the plumbing inert-but-ready today (nothing to strip yet,
no financial alert types exist yet) plus unit coverage — the cross-surface leak tripwires
live in plan 04's phase E2E suite.

Purpose: FINSEC-04 — the audit's shared plumbing. Per 30-RESEARCH.md Open Question 3, the
finance_scrub helper ships as a tested utility only and is deliberately NOT wired into the
ChecklistService/DashboardService dict-builders this phase (nothing to strip → wiring it
in now would be dead code per CLAUDE.md).
Output: finance_scrub.py, dashboard service+router changes, and scrub unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-CONTEXT.md
@.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-RESEARCH.md

<interfaces>
Current dashboard alerts path:
- backend/app/features/dashboard/service.py:730 —
  `async def get_alerts(self, project_id: uuid.UUID | None = None) -> list[DashboardAlert]:`
  body: if project_id → repository.get_for_project(project_id); else
  repository.get_unread_for_company().
- backend/app/features/dashboard/router.py:85 —
  `@router.get("/alerts", response_model=list[AlertResponse])` with
  `db: AsyncSession = Depends(get_db)`, `current_user: CurrentUser = Depends(get_current_user)`;
  body builds `svc = DashboardService(db)`, `alerts = await svc.get_alerts(project_id=project_id)`,
  returns `[AlertResponse.model_validate(a) for a in alerts]`.
- DashboardAlert has an `alert_type` column (existing types:
  'schedule_slip','rescheduling_suggestion','dependency_risk').
- `effective_permissions(current_user: CurrentUser, db: AsyncSession) -> set[str]`
  lives in backend/app/core/security.py:212 — single source of truth for granted keys.

AI dict-builders confirmed to contain NO finance fields today (audit targets, not modified
this phase): ChecklistService._build_user_content_from_dict (checklists/service.py),
DashboardService._build_slip_content_from_dict (dashboard/service.py).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: finance_scrub helper + permission-aware dashboard alert filter</name>
  <files>backend/app/core/finance_scrub.py, backend/app/features/dashboard/service.py, backend/app/features/dashboard/router.py</files>
  <read_first>
    - backend/app/features/dashboard/service.py (get_alerts at line 730; existing alert_type constants like SCHEDULE_SLIP_ALERT_TYPE)
    - backend/app/features/dashboard/router.py (get_alerts endpoint at line 85; imports of CurrentUser/get_db)
    - backend/app/core/security.py (effective_permissions signature, line 212)
    - .planning/phases/30-financial-schema-foundation-and-rbac-audit/30-RESEARCH.md (Code Examples 3 and 4 — exact helper + filter shape)
  </read_first>
  <behavior>
    - scrub_finance_fields(context, has_finance_access=True) returns the dict unchanged (identity no-op)
    - scrub_finance_fields(context, has_finance_access=False) returns a new dict with every key in FINANCE_FIELD_NAMES removed, other keys preserved
    - get_alerts(..., has_finance_view=False) drops alerts whose alert_type is in FINANCIAL_ALERT_TYPES; with has_finance_view=True returns all
    - Because FINANCIAL_ALERT_TYPES is empty today, the filter is a provable no-op right now
  </behavior>
  <action>
    1. Create backend/app/core/finance_scrub.py exactly per 30-RESEARCH.md Code Example 3:
       - `FINANCE_FIELD_NAMES: frozenset[str] = frozenset({"cost","actual_cost","margin",
         "margin_pct","budget","budget_status","hourly_cost","hourly_rate","labor_cost"})`
         (named module constant — no inline literals).
       - `def scrub_finance_fields(context: dict[str, object], has_finance_access: bool)
         -> dict[str, object]:` — if has_finance_access: return context; else
         `return {k: v for k, v in context.items() if k not in FINANCE_FIELD_NAMES}`.
         Keep the function ≤ ~10 lines; docstring notes it is shallow (callers with nested
         dicts must flatten first) — do NOT recurse.

    2. In backend/app/features/dashboard/service.py, add a module-level
       `FINANCIAL_ALERT_TYPES: frozenset[str] = frozenset()` constant (empty today;
       docstring/comment: "populated by Phase 36 — margin_erosion etc."). Change
       get_alerts to accept a keyword-only `has_finance_view: bool = False`:
       ```python
       async def get_alerts(self, project_id=None, *, has_finance_view: bool = False) -> list[DashboardAlert]:
           alerts = (await self.repository.get_for_project(project_id)
                     if project_id is not None
                     else await self.repository.get_unread_for_company())
           if has_finance_view:
               return alerts
           return [a for a in alerts if a.alert_type not in FINANCIAL_ALERT_TYPES]
       ```

    3. In backend/app/features/dashboard/router.py get_alerts endpoint, resolve granted
       permissions and pass the flag through:
       ```python
       from app.core.security import effective_permissions  # add if missing
       granted = await effective_permissions(current_user, db)
       alerts = await svc.get_alerts(project_id=project_id, has_finance_view="finance.view" in granted)
       ```
       Keep the endpoint thin. Do NOT change AlertResponse or the alert_type CHECK
       constraint. Do NOT wire finance_scrub into checklists/service.py or the AI
       dict-builders — nothing to strip yet (per Open Question 3).
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/unit/test_finance_scrub.py -q && python -c "import app.features.dashboard.service, app.features.dashboard.router"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/app/core/finance_scrub.py exists
    - `grep -n "FINANCE_FIELD_NAMES: frozenset\[str\]" backend/app/core/finance_scrub.py` returns a match
    - `grep -n "def scrub_finance_fields" backend/app/core/finance_scrub.py` returns a match
    - `grep -n "FINANCIAL_ALERT_TYPES: frozenset\[str\] = frozenset()" backend/app/features/dashboard/service.py` returns a match
    - `grep -n "has_finance_view" backend/app/features/dashboard/service.py` returns a match
    - `grep -n 'has_finance_view="finance.view" in granted' backend/app/features/dashboard/router.py` returns a match
    - `grep -c "scrub_finance_fields" backend/app/features/checklists/service.py` returns 0 (helper intentionally NOT wired in)
    - `cd backend && source .venv/bin/activate && python -c "import app.features.dashboard.service, app.features.dashboard.router, app.core.finance_scrub"` exits 0
  </acceptance_criteria>
  <done>finance_scrub helper exists; dashboard alerts endpoint filters FINANCIAL_ALERT_TYPES by finance.view (inert today); helper not prematurely wired into AI surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for the finance-scrub helper and the empty-set filter contract</name>
  <files>backend/tests/unit/test_finance_scrub.py</files>
  <read_first>
    - backend/app/core/finance_scrub.py (the helper under test)
    - backend/app/features/dashboard/service.py (FINANCIAL_ALERT_TYPES contract)
  </read_first>
  <action>
    Create backend/tests/unit/test_finance_scrub.py — pure unit (no DB). Import
    scrub_finance_fields and FINANCE_FIELD_NAMES from app.core.finance_scrub, and
    FINANCIAL_ALERT_TYPES from app.features.dashboard.service.

    - test_scrub_is_noop_with_access: given a dict containing both finance keys (e.g.
      {"cost": 5, "margin": 0.2}) and non-finance keys (e.g. {"title": "x"}),
      scrub_finance_fields(d, True) returns the same object / equal dict unchanged.
    - test_scrub_removes_finance_keys_without_access: scrub_finance_fields(d, False)
      returns a dict with all FINANCE_FIELD_NAMES keys removed and non-finance keys
      preserved. Assert result == {"title": "x"} for the example above.
    - test_scrub_removes_every_declared_finance_field: build a dict with one entry per key
      in FINANCE_FIELD_NAMES plus one safe key; assert without-access output == {safe key}.
    - test_scrub_does_not_mutate_input: assert the input dict is unchanged after a
      without-access call (helper returns a new dict).
    - test_financial_alert_types_currently_empty: assert FINANCIAL_ALERT_TYPES == frozenset()
      (documents the "inert today" contract; Phase 36 will update this test when it
      populates the set).
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/unit/test_finance_scrub.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/unit/test_finance_scrub.py exists
    - `grep -c "def test_" backend/tests/unit/test_finance_scrub.py` returns 5 or more
    - `grep -n "test_financial_alert_types_currently_empty" backend/tests/unit/test_finance_scrub.py` returns a match
    - `cd backend && source .venv/bin/activate && python -m pytest tests/unit/test_finance_scrub.py -q` exits 0
    - `cd backend && ruff check app/core/finance_scrub.py app/features/dashboard tests/unit/test_finance_scrub.py` reports no errors
  </acceptance_criteria>
  <done>Scrub helper no-op/strip behavior and the empty-alert-set contract are covered by green pure-unit tests.</done>
</task>

</tasks>

<verification>
- `cd backend && source .venv/bin/activate && python -m pytest tests/unit/test_finance_scrub.py -q` green.
- `cd backend && source .venv/bin/activate && python -m pytest tests/integration -q -k dashboard` still green (get_alerts signature change is backward-compatible: has_finance_view defaults False; existing callers unaffected).
- `cd backend && ruff check app tests && ruff format --check app` clean for touched files.
</verification>

<success_criteria>
- finance_scrub.py exists with FINANCE_FIELD_NAMES + scrub_finance_fields (no-op with access, strip without).
- DashboardService.get_alerts filters FINANCIAL_ALERT_TYPES by has_finance_view; router resolves it from effective_permissions.
- The filter is provably inert today (empty set) and ready for Phase 36.
- The helper is NOT wired into AI/checklist dict-builders (no dead code); plan 04 ships the leak tripwires.
</success_criteria>

<output>
After completion, create `.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-03-SUMMARY.md`
</output>
