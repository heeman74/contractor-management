---
phase: 30-financial-schema-foundation-and-rbac-audit
plan: 04
type: execute
wave: 3
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - backend/tests/test_phase_30_e2e.py
autonomous: true
requirements: [FINSEC-01, FINSEC-02, FINSEC-03, FINSEC-04]
must_haves:
  truths:
    - "Existing (pre-migration) companies have their project_manager row backfilled with finance keys; admin rows stay untouched"
    - "Each company has exactly 4 is_system cost categories after migration, and re-seeding is idempotent"
    - "The new financial tables enforce RLS: tenant B cannot read tenant A's cost/budget rows"
    - "GET /reports/dashboard returns no finance field names (D-06 tripwire)"
    - "GET /dashboard/alerts hides financial alert types from users without finance.view once the set is populated"
    - "The AI/checklist context dict-builders emit no finance field names today"
  artifacts:
    - path: "backend/tests/test_phase_30_e2e.py"
      provides: "Full FINSEC-01..04 integration + leak-tripwire suite (extends plan 01's file)"
      contains: "def test_existing_company_backfilled_with_finance_defaults"
  key_links:
    - from: "test_phase_30_e2e.py"
      to: "migration 0032 backfill + finance schema + dashboard filter"
      via: "integration assertions over seeded tenants"
      pattern: "FINANCE_FIELD_NAMES"
---

<objective>
Complete the Phase 30 authorization/audit contract by filling in the remaining integration
tests in test_phase_30_e2e.py (created by plan 01). These are the phase-gate tests that
tie the schema (plan 02) and audit plumbing (plan 03) to the FINSEC success criteria: the
existing-company backfill, the cost-category seed + idempotency, RLS isolation on the new
tables, and the three legacy-surface leak tripwires (reports response, dashboard-alert
filter, AI context builders).

Purpose: FINSEC-01/02/03/04 phase gate — converts the "audit" requirement into enforced,
automated contracts and proves the migration backfill actually reached existing companies.
Output: extended backend/tests/test_phase_30_e2e.py; full backend suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-CONTEXT.md
@.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-RESEARCH.md
@.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-VALIDATION.md

<interfaces>
Existing test scaffold (from plan 01, backend/tests/test_phase_30_e2e.py): shared
`_token(company_id, roles)` helper, `FINANCE_KEYS` constant, and 3 RBAC tests
(new-company-seeded, admin-exclusion, owner-grant-to-custom-role). This plan ADDS
functions to that file — do not duplicate the helper.

From plan 02:
- Migration 0032 seeds cost_categories (4 is_system rows/company) and backfills
  project_manager rows via per-company `SET LOCAL app.current_company_id` loop with the
  idempotency guard `NOT (permissions @> '["finance.view"]'::jsonb)`.
- finance models: CostEntry (cost_entries), Budget (budgets), CostCategory (cost_categories).

From plan 03:
- `FINANCIAL_ALERT_TYPES: frozenset[str]` in app.features.dashboard.service (empty today —
  monkeypatch it to a non-empty set in the alert-filter test).
- `FINANCE_FIELD_NAMES` in app.core.finance_scrub (reuse for the reports/AI tripwires).

Fixtures (conftest.py): seed_two_tenants, tenant_a_client, tenant_b_client. RLS-isolation
convention (SKILL.md): tenant B reaching tenant A's row must get 404/empty. Reports
endpoints: GET /api/v1/reports/dashboard (require_admin, revenue-only per D-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration-effect tests — PM backfill, category seed + idempotency, RLS isolation</name>
  <files>backend/tests/test_phase_30_e2e.py</files>
  <read_first>
    - backend/tests/test_phase_30_e2e.py (plan 01's scaffold — reuse _token + FINANCE_KEYS)
    - backend/migrations/versions/0032_financial_schema_and_rbac.py (backfill loop + seed SQL to assert against)
    - backend/tests/conftest.py (seed_two_tenants, tenant clients; how the test DB is migrated)
    - .claude/skills/e2e-feature-tests/SKILL.md (RLS-isolation test shape)
  </read_first>
  <action>
    Add to backend/tests/test_phase_30_e2e.py:

    - test_existing_company_backfilled_with_finance_defaults: simulate a pre-migration
      company by seeding a company row + a company_role_permissions project_manager row
      whose permissions list LACKS the finance keys (write it directly with tenant context
      set via `SET LOCAL app.current_company_id`), plus an admin row with a known non-finance
      list. Then execute the SAME backfill logic the migration uses (per-company
      `SET LOCAL app.current_company_id = '<cid>'` then the guarded
      `UPDATE ... SET permissions = permissions || '[...]'::jsonb WHERE role='project_manager'
      AND NOT (permissions @> '["finance.view"]'::jsonb)`). Assert: the PM row now contains
      all three FINANCE_KEYS; the admin row is byte-for-byte unchanged; running the same
      UPDATE a second time changes nothing (idempotency).

    - test_cost_categories_seeded_per_company: for a seeded tenant, query cost_categories
      (with tenant context) and assert exactly 4 rows exist with is_system=true and names
      {"labor","materials","subcontractor","other"}. Assert re-running the seed INSERT
      (`... ON CONFLICT (company_id, name) DO NOTHING`) leaves the count at 4 (idempotent).

    - test_cost_entry_rls_isolation: as tenant A, insert a cost_entries row (anchored to a
      tenant-A job or trade scope, category = tenant A's 'materials' category) with tenant A
      context set. As tenant B (tenant B's company context), SELECT that row id → expect 0
      rows (RLS blocks cross-tenant read). Mirror the SKILL.md isolation pattern. Do the
      same spot-check for one budgets row (tenant B cannot see tenant A's budget).

    Use raw SQL via the async session for direct table assertions (no CRUD endpoints exist
    this phase), setting `SET LOCAL app.current_company_id` before tenant-scoped reads/writes,
    exactly as scripts/seed_data.py does. Keep helpers small and named.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_30_e2e.py -q -k "backfilled or seeded or rls"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_existing_company_backfilled_with_finance_defaults" backend/tests/test_phase_30_e2e.py` returns a match
    - `grep -n "def test_cost_categories_seeded_per_company" backend/tests/test_phase_30_e2e.py` returns a match
    - `grep -n "def test_cost_entry_rls_isolation" backend/tests/test_phase_30_e2e.py` returns a match
    - `grep -n "SET LOCAL app.current_company_id" backend/tests/test_phase_30_e2e.py` returns a match
    - `cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_30_e2e.py -q -k "backfilled or seeded or rls"` exits 0
  </acceptance_criteria>
  <done>Backfill reach, category seed + idempotency, and new-table RLS isolation are proven by green integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Legacy-surface leak tripwires — reports, dashboard-alert filter, AI context</name>
  <files>backend/tests/test_phase_30_e2e.py</files>
  <read_first>
    - backend/tests/test_phase_30_e2e.py (append; reuse _token/FINANCE_KEYS)
    - backend/app/core/finance_scrub.py (FINANCE_FIELD_NAMES — the leak field set to assert against)
    - backend/app/features/dashboard/service.py (FINANCIAL_ALERT_TYPES to monkeypatch; get_alerts filter)
    - backend/app/features/reports/schemas.py (DashboardResponse fields — revenue-only today)
    - backend/app/features/checklists/service.py + backend/app/features/dashboard/service.py (the AI dict-builders to assert emit no finance fields)
  </read_first>
  <action>
    Add to backend/tests/test_phase_30_e2e.py, importing FINANCE_FIELD_NAMES from
    app.core.finance_scrub and FINANCIAL_ALERT_TYPES from app.features.dashboard.service:

    - test_reports_dashboard_leaks_no_finance_fields: as an admin token, GET
      /api/v1/reports/dashboard → 200. Recursively collect all dict keys in the JSON
      response and assert the set is disjoint from FINANCE_FIELD_NAMES. This is the D-06
      tripwire: it fails loudly if a future phase adds a cost/margin field to the wrong
      (admin-gated, non-finance) endpoint.

    - test_dashboard_alerts_filtered_by_finance_permission: monkeypatch
      app.features.dashboard.service.FINANCIAL_ALERT_TYPES to a non-empty set containing a
      test alert type (e.g. {"margin_erosion"}). Seed one alert of that type and one normal
      alert (e.g. schedule_slip) for a tenant. GET /dashboard/alerts as a user WITHOUT
      finance.view (e.g. a gc token) → the margin_erosion alert is absent, the schedule_slip
      alert is present. GET as a user WITH finance.view (owner/PM token) → both present.
      (If seeding an alert row requires an allowed alert_type per the CHECK constraint, seed
      it directly and skip the type check, or use an existing allowed type as the "financial"
      one purely for this monkeypatched test — document the choice inline.)

    - test_ai_context_builders_leak_no_finance_fields: invoke (or construct inputs for)
      ChecklistService's and DashboardService's dict-builder methods
      (_build_user_content_from_dict / _build_slip_content_from_dict) with representative
      data and assert the produced dict's keys are disjoint from FINANCE_FIELD_NAMES. This
      is a tripwire proving today's AI context carries no finance fields (nothing to strip
      yet); it will fail loudly if a later phase adds finance data to these builders without
      routing through scrub_finance_fields.

    After adding all tests, run the FULL phase file and then the affected layer's broader
    suite per SKILL.md.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_30_e2e.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_reports_dashboard_leaks_no_finance_fields" backend/tests/test_phase_30_e2e.py` returns a match
    - `grep -n "def test_dashboard_alerts_filtered_by_finance_permission" backend/tests/test_phase_30_e2e.py` returns a match
    - `grep -n "def test_ai_context_builders_leak_no_finance_fields" backend/tests/test_phase_30_e2e.py` returns a match
    - `grep -n "FINANCE_FIELD_NAMES" backend/tests/test_phase_30_e2e.py` returns a match
    - `grep -n "FINANCIAL_ALERT_TYPES" backend/tests/test_phase_30_e2e.py` returns a match (monkeypatched)
    - `cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_30_e2e.py -q` exits 0
    - `cd backend && ruff check tests/test_phase_30_e2e.py` reports no errors
  </acceptance_criteria>
  <done>Reports response, dashboard-alert filter, and AI context builders each have a green leak-tripwire test; FINSEC-04 is enforced, not manually inspected.</done>
</task>

</tasks>

<verification>
- `cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_30_e2e.py -q` — all Phase 30 integration tests green.
- `cd backend && source .venv/bin/activate && python -m pytest -q` — FULL backend suite green (per SKILL.md: run the layer's full suite at the end so the catalog growth to 51 keys and the dashboard get_alerts signature change didn't break a sibling test; specifically confirm tests/integration/test_role_permissions.py still passes).
- `cd backend && ruff check tests/test_phase_30_e2e.py && ruff format --check tests/test_phase_30_e2e.py` clean.
</verification>

<success_criteria>
- Existing-company PM backfill reach + idempotency proven; admin rows untouched.
- 4 is_system categories per company + idempotent re-seed proven.
- New financial tables enforce cross-tenant RLS isolation.
- Reports response, dashboard-alert filter, and AI context builders each pass a finance-leak tripwire.
- Full backend suite is green (no regression from the 3 new catalog keys or the alerts signature change).
</success_criteria>

<output>
After completion, create `.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-04-SUMMARY.md`
</output>
