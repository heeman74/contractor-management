---
phase: 30-financial-schema-foundation-and-rbac-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/permissions.py
  - backend/tests/unit/test_permissions_finance_keys.py
  - backend/tests/test_phase_30_e2e.py
autonomous: true
requirements: [FINSEC-01, FINSEC-02, FINSEC-03]
must_haves:
  truths:
    - "The permission catalog exposes three finance.* keys in a 'Finance' group so they render as toggles in the existing Roles & Permissions matrix"
    - "owner and project_manager hold all three finance keys by default; no other role does"
    - "The admin role's derived permission set contains zero finance.* keys, proven by an automated test"
    - "An owner can grant finance.view to a non-default role via the existing matrix endpoint and it takes effect immediately"
  artifacts:
    - path: "backend/app/core/permissions.py"
      provides: "3 finance catalog entries + _FINANCE_ONLY_KEYS exclusion + project_manager defaults"
      contains: "_FINANCE_ONLY_KEYS"
    - path: "backend/tests/unit/test_permissions_finance_keys.py"
      provides: "Pure-unit admin-exclusion + PM-inclusion regression"
    - path: "backend/tests/test_phase_30_e2e.py"
      provides: "Phase E2E scaffold + shared token helper + RBAC integration tests"
  key_links:
    - from: "PERMISSION_CATALOG (permissions.py)"
      to: "_ADMIN_KEYS derivation"
      via: "set subtraction of _FINANCE_ONLY_KEYS"
      pattern: "PERMISSION_KEYS - set\\(_OWNER_ONLY_KEYS\\) - set\\(_FINANCE_ONLY_KEYS\\)"
---

<objective>
Extend the permission catalog with the three finance.* keys and make the admin role's
derived permission set exclude them by construction, verified by an automated regression
test. This is the foundational plan every other Phase 30 plan and later financial phase
depends on — the exact key strings defined here are the contract.

Purpose: Satisfies FINSEC-01 (finance.* granted only to owner + project_manager by
default), FINSEC-02 (keys appear in the generic matrix so companies can adjust grants),
and FINSEC-03 (admin explicitly excluded, proven by test not inspection).
Output: Edited permissions.py, a pure-unit regression test, and the phase E2E test file
scaffold (shared token helper + the RBAC integration tests that pass with permissions
alone). Downstream plans (02, 03) create their own test files; plan 04 fills in the rest
of test_phase_30_e2e.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-CONTEXT.md
@.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-RESEARCH.md
@.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-UI-SPEC.md

<interfaces>
Current permissions.py structure (backend/app/core/permissions.py):
- `WILDCARD = "*"` (line 15)
- `_OWNER_ONLY_KEYS = ("company.settings.manage", "company.billing.manage")` (line 19)
- `PERMISSION_CATALOG: list[dict[str, str]]` — 48 entries, each `{"key","label","group"}`.
  Docstring: "Order drives UI grouping." Last entry today is
  `{"key": "portal.access", "label": "Access client portal", "group": "Portal"}`.
- `PERMISSION_KEYS: frozenset[str] = frozenset(entry["key"] for entry in PERMISSION_CATALOG)`
- `_ADMIN_KEYS: list[str] = sorted(PERMISSION_KEYS - set(_OWNER_ONLY_KEYS))` (line 77)
- `DEFAULT_ROLE_PERMISSIONS: dict[str, list[str]]` with keys owner/admin/project_manager/
  gc/foreman/contractor/worker/client. `"owner": [WILDCARD]`, `"admin": _ADMIN_KEYS`,
  `"project_manager"` is a hand-maintained literal list ending with `"tasks.delete",`.

Test helper to reuse (backend/tests/integration/test_role_permissions.py): a `_token(...)`
helper that mints a role JWT via `create_access_token(uuid4(), company_id, [role])`.
Fixtures available from conftest.py: `seed_two_tenants`, `tenant_a_client`, `tenant_b_client`.
`effective_permissions(current_user, db) -> set[str]` lives in app/core/security.py:212.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add finance.* keys to the catalog and exclude them from admin</name>
  <files>backend/app/core/permissions.py</files>
  <read_first>
    - backend/app/core/permissions.py (see current catalog, _OWNER_ONLY_KEYS, _ADMIN_KEYS, DEFAULT_ROLE_PERMISSIONS)
    - .planning/phases/30-financial-schema-foundation-and-rbac-audit/30-UI-SPEC.md (Copywriting Contract — exact labels + group placement)
  </read_first>
  <behavior>
    - `_ADMIN_KEYS` (and thus `DEFAULT_ROLE_PERMISSIONS["admin"]`) contains none of finance.view / finance.manage / finance.rates.manage
    - `DEFAULT_ROLE_PERMISSIONS["project_manager"]` contains all three finance keys
    - `PERMISSION_KEYS` contains all three finance keys
    - The three catalog entries carry `"group": "Finance"` and appear after the "Portal" entry (last)
    - owner is unchanged (still `[WILDCARD]`)
  </behavior>
  <action>
    Edit backend/app/core/permissions.py:

    1. Below the existing `_OWNER_ONLY_KEYS` line (line 19) add:
       `_FINANCE_ONLY_KEYS = ("finance.view", "finance.manage", "finance.rates.manage")`

    2. Append three entries to the END of `PERMISSION_CATALOG` (after the
       `portal.access` entry so "Finance" renders as the final group — per UI-SPEC
       which requires the group appended last, below "Portal"). Use these EXACT
       label strings from 30-UI-SPEC.md's Copywriting Contract:
         {"key": "finance.view", "label": "View costs, margins & budgets", "group": "Finance"},
         {"key": "finance.manage", "label": "Manage costs & budgets", "group": "Finance"},
         {"key": "finance.rates.manage", "label": "Manage labor pay rates", "group": "Finance"},

    3. Change the `_ADMIN_KEYS` derivation (line 77) to also subtract the finance set:
       `_ADMIN_KEYS: list[str] = sorted(PERMISSION_KEYS - set(_OWNER_ONLY_KEYS) - set(_FINANCE_ONLY_KEYS))`

    4. In `DEFAULT_ROLE_PERMISSIONS["project_manager"]`, append the three keys after
       the existing final `"tasks.delete",` entry:
         "finance.view",
         "finance.manage",
         "finance.rates.manage",

    Do NOT touch owner (wildcard already covers finance), admin, gc, foreman,
    contractor, worker, or client role lists. Do NOT add a DB CHECK constraint or any
    endpoint here — this task is catalog-only.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/unit/test_permissions_finance_keys.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '_FINANCE_ONLY_KEYS = ("finance.view", "finance.manage", "finance.rates.manage")' backend/app/core/permissions.py` returns a match
    - `grep -n 'PERMISSION_KEYS - set(_OWNER_ONLY_KEYS) - set(_FINANCE_ONLY_KEYS)' backend/app/core/permissions.py` returns a match
    - `grep -c '"group": "Finance"' backend/app/core/permissions.py` returns 3
    - `grep -n '"label": "View costs, margins & budgets"' backend/app/core/permissions.py` returns a match
    - `grep -n '"label": "Manage costs & budgets"' backend/app/core/permissions.py` returns a match
    - `grep -n '"label": "Manage labor pay rates"' backend/app/core/permissions.py` returns a match
    - `cd backend && source .venv/bin/activate && python -c "from app.core.permissions import DEFAULT_ROLE_PERMISSIONS as d; assert not ({'finance.view','finance.manage','finance.rates.manage'} & set(d['admin'])); assert {'finance.view','finance.manage','finance.rates.manage'} <= set(d['project_manager']); print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>permissions.py exposes 3 finance keys in a "Finance" group; admin derived set excludes them; PM defaults include them; owner unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Pure-unit regression test for admin exclusion and PM inclusion</name>
  <files>backend/tests/unit/test_permissions_finance_keys.py</files>
  <read_first>
    - backend/app/core/permissions.py (the edited catalog/derivation)
    - backend/tests/test_rbac_helpers.py (existing pure-unit permission-test style, no DB)
  </read_first>
  <action>
    Create backend/tests/unit/test_permissions_finance_keys.py (create the
    tests/unit/ directory if it does not exist; add an empty __init__.py only if the
    existing tests/unit package requires it — check tests/unit for precedent first).

    Import from app.core.permissions: DEFAULT_ROLE_PERMISSIONS, PERMISSION_KEYS,
    _FINANCE_ONLY_KEYS, PERMISSION_CATALOG. Define a module-level constant
    `FINANCE_KEYS = frozenset({"finance.view", "finance.manage", "finance.rates.manage"})`
    (do not hardcode the strings inline in each assert — one named constant, per
    CLAUDE.md no-magic-strings).

    Write these pure-unit tests (no DB, no fixtures — run in milliseconds):
    - test_admin_default_has_no_finance_keys: assert
      `FINANCE_KEYS.isdisjoint(set(DEFAULT_ROLE_PERMISSIONS["admin"]))`.
    - test_project_manager_default_has_all_finance_keys: assert
      `FINANCE_KEYS <= set(DEFAULT_ROLE_PERMISSIONS["project_manager"])`.
    - test_finance_keys_present_in_catalog_with_finance_group: assert every finance key
      appears in PERMISSION_CATALOG with `entry["group"] == "Finance"`, and that exactly
      3 catalog entries have group "Finance".
    - test_finance_exclusion_tuple_matches_finance_keys: assert
      `set(_FINANCE_ONLY_KEYS) == FINANCE_KEYS`.
    - test_gc_and_worker_defaults_have_no_finance_keys: assert FINANCE_KEYS disjoint from
      both `DEFAULT_ROLE_PERMISSIONS["gc"]` and `DEFAULT_ROLE_PERMISSIONS["worker"]`.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/unit/test_permissions_finance_keys.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/unit/test_permissions_finance_keys.py exists
    - `grep -c "def test_" backend/tests/unit/test_permissions_finance_keys.py` returns 5 or more
    - `grep -n "FINANCE_KEYS" backend/tests/unit/test_permissions_finance_keys.py` returns a match (named constant, not inline literals in every assert)
    - `cd backend && source .venv/bin/activate && python -m pytest tests/unit/test_permissions_finance_keys.py -q` exits 0
  </acceptance_criteria>
  <done>Pure-unit regression suite proves admin exclusion + PM inclusion + catalog grouping; runs with no DB.</done>
</task>

<task type="auto">
  <name>Task 3: Phase E2E scaffold + RBAC integration tests (matrix grant + admin exclusion)</name>
  <files>backend/tests/test_phase_30_e2e.py</files>
  <read_first>
    - backend/tests/integration/test_role_permissions.py (copy the `_token(...)` JWT-minting helper + fixture usage; matrix PUT/GET endpoint paths)
    - backend/tests/conftest.py (seed_two_tenants, tenant_a_client, tenant_b_client fixtures)
    - backend/app/core/security.py (effective_permissions signature, line 212)
    - .claude/skills/e2e-feature-tests/SKILL.md (backend pytest E2E conventions)
  </read_first>
  <action>
    Create backend/tests/test_phase_30_e2e.py — this is the phase E2E file (later plans
    03/04 add to it). Include:

    1. A shared token helper: copy or import the `_token(company_id, roles)` pattern from
       tests/integration/test_role_permissions.py (mints `create_access_token(uuid4(),
       company_id, roles)`). Define `FINANCE_KEYS = {"finance.view","finance.manage",
       "finance.rates.manage"}` as a module constant.

    2. The RBAC integration tests that pass with only the permissions.py change (no
       migration, no new endpoints needed — test companies are created fresh by
       seed_two_tenants AFTER the code change, so seed_defaults already writes the new
       PM/admin lists):

       - test_new_company_seeded_with_finance_defaults: using a seeded tenant, mint a
         project_manager token and assert `effective_permissions` (or GET
         /api/v1/me/permissions) includes all FINANCE_KEYS; mint an owner token and
         assert it includes them (wildcard); confirm via the same call that the admin
         role does NOT.
       - test_admin_never_gets_finance_via_defaults: mint an admin-role token for a
         seeded company; assert FINANCE_KEYS is disjoint from that user's
         effective_permissions (call effective_permissions directly with a DB session,
         OR hit GET /api/v1/me/permissions with the admin token and assert none of the
         finance keys appear). This is the FINSEC-03 integration counterpart to Task 2's
         unit test.
       - test_owner_can_grant_finance_to_custom_role: as owner, PUT
         /api/v1/roles/gc/permissions adding "finance.view" to gc's existing list
         (read current list first via GET /api/v1/roles/permissions or the roles matrix
         endpoint, append finance.view, PUT it back); then mint a gc token and assert
         "finance.view" now appears in its effective_permissions. Assert
         "finance.manage" does NOT (only the granted key follows). This proves FINSEC-02.

    Follow the SKILL.md backend shape: full ASGI stack via the tenant clients, assert the
    persisted effect via a follow-up read. Do NOT add stub/xfail placeholder tests for
    the surfaces owned by plans 02/03/04 — those plans add their own test functions to
    this file.
  </action>
  <verify>
    <automated>cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_30_e2e.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_phase_30_e2e.py exists
    - `grep -n "def test_new_company_seeded_with_finance_defaults" backend/tests/test_phase_30_e2e.py` returns a match
    - `grep -n "def test_admin_never_gets_finance_via_defaults" backend/tests/test_phase_30_e2e.py` returns a match
    - `grep -n "def test_owner_can_grant_finance_to_custom_role" backend/tests/test_phase_30_e2e.py` returns a match
    - `cd backend && source .venv/bin/activate && python -m pytest tests/test_phase_30_e2e.py -q` exits 0
    - `cd backend && ruff check app/core/permissions.py tests/test_phase_30_e2e.py tests/unit/test_permissions_finance_keys.py` reports no errors
  </acceptance_criteria>
  <done>test_phase_30_e2e.py exists with a shared token helper and 3 green RBAC integration tests covering FINSEC-01/02/03 at the API/matrix layer.</done>
</task>

</tasks>

<verification>
- `cd backend && source .venv/bin/activate && python -m pytest tests/unit/test_permissions_finance_keys.py tests/test_phase_30_e2e.py -q` is green.
- `cd backend && source .venv/bin/activate && python -m pytest tests/integration/test_role_permissions.py -q` still green (catalog grew from 48 to 51 keys; confirm no hardcoded-count assertion broke — research verified none exists, but re-run to be sure).
- `cd backend && ruff check app tests && ruff format --check app tests` clean for touched files.
</verification>

<success_criteria>
- finance.view / finance.manage / finance.rates.manage exist in PERMISSION_CATALOG under group "Finance", appended last.
- admin's derived permission set (unit AND integration) contains zero finance keys.
- project_manager and owner defaults include all three finance keys.
- An owner can grant finance.view to a non-default role and it takes effect immediately.
- test_phase_30_e2e.py exists as the phase E2E anchor with a reusable token helper.
</success_criteria>

<output>
After completion, create `.planning/phases/30-financial-schema-foundation-and-rbac-audit/30-01-SUMMARY.md`
</output>
