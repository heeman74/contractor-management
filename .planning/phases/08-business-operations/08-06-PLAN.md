---
phase: 08-business-operations
plan: 06
type: execute
wave: 4
depends_on: ["08-04", "08-05"]
files_modified:
  - backend/tests/test_phase_8_e2e.py
  - mobile/test/e2e/phase_8_quoting_e2e_test.dart
  - mobile/test/e2e/phase_8_invoicing_e2e_test.dart
  - mobile/test/e2e/phase_8_reports_e2e_test.dart
autonomous: true
requirements:
  - BIZ-01
  - BIZ-02
  - BIZ-03
  - BIZ-04

must_haves:
  truths:
    - "Backend E2E tests verify complete quote-to-invoice lifecycle"
    - "Flutter E2E tests verify quote builder UI and approval flow"
    - "Flutter E2E tests verify invoice generation and detail view"
    - "Flutter E2E tests verify reporting dashboard renders charts"
    - "All tests pass without manual intervention"
  artifacts:
    - path: "backend/tests/test_phase_8_e2e.py"
      provides: "Backend integration tests for quotes, invoices, reporting"
      min_lines: 200
    - path: "mobile/test/e2e/phase_8_quoting_e2e_test.dart"
      provides: "Flutter E2E tests for quote builder and approval"
      min_lines: 150
    - path: "mobile/test/e2e/phase_8_invoicing_e2e_test.dart"
      provides: "Flutter E2E tests for invoice generation and detail"
      min_lines: 100
    - path: "mobile/test/e2e/phase_8_reports_e2e_test.dart"
      provides: "Flutter E2E tests for reporting dashboard"
      min_lines: 100
  key_links:
    - from: "backend/tests/test_phase_8_e2e.py"
      to: "backend/app/features/quotes/router.py"
      via: "ASGI client HTTP calls"
      pattern: "/api/v1/quotes"
    - from: "mobile/test/e2e/phase_8_quoting_e2e_test.dart"
      to: "mobile/lib/features/quotes/presentation/screens/quote_builder_screen.dart"
      via: "widget test pumpWidget"
      pattern: "QuoteBuilderScreen"
---

<objective>
Create comprehensive E2E tests for all Phase 8 features: backend integration tests for the full quote-to-invoice lifecycle, and Flutter widget E2E tests for quote builder, approval flow, invoice detail, and reporting dashboard.

Purpose: CLAUDE.md mandates E2E tests for every feature and every phase before completion. These tests verify all 4 BIZ requirements.
Output: Passing backend and Flutter test suites covering all Phase 8 verification items.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-business-operations/08-CONTEXT.md
@.planning/phases/08-business-operations/08-04-SUMMARY.md
@.planning/phases/08-business-operations/08-05-SUMMARY.md

@backend/tests/conftest.py
@backend/tests/test_phase_7_e2e.py
@mobile/test/e2e/phase_7_client_portal_e2e_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend integration tests for quote-to-invoice lifecycle and reporting</name>
  <files>backend/tests/test_phase_8_e2e.py</files>
  <action>
    Create test_phase_8_e2e.py following established conftest.py patterns (async_client fixture, JWT Bearer auth, clean_tables). Add quotes, quote_line_items, invoices, invoice_line_items, quote_templates to clean_tables TRUNCATE list.

    Import side-effect: import scheduling.models before CRM imports (same pattern as Phase 4 tests).

    Test cases for BIZ-01 (Digital Quoting):
    - test_create_quote_with_line_items: Admin creates a quote with 3 line items (2 labor, 1 material). Assert 201, quote has status=draft, line items saved correctly with types/units.
    - test_update_draft_quote: Update line items on draft quote. Assert old items replaced, new items saved.
    - test_cannot_update_sent_quote: Send quote, then try PATCH. Assert 400/422.
    - test_load_and_save_template: Save quote as template, load template, verify line items match.
    - test_quote_expiry_blocks_approval: Set expiry_date in past, send, try approve. Assert rejected.

    Test cases for BIZ-02 (Quote Approval Flow):
    - test_send_quote_updates_status: POST /quotes/{id}/send. Assert status=sent, sent_at set, job status_history has "quote_sent" entry.
    - test_client_view_records_receipt: Client GET /quotes/{id}. Assert viewed_at set on first access, not changed on second access.
    - test_approve_quote_transitions_job: Client POST /quotes/{id}/approve. Assert quote status=approved, job transitions Quote->Scheduled.
    - test_decline_quote_with_reason: Client POST /quotes/{id}/decline with reason="too_expensive". Assert decline_reason and decline_detail saved.
    - test_revise_declined_quote: Admin POST /quotes/{id}/revise. Assert old quote status=revised, new quote created with revision_number+1.
    - test_full_approval_flow: Create -> send -> view -> approve. Verify all status_history events.

    Test cases for BIZ-03 (Digital Invoicing):
    - test_generate_invoice_from_quote: Complete job with approved quote, POST /invoices/generate/{job_id}. Assert invoice created with sequential number, line items copied from quote, job status=invoiced.
    - test_sequential_invoice_numbers: Generate 3 invoices. Assert numbers are INV-0001, INV-0002, INV-0003.
    - test_update_payment_status: PATCH /invoices/{id}/payment with status=paid. Assert updated.
    - test_finalize_prevents_edit: Finalize invoice, try PATCH. Assert rejected.
    - test_download_quote_pdf: GET /quotes/{id}/pdf. Assert response is application/pdf with content.
    - test_download_invoice_pdf: GET /invoices/{id}/pdf. Assert response is application/pdf.

    Test cases for BIZ-04 (Reporting):
    - test_admin_dashboard: GET /reports/dashboard with date range. Assert response contains jobs_by_status, revenue_by_month, contractor_utilization, quote_conversion. Verify at least one non-zero count.
    - test_contractor_limited_stats: Contractor GET /reports/contractor. Assert no revenue data, only own utilization and jobs.
    - test_dashboard_date_range_filter: Create jobs in different months, filter by specific month, verify only matching data returned.

    RLS tests:
    - test_quote_tenant_isolation: Company A creates quote, Company B cannot access it.
    - test_invoice_tenant_isolation: Same pattern.

    Use asyncio.gather with separate AsyncClient instances for concurrent tests (Phase 3 pattern). Each test creates required seed data (company, user, job at correct status).
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run python -m pytest tests/test_phase_8_e2e.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All backend integration tests pass: quote lifecycle (create, send, view, approve, decline, revise), invoice generation with sequential numbers, PDF download, reporting dashboard queries, and RLS isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Flutter E2E widget tests for quoting, invoicing, and reporting</name>
  <files>
    mobile/test/e2e/phase_8_quoting_e2e_test.dart,
    mobile/test/e2e/phase_8_invoicing_e2e_test.dart,
    mobile/test/e2e/phase_8_reports_e2e_test.dart
  </files>
  <action>
    Follow established E2E patterns from phase_7_client_portal_e2e_test.dart: ProviderScope overrides, MockDio via MockDioClient.instance, real Drift in-memory DB where possible, pump() (NOT pumpAndSettle for Drift streams per MEMORY.md).

    phase_8_quoting_e2e_test.dart:
    - test "Admin builds quote with Labor and Material line items":
      - Pump QuoteBuilderScreen with mock job data
      - Tap "Add Line Item", fill description, select Labor type, set quantity/unit/price
      - Tap "Add Line Item" again, fill Material type item
      - Verify QuoteSummaryCard shows correct subtotal and total
      - Tap "Save Draft", verify QuoteDao.createQuote called (or mock Dio captures POST)
    - test "Admin loads template pre-fills line items":
      - Seed a template in Drift, pump QuoteBuilderScreen
      - Tap "Load Template", select template
      - Verify line items populated from template data
    - test "Quote preview shows client view":
      - Pump QuotePreviewScreen with mock quote data
      - Verify all line items rendered, summary card visible
      - Verify "Send to Client" button exists
    - test "Client approves quote":
      - Pump QuoteDetailScreen with mock sent quote
      - Verify status badge shows "Sent"
      - Tap "Approve", confirm dialog
      - Verify MockDio captured POST to /quotes/{id}/approve
    - test "Client declines with reason":
      - Pump QuoteDetailScreen, tap "Decline"
      - Select "Too expensive" reason, enter detail text
      - Confirm, verify MockDio captured POST with decline_reason
    - test "Expired quote blocks approval":
      - Pump QuoteDetailScreen with expired quote (expiry_date in past, status=expired)
      - Verify "Expired" badge shown
      - Verify approve button is disabled or hidden

    phase_8_invoicing_e2e_test.dart:
    - test "Generate Invoice button on completed job":
      - Pump admin job detail with Complete status and approved quote
      - Verify "Generate Invoice" button visible
      - Tap it, confirm dialog
      - Verify MockDio captured POST to /invoices/generate/{jobId}
    - test "Invoice detail shows line items and payment status":
      - Seed invoice data, pump InvoiceDetailScreen
      - Verify invoice number, line items table, summary card, payment status badge
    - test "Admin updates payment status":
      - Pump InvoiceDetailScreen as admin
      - Change payment status dropdown to "Paid"
      - Verify MockDio captured PATCH to /invoices/{id}/payment
    - test "Client sees invoice in portal":
      - Pump client job detail with invoice data
      - Verify invoice summary card visible with "View Invoice" link

    phase_8_reports_e2e_test.dart:
    - test "Admin reports screen renders 4 metric cards":
      - Override adminDashboardProvider with mock DashboardResponse data
      - Pump AdminReportsScreen, pump()
      - Verify 4 cards visible (find by card title text: "Jobs by Status", "Revenue Summary", "Contractor Utilization", "Quote Conversion")
    - test "Date range presets update data":
      - Pump AdminReportsScreen
      - Tap "This Quarter" chip
      - Verify provider refreshed (mock Dio captures new GET with updated date range)
    - test "Contractor sees limited view":
      - Pump ContractorReportsScreen with mock contractor stats
      - Verify "My Jobs" and "My Utilization" cards visible
      - Verify NO revenue card exists (find "Revenue" returns empty)
    - test "Reports tab visible in bottom navigation for admin":
      - Pump AppShell with admin role
      - Verify NavigationDestination with label "Reports" exists

    All tests use pump() not pumpAndSettle() for Drift stream providers. Mock Dio responses for API calls. Use ProviderScope.overrideWith for stream providers.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/e2e/phase_8_quoting_e2e_test.dart test/e2e/phase_8_invoicing_e2e_test.dart test/e2e/phase_8_reports_e2e_test.dart 2>&1 | tail -20</automated>
  </verify>
  <done>All Flutter E2E tests pass: quote builder creates line items, templates load, preview matches client view, approve/decline work, expired blocks approval, invoice generation works, payment status updates, reporting dashboard renders 4 charts, contractor sees limited view.</done>
</task>

</tasks>

<verification>
- Backend: `uv run python -m pytest tests/test_phase_8_e2e.py -x -v` all pass
- Flutter: `flutter test test/e2e/phase_8_*` all pass
- Full quote-to-invoice lifecycle tested end-to-end
- Reporting data accuracy verified
- RLS isolation confirmed
</verification>

<success_criteria>
- Backend tests cover: quote CRUD, send/view/approve/decline/revise lifecycle, invoice generation with sequential numbers, PDF download, reporting queries, RLS isolation
- Flutter tests cover: quote builder UI, template loading, preview, approve/decline, expired blocking, invoice generation button, invoice detail, payment status, admin dashboard 4 charts, contractor limited view, Reports tab visibility
- All tests pass in CI (no manual steps required)
- Phase 8 considered complete when all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-operations/08-06-SUMMARY.md`
</output>
