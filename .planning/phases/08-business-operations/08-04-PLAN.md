---
phase: 08-business-operations
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - mobile/lib/features/quotes/presentation/providers/quote_providers.dart
  - mobile/lib/features/quotes/presentation/screens/quote_builder_screen.dart
  - mobile/lib/features/quotes/presentation/screens/quote_preview_screen.dart
  - mobile/lib/features/quotes/presentation/screens/quote_detail_screen.dart
  - mobile/lib/features/quotes/presentation/widgets/line_item_form.dart
  - mobile/lib/features/quotes/presentation/widgets/quote_summary_card.dart
  - mobile/lib/shared/routing/app_router.dart
  - mobile/lib/shared/routing/route_names.dart
autonomous: true
requirements:
  - BIZ-01
  - BIZ-02

must_haves:
  truths:
    - "Admin can build a quote with Labor and Material line items"
    - "Admin can preview the quote as client will see it before sending"
    - "Admin can send the quote to the client from preview screen"
    - "Client can view the full quote with line items in the portal"
    - "Client can approve or decline a quote with decline reason"
    - "Admin can load a template to pre-fill line items"
    - "Expired quotes show Expired badge and block approval"
  artifacts:
    - path: "mobile/lib/features/quotes/presentation/screens/quote_builder_screen.dart"
      provides: "Admin quote creation and editing screen"
      min_lines: 100
    - path: "mobile/lib/features/quotes/presentation/screens/quote_detail_screen.dart"
      provides: "Client-facing quote view with approve/decline"
      min_lines: 80
    - path: "mobile/lib/features/quotes/presentation/providers/quote_providers.dart"
      provides: "Riverpod providers for quote state"
  key_links:
    - from: "mobile/lib/features/quotes/presentation/screens/quote_builder_screen.dart"
      to: "mobile/lib/features/quotes/data/quote_dao.dart"
      via: "Riverpod provider -> QuoteDao"
      pattern: "quoteDao"
    - from: "mobile/lib/features/quotes/presentation/screens/quote_detail_screen.dart"
      to: "mobile/lib/features/quotes/presentation/providers/quote_providers.dart"
      via: "ref.watch for quote stream"
      pattern: "quoteForJobProvider"
---

<objective>
Build the admin quote builder UI (line item editor with Labor/Material types, units, tax, discount, templates) and the client-facing quote detail screen (view, approve, decline). Wire routes and providers.

Purpose: BIZ-01 (digital quoting) and BIZ-02 (quote approval flow) require both admin creation and client approval screens.
Output: Complete quote creation, preview, and approval UI.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-business-operations/08-CONTEXT.md
@.planning/phases/08-business-operations/08-01-SUMMARY.md
@.planning/phases/08-business-operations/08-02-SUMMARY.md
@.planning/phases/08-business-operations/08-03-SUMMARY.md

@mobile/lib/features/jobs/presentation/screens/job_wizard_screen.dart
@mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart
@mobile/lib/shared/routing/app_router.dart
@mobile/lib/shared/routing/route_names.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quote providers and admin quote builder screen with line item editor</name>
  <files>
    mobile/lib/features/quotes/presentation/providers/quote_providers.dart,
    mobile/lib/features/quotes/presentation/screens/quote_builder_screen.dart,
    mobile/lib/features/quotes/presentation/widgets/line_item_form.dart,
    mobile/lib/features/quotes/presentation/widgets/quote_summary_card.dart
  </files>
  <action>
    Create Riverpod providers (quote_providers.dart):
    - quoteForJobProvider(jobId) — StreamProvider.autoDispose.family watching QuoteDao.watchQuotesForJob(jobId), returns latest non-revised quote
    - quoteDaoProvider — Provider accessing QuoteDao from GetIt
    - quoteTemplatesProvider — StreamProvider watching QuoteDao.watchTemplates()
    - quoteBuilderNotifierProvider — StateNotifierProvider managing in-memory quote being built (line items list, tax rate, discount type/value, expiry date, admin notes). Methods: addLineItem, removeLineItem, updateLineItem, reorderLineItem, setTaxRate, setDiscount, setExpiry, loadFromTemplate, reset.

    Create QuoteBuilderScreen (admin only):
    - AppBar title: "Create Quote" or "Edit Quote v{N}"
    - If job originated from client request with budget range, show reference banner: "Client budget: $X–$Y" (read from job's request data)
    - Template selector dropdown at top: "Load Template" button opens a dialog listing saved templates. Selecting one pre-fills line items + tax rate.
    - Scrollable line item list using ReorderableListView:
      - Each item shows: type toggle (Labor/Material chip), description TextField, quantity field, unit dropdown (each, hour, day, sqm, sqft, meter, liter), unit price field, computed subtotal display ("Qty x Unit @ $Price = $Subtotal")
      - Swipe-to-delete or trailing delete icon
      - "Add Line Item" button at bottom (FloatingActionButton or TextButton)
      - Reorderable via drag handle
    - Below line items: Tax rate field (% input), Discount toggle (None/Percentage/Fixed) with value field
    - Expiry date picker (optional)
    - Admin notes text field (optional)
    - Bottom summary card showing: Subtotal, Discount amount, Tax amount, Total
    - AppBar actions: "Save Draft" (saves to Drift via QuoteDao, enqueues sync), "Preview" (navigates to QuotePreviewScreen)
    - Validate: at least 1 line item, all descriptions non-empty, quantities > 0, unit prices >= 0. Show validation errors inline.
    - "Save as Template" action in overflow menu — prompts for template name, saves via QuoteDao.createTemplate

    Create LineItemForm widget:
    - Compact form row for a single line item
    - SegmentedButton or ChoiceChip for Labor/Material toggle
    - TextFormField for description (required)
    - Row with quantity (NumberInput), unit dropdown, unit price (currency input)
    - Display computed subtotal: quantity * unitPrice
    - Callback: onChanged(LineItemEntity), onDelete()

    Create QuoteSummaryCard widget:
    - Displays subtotal, discount (with type label), tax, total
    - Updates reactively as line items change
    - Reusable in both builder and preview screens
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/quotes/presentation/ 2>&1 | tail -5</automated>
  </verify>
  <done>Admin can build quotes with typed line items (Labor/Material), units, quantities, tax, discount. Line items are reorderable and validatable. Templates can be loaded. Summary card shows computed totals.</done>
</task>

<task type="auto">
  <name>Task 2: Quote preview, client quote detail with approve/decline, and route wiring</name>
  <files>
    mobile/lib/features/quotes/presentation/screens/quote_preview_screen.dart,
    mobile/lib/features/quotes/presentation/screens/quote_detail_screen.dart,
    mobile/lib/shared/routing/app_router.dart,
    mobile/lib/shared/routing/route_names.dart
  </files>
  <action>
    Create QuotePreviewScreen (admin sees quote as client will see it):
    - Read-only layout matching QuoteDetailScreen format (line item table, summary)
    - Shows company header, job reference, all line items in table format
    - QuoteSummaryCard at bottom
    - Admin notes section (if present)
    - Expiry date display (if set)
    - Primary action button: "Send to Client" — calls QuoteService.sendQuote via API (DioClient POST /quotes/{id}/send). Shows confirmation dialog first: "Send this quote to [client name]?"
    - On send success: navigate back to job detail, show success snackbar "Quote sent to client"

    Create QuoteDetailScreen (client-facing, in portal):
    - Header: company name, "Quote v{revision_number}" title
    - Status badge: Draft (grey, shouldn't appear for clients), Sent (blue), Viewed (blue), Approved (green), Declined (red), Expired (orange)
    - If expired: "This quote has expired" banner, approve/decline buttons disabled
    - Full line items table: Description | Type | Qty x Unit @ Price | Subtotal
    - QuoteSummaryCard (subtotal, discount, tax, total)
    - Admin notes (if any)
    - Expiry date (if set)
    - If status is sent or viewed and not expired:
      - "Approve" primary button — confirmation dialog, then POST /quotes/{id}/approve
      - "Decline" outlined button — opens bottom sheet with decline reason picker (too expensive, wrong scope, changed mind, other) + optional detail text field, then POST /quotes/{id}/decline
    - On approve: show success state, navigate back
    - On decline: show "Quote declined" confirmation, navigate back

    Route wiring (app_router.dart, route_names.dart):
    - Add RouteNames: quoteBuilder = '/jobs/:jobId/quote/build', quotePreview = '/jobs/:jobId/quote/preview', quoteDetail = '/quotes/:quoteId'
    - Add GoRoutes:
      - quoteBuilder as child route of job detail (admin only)
      - quotePreview as child route of job detail (admin only)
      - quoteDetail under client portal branch for client access
    - Add "Create Quote" / "View Quote" button to admin job detail screen (when job is in Quote status) — navigate to quoteBuilder or quotePreview
    - Add Quote tab/section to client_job_detail_screen.dart — shows QuoteDetailScreen content when a quote exists for the job

    When client opens quote detail and quote status is 'sent', trigger read receipt by calling GET /quotes/{id} which backend records as viewed (per research Pattern 4). Show "Viewed by client" timestamp on admin side.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/quotes/presentation/screens/quote_preview_screen.dart lib/features/quotes/presentation/screens/quote_detail_screen.dart 2>&1 | tail -5</automated>
  </verify>
  <done>Admin can preview and send quotes. Client can view full quote with line items. Client can approve (with confirmation) or decline (with reason picker). Read receipt tracked on first client view. Routes wired in GoRouter.</done>
</task>

</tasks>

<verification>
- Admin quote builder creates quotes with line items, tax, discount
- Quote preview matches client view layout
- Send action triggers API call and updates status
- Client approve/decline work with proper validation
- Expired quotes cannot be approved
- Routes registered and navigable
</verification>

<success_criteria>
- Quote builder supports 1-100 line items with Labor/Material types and unit selection
- Template loading pre-fills line items
- Preview screen matches client view exactly
- Client can approve (auto-transitions job) or decline (with reason) from portal
- Read receipt recorded on first client view
- All new routes added to GoRouter without breaking existing navigation
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-operations/08-04-SUMMARY.md`
</output>
