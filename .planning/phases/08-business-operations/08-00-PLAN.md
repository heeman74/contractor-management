---
phase: 08-business-operations
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/integration/test_phase_8_e2e.py
  - backend/tests/unit/test_quote_validation.py
  - mobile/test/e2e/phase_8_business_ops_e2e_test.dart
  - backend/tests/conftest.py
autonomous: true
requirements:
  - BIZ-01
  - BIZ-02
  - BIZ-03
  - BIZ-04

must_haves:
  truths:
    - "Test stub files exist and can be imported before any feature code is written"
    - "conftest.py TRUNCATE list includes all Phase 8 tables"
    - "WeasyPrint and fl_chart dependencies are installed"
  artifacts:
    - path: "backend/tests/integration/test_phase_8_e2e.py"
      provides: "Backend integration test stubs for all BIZ requirements"
      contains: "test_create_quote_with_line_items"
    - path: "backend/tests/unit/test_quote_validation.py"
      provides: "Quote validation unit test stubs"
      contains: "test_expired_quote_blocks_approval"
    - path: "mobile/test/e2e/phase_8_business_ops_e2e_test.dart"
      provides: "Flutter E2E test stubs for full quote-to-invoice flow"
      contains: "phase_8_business_ops"
  key_links: []
---

<objective>
Create Wave 0 test stubs and install dependencies required by VALIDATION.md before any feature implementation begins.

Purpose: VALIDATION.md sampling contract requires test files to exist so `pytest` and `flutter test` commands can run after every task commit. Dependencies (WeasyPrint, fl_chart) must be installed before plans that use them.
Output: Stub test files at VALIDATION.md paths, conftest.py updated, dependencies installed.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-business-operations/08-CONTEXT.md
@.planning/phases/08-business-operations/08-VALIDATION.md

@backend/tests/conftest.py
@mobile/pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend test stubs, update conftest, and install WeasyPrint</name>
  <files>
    backend/tests/integration/test_phase_8_e2e.py,
    backend/tests/unit/test_quote_validation.py,
    backend/tests/conftest.py
  </files>
  <action>
    Create `backend/tests/integration/` directory if it does not exist.

    Create `backend/tests/integration/test_phase_8_e2e.py` with stub test functions that assert False with "not yet implemented" messages. Include stubs for every test function listed in VALIDATION.md:
    - test_create_quote_with_line_items
    - test_quote_template_crud
    - test_send_quote_triggers_notification
    - test_quote_read_receipt
    - test_quote_approval_job_transition
    - test_quote_decline_and_revise
    - test_generate_invoice_from_job
    - test_invoice_number_sequential
    - test_invoice_prefilled_from_quote
    - test_invoice_transitions_job_to_invoiced
    - test_pdf_download
    - test_dashboard_jobs_by_status
    - test_dashboard_revenue_by_month
    - test_dashboard_contractor_utilization
    - test_dashboard_role_scoping
    - test_dashboard_date_range_filter

    Each stub: `async def test_xxx(): assert False, 'not yet implemented'` with pytest.mark.asyncio decorator.

    Create `backend/tests/unit/test_quote_validation.py` with stubs:
    - test_expired_quote_blocks_approval
    - test_line_item_quantity_positive
    - test_discount_value_validation
    - test_tax_rate_range

    Each stub: `def test_xxx(): assert False, 'not yet implemented'`

    Update `backend/tests/conftest.py`: add `quotes`, `quote_line_items`, `quote_templates`, `invoices`, `invoice_line_items` to the TRUNCATE list in `clean_tables` fixture.

    Install WeasyPrint: run `cd backend && uv add weasyprint` from backend directory.

    Create `backend/tests/integration/__init__.py` if needed.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run python -m pytest tests/integration/test_phase_8_e2e.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>Backend test stubs exist at VALIDATION.md paths and are discoverable by pytest. conftest.py truncates all Phase 8 tables. WeasyPrint installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create Flutter E2E test stub and add fl_chart dependency</name>
  <files>
    mobile/test/e2e/phase_8_business_ops_e2e_test.dart,
    mobile/pubspec.yaml
  </files>
  <action>
    Create `mobile/test/e2e/phase_8_business_ops_e2e_test.dart` with stub test groups matching VALIDATION.md expectations. Use `flutter_test` import and `group`/`test` structure:

    - group('Quote Flow', () { ... }) tagged 'quote_flow':
      - test 'Admin builds quote with line items' — skip with reason 'not yet implemented'
      - test 'Admin loads template pre-fills line items' — skip
      - test 'Quote preview shows client view' — skip
      - test 'Client approves quote' — skip
      - test 'Client declines with reason' — skip
      - test 'Expired quote blocks approval' — skip

    - group('Invoice Flow', () { ... }):
      - test 'Generate Invoice button on completed job' — skip
      - test 'Invoice detail shows line items and payment status' — skip
      - test 'Admin updates payment status' — skip
      - test 'Client sees invoice in portal' — skip

    - group('Reports', () { ... }):
      - test 'Admin reports screen renders 4 metric cards' — skip
      - test 'Date range presets update data' — skip
      - test 'Contractor sees limited view' — skip
      - test 'Reports tab visible in bottom navigation for admin' — skip

    Add `fl_chart: ^1.2.0` to `mobile/pubspec.yaml` dependencies section.

    Run `cd mobile && flutter pub get` to resolve dependencies.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/e2e/phase_8_business_ops_e2e_test.dart 2>&1 | tail -5</automated>
  </verify>
  <done>Flutter E2E test stub exists at VALIDATION.md path and runs (all skipped). fl_chart dependency installed.</done>
</task>

</tasks>

<verification>
- `pytest tests/integration/test_phase_8_e2e.py --collect-only` discovers all stub tests
- `pytest tests/unit/test_quote_validation.py --collect-only` discovers validation stubs
- `flutter test test/e2e/phase_8_business_ops_e2e_test.dart` runs (all skipped, no failures)
- WeasyPrint importable: `python -c "import weasyprint"`
- fl_chart in pubspec.lock
</verification>

<success_criteria>
- All three test files exist at exact VALIDATION.md paths
- conftest.py TRUNCATE includes Phase 8 tables
- WeasyPrint and fl_chart dependencies installed
- VALIDATION.md sampling commands can run without "file not found" errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-operations/08-00-SUMMARY.md`
</output>
