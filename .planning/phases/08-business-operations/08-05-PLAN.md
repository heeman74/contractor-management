---
phase: 08-business-operations
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - mobile/lib/features/invoices/presentation/providers/invoice_providers.dart
  - mobile/lib/features/invoices/presentation/screens/invoice_detail_screen.dart
  - mobile/lib/features/reports/presentation/providers/reports_providers.dart
  - mobile/lib/features/reports/presentation/screens/admin_reports_screen.dart
  - mobile/lib/features/reports/presentation/screens/contractor_reports_screen.dart
  - mobile/lib/shared/widgets/app_shell.dart
  - mobile/lib/shared/routing/app_router.dart
  - mobile/lib/shared/routing/route_names.dart
  - mobile/lib/features/client/presentation/screens/client_job_detail_screen.dart
autonomous: true
requirements:
  - BIZ-03
  - BIZ-04

must_haves:
  truths:
    - "Admin can generate an invoice from a completed job with one tap"
    - "Invoice detail shows line items, totals, and payment status"
    - "Admin can download invoice and quote PDFs"
    - "Reports tab shows 4 metrics with date range filter"
    - "Contractors see limited report view (own stats only, no revenue)"
    - "Reports tab appears in bottom navigation for admin and contractor"
  artifacts:
    - path: "mobile/lib/features/invoices/presentation/screens/invoice_detail_screen.dart"
      provides: "Invoice detail with PDF download and payment status"
      min_lines: 80
    - path: "mobile/lib/features/reports/presentation/screens/admin_reports_screen.dart"
      provides: "Admin dashboard with 4 chart metrics"
      min_lines: 100
    - path: "mobile/lib/features/reports/presentation/screens/contractor_reports_screen.dart"
      provides: "Contractor limited stats view"
      min_lines: 60
  key_links:
    - from: "mobile/lib/shared/widgets/app_shell.dart"
      to: "mobile/lib/features/reports/presentation/screens/admin_reports_screen.dart"
      via: "Reports tab in bottom navigation"
      pattern: "Reports"
    - from: "mobile/lib/features/reports/presentation/providers/reports_providers.dart"
      to: "/api/v1/reports/dashboard"
      via: "DioClient API call"
      pattern: "reports/dashboard"
---

<objective>
Build the invoice detail screen with PDF download, admin reporting dashboard with 4 chart metrics (fl_chart), contractor limited stats view, and wire the Reports tab into bottom navigation.

Purpose: BIZ-03 (invoicing) and BIZ-04 (reporting dashboard) complete the business operations feature set.
Output: Invoice management UI, reporting dashboard, and Reports navigation tab.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-business-operations/08-CONTEXT.md
@.planning/phases/08-business-operations/08-02-SUMMARY.md
@.planning/phases/08-business-operations/08-03-SUMMARY.md

@mobile/lib/shared/widgets/app_shell.dart
@mobile/lib/shared/routing/app_router.dart
@mobile/lib/shared/routing/route_names.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoice detail screen with PDF download and payment tracking</name>
  <files>
    mobile/lib/features/invoices/presentation/providers/invoice_providers.dart,
    mobile/lib/features/invoices/presentation/screens/invoice_detail_screen.dart
  </files>
  <action>
    Create invoice providers (invoice_providers.dart):
    - invoiceForJobProvider(jobId) — StreamProvider.autoDispose.family watching InvoiceDao.watchInvoicesForJob(jobId)
    - invoiceDaoProvider — Provider accessing InvoiceDao from GetIt
    - generateInvoiceProvider — FutureProvider that calls POST /invoices/generate/{jobId} via DioClient

    Create InvoiceDetailScreen:
    - Header: "Invoice {invoice_number}" (e.g., "Invoice INV-0001")
    - Status badges: Unpaid (red), Partially Paid (orange), Paid (green)
    - Invoice metadata: Issued date, Due date (if set), Finalized date (if set)
    - Line items table: Description | Type | Qty x Unit @ Price | Subtotal (same layout as quote detail)
    - QuoteSummaryCard reused for subtotal/discount/tax/total display
    - Payment status section:
      - If admin: dropdown/segmented control to change payment status (Unpaid -> Partially Paid -> Paid). On change, call PATCH /invoices/{id}/payment via API. Show confirmation dialog.
      - If client: read-only display of payment status
    - PDF download button in AppBar actions:
      - On tap: GET /invoices/{id}/pdf via DioClient (responseType: ResponseType.bytes)
      - Save to temp directory, open with share sheet (share_plus or open_file package) or display in-app
      - Show loading indicator during download
    - "Edit Invoice" button (admin only, only if not finalized):
      - Navigate to invoice editor (reuse line item editing pattern from quote builder)
    - "Finalize" button (admin only, if not finalized):
      - Confirmation dialog, then POST /invoices/{id}/finalize
      - After finalize, edit button disabled

    Add "Generate Invoice" button to admin job detail screen:
    - Visible when job status is Complete and has an approved quote
    - On tap: confirmation dialog ("Generate invoice from quote?"), then POST /invoices/generate/{job_id}
    - On success: navigate to invoice detail, show success snackbar
    - Also add "View Invoice" button when job already has an invoice

    Add Invoice section to client_job_detail_screen.dart:
    - When invoice exists for the job, show invoice summary card with "View Invoice" link
    - Include PDF download for client


    Update History tab rendering in client_job_detail_screen.dart to handle Phase 8 event types:
    - Render quote events (quote_created, quote_sent, quote_viewed, quote_approved, quote_declined, quote_revised) with appropriate icons and labels in the History tab timeline
    - Render invoice events (invoice_generated) with invoice icon and label
    - These events come from job.status_history JSONB and must display alongside existing lifecycle events

    Wire routes:
    - RouteNames.invoiceDetail = '/invoices/:invoiceId'
    - GoRoute for invoice detail screen (accessible by admin and client)
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/invoices/presentation/ 2>&1 | tail -5</automated>
  </verify>
  <done>Invoice detail shows line items, totals, payment status. Admin can update payment status, edit before finalize, and finalize. PDF download works. Generate Invoice button on completed job detail. Client sees invoice in portal.</done>
</task>

<task type="auto">
  <name>Task 2: Reporting dashboard with fl_chart and Reports bottom nav tab</name>
  <files>
    mobile/lib/features/reports/presentation/providers/reports_providers.dart,
    mobile/lib/features/reports/presentation/screens/admin_reports_screen.dart,
    mobile/lib/features/reports/presentation/screens/contractor_reports_screen.dart,
    mobile/lib/shared/widgets/app_shell.dart,
    mobile/lib/shared/routing/app_router.dart,
    mobile/lib/shared/routing/route_names.dart
  </files>
  <action>
    Create reports providers (reports_providers.dart):
    - dateRangeProvider — StateProvider<DateTimeRange> initialized to "This Month" (first day of current month to today)
    - datePresetProvider — StateProvider<String> for quick preset labels
    - adminDashboardProvider — AsyncNotifier that calls GET /reports/dashboard?start_date={}&end_date={} via DioClient. Watches dateRangeProvider to refetch on range change. Returns DashboardResponse parsed from JSON.
    - contractorStatsProvider — AsyncNotifier calling GET /reports/contractor?start_date={}&end_date={}. Returns contractor-specific stats.

    Create AdminReportsScreen:
    - AppBar: "Reports" title
    - Date range selector at top:
      - Row of FilterChip/ChoiceChip for presets: This Week, This Month, Last 30 Days, This Quarter, This Year, All Time
      - "Custom" chip that opens showDateRangePicker()
      - Selecting a preset updates dateRangeProvider and datePresetProvider
    - Scrollable column of 4 metric cards (use Card with padding):

      1. **Jobs by Status** — PieChart (fl_chart PieChart) with colored sections per status (Quote=grey, Scheduled=blue, In Progress=amber, Complete=green, Invoiced=purple). Center text shows total count. Legend below.

      2. **Revenue Summary** — BarChart (fl_chart BarChart) with monthly bars. Each bar stacked: paid (green bottom) + unpaid (orange top). X-axis: month labels. Y-axis: dollar amounts. Show up to 12 months.

      3. **Contractor Utilization** — Horizontal BarChart ranking contractors by utilization %. Each bar shows contractor name on Y-axis, utilization % on X-axis. Color gradient from red (<50%) to yellow (50-75%) to green (>75%).

      4. **Quote Conversion Rate** — PieChart with two sections: Approved (green) and Declined (red). Center text shows conversion rate percentage. Below: "X approved, Y declined, Z pending" text.

    - Handle loading state (shimmer/skeleton or CircularProgressIndicator per card)
    - Handle empty state ("No data for selected period")

    Create ContractorReportsScreen:
    - AppBar: "My Stats" title
    - Same date range selector as admin
    - Two metric cards only:
      1. **My Jobs** — simple count cards or PieChart showing own jobs by status
      2. **My Utilization** — single horizontal bar showing own booked vs available hours, utilization %
    - NO revenue data (per locked decision)

    Update AppShell (app_shell.dart):
    - Add Reports tab to _buildTabs():
      - For admin: NavigationDestination(label: 'Reports', icon: Icons.bar_chart_outlined, selectedIcon: Icons.bar_chart)
      - For contractor: same tab but navigates to ContractorReportsScreen
      - Client role: NO Reports tab (per locked decision — admin and contractor only)
    - Follow existing role-based tab visibility pattern

    Update app_router.dart:
    - Add new StatefulShellBranch for Reports (new branch index)
    - GoRoute path: RouteNames.reports = '/reports'
    - Builder: role-based screen selection (isAdmin ? AdminReportsScreen : ContractorReportsScreen) — same pattern as schedule branch
    - Add RouteNames.reports constant

    Ensure the branch index for Reports is correct and all existing branch indices remain unchanged. Tab ordering: existing tabs first, Reports tab last.
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/reports/presentation/ lib/shared/widgets/app_shell.dart 2>&1 | tail -5</automated>
  </verify>
  <done>Admin sees full dashboard with 4 chart metrics and date range filter. Contractor sees limited stats (own jobs + utilization). Reports tab visible in bottom nav for admin and contractor. Charts render via fl_chart.</done>
</task>

</tasks>

<verification>
- Invoice generation from completed job produces sequential number
- Invoice PDF downloads and opens
- Payment status transitions work (unpaid -> partially_paid -> paid)
- Admin dashboard shows all 4 metrics with date range filtering
- Contractor dashboard shows own stats only (no revenue)
- Reports tab appears in bottom navigation
- Date range presets filter data correctly
</verification>

<success_criteria>
- One-tap invoice generation from approved quote works end-to-end
- Invoice detail shows full line items, totals, and payment tracking
- PDF download works for both quotes and invoices
- Admin Reports screen shows PieChart for jobs/conversion, BarChart for revenue/utilization
- Contractor Reports screen shows own stats only
- Reports tab added to StatefulShellRoute with correct branch index
- Date range selector with presets and custom picker works
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-operations/08-05-SUMMARY.md`
</output>
