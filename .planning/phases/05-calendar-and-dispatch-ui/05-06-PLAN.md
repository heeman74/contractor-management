---
phase: 05-calendar-and-dispatch-ui
plan: 06
type: execute
wave: 6
depends_on: ["05-03", "05-04", "05-05"]
files_modified:
  - backend/tests/integration/test_delay_endpoint.py
  - mobile/test/unit/features/schedule/overdue_service_test.dart
  - mobile/test/unit/features/schedule/booking_dao_test.dart
  - mobile/test/widget/features/schedule/schedule_screen_test.dart
  - mobile/test/widget/features/schedule/delay_dialog_test.dart
  - mobile/test/widget/features/schedule/calendar_day_view_test.dart
autonomous: true
requirements:
  - SCHED-03
  - SCHED-08
  - SCHED-09

must_haves:
  truths:
    - "Backend delay endpoint has integration tests covering happy path, wrong status, version conflict, and missing job"
    - "Overdue service unit tests verify severity computation across all tier boundaries"
    - "BookingDao tests verify transactional outbox dual-write for insert, update, and soft-delete"
    - "Calendar screen widget tests verify view mode switching and booking card rendering"
    - "Delay dialog widget tests verify validation (empty reason, no ETA) prevents submission"
    - "E2E flow test covers drag-to-schedule and delay report full path"
  artifacts:
    - path: "backend/tests/integration/test_delay_endpoint.py"
      provides: "Backend delay endpoint integration tests"
      contains: "test_report_delay"
    - path: "mobile/test/unit/features/schedule/overdue_service_test.dart"
      provides: "Overdue computation unit tests"
      contains: "computeSeverity"
    - path: "mobile/test/unit/features/schedule/booking_dao_test.dart"
      provides: "BookingDao Drift tests"
      contains: "insertBooking"
    - path: "mobile/test/widget/features/schedule/schedule_screen_test.dart"
      provides: "Calendar screen widget tests"
      contains: "ScheduleScreen"
    - path: "mobile/test/widget/features/schedule/delay_dialog_test.dart"
      provides: "Delay dialog validation tests"
      contains: "DelayJustificationDialog"
  key_links:
    - from: "backend/tests/integration/test_delay_endpoint.py"
      to: "backend/app/features/jobs/router.py"
      via: "ASGI HTTP test client"
      pattern: "PATCH.*delay"
    - from: "mobile/test/unit/features/schedule/overdue_service_test.dart"
      to: "mobile/lib/features/schedule/domain/overdue_service.dart"
      via: "Direct function calls"
      pattern: "OverdueService.computeSeverity"
---

<objective>
Comprehensive test suite for Phase 5: backend delay endpoint integration tests, overdue service unit tests, BookingDao Drift tests, calendar widget tests, delay dialog validation tests, and E2E scheduling flow test.

Purpose: Every new feature in Phase 5 must have corresponding tests (per CLAUDE.md Testing Rules). This plan covers backend integration tests, mobile unit tests, and widget tests including E2E flows.
Output: Full test coverage for delay endpoint, overdue detection, booking data layer, calendar UI interactions, and delay justification dialog.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calendar-and-dispatch-ui/05-CONTEXT.md
@.planning/phases/05-calendar-and-dispatch-ui/05-RESEARCH.md
@.planning/phases/05-calendar-and-dispatch-ui/05-01-SUMMARY.md
@.planning/phases/05-calendar-and-dispatch-ui/05-02-SUMMARY.md
@.planning/phases/05-calendar-and-dispatch-ui/05-03-SUMMARY.md
@.planning/phases/05-calendar-and-dispatch-ui/05-04-SUMMARY.md
@.planning/phases/05-calendar-and-dispatch-ui/05-05-SUMMARY.md
@backend/tests/conftest.py
@mobile/test/unit/features/jobs/job_dao_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend delay endpoint integration tests and mobile overdue/BookingDao unit tests</name>
  <files>
    backend/tests/integration/test_delay_endpoint.py
    mobile/test/unit/features/schedule/overdue_service_test.dart
    mobile/test/unit/features/schedule/booking_dao_test.dart
  </files>
  <action>
    **Create `backend/tests/integration/test_delay_endpoint.py`:**
    Integration tests using the ASGI HTTP test client (httpx + ASGITransport) following the pattern from existing tests in `backend/tests/integration/`:
    1. `test_report_delay_happy_path` — Create a job in 'scheduled' status, PATCH /jobs/{id}/delay with valid reason + new_eta + version. Assert 200, response has updated scheduled_completion_date matching new_eta, status_history[-1] has type="delay" with the reason.
    2. `test_report_delay_wrong_status` — Create a job in 'complete' status, attempt delay report. Assert 422 with message about wrong status.
    3. `test_report_delay_version_conflict` — Create a job, report delay with wrong version number. Assert 409.
    4. `test_report_delay_not_found` — PATCH /jobs/{random_uuid}/delay. Assert 404.
    5. `test_report_delay_multiple` — Create a job, report delay twice with different reasons/ETAs. Assert both entries in status_history, scheduled_completion_date equals latest new_eta.
    6. `test_report_delay_in_progress` — Create a job in 'in_progress' status, report delay. Assert 200 (both scheduled and in_progress are valid).
    7. `test_report_delay_empty_reason` — PATCH with empty reason string. Assert 422 (Pydantic min_length=1 validation).

    Use existing conftest.py fixtures: `async_client`, `seed_two_tenants`, `clean_tables`. Create jobs using POST /api/v1/jobs/ then transition to the needed status before testing delay.

    **Create `mobile/test/unit/features/schedule/overdue_service_test.dart`:**
    Pure unit tests for OverdueService (no Flutter, no mocking needed):
    1. `test_computeSeverity_none_when_not_past` — date in the future returns OverdueSeverity.none
    2. `test_computeSeverity_none_when_null` — null date returns OverdueSeverity.none
    3. `test_computeSeverity_warning_1_day_overdue` — 1 day past returns warning
    4. `test_computeSeverity_warning_3_days_overdue` — 3 days past returns warning
    5. `test_computeSeverity_critical_4_days_overdue` — 4 days past returns critical
    6. `test_computeSeverity_critical_30_days_overdue` — 30 days past returns critical
    7. `test_isOverdue_true_scheduled_past_date` — scheduled status + past date returns true
    8. `test_isOverdue_true_in_progress_past_date` — in_progress status + past date returns true
    9. `test_isOverdue_false_complete_status` — complete status + past date returns false (not active)
    10. `test_isOverdue_false_future_date` — scheduled status + future date returns false

    **Create `mobile/test/unit/features/schedule/booking_dao_test.dart`:**
    Drift in-memory database tests following the pattern from existing DAO tests:
    1. `test_insertBooking_creates_booking_and_sync_entry` — Insert a booking, verify it appears in Drift AND a sync queue entry with operation CREATE is created.
    2. `test_watchBookingsByContractorAndDate_filters_correctly` — Insert bookings for 2 contractors on 2 dates. Watch for one contractor + one date. Assert only matching bookings returned.
    3. `test_softDeleteBooking_sets_deletedAt` — Insert, soft-delete, verify deletedAt is set and booking is excluded from watch queries (deletedAt.isNull filter).
    4. `test_updateBookingTime_changes_range_and_bumps_version` — Insert, update time range, verify new start/end and version incremented. Verify sync queue UPDATE entry.
    5. `test_upsertBookingFromSync_creates_new` — Upsert a booking that doesn't exist yet. Verify created. Verify NO sync queue entry (sync pull should not re-queue).
    6. `test_upsertBookingFromSync_updates_existing` — Insert then upsert with updated fields. Verify fields updated. Verify NO sync queue entry.
    7. `test_watchUnscheduledJobs_excludes_jobs_with_bookings` — Insert a job with a booking and a job without. Watch unscheduled jobs. Assert only the unbooked job appears.

    Use `AppDatabase(NativeDatabase.memory())` or equivalent in-memory Drift setup from existing test patterns. Import `package:drift/drift.dart hide isNotNull, isNull;` to avoid matcher conflicts.
  </action>
  <verify>
    Run `cd backend && uv run python -m pytest tests/integration/test_delay_endpoint.py -v --timeout=30` — all 7 tests pass. Run `cd mobile && flutter test test/unit/features/schedule/` — all overdue and booking DAO tests pass.
  </verify>
  <done>
    Backend delay endpoint: 7 integration tests covering happy path, wrong status, version conflict, not found, multiple delays, in_progress status, and empty reason validation. Overdue service: 10 unit tests covering all severity tiers and status/date combinations. BookingDao: 7 tests covering insert+sync, watch filtering, soft-delete, time update, sync upsert, and unscheduled jobs LEFT JOIN query.
  </done>
</task>

<task type="auto">
  <name>Task 2: Calendar widget tests, delay dialog tests, and E2E scheduling flow test</name>
  <files>
    mobile/test/widget/features/schedule/schedule_screen_test.dart
    mobile/test/widget/features/schedule/delay_dialog_test.dart
    mobile/test/widget/features/schedule/calendar_day_view_test.dart
  </files>
  <action>
    **Create `mobile/test/widget/features/schedule/schedule_screen_test.dart`:**
    Widget tests using ProviderScope overrides (established project pattern):
    1. `test_schedule_screen_shows_day_view_by_default` — Pump ScheduleScreen with mock providers. Assert CalendarDayView is present.
    2. `test_view_mode_toggle_switches_to_week` — Tap "Week" segmented button. Assert CalendarWeekView is present.
    3. `test_view_mode_toggle_switches_to_month` — Tap "Month" segmented button. Assert CalendarMonthView is present.
    4. `test_today_button_resets_date` — Change date via navigation arrows, tap "Today". Assert calendarDateProvider reset to today.
    5. `test_overdue_badge_shows_count` — Provide mock overdueJobCountProvider returning 3. Assert Badge with "3" text is visible.
    6. `test_trade_type_filter_narrows_contractors` — Select a trade type. Assert filteredContractorsProvider updates (verify via mock).

    **Create `mobile/test/widget/features/schedule/delay_dialog_test.dart`:**
    Widget tests for DelayJustificationDialog:
    1. `test_submit_disabled_without_reason` — Open dialog, select ETA but leave reason empty. Tap Submit. Assert dialog still visible (not dismissed).
    2. `test_submit_disabled_without_eta` — Open dialog, enter reason but don't select ETA. Tap Submit. Assert dialog still visible.
    3. `test_submit_enabled_with_both_fields` — Open dialog, enter reason, select ETA. Tap Submit. Assert dialog dismissed with true.
    4. `test_cancel_dismisses_dialog` — Open dialog, tap Cancel. Assert dialog dismissed with false.
    5. `test_eta_date_picker_restricts_to_future` — Open dialog, tap date picker. Assert firstDate is tomorrow (not today or past).
    6. `test_multiple_delays_each_append_to_history` — Mock a job with existing delay in status_history. Open dialog, submit new delay. Assert status_history now has 2 delay entries.

    Use `showDialog` pump pattern: create a MaterialApp with a button that opens the dialog, pump, tap button, interact with dialog.

    **Create `mobile/test/widget/features/schedule/calendar_day_view_test.dart`:**
    Widget tests for the day view and booking card rendering:
    1. `test_booking_card_shows_status_color` — Provide bookings with different statuses. Assert each card has correct background color from statusColorMap.
    2. `test_overdue_booking_shows_warning_border` — Provide a booking linked to a job that is 2 days overdue. Assert yellow/orange border on card.
    3. `test_overdue_booking_shows_critical_border` — Provide a booking linked to a job that is 5 days overdue. Assert red border on card.
    4. `test_contractor_lanes_show_5_per_page` — Provide 7 contractors. Assert only 5 lanes visible. Assert pagination controls present.
    5. `test_tap_booking_navigates_to_job_detail` — Tap a booking card. Assert navigation to job detail route.
    6. `test_now_line_visible_for_today` — Render day view for today's date. Assert the "now" line widget/painter is present (can check by finding a specific key or widget type).

    **E2E scheduling flow (in schedule_screen_test.dart):**
    7. `test_e2e_drag_schedule_and_delay_report` — Full flow:
      a. Seed mock bookings and jobs
      b. Verify schedule screen renders with bookings
      c. Verify overdue badge count
      d. Navigate to job detail
      e. Tap Report Delay
      f. Fill reason + ETA
      g. Submit
      h. Verify status_history updated
      i. Verify scheduled_completion_date changed

    For mock setup: use ProviderScope overrides with pre-seeded Drift in-memory database or mock providers. Follow the pattern from existing widget tests in `mobile/test/widget/`.

    IMPORTANT: Widget tests require `import 'package:drift/drift.dart' hide isNotNull, isNull;` to avoid matcher conflicts (project pattern from MEMORY.md).
    IMPORTANT: Mock DioClient at the Dio level using MockDioClient.instance (established E2E pattern per CLAUDE.md).
    IMPORTANT: For GoRouter navigation assertions, use `MockGoRouter` or verify the `context.push` call.
  </action>
  <verify>
    Run `cd mobile && flutter test test/widget/features/schedule/` — all widget tests pass. Run full test suite: `cd mobile && flutter test` — no regressions. Run `cd backend && uv run python -m pytest tests/ -x -q --timeout=60` — all backend tests pass.
  </verify>
  <done>
    Schedule screen: 6 widget tests covering view mode switching, date navigation, overdue badge, and trade filter. Delay dialog: 6 widget tests covering validation (empty reason, no ETA), cancel, and multiple delays. Calendar day view: 6 widget tests covering status colors, overdue borders, pagination, tap navigation, and now line. E2E flow: 1 integration test covering drag-to-schedule + delay report full path. All backend and mobile tests pass.
  </done>
</task>

</tasks>

<verification>
- Backend: 7 delay endpoint tests pass (happy path, wrong status, version conflict, not found, multiple, in_progress, empty reason)
- Mobile unit: 10 overdue service tests pass (all severity tiers and status combinations)
- Mobile unit: 7 BookingDao tests pass (insert+sync, watch filter, soft-delete, time update, sync upsert, unscheduled jobs query)
- Mobile widget: 6 schedule screen tests pass (view modes, date nav, badge, filter)
- Mobile widget: 6 delay dialog tests pass (validation, cancel, multiple delays)
- Mobile widget: 6 calendar day view tests pass (colors, overdue borders, pagination, tap, now line)
- Mobile E2E: 1 full-flow test covering scheduling + delay report
- No regressions in existing test suites
</verification>

<success_criteria>
Full test coverage for Phase 5 features: delay endpoint, overdue detection, booking data layer, calendar UI, delay dialog, and E2E flows. All tests pass. No regressions in existing suites.
</success_criteria>

<output>
After completion, create `.planning/phases/05-calendar-and-dispatch-ui/05-06-SUMMARY.md`
</output>
