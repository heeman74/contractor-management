---
phase: 05-calendar-and-dispatch-ui
plan: 05
type: execute
wave: 5
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - mobile/lib/features/schedule/presentation/widgets/calendar_week_view.dart
  - mobile/lib/features/schedule/presentation/widgets/calendar_month_view.dart
  - mobile/lib/features/schedule/presentation/screens/contractor_schedule_screen.dart
  - mobile/lib/features/schedule/presentation/screens/schedule_settings_screen.dart
  - mobile/lib/features/schedule/presentation/screens/schedule_screen.dart
  - mobile/lib/features/schedule/presentation/widgets/contractor_lane.dart
  - mobile/lib/core/routing/app_router.dart
  - mobile/lib/core/routing/route_names.dart
autonomous: true
requirements:
  - SCHED-03
  - SCHED-08

must_haves:
  truths:
    - "Week view shows collapsed job cards per contractor without time axis"
    - "Month view shows job count badges per day cell"
    - "Tapping a day in month view switches to day view for that date"
    - "Tapping a day in week view drills down to day view"
    - "Contractor sees personal single-lane calendar with their bookings"
    - "Contractor sees date-grouped list view of their jobs with toggle to calendar"
    - "Contractor view shows overdue prompt and Report Delay button on job cards"
    - "Schedule settings screen manages weekly working-hour templates"
    - "Admin can long-press contractor lane header to open schedule settings for that contractor"
  artifacts:
    - path: "mobile/lib/features/schedule/presentation/widgets/calendar_week_view.dart"
      provides: "Week view with collapsed job cards per contractor"
      contains: "class CalendarWeekView"
    - path: "mobile/lib/features/schedule/presentation/widgets/calendar_month_view.dart"
      provides: "Month view with job count badges per day"
      contains: "class CalendarMonthView"
    - path: "mobile/lib/features/schedule/presentation/screens/contractor_schedule_screen.dart"
      provides: "Contractor personal schedule view"
      contains: "class ContractorScheduleScreen"
    - path: "mobile/lib/features/schedule/presentation/screens/schedule_settings_screen.dart"
      provides: "Weekly template management screen"
      contains: "class ScheduleSettingsScreen"
  key_links:
    - from: "mobile/lib/features/schedule/presentation/screens/schedule_screen.dart"
      to: "mobile/lib/features/schedule/presentation/widgets/calendar_week_view.dart"
      via: "View mode switch"
      pattern: "CalendarWeekView"
    - from: "mobile/lib/features/schedule/presentation/screens/schedule_screen.dart"
      to: "mobile/lib/features/schedule/presentation/widgets/calendar_month_view.dart"
      via: "View mode switch"
      pattern: "CalendarMonthView"
    - from: "mobile/lib/features/schedule/presentation/screens/contractor_schedule_screen.dart"
      to: "mobile/lib/features/schedule/data/booking_dao.dart"
      via: "Watch bookings for current contractor"
      pattern: "watchBookingsByContractor"
    - from: "mobile/lib/features/schedule/presentation/widgets/contractor_lane.dart"
      to: "mobile/lib/features/schedule/presentation/screens/schedule_settings_screen.dart"
      via: "GestureDetector onLongPress on contractor name header navigates to settings"
      pattern: "RouteNames.scheduleSettings"
---

<objective>
Week and month calendar views, contractor personal schedule screen (list + single-lane calendar toggle), schedule settings screen for weekly working-hour templates, inline long-press access to schedule settings from contractor lane header, and route registration.

Purpose: Completes the three calendar view modes (day/week/month) and the contractor-facing schedule experience. Adds navigation routes for contractor calendar and settings. Implements the locked decision for inline schedule management via long-press on contractor lane header.
Output: All three calendar view modes functional, contractor personal schedule with list/calendar toggle, schedule settings for weekly template management, long-press navigation from contractor lanes.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calendar-and-dispatch-ui/05-CONTEXT.md
@.planning/phases/05-calendar-and-dispatch-ui/05-RESEARCH.md
@.planning/phases/05-calendar-and-dispatch-ui/05-02-SUMMARY.md
@.planning/phases/05-calendar-and-dispatch-ui/05-03-SUMMARY.md
@mobile/lib/features/schedule/presentation/screens/schedule_screen.dart
@mobile/lib/features/schedule/presentation/widgets/calendar_day_view.dart
@mobile/lib/features/schedule/presentation/widgets/contractor_lane.dart
@mobile/lib/features/schedule/presentation/providers/calendar_providers.dart
@mobile/lib/core/routing/app_router.dart
@mobile/lib/core/routing/route_names.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Week view, month view, and schedule screen view mode integration</name>
  <files>
    mobile/lib/features/schedule/presentation/widgets/calendar_week_view.dart
    mobile/lib/features/schedule/presentation/widgets/calendar_month_view.dart
    mobile/lib/features/schedule/presentation/screens/schedule_screen.dart
  </files>
  <action>
    **Create `mobile/lib/features/schedule/presentation/widgets/calendar_week_view.dart`:**
    Collapsed week summary — no time axis, one card per job per contractor:
    - Layout: 7-column grid (Mon-Sun) with contractor rows. Each cell shows compact job cards stacked vertically.
    - Column headers: day abbreviation + date number (Mon 10, Tue 11, etc.). Today's column header highlighted.
    - Contractor rows: contractor name on left, 7 day cells across.
    - Each job card: small colored chip (status color from statusColorMap) with truncated job description (1 line). Overdue jobs get warning/critical border per OverdueService.computeSeverity.
    - If more than 3 jobs per cell: show "+N more" badge. Tapping shows full list in a popup.
    - Tap any day cell: update calendarDateProvider to that date and switch calendarViewModeProvider to CalendarViewMode.day (drill down to day view).
    - Swipe left/right: navigate to previous/next week (update calendarDateProvider by +/- 7 days).
    - Data: watch bookings for the full 7-day range via BookingDao.watchBookingsByCompanyAndDateRange.
    - Paginate contractors: same 5-per-page pagination as day view, using contractorPageIndexProvider.
    - Date overrides (days off): show "Off" label in cell with grayed background.

    **Create `mobile/lib/features/schedule/presentation/widgets/calendar_month_view.dart`:**
    Month grid with job count badges per day:
    - Standard 7-column, 5-6 row calendar grid for the month.
    - Column headers: Mon, Tue, Wed, Thu, Fri, Sat, Sun.
    - Each day cell: date number + small circular badge showing total booking count for that day (across all contractors). Badge color: blue for normal, yellow/red if any overdue jobs that day.
    - Today's cell: highlighted background.
    - Days outside current month: dimmed.
    - Tap day cell: update calendarDateProvider to that date and switch to day view.
    - Swipe left/right: navigate to previous/next month.
    - Month header: "March 2026" with left/right arrows.
    - Data: aggregate bookings per day for the month. Use a single query for the full month range, then group by date.
    - No contractor lanes in month view — this is an overview.

    **Update `mobile/lib/features/schedule/presentation/screens/schedule_screen.dart`:**
    Replace the "Coming soon" placeholders for week and month modes:
    - In the body, switch on calendarViewModeProvider:
      - CalendarViewMode.day -> CalendarDayView (already implemented)
      - CalendarViewMode.week -> CalendarWeekView
      - CalendarViewMode.month -> CalendarMonthView
    - The header segmented buttons (Day/Week/Month) already exist from Plan 02; ensure they update calendarViewModeProvider.
    - Date navigation arrows behavior changes by mode:
      - Day mode: +/- 1 day
      - Week mode: +/- 7 days
      - Month mode: +/- 1 month
    - "Today" button: resets calendarDateProvider to DateTime.now() regardless of mode.
    - Date display in header: Day mode shows "Mon, Mar 10", Week mode shows "Mar 10 - 16", Month mode shows "March 2026".

    IMPORTANT: Swipe navigation uses PageView pattern — the week and month views should be wrapped in a PageView or use GestureDetector for horizontal swipe detection. Do NOT conflict with the vertical scroll of day view.
    IMPORTANT: Month view booking counts come from local Drift data — no HTTP calls. The existing bookings stream for a date range is sufficient.
  </action>
  <verify>
    Run `dart analyze mobile/lib/features/schedule/` — no analysis errors. Verify schedule_screen.dart switches between all three views based on calendarViewModeProvider. Verify week view has 7-column grid with contractor rows. Verify month view has day cells with booking count badges. Verify tap-to-drill-down updates provider and switches to day view.
  </verify>
  <done>
    All three calendar views (day, week, month) functional. Week view shows collapsed cards per contractor per day. Month view shows booking count badges per day. Both views support swipe navigation and tap-to-drill-down to day view. Today highlighted in all views.
  </done>
</task>

<task type="auto">
  <name>Task 2: Contractor personal schedule, schedule settings, long-press navigation, and route registration</name>
  <files>
    mobile/lib/features/schedule/presentation/screens/contractor_schedule_screen.dart
    mobile/lib/features/schedule/presentation/screens/schedule_settings_screen.dart
    mobile/lib/features/schedule/presentation/widgets/contractor_lane.dart
    mobile/lib/core/routing/app_router.dart
    mobile/lib/core/routing/route_names.dart
  </files>
  <action>
    **Create `mobile/lib/features/schedule/presentation/screens/contractor_schedule_screen.dart`:**
    Contractor's personal schedule view with list/calendar toggle:
    - **List view** (default): Date-grouped list of the contractor's bookings. Group headers: "Today", "Tomorrow", "Wed, Mar 12", etc. for the next 7 days. Each card shows: job description, client name, time range, address, status chip.
    - **Calendar view**: Single-lane day view reusing ContractorLane widget from Plan 02 — one lane for the logged-in contractor. Time axis on left, bookings positioned by time. Use the same CalendarGridPainter for background.
    - Toggle: SegmentedButton or ToggleButtons at the top (List / Calendar).
    - Date navigation: same as admin day view — prev/next day arrows + today button.
    - Overdue jobs: show a different prompt message — "This job is past its scheduled completion — update status or report a delay" (per CONTEXT.md). Use a Card with amber/red background for overdue items.
    - "Report Delay" button: visible on each job card for Scheduled/In Progress status. Tapping opens DelayJustificationDialog (from Plan 04).
    - Pull-to-refresh: RefreshIndicator triggering SyncEngine.syncNow().
    - Data source: BookingDao.watchBookingsByContractorAndDate with the current user's contractor ID from auth state.
    - This screen is shown when the logged-in user has contractor role and taps the Schedule tab.

    **Create `mobile/lib/features/schedule/presentation/screens/schedule_settings_screen.dart`:**
    Weekly working-hour template management for contractors:
    - Shows 7 rows (Monday through Sunday), each with:
      - Day name
      - Toggle: working / day off
      - Start time + end time pickers (TimeOfDay)
      - "Set as day off" button
    - Current values loaded from backend (GET /api/v1/scheduling/schedules/{contractor_id}/weekly) via Dio.
    - Save button: PATCHes weekly schedule to backend. If offline, queue the update in sync queue.
    - "Quick actions" section: Set recurring day off (e.g., "Every Saturday off"), copy one day's hours to all weekdays.
    - Navigation: accessible from contractor's schedule screen via a settings gear icon, or from the admin calendar via long-press on a contractor lane header (see below).
    - Admin can also access this for any contractor (pass contractorId as route parameter).

    **Update `mobile/lib/features/schedule/presentation/widgets/contractor_lane.dart`:**
    Add inline long-press navigation to schedule settings on the contractor name header (locked decision from CONTEXT.md: "Contractor schedule management: both inline quick actions from calendar (long-press for day off, adjust hours) AND a separate settings screen"):
    - Wrap the contractor name + avatar header at the top of the ContractorLane widget in a `GestureDetector(onLongPress: () => context.push(RouteNames.scheduleSettings, extra: contractorId))`.
    - This gives admin users a quick inline path to manage a contractor's working hours directly from the dispatch calendar without navigating away to a separate screen.
    - Only active for admin role users (contractors see their own schedule settings via the gear icon on their personal schedule screen).

    **Update `mobile/lib/core/routing/route_names.dart`:**
    Add new route constants:
    - `static const contractorSchedule = '/contractor/schedule';`
    - `static const scheduleSettings = '/schedule/settings';`

    **Update `mobile/lib/core/routing/app_router.dart`:**
    - Add GoRoute for contractorSchedule — available to contractor role.
    - Add GoRoute for scheduleSettings — available to contractor and admin roles. Accept optional `contractorId` query parameter (admin accessing another contractor's settings).
    - For the Schedule tab: if user role is contractor, the Schedule branch should show ContractorScheduleScreen instead of ScheduleScreen (admin dispatch). Use a provider or router redirect to select the appropriate screen based on role.
    - The Schedule tab in AppShell already exists — just swap the target screen based on role.

    IMPORTANT: The admin dispatch calendar (ScheduleScreen) and contractor personal calendar (ContractorScheduleScreen) share the same bottom nav tab but show different screens based on role. This is how Phase 4 handles the Jobs tab (admin sees pipeline, contractor sees their jobs).
    IMPORTANT: Contractor schedule management has two entry points per CONTEXT.md: inline from calendar (long-press) and separate settings screen. The settings screen is the separate entry point; the inline quick action (long-press on calendar) opens the same settings screen via navigation.
    IMPORTANT: Schedule settings screen loads data from backend API — this requires connectivity for initial load. If offline, show cached data or "Connect to manage schedule" message.
  </action>
  <verify>
    Run `dart analyze mobile/lib/features/schedule/ mobile/lib/core/routing/` — no analysis errors. Verify contractor_schedule_screen has list/calendar toggle. Verify schedule_settings_screen has 7-day weekly template form. Verify contractor_lane.dart has GestureDetector(onLongPress) on the name header navigating to RouteNames.scheduleSettings. Verify app_router.dart has new routes for contractor schedule and settings. Verify role-based screen selection on Schedule tab.
  </verify>
  <done>
    Contractor sees personal schedule with list/calendar toggle, overdue prompts, and Report Delay buttons. Schedule settings screen manages weekly working-hour templates. Long-press on contractor lane header in admin calendar navigates to that contractor's schedule settings. Routes registered for contractor schedule and settings. Admin dispatch vs contractor personal calendar selected by role on Schedule tab.
  </done>
</task>

</tasks>

<verification>
- Week view shows 7-column grid with contractor rows and job cards
- Month view shows day cells with booking count badges
- Tap day in week/month drills down to day view for that date
- Swipe left/right navigates between weeks/months
- Contractor Schedule tab shows personal list view by default
- Contractor can toggle to single-lane calendar view
- Overdue jobs in contractor view show "update status or report a delay" prompt
- Report Delay button works from contractor job cards
- Schedule settings shows 7-day weekly template form
- Admin can access contractor schedule settings via long-press on contractor lane header
- Admin can also access contractor schedule settings directly
- Route registration works for contractor schedule and settings
- Role-based screen selection on Schedule tab (admin=dispatch, contractor=personal)
</verification>

<success_criteria>
All three calendar view modes functional and navigable. Contractor personal schedule operational with both list and calendar views. Schedule settings screen manages weekly working hours. Long-press on contractor lane header opens schedule settings (locked decision). Routing complete for all new screens.
</success_criteria>

<output>
After completion, create `.planning/phases/05-calendar-and-dispatch-ui/05-05-SUMMARY.md`
</output>
