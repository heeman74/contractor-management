---
phase: 10-ui-backend-wiring-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/schedule/presentation/screens/schedule_screen.dart
  - mobile/lib/features/jobs/presentation/screens/job_detail_screen.dart
  - mobile/lib/features/quotes/presentation/screens/quote_builder_screen.dart
  - backend/app/core/config.py
  - backend/app/features/scheduling/router.py
  - mobile/test/e2e/phase_10_ui_wiring_e2e_test.dart
  - backend/tests/integration/test_phase_10_e2e.py
autonomous: true
requirements: [SCHED-08, BIZ-01, BIZ-02, SCHED-06]

must_haves:
  truths:
    - "Admin sees overdue jobs panel on the schedule screen (not a placeholder Container)"
    - "Admin can navigate from job detail to QuoteBuilderScreen via Create Quote button"
    - "Admin sees View/Edit Quote button when a quote already exists for the job"
    - "SchedulingService receives CachedTravelTimeProvider when ORS_API_KEY is set"
    - "SchedulingService receives travel_provider=None when ORS_API_KEY is absent"
  artifacts:
    - path: "mobile/lib/features/schedule/presentation/screens/schedule_screen.dart"
      provides: "OverduePanel widget rendered instead of placeholder Container"
      contains: "OverduePanel()"
    - path: "mobile/lib/features/jobs/presentation/screens/job_detail_screen.dart"
      provides: "Create Quote / View Quote button in _DetailsTab"
      contains: "quoteBuilderPath"
    - path: "backend/app/core/config.py"
      provides: "ors_api_key field on Settings"
      contains: "ors_api_key"
    - path: "backend/app/features/scheduling/router.py"
      provides: "get_scheduling_service dependency with travel provider injection"
      contains: "get_scheduling_service"
    - path: "mobile/test/e2e/phase_10_ui_wiring_e2e_test.dart"
      provides: "E2E tests for SCHED-08, BIZ-01, BIZ-02"
      min_lines: 50
    - path: "backend/tests/integration/test_phase_10_e2e.py"
      provides: "Integration tests for SCHED-06"
      min_lines: 20
  key_links:
    - from: "schedule_screen.dart"
      to: "overdue_panel.dart"
      via: "import + const OverduePanel()"
      pattern: "import.*overdue_panel"
    - from: "job_detail_screen.dart"
      to: "QuoteBuilderScreen"
      via: "context.push(RouteNames.quoteBuilderPath(job.id))"
      pattern: "quoteBuilderPath"
    - from: "scheduling/router.py"
      to: "CachedTravelTimeProvider"
      via: "get_scheduling_service dependency"
      pattern: "get_scheduling_service"
---

<objective>
Wire three orphaned components into production: OverduePanel in schedule screen, QuoteBuilder navigation from job detail, and TravelTimeCacheService injection in scheduling router. All feature implementations are complete — this plan adds only connection tissue (imports, dependency parameters, UI entry points) and corresponding E2E/integration tests.

Purpose: Close the final three gaps from the v1.0 milestone audit, satisfying SCHED-08, BIZ-01, BIZ-02, and SCHED-06.
Output: Three wiring fixes + comprehensive tests proving all four requirements are met.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-backend-wiring-gap-closure/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From mobile/lib/features/schedule/presentation/widgets/overdue_panel.dart:
- `OverduePanel` is a `ConsumerWidget` (362 lines, fully implemented)
- Reads `showOverduePanelProvider` internally — no props needed
- Uses `AnimatedContainer(height: isVisible ? null : 0)` for animated show/hide
- The outer `if (showOverduePanel)` guard in schedule_screen.dart is REDUNDANT — remove it

From mobile/lib/features/schedule/presentation/screens/schedule_screen.dart:
- Lines 108-131: placeholder Container to replace
- Line 93: `final showOverduePanel = ref.watch(showOverduePanelProvider);` — keep for onTapOverdueBadge toggle (line 100-101), but remove the `if (showOverduePanel)` guard around the placeholder

From mobile/lib/features/jobs/presentation/screens/job_detail_screen.dart:
- `_DetailsTabState` at line 255 has `isAdmin` check at line 262
- Invoice section starts at line 316 — Quote section goes ABOVE this
- Pattern: `invoicesAsync = ref.watch(invoicesForJobProvider(job.id))` — follow same for quotes

From mobile/lib/features/quotes/presentation/providers/quote_providers.dart:
- `quoteForJobProvider` — `StreamProvider.autoDispose.family<List<QuoteEntity>, String>`

From mobile/lib/core/routing/route_names.dart:
- `RouteNames.quoteBuilderPath(String jobId)` — helper already exists

From mobile/lib/features/quotes/presentation/screens/quote_builder_screen.dart:
- QuoteBuilderScreen accepts jobId, optionally an existing quote via route extra

From backend/app/core/config.py:
- `Settings(BaseSettings)` at line 5 — add `ors_api_key: str | None = None`
- `settings = Settings()` singleton at line 36

From backend/app/features/scheduling/router.py:
- 10 occurrences of `SchedulingService(db)` at lines 100, 122, 158, 203, 300, 333, 395, 434, 467, 506
- Some endpoints (list_bookings, weekly schedule, date overrides) also do inline ORM queries — keep their `db` param

From backend/app/features/scheduling/service.py:
- `SchedulingService.__init__(self, db, travel_provider: TravelTimeProvider | None = None)`

From backend/app/features/scheduling/travel/cache.py:
- `CachedTravelTimeProvider(cache_service, company_id)`
- `TravelTimeCacheService(db, provider)`

From backend/app/features/scheduling/travel/ors_provider.py:
- `OpenRouteServiceProvider(api_key, client)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire OverduePanel, QuoteBuilder navigation, and TravelTime injection</name>
  <files>
    mobile/lib/features/schedule/presentation/screens/schedule_screen.dart,
    mobile/lib/features/jobs/presentation/screens/job_detail_screen.dart,
    mobile/lib/features/quotes/presentation/screens/quote_builder_screen.dart,
    backend/app/core/config.py,
    backend/app/features/scheduling/router.py
  </files>
  <action>
Three surgical wiring edits — no new business logic, only connection tissue.

**Fix 1 — OverduePanel (SCHED-08):**
In `schedule_screen.dart`:
1. Add import: `import '../widgets/overdue_panel.dart';`
2. Replace the entire placeholder block (lines 108-131, the `if (showOverduePanel) Container(...)`) with just: `const OverduePanel(),`
3. IMPORTANT: Remove the `if (showOverduePanel)` wrapper — OverduePanel manages its own visibility via AnimatedContainer internally. Keep the `showOverduePanel` variable declaration (line 93) because it is still used by `onTapOverdueBadge` (line 100-101).

**Fix 2 — QuoteBuilder navigation (BIZ-01, BIZ-02):**
In `job_detail_screen.dart`:
1. Add imports:
   - `import '../../../../features/quotes/presentation/providers/quote_providers.dart';`
   - `import '../../../../features/quotes/domain/quote_entity.dart';`
   - `import '../../../../core/routing/route_names.dart';` (if not already imported)
2. In `_DetailsTabState.build()`, after the first Card (job details, ends line ~314) and BEFORE the Invoice section (line 316), add a Quote section:
   - Watch `quoteForJobProvider(job.id)` same pattern as invoicesAsync
   - `final quotes = quotesAsync.maybeWhen(data: (q) => q, orElse: () => <QuoteEntity>[]);`
   - `final hasQuote = quotes.isNotEmpty;`
   - Show Quote card only for admin: `if (isAdmin) ...[` with:
     - If `hasQuote`: OutlinedButton.icon "View / Edit Quote" that calls `context.push(RouteNames.quoteBuilderPath(job.id))`
     - If `!hasQuote`: FilledButton.icon "Create Quote" that calls `context.push(RouteNames.quoteBuilderPath(job.id))`
   - Hide for terminal statuses: wrap the entire quote section in `if (isAdmin && job.jobStatus != JobStatus.cancelled && job.jobStatus != JobStatus.invoiced)`
3. In `quote_builder_screen.dart`: check that it handles null `extra` parameter gracefully (creates new quote when no existing quote passed). If it already does, no changes needed.

**Fix 3 — TravelTime injection (SCHED-06):**
In `backend/app/core/config.py`:
1. Add field to Settings: `ors_api_key: str | None = None`

In `backend/app/features/scheduling/router.py`:
1. Add a `get_scheduling_service` async dependency function (see research Pattern 3 for exact implementation):
   - Accepts `db: AsyncSession = Depends(get_db)` and `current_user: CurrentUser = Depends(get_current_user)`
   - If `settings.ors_api_key` is set: create `httpx.AsyncClient`, `OpenRouteServiceProvider`, `TravelTimeCacheService`, `CachedTravelTimeProvider` with `company_id=get_current_tenant_id()`
   - Return `SchedulingService(db=db, travel_provider=travel_provider)`
   - Add comment: `# NOTE: per-request httpx client — tech debt for lifespan-managed shared client`
2. Update all endpoint functions that construct `SchedulingService(db)` to use `svc: SchedulingService = Depends(get_scheduling_service)` instead
3. CRITICAL: Some endpoints (`list_bookings`, `get_weekly_schedule`, `get_date_overrides`) do direct ORM queries — these still need `db: AsyncSession = Depends(get_db)`. For those endpoints, add BOTH `db` and `svc` dependencies, or keep `db` and add `svc` only if the endpoint actually calls service methods. Audit each of the 10 occurrences individually.
4. Add necessary imports at the top: `TravelTimeProvider` from service module
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && dart analyze lib/features/schedule/presentation/screens/schedule_screen.dart lib/features/jobs/presentation/screens/job_detail_screen.dart 2>&1 | tail -5 && cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run python -c "from app.features.scheduling.router import get_scheduling_service; print('OK')" && uv run ruff check app/features/scheduling/router.py app/core/config.py</automated>
  </verify>
  <done>
    - schedule_screen.dart renders `const OverduePanel()` (not placeholder Container)
    - job_detail_screen.dart shows Create Quote / View Quote button for admin users on non-terminal jobs
    - config.py has `ors_api_key: str | None = None` field
    - router.py has `get_scheduling_service` dependency and all relevant endpoints use it
    - dart analyze passes on modified files
    - ruff check passes on modified files
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E and integration tests for all wiring fixes</name>
  <files>
    mobile/test/e2e/phase_10_ui_wiring_e2e_test.dart,
    backend/tests/integration/test_phase_10_e2e.py
  </files>
  <action>
Create test files per VALIDATION.md test map. Follow existing E2E test patterns from phase 9.

**Flutter E2E tests** (`mobile/test/e2e/phase_10_ui_wiring_e2e_test.dart`):
Use mocktail for mocking, ProviderScope overrides for Riverpod providers, real Drift in-memory DB where applicable. Use `pump()` not `pumpAndSettle()` for Drift stream providers (project convention).

SCHED-08 tests:
- `sched08`: Render ScheduleScreen with seeded overdue jobs in provider override — verify OverduePanel renders with overdue job text (not "Overdue panel loading..." placeholder text)
- `sched08_empty`: Render ScheduleScreen with no overdue jobs — verify OverduePanel renders but shows empty/collapsed state (height 0 via AnimatedContainer)
- `sched08_nav`: Verify tapping a job in OverduePanel triggers navigation (mock GoRouter or verify the right method call)

BIZ-01 tests:
- `biz01_create`: Render _DetailsTab (via JobDetailScreen) with admin auth state and no existing quotes — verify "Create Quote" FilledButton is found
- `biz01_no_button`: Render _DetailsTab with non-admin auth state — verify "Create Quote" button is NOT found

BIZ-02 tests:
- `biz02_view`: Render _DetailsTab with admin auth state and a seeded quote for the job — verify "View / Edit Quote" OutlinedButton is found
- `biz02_nav`: Verify tapping "Create Quote" button calls context.push with quoteBuilderPath

Test setup pattern: Override `authNotifierProvider` with `AuthAuthenticated` state (admin roles for admin tests, contractor role for non-admin test). Override `quoteForJobProvider` with stream returning seeded data or empty list. Override `overdueJobsProvider` for schedule screen tests.

**Backend integration tests** (`backend/tests/integration/test_phase_10_e2e.py`):
Use pytest with existing conftest.py fixtures (async_client, auth tokens).

SCHED-06 tests:
- `test_travel_provider_injected`: Monkeypatch `settings.ors_api_key = "test-key"`, call `get_scheduling_service` dependency, assert returned service has `travel_provider is not None`
- `test_travel_provider_absent`: Ensure `settings.ors_api_key is None` (default), call `get_scheduling_service`, assert `travel_provider is None`
- `test_scheduling_endpoint_uses_service`: Hit POST /api/v1/scheduling/availability with valid contractor — verify 200 response (smoke test that the dependency wiring doesn't break existing endpoints)
  </action>
  <verify>
    <automated>cd /Users/heechung/AndroidStudioProjects/contractormanagement/mobile && flutter test test/e2e/phase_10_ui_wiring_e2e_test.dart && cd /Users/heechung/AndroidStudioProjects/contractormanagement/backend && uv run python -m pytest tests/integration/test_phase_10_e2e.py -x -v</automated>
  </verify>
  <done>
    - All SCHED-08 widget tests pass (overdue panel renders, empty state works, navigation triggers)
    - All BIZ-01 widget tests pass (Create Quote visible for admin, hidden for non-admin)
    - All BIZ-02 widget tests pass (View Quote shown with existing quote, navigation works)
    - All SCHED-06 integration tests pass (travel provider injected/absent based on ORS_API_KEY)
    - No regressions in existing test suites
  </done>
</task>

</tasks>

<verification>
1. `cd mobile && dart analyze lib/features/schedule/presentation/screens/schedule_screen.dart lib/features/jobs/presentation/screens/job_detail_screen.dart` — no errors
2. `cd backend && uv run ruff check app/features/scheduling/router.py app/core/config.py` — no errors
3. `cd mobile && flutter test test/e2e/phase_10_ui_wiring_e2e_test.dart` — all tests pass
4. `cd backend && uv run python -m pytest tests/integration/test_phase_10_e2e.py -x -v` — all tests pass
5. `grep -r "Overdue panel loading" mobile/lib/` — returns NO results (placeholder removed)
6. `grep -r "quoteBuilderPath" mobile/lib/features/jobs/` — returns results in job_detail_screen.dart
7. `grep -r "get_scheduling_service" backend/app/features/scheduling/router.py` — returns results
</verification>

<success_criteria>
- OverduePanel renders on schedule screen with animated show/hide (not static placeholder)
- Admin can reach QuoteBuilderScreen from job detail via Create Quote or View/Edit Quote button
- Non-admin users do not see the quote button
- SchedulingService gets travel provider when ORS_API_KEY is configured
- All 9+ E2E/integration tests pass
- No regressions in existing test suites
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-backend-wiring-gap-closure/10-01-SUMMARY.md`
</output>
