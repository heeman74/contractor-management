---
phase: 02-offline-sync-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/versions/0002_soft_delete_sync.py
  - backend/app/features/sync/__init__.py
  - backend/app/features/sync/schemas.py
  - backend/app/features/sync/service.py
  - backend/app/features/sync/router.py
  - backend/app/features/companies/models.py
  - backend/app/features/companies/service.py
  - backend/app/features/users/models.py
  - backend/app/features/users/service.py
  - backend/app/main.py
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "GET /api/v1/sync?cursor=<ISO8601> returns all companies, users, and user_roles changed since cursor in a single response"
    - "Tombstones (records with deleted_at set) are included in delta sync response when deleted_at > cursor"
    - "POST to company/user/role endpoints with a duplicate UUID returns the existing record silently (INSERT ON CONFLICT DO NOTHING)"
    - "updated_at auto-advances on every UPDATE via PostgreSQL trigger (not SQLAlchemy ORM hook)"
    - "All entity tables have deleted_at column via Alembic migration 0002"
  artifacts:
    - path: "backend/migrations/versions/0002_soft_delete_sync.py"
      provides: "Alembic migration adding deleted_at, updated_at trigger"
      contains: "def upgrade"
    - path: "backend/app/features/sync/router.py"
      provides: "GET /api/v1/sync delta endpoint"
      contains: "delta_sync"
    - path: "backend/app/features/sync/service.py"
      provides: "Multi-table delta query service"
      contains: "get_companies_since"
    - path: "backend/app/features/sync/schemas.py"
      provides: "SyncResponse Pydantic model"
      contains: "class SyncResponse"
    - path: "backend/app/features/companies/service.py"
      provides: "Idempotent create with ON CONFLICT DO NOTHING"
      contains: "on_conflict_do_nothing"
  key_links:
    - from: "backend/app/features/sync/router.py"
      to: "backend/app/features/sync/service.py"
      via: "FastAPI Depends injection"
      pattern: "service\\.get_.*_since"
    - from: "backend/app/features/sync/service.py"
      to: "backend/app/features/companies/models.py"
      via: "SQLAlchemy select query with updated_at/deleted_at filter"
      pattern: "or_.*updated_at.*deleted_at"
    - from: "backend/app/main.py"
      to: "backend/app/features/sync/router.py"
      via: "app.include_router"
      pattern: "sync.*router"
---

<objective>
Create the backend sync infrastructure: Alembic migration 0002 (deleted_at columns, updated_at PostgreSQL triggers), delta sync endpoint (GET /api/v1/sync?cursor=), and idempotent mutation support (INSERT ON CONFLICT DO NOTHING) for all entity endpoints.

Purpose: INFRA-04 requires the server side of the sync engine. The delta endpoint enables efficient pull-based sync. Idempotent creates handle retry deduplication. The updated_at trigger ensures delta cursors advance correctly even for bulk/ORM operations.

Output: Alembic migration 0002, sync feature module (router, service, schemas), idempotent create services, updated_at trigger.
</objective>

<execution_context>
@/Users/heechung/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heechung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-offline-sync-engine/02-CONTEXT.md
@.planning/phases/02-offline-sync-engine/02-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

Key existing files to read before modifying:
@backend/app/features/companies/models.py
@backend/app/features/companies/service.py
@backend/app/features/companies/schemas.py
@backend/app/features/companies/router.py
@backend/app/features/users/models.py
@backend/app/features/users/service.py
@backend/app/features/users/router.py
@backend/app/main.py
@backend/app/core/database.py
@backend/app/core/tenant.py
@backend/migrations/versions/0001_initial.py
@backend/migrations/env.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration 0002 with deleted_at columns and updated_at PostgreSQL trigger</name>
  <files>
    backend/migrations/versions/0002_soft_delete_sync.py
    backend/app/features/companies/models.py
    backend/app/features/users/models.py
  </files>
  <action>
    Create `backend/migrations/versions/0002_soft_delete_sync.py`:
    - Revision ID: use a short hex string (e.g., 0002_soft_delete_sync)
    - Depends on: 0001_initial revision ID (read it from 0001_initial.py)
    - upgrade():
      1. Add `deleted_at` (DateTime with timezone, nullable) to companies, users, user_roles tables
      2. Add `updated_at` to user_roles table if not present (RESEARCH.md Open Question 3 — needed for delta sync filter)
      3. Create `set_updated_at()` PostgreSQL function:
         ```sql
         CREATE OR REPLACE FUNCTION set_updated_at()
         RETURNS TRIGGER AS $$
         BEGIN
             NEW.updated_at = NOW();
             RETURN NEW;
         END;
         $$ LANGUAGE plpgsql;
         ```
      4. Create triggers on companies, users, user_roles:
         ```sql
         CREATE TRIGGER set_{table}_updated_at
         BEFORE UPDATE ON {table}
         FOR EACH ROW EXECUTE FUNCTION set_updated_at();
         ```
         CRITICAL: This PostgreSQL trigger ensures updated_at advances on ALL updates including bulk SQLAlchemy operations (Pitfall 6 from RESEARCH.md). The SQLAlchemy `onupdate=func.now()` is ORM-level only.
    - downgrade():
      1. Drop triggers
      2. Drop function
      3. Drop deleted_at columns from all 3 tables
      4. Drop updated_at from user_roles if we added it

    Update `backend/app/features/companies/models.py`:
    - Add `deleted_at = Column(DateTime(timezone=True), nullable=True)` to Company model
    - Ensure `updated_at` column exists (it should from Phase 1)

    Update `backend/app/features/users/models.py`:
    - Add `deleted_at = Column(DateTime(timezone=True), nullable=True)` to User model
    - Add `deleted_at = Column(DateTime(timezone=True), nullable=True)` to UserRole model
    - Add `updated_at = Column(DateTime(timezone=True), server_default=func.now(), nullable=True)` to UserRole model if not present (needed for delta sync)

    IMPORTANT: Follow Phase 1 Alembic patterns. The migration must be a standalone file with proper revision chain. RLS policies from migration 0001 must remain intact.
  </action>
  <verify>
    Verify migration file exists and has proper structure:
    - `ls backend/migrations/versions/0002_soft_delete_sync.py`
    - `grep 'deleted_at' backend/migrations/versions/0002_soft_delete_sync.py`
    - `grep 'set_updated_at' backend/migrations/versions/0002_soft_delete_sync.py`
    - `grep 'CREATE TRIGGER' backend/migrations/versions/0002_soft_delete_sync.py`
    Verify models updated:
    - `grep 'deleted_at' backend/app/features/companies/models.py`
    - `grep 'deleted_at' backend/app/features/users/models.py`
  </verify>
  <done>
    Alembic migration 0002 adds deleted_at to all 3 entity tables, updated_at to user_roles, and creates set_updated_at() PostgreSQL trigger on all entity tables. SQLAlchemy models reflect the new columns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delta sync endpoint and idempotent mutation services</name>
  <files>
    backend/app/features/sync/__init__.py
    backend/app/features/sync/schemas.py
    backend/app/features/sync/service.py
    backend/app/features/sync/router.py
    backend/app/features/companies/service.py
    backend/app/features/companies/schemas.py
    backend/app/features/users/service.py
    backend/app/main.py
  </files>
  <action>
    Create `backend/app/features/sync/__init__.py` — empty init file.

    Create `backend/app/features/sync/schemas.py`:
    - SyncResponse(BaseModel):
      - companies: list[CompanyResponse]
      - users: list[UserResponse]
      - user_roles: list[UserRoleResponse]
      - server_timestamp: str (ISO8601)
    - Import CompanyResponse, UserResponse, UserRoleResponse from existing schema files
    - Add `deleted_at: datetime | None = None` to CompanyResponse, UserResponse, UserRoleResponse if not already present (modify the existing schema files)

    Create `backend/app/features/sync/service.py`:
    - `get_companies_since(db: AsyncSession, since: datetime) -> list[Company]`:
      Uses `select(Company).where(or_(Company.updated_at > since, Company.deleted_at > since))`
      CRITICAL: Include deleted_at > since filter for tombstone propagation (Pitfall 4)
    - `get_users_since(db: AsyncSession, since: datetime) -> list[User]`:
      Same pattern with User model
    - `get_user_roles_since(db: AsyncSession, since: datetime) -> list[UserRole]`:
      Same pattern with UserRole model
    - Import `or_` from SQLAlchemy, all 3 models from their feature modules

    Create `backend/app/features/sync/router.py`:
    - APIRouter with prefix="/sync", tags=["sync"]
    - `GET ""` endpoint `delta_sync(cursor: datetime | None = Query(None), db: AsyncSession = Depends(get_db))`:
      - Default cursor to datetime(2000, 1, 1, tzinfo=timezone.utc) if None (first launch = full download)
      - Call all 3 service functions
      - Return SyncResponse with server_timestamp = datetime.now(timezone.utc).isoformat()
    - RLS is automatically enforced via TenantMiddleware — only current company's data returned

    Update `backend/app/features/companies/service.py`:
    - Add `create_company_idempotent(db: AsyncSession, data: CompanyCreate) -> Company`:
      Uses `from sqlalchemy.dialects.postgresql import insert`
      `insert(Company).values(...).on_conflict_do_nothing(index_elements=["id"])`
      After execute, fetch by ID and return (existing or new)
    - Modify existing `create_company` to call `create_company_idempotent` (or replace it)
    - Add `update_company_server_wins(db: AsyncSession, data) -> Company`:
      Upserts the company: if exists, overwrites all fields; if not, creates. Use `on_conflict_do_update`.
      Server-wins per user decision — no version comparison.

    Update `backend/app/features/companies/schemas.py`:
    - Add `deleted_at: datetime | None = None` to CompanyResponse
    - Add `id: str` to CompanyCreate (client-provided UUID)

    Update `backend/app/features/users/service.py`:
    - Add idempotent create for User (same ON CONFLICT DO NOTHING pattern)
    - Add idempotent create for UserRole

    Update `backend/app/main.py`:
    - Import sync router
    - Add `app.include_router(sync_router, prefix="/api/v1")` alongside existing routers

    Use existing patterns: get_db dependency, TenantMiddleware ContextVar for tenant isolation, Pydantic model_validate for response serialization.
  </action>
  <verify>
    Verify sync module files exist:
    - `ls backend/app/features/sync/__init__.py backend/app/features/sync/schemas.py backend/app/features/sync/service.py backend/app/features/sync/router.py`
    Verify sync router registered:
    - `grep 'sync' backend/app/main.py`
    Verify idempotent creates:
    - `grep 'on_conflict_do_nothing' backend/app/features/companies/service.py`
    - `grep 'on_conflict_do_nothing' backend/app/features/users/service.py`
    Verify delta query includes tombstones:
    - `grep 'deleted_at' backend/app/features/sync/service.py`
    Verify SyncResponse schema:
    - `grep 'server_timestamp' backend/app/features/sync/schemas.py`
    Run Python syntax check:
    - `python3 -c "import ast; ast.parse(open('backend/app/features/sync/router.py').read()); print('OK')"` (repeat for service.py, schemas.py)
  </verify>
  <done>
    GET /api/v1/sync?cursor= returns SyncResponse with companies, users, user_roles changed since cursor (including tombstones). Company and user create endpoints use INSERT ON CONFLICT DO NOTHING for idempotency. Sync router registered in FastAPI app. All responses include deleted_at field for tombstone detection.
  </done>
</task>

</tasks>

<verification>
- Alembic migration 0002 exists with deleted_at columns, updated_at trigger function
- GET /api/v1/sync delta endpoint returns multi-entity response with server_timestamp
- Delta query includes `or_(updated_at > since, deleted_at > since)` for tombstone propagation
- Company and User create services use `on_conflict_do_nothing` for UUID idempotency
- SyncResponse Pydantic model includes all 3 entity types + server_timestamp
- All files pass Python syntax check via `ast.parse`
- Sync router included in main.py app
</verification>

<success_criteria>
- Delta sync endpoint returns all entity types changed since cursor timestamp
- Tombstoned records (deleted_at set) are included in delta response
- Duplicate UUID creates return existing record silently (no 409, no error)
- PostgreSQL trigger auto-updates updated_at on all entity UPDATE operations
- Endpoint respects RLS tenant isolation via TenantMiddleware
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-sync-engine/02-02-SUMMARY.md`
</output>
