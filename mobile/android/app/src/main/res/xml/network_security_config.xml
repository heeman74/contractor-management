<?xml version="1.0" encoding="utf-8"?>
<!-- Network Security Configuration — HTTPS-only with localhost/emulator exceptions.

     Production: All traffic must use HTTPS (cleartextTrafficPermitted=false).
     Development: Allow cleartext to localhost (127.0.0.1) and Android emulator
     host (10.0.2.2) for local backend access.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext for local development only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
