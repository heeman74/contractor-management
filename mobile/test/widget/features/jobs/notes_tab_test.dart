import 'package:flutter_test/flutter_test.dart';

void main() {
  group('NotesTab', () {
    test('renders note entries with author and timestamp',
        skip: 'Wave 0 stub — implementation in plan 06-03', () {
      // Will test: note list items display author name and formatted timestamp
    });

    test('empty state shows no notes message',
        skip: 'Wave 0 stub — implementation in plan 06-03', () {
      // Will test: when note list is empty, a "No notes yet" empty-state widget is shown
    });

    test('FAB opens Add Note bottom sheet',
        skip: 'Wave 0 stub — implementation in plan 06-03', () {
      // Will test: tapping FAB opens the AddNoteBottomSheet modal
    });
  });
}
