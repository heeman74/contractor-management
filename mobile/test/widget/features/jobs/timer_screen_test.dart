import 'package:flutter_test/flutter_test.dart';

void main() {
  group('TimerScreen', () {
    test('renders elapsed time display',
        skip: 'Wave 0 stub — implementation in plan 06-05', () {
      // Will test: screen shows an HH:MM:SS formatted elapsed time counter
    });

    test('shows Clock In button when no active session',
        skip: 'Wave 0 stub — implementation in plan 06-05', () {
      // Will test: "Clock In" button is visible when TimerNotifier state has no active session
    });

    test('shows session history list',
        skip: 'Wave 0 stub — implementation in plan 06-05', () {
      // Will test: past time entries are listed below the clock with date and duration
    });
  });
}
