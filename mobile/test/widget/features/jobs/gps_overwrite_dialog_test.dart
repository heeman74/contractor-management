import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GpsOverwriteDialog', () {
    test('shows confirm dialog when address exists',
        skip: 'Wave 0 stub — implementation in plan 06-04', () {
      // Will test: dialog is presented when an existing GPS address is present before capture
    });

    test('cancelling dialog preserves existing address',
        skip: 'Wave 0 stub — implementation in plan 06-04', () {
      // Will test: tapping Cancel dismisses dialog without modifying the existing gpsAddress
    });
  });
}
