import 'package:flutter_test/flutter_test.dart';

void main() {
  group('DrawingSave', () {
    test('exports PNG to app support directory',
        skip: 'Wave 0 stub — implementation in plan 06-04', () {
      // Will test: save() writes a PNG file under getApplicationSupportDirectory()
    });

    test('returns file path via Navigator.pop',
        skip: 'Wave 0 stub — implementation in plan 06-04', () {
      // Will test: Navigator.pop is called with the saved file path string after export
    });
  });
}
